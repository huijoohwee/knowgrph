#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "${ROOT_DIR}"
node ./scripts/check-pre-push-refs.mjs
echo "[knowgrph] pre-push: running collaboration and conflict compliance checks..."
npm run ci:integration
