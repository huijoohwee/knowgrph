# Knowgrph Cache Catalog

| Area            | Responsibility                                 | Modules                                                                          | Classes/Objects      | Functions/Methods              | Key                         | Type   | Value/Default | Dependencies / Imports | Notes                                                                                           |
| --------------- | ---------------------------------------------- | -------------------------------------------------------------------------------- | -------------------- | ----------------------------- | --------------------------- | ------ | ------------- | ---------------------- | ----------------------------------------------------------------------------------------------- |
| Parser Cache    | Cache parse results for identical inputs       | `canvas/src/features/parsers/cache.ts`                                           | LRU cache            | `getCachedOrParse`            | parser result cache         | object | capacity 32   | —                      | Avoids re‑parsing the same dataset; keyed by parser id and file hash                            |
| Schema Derive   | Cache derived node types and edge labels       | `canvas/src/features/schema/derive.ts`                                           | LRU cache            | `uniqueNodeTypes`, `uniqueEdgeLabels` | `schemaDeriveCacheCapacity` | number | 16            | —                      | Capacity is configurable via settings; prevents repeated scans over schema‑sized structures     |
| Toolbar Search  | Cache search results for recent queries        | `canvas/src/features/toolbar/utils.ts`                                           | cache helper         | `getCachedSearchResults`      | toolbar search cache        | object | —             | —                      | Keyed by `graphId|historyIndex|query|limit`; improves responsiveness in large graphs           |
| 3D Layout State | Reuse positions across ticks in Physics3D      | `canvas/src/features/three/layout.ts`, `canvas/src/features/three/ThreeGraph.tsx` | positions arrays     | `Physics3D` internal buffers  | `positions[id]`             | arrays | per node      | `three`                | Positions and velocities stored in typed arrays; tick loop mutates in place to reduce GC churn |
| Workflow Presets | Persist preset catalog for parser/dataset/schema triples | `canvas/src/features/panels/views/ParserView.tsx`, `canvas/src/lib/graph/graphragTraversal.ts` | localStorage entries | `onApplyWorkflowPreset`, `findGraphRagTraversalEdgeIds` | `kg:workflowPresets:*`      | JSON   | 2 presets      | `window.localStorage`  | Catalog and last-applied triple stored as JSON (`parserSpecId`, `datasetFileName`, `schemaFileName`) for unicorn/AI‑KG workflows; AI‑KG preset is tuned for the AI‑KG schema, traversal helper, and 3D styling so it can be re-applied without losing visualization or query-path semantics |
