# todo-log

- **Canonical directive**: **STRICTLY COMPLY** with one-row-one-directive (Max 50 words)
- **Table cells**: MUST fill-up all cells in the table; FORBID empty/`-` cells.

## 2026-03-31

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Canvas / Multi-dimensional Table / RxDB | Document Multi-dimensional Table and RxDB GraphTableDb as JSON backbone | Update cross-repo docs to describe Multi-dimensional Table as RxDB-backed GraphTableDb over JSON GraphData, clarify nodes/edges tables, property columns, and sync gating, and forbid alternate table backends or non-JSON persistence paths. | `knowgrph/docs/documents/knowgrph-graph-data-table-fast-grid-document.md`, `huijoohwee.github.io/schema/AgenticRAG/README.md`, `knowgrph/todo-log.md` | Cross-repo GraphTableDb + Multi-dimensional Table docs | GraphTableDb + Graph Data Table workspace | GraphData + schema + workspace view + Interaction/Sync modes | Updated JSON-based Multi-dimensional Table docs + aligned schema text | Document only code-backed RxDB GraphTableDb behavior; keep JSON table contract neutral; forbid additional persistent backends. | Run `npm -C knowgrph/canvas run doc:lint`, `npm -C knowgrph/canvas run doc:sanity`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-03-31 |

## 2026-03-30

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Canvas / 3D Voxel Mode | Keep Voxel Mode docs aligned with D3 Bipartite XY parity | Document Voxel Mode as a 3D D3-Bipartite sub-mode: XY parity on the ground plane, Z columns forbidden, layout-mode keyed cache, and view-only toggles across 2D/3D/Voxel and semantic/layout modes. | `knowgrph/docs/documents/knowgrph-renderer-document.md`, `knowgrph/todo-log.md`, `huijoohwee.github.io/schema/AgenticRAG/{README.md,canvas.jsonld}` | Cross-repo Voxel Mode docs + AgenticRAG canvas contract | Update Markdown + JSON-LD text | Implemented voxel XY parity + ground-plane Z=0 + cache-key fixes | Updated docs + schema text + sync_map alignment | Treat voxel as a view-only 3D sub-mode that reuses D3 Bipartite seeds and SSOT layout caches; forbid voxel-specific derivation pipelines. | Run `npm -C knowgrph/canvas run doc:lint` then `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-03-30 |

## 2026-03-29

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Canvas / Edge Types | Keep edge-type docs aligned with global renderer SSOT | Document global Edge Types (bezier, straight, step, smoothstep) as schema.layout.edges.type SSOT and describe how FloatingPanel Edge Types and AgenticRAG edge properties drive cross-renderer visuals without affecting graph semantics. | `knowgrph/docs/documents/knowgrph-renderer-document.md`, `knowgrph/todo-log.md`, `huijoohwee.github.io/schema/AgenticRAG/{README.md,edge-schema.jsonld,canvas.jsonld}` | Cross-repo Edge Types docs + AgenticRAG schema | Update Markdown + JSON-LD text | Implemented global edgeTypes utility + renderer wiring | Updated docs + schema text + sync_map alignment | Document only code-backed global edge-type behavior; keep schema fields structural-only; forbid renderer-specific edge semantics or duplicate per-surface type switches. | Run `npm -C knowgrph/canvas run doc:lint`, then `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-03-29 |
| Docs / Canvas / 2D D3 + 3D Animation | Document radial orbit animation + toolbar Animation switch | Update cross-repo docs+schema to describe 2D D3 radial Animation modes (Force-directed vs Orbit) and 3D globe-based animation, including toolbar Animation toggle, applicability guards, and SSOT layout/derivation invariants; forbid per-renderer drift, duplicate pipelines, and animation-specific recompute loops. | `knowgrph/docs/documents/{knowgrph-renderer-document.md,knowgrph-graph-canvas-layout-ssot.md}`, `knowgrph/todo-log.md`, `huijoohwee.github.io/schema/AgenticRAG/{README.md}`, `sandbox/demo/knowgrph-animation-demo.md` | Cross-repo 2D/3D animation docs | Update Markdown text | Implemented 2D D3 radial orbit animation, toolbar Animation switch, and 3D globe-based animation | Updated docs + demo markdown | Treat Animation as view-only: preserve SSOT GraphData/layout; restrict Orbit mode to D3 radial with bounded motion and no re-derivation loops. | Run `npm -C knowgrph/canvas run doc:lint` and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-03-29 |

## 2026-03-28

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Canvas / 2D D3 Bipartite super-groups | Keep Bipartite layout + group hierarchy docs aligned with implementation | Update cross-repo docs+schema for 2D D3 Bipartite: document left/right lane layout, nested super-group→group→member hierarchy built from kg:subgraphs, and visibility invariants for clusters/headers/edges; forbid domain-specific rules, duplicate pipelines, recompute loops, or renderer drift. | `knowgrph/docs/documents/knowgrph-renderer-document.md`, `knowgrph/todo-log.md`, `huijoohwee.github.io/schema/AgenticRAG/{README.md,canvas.jsonld}` | 2D D3 Bipartite renderer docs + AgenticRAG canvas contract | Update Markdown + JSON-LD text | Implemented Bipartite layout + super-group/group hierarchy | Updated docs + schemaVersion bump + cross-repo invariants | Document only code-backed behavior; keep hierarchy neutral and renderer-agnostic; avoid duplicating layout pipelines; run `npm -C knowgrph/canvas run doc:lint` and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-03-28 |

## 2026-03-23

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Export / HTML Canvas / Standalone fidelity | Make exported `.html` visually match in-app Canvas without server routes | Rewrite SVG and overlay asset URLs for standalone HTML export: unwrap local proxy endpoints (`/__fetch_remote`, `/__webpage_proxy`, `/__webpage_asset_path`) to absolute URLs; inline `/__repo_file` assets to `data:*;base64` up to a bounded per-asset max; add missing CSS vars (media header bg); forbid exported HTML containing stale proxy-only paths. | Historical path snapshot: `canvas/src/components/BottomPanel/markdownWorkspace/main/exports/exportHtmlCanvas.ts`, `canvas/src/lib/graph/htmlViewer/{buildGraphHtmlViewerMarkup.ts,rewriteSvgMarkupForStandaloneHtmlExport.ts,standaloneAssetRewrite.ts}` | HTML Canvas export pipeline + standalone viewer | `exportHtmlCanvasFromWorkspace`, `rewriteSvgMarkupForStandaloneHtmlExport`, `buildGraphHtmlViewerMarkup` | GraphData + Schema + SVG snapshot | Standalone `.canvas-2d.html/.canvas-3d.html` with correct media and styles | For each SVG `href/src`: unwrap proxies; inline repo files if fetchable and within size cap; otherwise keep absolute URL; avoid per-frame work by doing all rewriting at export-time. | Smoke: export a doc that contains repo-file images + remote images + rich media panels; open exported HTML with no dev server and confirm media renders and pan/zoom works. | 2026-03-23 |
| Export / HTML Canvas / Fidelity and Animation | Ensure exported HTML matches 2D D3 styles and animations | Apply `useGraphCanvasStyles` visual fidelity (strokes, colors, `kgNodeBob` animations) to the generated SVG elements before capturing the snapshot; refactor style application into a shared standalone function. | `canvas/src/components/GraphCanvas/useGraphCanvasStyles.ts`, `canvas/src/lib/graph/htmlCanvasSvgExport.ts` | GraphCanvas styles + SVG export | `applyGraphCanvasStyles2d`, `renderGraphCanvasSvgForHtmlExport` | SVG D3 selections + Schema | SVG snapshot with CSS variables, animations, and accurate strokes | Call the shared style applicator after simulation ticks so exported DOM is fully styled and parity with workspace is maintained. | Export a 2D canvas with node animations enabled and confirm bobbing and strokes appear in the HTML file. | 2026-03-23 |
| Export / HTML Canvas / Full Parity Elements | Ensure exported HTML contains all DOM overlays like Markdown blocks, tables, blockquotes | Inject `[data-kg-markdown-design-block]` DOM elements as `<foreignObject>` inside the SVG snapshot `<g data-kg-layer="root">` during capture; apply `HTML_STYLE_PROPS` computed styles to preserve visual parity without needing React at runtime. | `canvas/src/lib/graph/svgSnapshot.ts`, `canvas/src/features/markdown-edgeless/MarkdownDesignOverlay.tsx` | SVG Snapshot + Markdown Overlay | `buildViewportSvgMarkupFromElement`, `MarkdownDesignOverlay` | DOM elements + bounding boxes | SVG snapshot containing cloned HTML elements inside `<foreignObject>` | Before serializing the cloned SVG, query for markdown overlay blocks in the DOM, clone them, attach computed HTML styles, and append them as `<foreignObject>`s using world coordinates so they pan/zoom natively in the standalone viewer. | Smoke test: open a markdown doc with a table or blockquote, export to HTML, and confirm the table is visible and pans/zooms correctly. | 2026-03-23 |
| Docs / Performance patterns / Keyword Mode | Keep docs+schema aligned with bounded performance surfaces | Update cross-repo docs+schema to reflect existing debounce, bounded worker parsing, LRU caching, idle yielding, and opt-in perf metrics; forbid docs claiming non-existent behavior; keep all metrics structural-only and optional. | `knowgrph/docs/documents/{knowgrph-pipeline-deep-dive-document.md,knowgrph-parser-document.md}`, `knowgrph/todo-log.md`, `huijoohwee.github.io/schema/AgenticRAG/{metadata.jsonld,keyword-mode.jsonld,parser.jsonld}` | Cross-repo documentation + AgenticRAG schema | Update Markdown + JSON-LD contract text | Existing Canvas implementations | Updated docs + schemaVersion bumps | Prefer code-derived SSOT; record stable keys and caching surfaces; only document behavior already implemented; preserve schema neutrality (no domain semantics). | Run `npm -C knowgrph/canvas run doc:lint`, `npm -C knowgrph/canvas run doc:sanity`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-02-20 |

## 2026-02-23

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Import→Render / In-repo artifacts / Cross-repo search | Avoid absolute-path coupling and speed up pipeline debugging | Serve repo-relative artifacts via dev/preview `__codebase_file`; load markdown-pipeline artifacts via this route; add a bounded cross-repo search CLI; update docs to remove user-specific `file:///Users/...` links. | `knowgrph/canvas/{vite.config.ts,src/features/panels/hooks/markdownPipelineActions.ts,src/cli/codebase-search.ts}`, `knowgrph/docs/documents/knowgrph-pipeline-deep-dive-document.md`, `huijoohwee.github.io/schema/AgenticRAG/README.md`, `knowgrph/todo-log.md` | Vite middleware + pipeline loader + CLI tooling | `createCodebaseFileHandler`, `fetchTextFromRelPath`, `codebase-search.ts` | repo-relative `path`, search `query` | Repo text artifacts + bounded match list | Reject traversal/absolute paths; cap results; fall back to `/@fs` only when the endpoint is unavailable. | Run `npm -C knowgrph/canvas test`, then `npm -C knowgrph/canvas run doc:lint` and `npm -C knowgrph/canvas run doc:sanity`. | 2026-02-23 |

## 2026-02-24

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Canvas / Infinite canvas interaction performance | Keep cross-repo docs+schema aligned with the implemented interaction SSOT | Update docs+schema: Interaction floating panel (mode/layout/center/distribute/perf) is SSOT, paired right of Props. Remove Arrange overlays/tabs from docs. Document pan-vs-drag gating and opt-in perf overlay. Forbid duplicate/legacy panels and recompute/rerender loops. | `knowgrph/docs/documents/{knowgrph-infinite-canvas-interaction-performance-document.md,knowgrph-canvas-document.md,knowgrph-frontend-document.md}`, `huijoohwee.github.io/schema/AgenticRAG/{panels.jsonld,README.md}`, `knowgrph/todo-log.md` | Cross-repo documentation + AgenticRAG panel contract | Update Markdown + JSON-LD contract text | Implemented floating Interaction panel and removal of Arrange surfaces | Updated docs + schemaVersion bump | Document only code-backed behavior; keep single-surface controls; forbid duplicate UI paths and background work. | Run `npm -C knowgrph/canvas run doc:lint`, `npm -C knowgrph/canvas run doc:sanity`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-02-24 |
| Docs / Canvas / Interaction viewport+scope mapping | Keep Interaction tab + viewport readout aligned with settings and AgenticRAG scope map | Update cross-repo docs+schema: Interaction tab owns viewport, Interaction, Centering/Centroid, Even Spread, and Performance controls; Viewport section surfaces all zoom/pan/wheel/Flow settings as read-only SSOT fields. Map these controls to AgenticRAG panel+settings schema (Canvas Zoom Modes, Wheel Zoom, Flow Zoom, Flow Editor, Viewport Controls). Forbid stale docs that reference legacy overlays, missing fields, or alternative Interaction UIs. | `knowgrph/docs/documents/{knowgrph-infinite-canvas-interaction-performance-document.md,knowgrph-renderer-document.md,knowgrph-canvas-document.md,knowgrph-local-storage-document.md}`, `knowgrph/todo-log.md`, `huijoohwee.github.io/schema/AgenticRAG/{README.md,canvas.jsonld,panels.jsonld,settings.jsonld}` | Cross-repo documentation + AgenticRAG scope mapping | Update Markdown + JSON-LD contract text | Implemented Interaction tab Viewport groups + collapsible sections + store-backed settings | Updated docs + schemaVersion bumps and sync_map alignment | Use Interaction tab as scope SSOT; document the mapping from UI groups to settings keys; keep schema/tooltips synchronized and forbid parallel Interaction specs in other panels. | Run `npm -C knowgrph/canvas run doc:lint`, `npm -C knowgrph/canvas run doc:sanity`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-02-24 |
| Git / Rebase / Conflict Resolution | Restore clean main↔origin/main sync without losing renderer invariants | Resolve divergent branches via rebase; fix conflicts in `scene.ts` by preserving collision relaxation + fit policy and removing duplicate init transforms; never hand-merge `tsconfig.tsbuildinfo` (regenerate via build). | `knowgrph/canvas/src/components/GraphCanvas/scene.ts`, `knowgrph/canvas/tsconfig.tsbuildinfo`, `knowgrph/todo-log.md` | Git workflow | `git pull --rebase`, `git rebase --continue`, `tsc -b` | Divergent branches + conflict markers | Clean history + synced `main` | Prefer rebase over merge; keep SSOT semantics (idempotent init transform + stable fit); regenerate build-info outputs. | Run `npm run lint && npm run check && npm test`, then `git push`; if `tsconfig.tsbuildinfo` conflicts recur, consider ignoring it. | 2026-02-20 |
| Docs / Editor workspace / Markdown Viewer | Keep Editor workspace docs aligned with implemented Markdown UI behavior | Update cross-repo docs+schema: Viewer `article` width stays 80% of Viewer `section` across Explorer/Canvas toggles and Split view; `Save As...` supports Markdown/JSON/JSON-LD/PDF exports; PDF export uses non-popup print pipeline; persist canvas pane open state via `kg:ui:workspace:canvasPaneOpen`. | `knowgrph/docs/documents/{knowgrph-canvas-document.md,knowgrph-markdown-ssot-ui-contract-document.md,knowgrph-local-storage-document.md}`, `knowgrph/todo-log.md`, `huijoohwee.github.io/schema/AgenticRAG/{markdown.jsonld,panels.jsonld}` | Cross-repo documentation + AgenticRAG schema | Update Markdown + JSON-LD contract text | Implemented Editor workspace Markdown UI behavior | Updated docs + schemaVersion bumps | Document only code-backed behavior; keep Markdown header SSOT; treat layout toggles as UI-only; persist via canonical LS keys. | Run `npm -C knowgrph/canvas run doc:lint`, `npm -C knowgrph/canvas run doc:sanity`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-02-20 |
| Docs / Canvas / 2D Renderer / D3 Keyword Mode | Keep Keyword layout stable and anchored to Document baseline | Update cross-repo docs: D3 Keyword Mode reuses Document Structure baseline seeding; detect unstable cached positions; override skip-initial-layout to force bounded relayout; apply post-fit-to-viewport to keep Nodes/Clusters/Edges centered and prevent uncontrolled expansion. | `knowgrph/docs/documents/{knowgrph-renderer-document.md,knowgrph-canvas-default-initialization-document.md}`, `knowgrph/todo-log.md`, `huijoohwee.github.io/schema/AgenticRAG/{README.md,canvas.jsonld}` | Cross-repo documentation + AgenticRAG schema | Update Markdown + JSON-LD contract text | Implemented `scene.ts` Keyword layout stabilization | Updated docs + schema contract text | Anchor to Document baseline; if instability detected then force relayout + post-fit; derive visibility once (SSOT) and reuse for layout/fit/render. | Run `npm -C knowgrph/canvas run doc:lint`, `npm -C knowgrph/canvas run doc:sanity`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-02-20 |
| Canvas / 2D Renderer switching / Zoom state isolation | Prevent renderer switch from reusing incompatible zoom/camera transforms | Isolate zoom view keys by 2D renderer variant (D3/Flow/Design/Flow Editor) while still including semantic mode and schema layout fingerprint; forbid cross-renderer zoom state contamination that can push Nodes/Clusters/Edges offscreen or create “messy” fits after switching. | `knowgrph/canvas/src/components/GraphCanvas/zoomViewKey.ts`, `knowgrph/canvas/src/__tests__/zoomViewKeySharedAcross2dRenderers.test.ts`, `knowgrph/docs/documents/knowgrph-renderer-document.md`, `knowgrph/todo-log.md` | Zoom view key SSOT | `buildZoomViewKey` | `canvasRenderMode=2d`, `canvas2dRenderer`, schema fingerprint | Per-renderer zoom view keys | Include `canvas2dRenderer` only when `canvasRenderMode=2d`; preserve other key parts unchanged to keep per-mode zoom isolation. | Switch D3↔Flow↔Design↔Flow Editor and confirm each restores its own zoom without pushing the graph offscreen. | 2026-02-20 |

## 2026-02-22

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Canvas / Layout+Init SSOT | Keep viewport/layout behavior consistent across all renderers/modes | Update cross-repo docs: reuse layerOrder2d, collision relax (Flow+D3+Design), preset-driven pan/zoom, and bounded cache writes; forbid overlapping layouts, infinite recompute loops, and preset overrides. | `knowgrph/todo-log.md`, `knowgrph/docs/documents/*`, `huijoohwee.github.io/schema/AgenticRAG/{README.md,canvas.jsonld}` | Cross-repo documentation | Update Markdown + JSON-LD contract text | Implemented layout+init parity changes | Updated docs + schemaVersion bump | Document only code-backed behavior; keep SSOT keys stable; batch store writes; gate inactive surfaces; bound relax steps by size. | Run `npm -C knowgrph/canvas run doc:lint`, `npm -C knowgrph/canvas run doc:sanity`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-02-22 |
| Docs / Canvas / 2D Renderer / Design | Keep 2D Design renderer docs aligned with grid and drag behavior | Update cross-repo docs+schema to document viewport-responsive frame grid, design-local auto-fit signatures, and batched frame-drag commits with visual-only in-flight transforms; forbid docs claiming fixed columns or per-frame write behavior. | `knowgrph/docs/documents/knowgrph-renderer-document.md`, `knowgrph/todo-log.md`, `huijoohwee.github.io/schema/AgenticRAG/canvas.jsonld` | Cross-repo documentation + AgenticRAG schema | Update Markdown + JSON-LD contract text | Implemented DesignCanvas grid and drag behavior | Updated docs + schemaVersion bump | Document only code-backed Design behavior; keep contracts renderer-neutral; describe grid+drag as SSOT rules. | Run `npm -C knowgrph/canvas run doc:lint`, `npm -C knowgrph/canvas run doc:sanity`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-02-22 |
| Canvas / Zoom / Reset | Make Reset reliably frame the whole graph | Redefine toolbar Reset as Fit-to-View framing: center on collective centroid and fit into a capped 16:9 frame; forbid reset forcing `k=1` when content is large or offscreen. | `knowgrph/canvas/src/lib/zoom/actions.ts`, `knowgrph/docs/documents/{knowgrph-renderer-document.md,knowgrph-canvas-default-initialization-document.md}`, `huijoohwee.github.io/schema/AgenticRAG/canvas.jsonld` | Zoom actions SSOT | `computeZoomTransformFromRequest` | zoomRequest=`reset` + viewport dims | Centered fit transform | Reset calls the same fit pipeline as Fit-to-View (schema-driven, group-aware bounds); no layout mutations. | Click Reset across D3/Flow/Design/Flow Editor and confirm graph stays in view without jitter. | 2026-02-22 |
| Canvas / Collision / Determinism | Prevent collision avoidance from degrading layout | Enforce deterministic, local-only collision relax: seed RNG by node ids; clamp displacement to avoid teleporting; throttle expensive passes to prevent thrash; forbid non-deterministic `Math.random` initializers and feedback loops. | `knowgrph/canvas/src/components/{GraphCanvas/sceneHandlers.ts,GraphCanvas/layout/relax.ts,FlowCanvas/relaxPositions.ts}`, `knowgrph/docs/documents/knowgrph-native-overlap-resolution-document.md`, `huijoohwee.github.io/schema/AgenticRAG/canvas.jsonld` | Collision relax SSOT | `relaxNodesWithCollision`, `relaxFlowPositionsWithCollision` | Nodes/groups + schema collision knobs | Stable, non-overlapping layout | Use bounded steps + max-shift clamps; initialize forces with seeded RNG for reproducible output. | Validate no overlaps and no drift after repeated toggles/zooms; run tests. | 2026-02-22 |
| Canvas / Flow / Packing+Labels | Keep subgraphs and labels readable in Flow layouts | Make Flow collective packing group-aware and label-safe: pack connected components using edges + group membership; gate edge-label placement by zoom/size; forbid label placement work when positions are not ready or graph is too large. | `knowgrph/canvas/src/components/{FlowCanvas/useFlowComputedPositions.ts,GraphCanvas/layout/collectivePackPositions.ts,FlowCanvas/nativeRuntime.ts}`, `knowgrph/docs/documents/{knowgrph-renderer-document.md,knowgrph-graph-canvas-layout-ssot.md}` | Flow layout + label placement | `packDisjointPositions2d`, `drawEdgeLabels` | Flow scene graph + schema/layout options | Cohesive groups + readable labels | Treat group membership as connectivity for packing; run edge labels only when stable and zoomed-in enough. | Load a sparse grouped graph and confirm groups stay together and labels don’t overlap nodes. | 2026-02-22 |
| Canvas / Import→Render / Workers + Layout Init | Reduce churn and prevent mode-switch layout drift | Respect stable cached layout positions during init; only seed missing positions or repair unstable/extreme layouts. Make singleton worker requests abortable so stale derives don’t leak results or UI warnings. Keep timeouts bounded and never hang. | `knowgrph/canvas/src/{lib/workers/singletonWorkerClient.ts,components/GraphCanvas/scene.ts,components/GraphCanvas/layout/initialization.ts,hooks/useActiveGraphData.ts,features/parsers/loader.ts}`, `huijoohwee.github.io/schema/AgenticRAG/canvas.jsonld` | Layout init SSOT + worker client | `initializeGraphLayout`, `requestFromSingletonWorker` | Cached positions + AbortSignal | Stable layout + canceled stale work | If cached positions stable → apply+return; else reseed/repair; abort resolves null and suppresses stale toasts. | Run `npm -C knowgrph/canvas run check` and `npm -C knowgrph/canvas run test:ci`. | 2026-02-22 |

## 2026-02-21

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Canvas / 2D renderers / Viewport fit | Enforce collective fit+center across 2D renderers | Enforce collective fit+center (not per-node) across D3, Flow, Design, and Flow Editor: fit uses display-derived graph bounds and includes node dimensions plus group envelopes (clusters/subgraphs/layers) so the visible graph is fully in-viewport and centered. | `knowgrph/canvas/src/components/{GraphCanvas.tsx,FlowCanvas.tsx,DesignCanvas.tsx}`, `knowgrph/canvas/src/components/GraphCanvas/{fit.ts,layout/fitConfig.ts}`, `knowgrph/canvas/src/lib/zoom/actions.ts`, `knowgrph/docs/documents/*`, `huijoohwee.github.io/schema/AgenticRAG/*` | Zoom+fit SSOT + cross-repo docs | `fitAllTransform`, `readFitAllOptions`, `computeZoomTransformFromRequest` | Visible graph (post filters/collapse) + viewport dims | Centered fit transform per renderer | Compute bounds from display graph; include `visual:width/height` when present; expand bounds by group label/shape padding; center on collective bbox. | Run `npm -C knowgrph/canvas test` and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-02-21 |
| Docs / Canvas / 2D Flow renderer | Align Flow docs with baseline lock + key isolation | Update docs: 2D zoom view keys are per-variant; Flow/Flow Editor disable auto zoom under baseline lock; Flow reuses SSOT visibility and collective fit (no D3 forces). Bump `canvas.jsonld` schemaVersion. | `knowgrph/docs/documents/knowgrph-renderer-document.md`, `huijoohwee.github.io/schema/AgenticRAG/canvas.jsonld`, `knowgrph/todo-log.md` | Cross-repo documentation | Update Markdown + JSON-LD directive text | Implemented 2D Flow parity | Updated docs + schema bump | Isolate zoom keys by renderer; respect baseline lock and fit parity; keep layout caches isolated. | Run `npm -C knowgrph/canvas run doc:lint && npm -C knowgrph/canvas run doc:sanity`; JSON-parse `canvas.jsonld`. | 2026-02-21 |

## 2026-02-19

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Canvas / 2D renderer initialization | Keep 2D initialization consistent across variants | Update cross-repo docs: 2D init is bounds-guarded + idempotent; skip invalid positions; stable node dims for fit; warm-mount effect gating. | `knowgrph/todo-log.md`, `knowgrph/docs/documents/{knowgrph-renderer-document.md,knowgrph-canvas-document.md,knowgrph-canvas-default-initialization-document.md,knowgrph-flow-editor-pan-zoom-overlay-failsafe-document.md}`, `huijoohwee.github.io/schema/AgenticRAG/{canvas.jsonld,README.md}` | Cross-repo documentation | Update Markdown + JSON-LD contract text | Renderer init invariants | Updated docs/contract text | Enforce SSOT parity across D3/Flow/Design/Flow Editor; forbid double-fit jumps; guard bounds before applying stored transforms; ignore invalid geometry to prevent one-long lines. | Run `python3 schema/AgenticRAG/sync_map.py --mode check` to validate docs↔schema alignment. | 2026-02-19 |
| Canvas / Flow Editor / Node Quick Editor / Layout Initialization | Prevent initial clustering and enforce centered layout | Fix Flow Editor Node Quick Editor initialization: calculate a centered grid layout for unpinned editors instead of top-left stacking; update collision relaxation to center the grid; forbid clustering/overlaying on one corner. | `canvas/src/components/FlowEditorCanvas.tsx` | FlowEditorCanvas | `scheduleOverlayCollisionResolve`, `seedGridAroundFixed` | Initial overlay node list + viewport dims | Centered, non-overlapping grid layout | Count unpinned items; compute grid cols/rows; calculate start offsets to center the grid in viewport; use centered grid for fallback positions and relaxation seeding. | Verify initial open of multiple Quick Editors shows a centered, spread-out layout. | 2026-02-19 |
| Canvas / 2D Renderer / D3 Initialization | Prevent initial clustering and off-center layout | Fix D3 renderer initialization: default to 16:9 preset when viewport is small; update centering forces on resize to respect new dimensions; strict validity check for heuristic seeding to force re-layout if nodes are clustered. | `canvas/src/components/GraphCanvas/{simulation.ts,GraphCanvas.tsx}`, `canvas/src/components/GraphCanvas/layout/heuristic-cluster.ts` | Simulation + GraphCanvas | `buildSimulation`, `updateForceSimulationPresentation`, `applyClusterAwareHeuristicSeedLayout` | Initial nodes/edges + viewport dims | Centered, well-spread layout | Use preset dims if width < 100; update x/y forces in presentation update; reject clustered positions in heuristic check. | Verify D3 graph starts centered and spread out even if initial viewport is small/zero. | 2026-02-19 |
| Canvas / Layout / Default Collision & Viewport | Eliminate clustering and artificial 16:9 constraints | Remove artificial 16:9 viewport constraints from simulation and fit logic; use actual container dimensions. Increase default collision padding to 20px, strength to 0.8, and charge to -800 to prevent clustering. | `canvas/src/lib/graph/layoutDefaults.ts`, `canvas/src/components/GraphCanvas/{simulation.ts,fit.ts}`, `canvas/src/components/GraphCanvas/layout/{fitConfig.ts,radial.ts}` | Layout Defaults + Simulation + Fit | `buildSimulation`, `fitAllTransform` | Viewport dims + Graph Schema | Centered, spread-out graph filling available space | If container > 100px, use actual dims; else fallback. Stronger repulsion + larger padding guarantees node separation. | Verify graphs initialize centered and spread out in non-16:9 containers (e.g. floating panel). | 2026-02-19 |
| Keyword Mode / Stats Sync / Pipeline | Ensure Keyword Mode stats sync and improve layout | Fix stats sync in MainPanel by using `useActiveGraphData`; improve D3 layout defaults (charge, link distance, center strength) to prevent clustering; disable box force by default. Increased collision padding to 80px and link distance to 350 to accommodate Flow Editors; added `forceCenter` for strict centering. Adjusted alpha targets to prevent infinite loops/re-computation churn. Refactored `fitAllTransform` to use node dimensions + padding for bounding box calculation instead of raw points, ensuring Flow Editor nodes are fully visible without zoom-in. Added min/max zoom caps for initial fit to prevent tiny graphs. Added fallback to ensure initial fit is always applied even if no explicit transform is present. | `canvas/src/features/panels/{MainPanel.tsx,WorkflowSection.tsx}`, `canvas/src/lib/graph/layoutDefaults.ts`, `canvas/src/components/GraphCanvas/{simulation.ts,GraphCanvas.tsx,fit.ts,scene.ts}` | MainPanel + Simulation + Fit | `useActiveGraphData`, `buildSimulation`, `fitAllTransform`, `applyInitialTransform` | Mode toggle + graph data | Synced stats + spread-out layout | Switch to `useActiveGraphData` to ensure all panels see the correct graph; increase repulsion (-1200) and collision padding (80px) to allow natural spread; enforce centering; reduce alpha target (0.12/0.02) to stabilize simulation faster; use bbox-based fit for initial view with safe min/max scale caps; force initial fit if transform is missing. | Verify Dashboard stats match Keyword Mode nodes; verify graph initializes centered and spread out; verify no infinite re-rendering loops. | 2026-02-19 |

## 2026-02-17

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Graph Data Table / Cross-repo contract | Keep Graph Data Table docs accurate across repos | Update Graph Data Table docs across repos: distinguish host fast-grid vs singabldr curation table; host fast-grid uses single scroll owner + DOM header overlay synced to body scroll; persist host column order via `kg:ui:graphTable:columnOrderByTableId`; keep frozen bands/width-sharing and empty-graph behavior specified for curation table. | `knowgrph/docs/documents/{singabldr-graph-data-curation-document.md,knowgrph-graph-data-curation-document.md}`, `huijoohwee.github.io/schema/AgenticRAG/{README.md,panels.jsonld}`, `knowgrph/todo-log.md` | Cross-repo documentation | Update Markdown + JSON-LD contract text | Graph Data Table behavior updates | Updated docs/contract text | Clarify ownership boundaries and UI behavior contracts; document drift-prone details as SSOT rules. | Run `python3 schema/AgenticRAG/sync_map.py --mode check` and ensure docs map stays consistent. | 2026-02-17 |
| Graph Data Table / Fast Grid / Column reorder + stale-cache prevention | Make Graph Data Table reorderable and consistent with Source Files state | Implement drag-to-reorder columns in host fast-grid header with drop hint; persist via sparse RxDB column ordering; fix per-table row cache isolation; ensure sourceFiles mutations recompute composed graph so empty Source Files yields empty tables (no stale rows). | `canvas/src/features/{graph-table/ui,graph-table-db,source-files}`, `canvas/src/__tests__/graphTableTypography.test.tsx` | GraphTableFastGrid + GraphTableDb + SourceFilesPersistenceBootstrap | `reorderGraphTableColumn`, `drawGrid`, `SourceFilesPersistenceBootstrap` | Header pointer drag + sourceFiles mutations | Reordered columns + cleared tables when no sources | Use pointerDrag + hitTest to compute target; patch only moved column order when possible; subscribe to sourceFiles and recompute graph on change. | Smoke: remove all Source Files, confirm graph/tables empty; drag header to reorder and reload to confirm persistence. | 2026-02-17 |
| Graph Data Table / Import→Render / DateEditor | Make date editing work end-to-end from imports | Infer ISO-like date property columns as `kind: date` (upgrade from text when safe) so DateEditor opens for imported values; key RxDB sync by revision + collapsedGroupIds; forbid mode-derived sync rewrites and no-op churn. | `canvas/src/features/{graph-table-db,graph-table/ui,graph-table/hooks}`, `docs/documents/knowgrph-graph-data-table-fast-grid-document.md` | GraphTableDb + GraphTableWorkspace | `inferKind`, `ensureColumnsForRowData`, `useGraphTableDbSync` | Imported GraphData + collapse toggles | Date columns + stable sync | Only label as date when all non-empty values are ISO-like; include viewKey in sync gate to avoid missed updates/loops. | Import a graph with `created: 2026-02-17`, open table, edit date, toggle group collapse; confirm stable table. | 2026-02-17 |

## 2026-02-18

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Workspace Layout / Editor + Graph Data Table / Canvas SSOT | Remove duplicate canvas previews and keep one live SVG | Make Editor workspace and Graph Data Table reuse a single in-app Canvas pane (`CanvasViewport`) and forbid any second “Canvas Preview” surface in these modes; reserve `?kgPreview=1`/`data-kg-preview="1"` iframe rendering for external embedded preview only. | `canvas/src/pages/Canvas.tsx`, `canvas/src/components/{EmbeddedEditorShell.tsx,CanvasViewport.tsx}`, `docs/documents/{knowgrph-canvas-document.md,knowgrph-ui-ux-design-document.md,knowgrph-local-storage-document.md}`, `huijoohwee.github.io/schema/AgenticRAG/panels.jsonld` | CanvasPage + Workspace mode docs | `CanvasPage`, `CanvasViewport` | Workspace view mode toggles | One live Canvas SVG resized into right pane | If mode is Editor or Table: render left tool surface + right canvas pane; do not mount a second Canvas or iframe preview; persist canvas pane width via `LS_KEYS.workspacePreviewWidthPx`. | Run `npm -C knowgrph/canvas test:ci` and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` to validate contract alignment. | 2026-02-18 |
| Flow Editor / Node Quick Editor / Drag behavior | Align pinned/unpinned drag behavior and docs across repos | Document pinned header drag as anchor-offset update that moves all pinned overlays together; unpinned drag moves freely in viewport and persists position; avoid bounce/pan proxy behavior. | `knowgrph/docs/documents/{knowgrph-flow-editor-node-quick-editor-document.md,knowgrph-renderer-document.md,knowgrph-canvas-document.md,knowgrph-graph-data-table-fast-grid-document.md}`, `huijoohwee.github.io/schema/AgenticRAG/{panels.jsonld,canvas.jsonld}`, `knowgrph/todo-log.md` | Node Quick Editor contract | Documentation updates | Flow Editor drag requirements | Updated docs and schema contracts | Keep pinned overlays node-anchored while allowing collective offset drag; keep unpinned overlays free and clamped. | Run `npm -C knowgrph/canvas run doc:lint` and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` to validate docs sync. | 2026-02-18 |

## 2026-02-15

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Import→Render / Local In-Repo URL / Performance | Support local file imports and optimize rendering | Support local in-repo Import URL + assets via `/__webpage_proxy`, `/__webpage_asset_proxy`, `/__repo_file/*`; render HTML/JSON via sandboxed `srcdoc`; cap HTML size and idle-yield sanitization; keep progress for large imports. | `canvas/vite.config.ts`, `canvas/src/lib/websites/webpageIframeSrcdoc.ts`, `docs/documents/knowgrph-webpage-proxy-and-srcdoc-iframe-document.md` | Vite middleware + Srcdoc builder | `createWebpageProxyHandler`, `createRepoFileHandler`, `fetchWebpageHtmlViaProxy` | Local file path + View mode | Sandboxed `srcdoc` rendering of local files + smooth UI | Resolve paths relative to repo root; block traversal; use `__repo_file` baseHref for relative assets; yield during sanitization. | Import `sandbox/test-data/my-site/index.html` and confirm CSS/images load in iframe without freezes. | 2026-02-15 |
| Import→Render / Webpage JS-render fidelity (native) | Remove headless browser dependencies while preserving JS-rendered content capture | Replace Playwright-based rendering with a browser-native, sandboxed iframe DOM export bridge (`kg-export-dom`) and use it to upgrade webpage Markdown when static conversion is incomplete; keep the approach domain-neutral and bounded, and forbid external headless browser dependencies. | Historical path snapshot: `canvas/src/lib/websites/{webpageDomExport.ts,webpageClientConvert.ts,webpageMarkdownQuality.ts,webpageIframeSrcdoc.ts}`, `canvas/src/components/BottomPanel/markdownWorkspace/workspaceImport.ts`, `canvas/vite.config.ts` | DOM export bridge + client convert fallback | `exportWebpageDomViaHiddenIframe`, `convertWebpageUrlToMarkdownViaBrowser`, `isLowQualityWebpageMarkdown`, `fetchWorkspaceUrlContent` | URL + bounded export options (maxChars/timeout/scrollCrawl/expandFaq) | Upgraded Markdown for JS-heavy pages + unchanged behavior for static pages | If the initial conversion is low quality, export rendered DOM text/HTML via hidden iframe → convert to Markdown → keep the better result; otherwise keep the first-pass conversion; sandbox and bounds must always apply. | Import a JS-heavy FAQ page and confirm (1) the saved Markdown includes FAQ content, (2) the HTML viewer still renders via sandboxed iframe, and (3) no Playwright install step exists in the repo or docs. | 2026-02-15 |
| Import→Render / Webpage HTML/JSON view | Enforce sandboxed iframe rendering and reduce freezes | Render `kgWebpageView∈{html,json}` via sandboxed `srcdoc`; treat HTML/JSON as view-only overlays; idle-defer `srcdoc` build; cap HTML size; sanitize by default (`webpageViewerScriptPolicy=strip`); ignore abort errors; reuse fetch caches and parse workers to prevent freezes. | Historical path snapshot: `canvas/src/components/BottomPanel/markdownWorkspace/{MarkdownWorkspaceMain.tsx,MarkdownWorkspace.tsx}`, `canvas/src/features/panels/utils/idle.ts`, `canvas/src/lib/websites/{webpageIframeSrcdoc.ts,webpageHtmlToMarkdownArtifact.ts}`, `canvas/src/lib/graph/{parseWorker.ts,parseWorker.worker.ts}`, `docs/documents/knowgrph-webpage-proxy-and-srcdoc-iframe-document.md` | Workspace iframe viewer + conversion + parse worker | `useWebpageIframeSrcdoc`, `buildWebpageHtmlSrcdocAsync`, `fetchWebpageHtmlAuto`, `convertWebpageHtmlToMarkdownArtifactAsync`, `parseGraphInWorker` | URL + view + settings | Stable iframe render + responsive UI | Prefer stored `raw.html` when present; otherwise fetch once; conversion yields to main; abort shows Cancelled; singleton worker reduces parse stalls. | Toggle Markdown→HTML→JSON rapidly during import/convert and confirm no freezes, no “Fetch is aborted”. | 2026-02-15 |
| Import→Render / Mode contract / Doc-scoped collapse | Make view switching deterministic and prevent “missing blocks” | Enforce mode contract: (1) Markdown SSOT default, (2) HTML view-only iframe, (3) JSON view-only iframe; scope collapsed headings by `activeDocumentKey`; default `websiteImportGenerateWebpageArtifactDocs=false`; generate artifact markdown only from stored `raw.html` when enabled. | Historical path snapshot: `canvas/src/components/BottomPanel/markdownWorkspace/{MarkdownWorkspaceMain.tsx,useWorkspaceFileActions.ts,workspaceImport.ts}`, `singabldr/src/features/markdown/ui/useMarkdownExplorerControls.ts` | Workspace + viewer controls | `useWebpageIframeSrcdoc`, `handleImportWebsite`, `useMarkdownExplorerControls` | Active doc + view toggle + import settings | Stable view switching + visible wireframes | Never write graph/layout/zoom on view change; HTML/JSON render `srcdoc`; doc-scoped LS key avoids cross-doc hidden sections. | Import ≤10 pages and confirm view toggles keep graph/zoom unchanged and wireframes visible. | 2026-02-15 |
| Import→Render / Webpage URL | Generic webpage conversion with fidelity and settings | Convert URL→Markdown using a browser-native, sandboxed iframe DOM export bridge with bounded proxy-fetch fallback; integrate into `sourceFilesIngestIntegration.ts` import routing; expose `webpageImportIncludeImages` + `webpageImportView` settings and keep view switching strictly view-only. | `canvas/src/lib/websites/{webpageDomExport.ts,webpageClientConvert.ts}`, `canvas/src/lib/net/remoteMarkdownConversions.ts`, `canvas/src/features/source-files/sourceFilesIngestIntegration.ts`, `canvas/src/features/settings/registry-ui.import-webpage.ts` | Webpage client converter + DOM export bridge | `convertWebpageUrlToMarkdownViaBrowser`, `exportWebpageDomViaHiddenIframe`, `fetchWebpageMarkdown` | URL + settings | Markdown (graph SSOT) + iframe HTML/JSON views | If not known ext/YouTube/PDF: convert via DOM export; if it fails, fall back to same-origin proxy fetch; apply `webpageImportIncludeImages` and persist `kgWebpageUrl/kgWebpageView` frontmatter without mutating graph/layout/zoom. | Verify Import URL for a JS-heavy page and a static page; toggle `Markdown/HTML/JSON`; confirm `[NAV]/[CTA]/[PRICE]/[TIME]` tokens remain visible. | 2026-02-15 |
| Import→Render / Local repo-relative HTML | Make local in-repo webpages render reliably | Fetch repo-relative `kgWebpageUrl` via `/__repo_file/*` in `fetchWebpageHtmlAuto`; prefer website-import `rawHtml` artifacts for both HTTP and local nodes; surface byte progress; keep HTML/JSON as sandboxed `srcdoc` view-only overlays. | Historical path snapshot: `canvas/src/lib/url.ts`, `canvas/src/lib/websites/webpageIframeSrcdoc.ts`, `canvas/src/components/BottomPanel/markdownWorkspace/MarkdownWorkspaceMain.tsx` | Repo-file URL utils + iframe srcdoc fetch | `buildRepoFilePath`, `fetchWebpageHtmlFromRepoFile`, `fetchWebpageHtmlAuto`, `useWebpageIframeSrcdoc` | Repo-relative path or HTTP URL | Sandboxed iframe render + progress | If `kgWebsiteImportId/nodeId` exists: fetch artifact first; else if HTTP: use proxy; else: `/__repo_file`; sanitize+idle-yield; forbid path traversal. | Import `canvas/index.html` via Import URL and confirm iframe loads without freezes. | 2026-02-15 |
| Canvas Preview / Flicker / Preview Sync | Stop preview flicker and reduce render churn | Prevent no-op `setGraphData` commits from bumping `graphDataRevision`; memoize `hashGraphDataForPreviewSync`; make preview iframes load with `?kgPreview=1` to avoid initial UI flash; RAF-throttle preview pane resize state updates; add no-op guard to `setCanvasRenderMode`. | `canvas/src/hooks/store/{graphDataSlice.ts,graphDataSliceUtils.ts,canvasSlice.ts}`, `canvas/src/pages/Canvas.tsx`, `canvas/src/components/EmbeddedEditorShell.tsx`, `canvas/src/features/graph-table/ui/GraphTableCanvasPreviewDock.tsx` | Store + preview iframe host | `setGraphData`, `hashGraphDataForPreviewSync`, `setCanvasRenderMode`, `EmbeddedCanvasPreviewFrame` | GraphData + preview sync messages | Stable preview without flicker | Only commit state when effective graph/mode changed; avoid per-pointermove reflow; ensure embedded preview mode is detected at load. | Type in MarkdownWorkspace with preview open and confirm no visible flicker. | 2026-02-15 |
| Webpage Artifact / Main Content / Pricing-like cards | Remove duplicate synthetic “Content” and render card blocks as grid | Mark seeded “Content” main sections as synthetic and avoid emitting the redundant fenced heading echo; coalesce consecutive plain-text card-like blocks into a Markdown table (grid-like) using neutral heuristics (short lines, no markup). | `canvas/src/lib/websites/{webpageHtmlToMarkdownArtifact.ts,webpageHtmlToMarkdownArtifactPrivate.ts}`, `canvas/src/__tests__/webpageHtmlToMarkdownArtifact.test.ts` | HTML→Markdown artifact converter | `extractMainSections`, `convertWebpageHtmlToMarkdownArtifact` | HTML page content | Cleaner artifact + table grid blocks | If section is synthetic `Content`, skip the repeated `## Content` echo; if 2–4 adjacent card blocks detected, render as Markdown table with inferred card titles. | Import `https://www.remotion.dev/` and confirm Pricing cards render as a table without duplicate Content headings. | 2026-02-15 |
| Code Maintenance / Repo size / History purge | Shrink repo size and prevent artifact churn | Purge large artifacts from git history using `git-filter-repo`: remove `.knowgrph-workspace/**`, `data/knowgrph-workflow-preview/**`, `node_modules/**`, `backups/**`, and `git-error-*`; run aggressive GC; restore `origin` and force-push rewritten `main` + tags; require fresh re-clone for collaborators. Applies across `/GitHub/{knowgrph,singabldr,gympgrph,chatgrph,sandbox}`. | `.gitignore`, `.git/filter-repo/*`, `docs/documents/knowgrph-repo-hygiene-document.md` | Git history rewrite + artifact policy | `git rm --cached`, `git filter-repo --invert-paths`, `git gc --aggressive` | Repo history + artifact paths | Small `.git` + clean status + faster clone/push | First stop tracking artifacts; then rewrite history and garbage-collect; never commit `.knowgrph-workspace` (stored under `sandbox` via symlink) and never commit `node_modules/**`. | Verify `.git` size drops and `git log` still contains feature commits; then `git push --force-with-lease`. | 2026-02-15 |
| Import→Render / Webpage HTML view / Large payloads | Prevent srcdoc failures for very large pages | Remove hard size-fail; sanitize first then apply domain-neutral shrink (strip scripts/handlers/comments; collapse whitespace; replace base64 images; drop oversized inline svg/style); only fall back to text if still too large. | `canvas/src/lib/websites/webpageIframeSrcdoc.ts`, `canvas/src/__tests__/webpageIframeSrcdocLargeHtml.test.ts` | Srcdoc builder | `buildWebpageHtmlSrcdocAsync`, `shrinkOversizedHtml` | Raw HTML + URL | Stable sandboxed iframe render | If sanitized output exceeds budget, shrink; never hardcode domains; avoid freezes via yielding. | Import a large JS app shell and confirm iframe renders (no “too large for srcdoc”). | 2026-02-15 |
| Canvas / Layout positions / Renderer toggles | Keep layout stable across D3, Flow, Flow Editor, editor ON/OFF | Make layout-position caching renderer-agnostic for `renderMode=2d` and treat 2D renderer toggles as no-op for seeding; preserve 3D variant isolation. | `canvas/src/components/GraphCanvas/layout/positioning.ts`, `canvas/src/__tests__/layoutPositioning.test.ts` | Layout cache key + seed policy | `buildLayoutPositionCacheKey`, `determineLayoutPositions` | Graph nodes with positions | No reseed on 2D renderer switch | Ignore `renderVariant` for 2D cache keys; only treat 3D variant changes as engine changes. | Toggle D3↔Flow↔Flow Editor repeatedly and confirm positions persist. | 2026-02-15 |
| Canvas / Flow Editor / Viewport controls | Restore Flow Editor pan/zoom and align gestures | Stop forcing `design` viewport preset in Flow Editor; add `flowEditorSelectionOnDrag` setting + LS key; use Shift-drag selection when disabled; keep zoom modes and wheel parity consistent across 2D renderers. | `canvas/src/components/FlowCanvas.tsx`, `canvas/src/hooks/store/canvasSlice.ts`, `canvas/src/features/settings/registry-ui.graph-and-orchestrator.ts` | FlowCanvas + GraphState | `bindFlowCanvasNativeInteractions`, `setFlowEditorSelectionOnDrag` | `canvas2dRenderer=flowEditor`, `viewportControlsPreset`, selection drag | Usable pan/zoom + optional selection-on-drag | Never override stored preset; selection-on-drag is opt-in via `flowEditorSelectionOnDrag`. | Enter Flow Editor, confirm left-drag pans, wheel zoom works; enable selection-on-drag only if desired. | 2026-02-15 |
| Markdown / Source fidelity / Round-trip | Enforce lossless MD→HTML→MD | Embed the exact Markdown source in rendered preview DOM (base64, non-executing script) and restore it during HTML→Markdown import before heuristic conversion; mark derived UI blocks as non-source. | `singabldr/src/features/markdown/ui/{MarkdownPreview.tsx,MarkdownPreviewViewer.tsx}`, `canvas/src/features/parsers/html-parser.ts` | Preview DOM payload + import guard | `encodeUtf8ToBase64`, `tryExtractLosslessMarkdownFromHtml` | Markdown text or imported HTML | Exact Markdown recovery | If embedded payload exists, return it exactly; otherwise fall back to HTML→Markdown traversal. | Copy rendered HTML and re-import; confirm Markdown matches byte-for-byte. | 2026-02-15 |
| Import→Render / Local In-Repo URL / Sitemap + Rich Media | Make local repo imports and rich embeds reliable | Treat `file://` as repo-relative in Import URL; generate `website.sitemap.md` with doc links to created page docs; promote standalone inline HTML (`svg/iframe/video/audio/img`) into HTML blocks for safe rich-media rendering; use shared progress ticker for long conversions. | Historical path snapshot: `canvas/src/components/BottomPanel/markdownWorkspace/{workspaceImport.ts,useWorkspaceFileActions.ts}`, `canvas/src/lib/websites/websiteSitemapMarkdown.ts`, `canvas/src/lib/progress/progressTicker.ts`, `singabldr/src/features/markdown/ui/markdownPreviewLexBlock.ts` | Workspace import + sitemap + markdown lex | `fetchWorkspaceUrlContent`, `handleImportWebsite`, `buildWebsiteSitemapMarkdown`, `createProgressTicker`, `buildBlockTokens` | `file://<repoRel>` + website root + markdown with `<svg>/<iframe>` | Stable local import + clickable sitemap docs + rendered embeds | Strip `file://` then enforce repo-root bounds; sitemap links use created doc paths (post-write); treat single-inline HTML blocks as block HTML for allowlisted rendering. | Import a local folder website, open `website.sitemap.md`, click `Doc` links, and verify `<svg>/<iframe>` blocks render in Markdown preview. | 2026-02-15 |
| Import→Parse / HTML↔Markdown / Unified fallback | Improve general-purpose HTML→Markdown without replacing markdown UI renderer | Add unified+rehype+remark HTML→Markdown conversion as a fallback path (prefer lossless embedded Markdown payload when present); keep Markdown UI rendering on existing markdown-it/custom token renderer (no duplication). | `canvas/src/features/parsers/html-parser.ts`, `canvas/src/lib/markdown/htmlToMarkdownUnified.ts`, `docs/documents/knowgrph-webpage-proxy-and-srcdoc-iframe-document.md` | HTML import parser + unified converter | `parseHtmlToMarkdownAllTextAsync`, `convertHtmlToMarkdownUnified` | Large HTML + optional baseUrl | Faster, more robust HTML import with idle scheduling | If payload script exists: restore exactly; else if large HTML: run unified pipeline in idle; otherwise use DOMParser traversal; always keep site-agnostic extraction. | Import a large HTML page and confirm conversion completes without UI freeze and links resolve against baseUrl. | 2026-02-15 |
| Markdown Workspace / Webpage View Modes / Fidelity | Increase HTML↔Markdown fidelity and per-doc granularity | Add per-doc webpage controls (View/Script/Imgs/Fid + Sync) in the Markdown toolbar; extend frontmatter to include script policy, include-images, and fidelity level; preserve rich media via safe HTML blocks; keep view switching and sync bounded and view-only. | Historical path snapshot: `canvas/src/components/BottomPanel/{MarkdownWorkspaceToolbar.tsx,markdownWorkspace/*}`, `canvas/src/lib/markdown/{frontmatter.ts,htmlToMarkdownUnified.ts}`, `singabldr/src/features/markdown/ui/markdownPreviewLinks.tsx` | Markdown toolbar + webpage meta | `upsertWebpageFrontmatterMeta`, `convertHtmlToMarkdownUnified` | Webpage-backed Markdown doc | Stable UI controls + richer preview | Prefer frontmatter overrides when present; apply selection-free, view-only switches; regenerate Markdown only on explicit Sync; preserve media as allowlisted HTML when fidelity is high. | Import a JS-heavy page, toggle `DOM/Raw/HTML/Markdown/JSON`, hit Sync, and confirm media blocks render and no graph/layout/zoom mutates. | 2026-02-15 |


## 2026-02-24

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Canvas / 2D D3 / Graph layout forces | Expose and document layout tuning presets | Add schema.layout.forces antiLine/postFit knobs and Floating Panel Layout controls (strength/alphaMax + strong spread preset + reset-to-default); keep toolbar Reset viewport-only and update cross-repo docs to treat these knobs as layout SSOT. | `canvas/src/lib/graph/{schema.ts,schemaTypes.ts}`, `canvas/src/hooks/useGraphStore.ts`, `canvas/src/features/toolbar/FloatingPropsPanel.tsx`, `knowgrph/docs/documents/{knowgrph-graph-canvas-layout-ssot.md,knowgrph-renderer-document.md,knowgrph-graph-elements-and-configs-semantics-document.md}`, `huijoohwee.github.io/schema/AgenticRAG/canvas.jsonld` | Graph layout schema + 2D renderer docs | `buildSimulation`, `buildKeywordSchemaPreset`, Floating Panel Layout view | Graph schema + semantic mode + renderer | Evenly spread 2D layouts with explicit, documented presets and resets | Use Floating Panel Layout controls to tune spread; confirm toolbar Reset only affects viewport and that AgenticRAG contracts describe these layout-force knobs as schema-level SSOT. | 2026-02-24 |

## 2026-02-25

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Markdown Editor SSOT / Webpage import | Keep HTML→Markdown SSOT output clean and cross-repo docs consistent | Update cross-repo docs+schema to reflect SSOT webpage Markdown conversion: prefer embedded Markdown payloads, keep output Markdown-only (no artifact wrappers, no HTML Head/YAML snapshot blocks), and document `/__codebase_file` + `/__codebase_asset` endpoints with `/__repo_file` as legacy. | `knowgrph/docs/documents/{knowgrph-markdown-ssot-ui-contract-document.md,knowgrph-markdown-editor-mode-contract-document.md,knowgrph-source-files-import-document.md,knowgrph-webpage-proxy-and-srcdoc-iframe-document.md,*}`, `huijoohwee.github.io/schema/AgenticRAG/{README.md,markdown.jsonld,settings.jsonld}`, `knowgrph/todo-log.md` | Documentation + schema contract | Doc/spec updates | Webpage URL + repo-relative path + frontmatter controls | Updated docs + schema text | Document only code-backed behavior; prefer Markdown payload extraction when present; enforce view-only HTML/JSON iframe; prefer `__codebase_*` endpoints for repo-relative reads and MIME-correct assets. | Run `npm -C knowgrph/canvas test`, then `npm -C knowgrph/canvas run doc:lint` and `npm -C knowgrph/canvas run doc:sanity`. | 2026-02-25 |

## 2026-02-26

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Markdown Editor SSOT / HTML/CSS/JS→Markdown | Keep webpage-backed Markdown neutral, URL-correct, and duplication-free | Update cross-repo docs+schema: HTML→Markdown must resolve href/src/action/poster/srcset + lazy-load data-* URLs against baseUrl, upsert missing img src from data-src/srcset, preserve grid spans and safe video/audio attrs in preview, and forbid duplicate content emissions in SSOT output. | `knowgrph/docs/documents/{knowgrph-webpage-proxy-and-srcdoc-iframe-document.md,knowgrph-markdown-editor-mode-contract-document.md,knowgrph-markdown-processing-document.md}`, `huijoohwee.github.io/schema/AgenticRAG/{markdown.jsonld,README.md}`, `knowgrph/todo-log.md` | Cross-repo documentation + AgenticRAG Markdown contract | Doc/spec updates aligned to converter + preview | HTML/DOM snapshot + baseUrl + per-doc fidelity frontmatter | Markdown SSOT + stable grid/media preview invariants | URL-agnostic rules only; spans apply regardless of element display; preserve safe attrs/sources; dedupe headings/cards/pricing/features. | Run `npm -C knowgrph/canvas test -- markdown.preview.htmlVideoAndGrid markdown.htmlToMdUnified.urlRewrite` and `npm -C knowgrph/canvas run doc:lint`. | 2026-02-26 |

## 2026-02-27

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Canvas / 2D Design renderer | Keep Design renderer + webpage wireframe docs aligned with DOM snapshot, layout glue pruning, synthetic sections, and interaction parity | Update cross-repo docs+schema: document Design 2D as a webpageLayout-backed wireframe renderer with neutral DOM→graph conversion (geometric nesting, wrapper pruning, landmark and major-section preservation, synthetic SECTION containers for repeated grids/lists) and shared SSOT shortcuts for lasso, snap-grid, align/distribute, and keyboard nudging across D3/Flow/Design; forbid host/URL-specific rules. | `knowgrph/docs/documents/{knowgrph-2d-renderer-enhancement-design.md,knowgrph-wireframe-enhanced-document.md,knowgrph-renderer-document.md}`, `huijoohwee.github.io/schema/AgenticRAG/{README.md,canvas.jsonld}`, `knowgrph/todo-log.md` | Cross-repo documentation + AgenticRAG canvas contract | Update Markdown + JSON-LD contract text | Implemented DesignCanvas webpageLayout integration, DOM→graph pruning/synthesis, and shared 2D shortcuts | Updated docs + schema text | Keep rules neutral and code-backed; treat webpageLayout as layout SSOT; share interaction semantics and shortcuts across 2D variants; forbid domain-specific branching or URL-dependent behavior. | Run `npm -C knowgrph/canvas run doc:lint`, `npm -C knowgrph/canvas run doc:sanity`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-02-27 |
| Docs / Canvas / 2D Design wireframe settings | Keep Design wireframe settings and label layout docs aligned with implementation | Update docs+schema: document Design wireframe FloatingPanel controls (`renderer:designWireframe`), label/meta chips, text/media previews, z-aware label collision, and optional edges; forbid host/URL-specific toggles and undocumented behavior. | `knowgrph/docs/documents/{knowgrph-2d-renderer-enhancement-design.md,knowgrph-wireframe-enhanced-document.md,knowgrph-renderer-document.md}`, `huijoohwee.github.io/schema/AgenticRAG/{canvas.jsonld,README.md}`, `knowgrph/todo-log.md` | Cross-repo documentation + AgenticRAG canvas contract | Update Markdown + JSON-LD contract text | Implemented DesignCanvas wireframe settings, labels, and edges | Updated docs + schema text | Document only code-backed settings; keep semantics renderer-neutral; reuse shared SSOT label/collision rules. | Run `npm -C knowgrph/canvas run doc:lint`, `npm -C knowgrph/canvas run doc:sanity`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-02-27 |

## 2026-02-28

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Markdown Webpage fidelity / Rich media SSOT | Keep Script/Imgs/Fid controls truly auto and SSOT across modes | Update cross-repo docs/schema: Script/Imgs/Fid defaults are auto, driven by shared rich-media + iframe heuristics; frontmatter overrides are optional escape hatches, not required or pinning; forbid docs implying manual per-doc fidelity pinning. | `knowgrph/docs/documents/{knowgrph-webpage-proxy-and-srcdoc-iframe-document.md,knowgrph-markdown-editor-mode-contract-document.md,knowgrph-source-files-import-document.md,knowgrph-settings-document.md}`, `huijoohwee.github.io/schema/AgenticRAG/{README.md,settings.jsonld}`, `knowgrph/todo-log.md` | Cross-repo documentation + AgenticRAG settings | Update Markdown + JSON-LD contract text | Implemented shared rich-media/iframe heuristics and auto Script/Imgs/Fid behavior | Updated docs + schema text | Document auto defaults only; treat frontmatter as optional overrides; keep SSOT shared across Markdown Viewer, Canvas 2D/3D, Design, and Document/Geospatial modes. | Run `npm -C knowgrph/canvas run doc:lint`, `npm -C knowgrph/canvas run doc:sanity`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-02-28 |

## 2026-03-01

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Canvas / Webpage→Design wireframe pipeline | Keep cross-repo docs+schema aligned with URL-agnostic webpage DOM export, layout snapshot, and Design wireframe rendering | Update docs/schema: document hidden-iframe DOM export (networkIdle+domQuiet, sandboxed proxy, viewport-aware), layout snapshot caching, neutral layout-to-graph pruning/synthesis, DesignCanvas webpage wireframe rendering (no legacy grid/empty fallback), and fidelity-based settings; forbid URL-specific rules and undocumented recompute/loop paths. | `knowgrph/docs/documents/{knowgrph-webpage-proxy-and-srcdoc-iframe-document.md,knowgrph-2d-renderer-enhancement-design.md,knowgrph-renderer-document.md,knowgrph-wireframe-enhanced-document.md}`, `knowgrph/todo-log.md`, `huijoohwee.github.io/schema/AgenticRAG/{README.md,canvas.jsonld,pipeline.jsonld}` | Cross-repo documentation + AgenticRAG canvas/pipeline contract | Update Markdown + JSON-LD contract text | Webpage URL + DOM snapshot + layout graph + Design wireframe settings | Updated docs + schema text | Treat DOM export/layout/Design wireframe as URL-agnostic SSOT; keep caching and viewport keys neutral; forbid host-specific CSS/DOM branches and infinite recompute or duplicate layout/render loops. | Run `npm -C knowgrph/canvas run doc:lint`, `npm -C knowgrph/canvas run doc:sanity`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-03-01 |

## 2026-03-10

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Canvas / Rich Media | Keep Rich Media docs aligned with 2D/3D overlays and viewer behavior | Update cross-repo docs+schema to describe shared Rich Media behavior (2D/3D overlays, auto Script/Imgs/Fid heuristics, export HTML Canvas hi-DPI snapshots), and forbid duplicate preview surfaces, per-mode-only heuristics, or docs claiming non-existent recomputation/loop behavior. | `knowgrph/docs/documents/{knowgrph-canvas-document.md,knowgrph-webpage-proxy-and-srcdoc-iframe-document.md,knowgrph-markdown-editor-mode-contract-document.md}`, `knowgrph/todo-log.md`, `huijoohwee.github.io/schema/AgenticRAG/README.md` | Cross-repo documentation + AgenticRAG schema | Update Markdown + README text | Implemented Rich Media overlay scheduling, flicker fixes, and hi-DPI HTML export | Updated docs + schema text | Document only code-backed behavior; treat Script/Imgs/Fid defaults as auto; keep Rich Media pools bounded and overlays single-surface; forbid duplicate previews and background recompute loops. | Run `npm -C knowgrph/canvas run doc:lint`, `npm -C knowgrph/canvas run doc:sanity`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-03-10 |

## 2026-03-14

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Canvas / 2D+3D renderer matrix / Rich Media / Frontmatter | Align renderer+export docs with implemented 2D/3D, Rich Media, and Frontmatter behavior | Update cross-repo docs+schema to describe 2D D3/Flow vs 3D renderer matrix, Rich Media On/Off overlay+export behavior, and Frontmatter Mode On/Off derivation (Flow frontmatter graph vs Markdown JSON-LD), ensuring all toggles are view-only and share SSOT GraphData/fit/zoom semantics across workspace and HTML Canvas exports. | `knowgrph/docs/documents/knowgrph-renderer-document.md`, `knowgrph/todo-log.md`, `huijoohwee.github.io/schema/AgenticRAG/canvas.jsonld` | Cross-repo documentation + AgenticRAG canvas contract | Update Markdown + JSON-LD contract text | Implemented 2D/3D renderer parity, Rich Media overlays/export, and frontmatter mode toggles | Updated docs + schemaVersion bump + renderer matrix section | Document only code-backed behavior; treat renderer/Rich Media/Frontmatter toggles as view-only; reuse SSOT visibility/fit semantics; forbid per-surface derivations or export-only behavior. | Run `npm -C knowgrph/canvas run doc:lint`, `npm -C knowgrph/canvas run doc:sanity`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-03-14 |
| Docs / Markdown / Frontmatter+Mermaid+Rich Media linking | Keep YAML/Mermaid frontmatter↔Markdown body↔Rich Media linking contract SSOT | Update cross-repo docs+schema to describe how YAML/Mermaid frontmatter graphs link to Markdown body anchors and Rich Media via `[[wikilinks]]`, `{{templates}}`, `frontmatterMermaidAnchors`, and node anchor properties; forbid absolute in-repo paths and duplicate, export-only graph derivations. | `knowgrph/docs/documents/{knowgrph-markdown-ssot-ui-contract-document.md,knowgrph-renderer-document.md}`, `knowgrph/todo-log.md`, `huijoohwee.github.io/schema/AgenticRAG/{markdown.jsonld,canvas.jsonld}` | Cross-repo documentation + AgenticRAG markdown/canvas contract | Update Markdown + JSON-LD text | Implemented frontmatter Flow graph + article anchors + Rich Media overlays | Updated docs + schemaVersion bumps | Document only code-backed linking patterns; keep anchors/links schema-neutral; forbid hardcoded repo paths and per-surface link semantics. | Run `npm -C knowgrph/canvas run doc:lint`, `npm -C knowgrph/canvas run doc:sanity`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-03-14 |
| Docs / Canvas / HTML Canvas export viewer HUD | Keep export viewer toggles and centering aligned with renderer SSOT | Document HTML Canvas export viewer HUD (3D/Rich/Media/Frontmatter), centroid-based fit, and mobile pan/zoom parity with Canvas; toggles remain view-only and must not re-derive GraphData or layout, only gate renderer, overlays, and media interaction. | `knowgrph/docs/documents/{knowgrph-renderer-document.md,knowgrph-canvas-document.md}`, `huijoohwee.github.io/schema/AgenticRAG/canvas.jsonld`, `knowgrph/todo-log.md` | Cross-repo documentation + AgenticRAG canvas export contract | Update Markdown + JSON-LD contract text | Implemented HTML Canvas export centering + viewer HUD toggles | Updated docs + schemaVersion bump + HUD/mode parity text | Document only code-backed behavior; treat HUD toggles as view-only; reuse SSOT GraphData/fit semantics; forbid export-only graphs or per-mode derivations. | Run `npm -C knowgrph/canvas run doc:lint`, `npm -C knowgrph/canvas run doc:sanity`, and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check`. | 2026-03-14 |

## 2026-03-25

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Canvas / 2D D3 / Force-directed layout stability | Stop post-settle layout snaps and malformed jumps | Document and enforce bounded force-directed layout: preserve simulation positions across scene rebuilds, disable strict-overlap relax once alpha is near-settled, and ensure overlay/panel changes never restart the simulation or mutate settled node positions. | `knowgrph/canvas/src/components/GraphCanvas/{scene.ts,simulation.ts}`, `knowgrph/canvas/src/components/GraphCanvasRoot/hooks/{useD3GraphScene2d.ts,useD3PresentationUpdates2d.ts}`, `knowgrph/canvas/src/lib/graph/physics2dTuning.ts`, `knowgrph/docs/documents/knowgrph-graph-canvas-layout-ssot.md`, `huijoohwee.github.io/schema/AgenticRAG/{README.md,canvas.jsonld}` | Force layout + overlap relax SSOT | `buildSimulation`, `computeStrictOverlapTuning2d`, `useD3GraphScene2d`, `useD3PresentationUpdates2d` | GraphData + schema + overlay membership + interaction mode | Stable force-directed layout with no post-settle snaps | Preserve positions on scene rebuilds, gate strict-overlap passes by alpha, and route overlay membership changes through presentation updates instead of restarting D3 or mutating layout caches. | Run `npm -C knowgrph/canvas run check && npm -C knowgrph/canvas test`, then `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` to keep docs/schema aligned. | 2026-03-25 |
| Canvas / Interaction Modes / Workspace Sync | Keep Canvas Interaction Mode + Workspace Sync Mode SSOT across Toolbar, Settings, and AgenticRAG schema | Document `infiniteCanvasInteractionMode∈{static,interactive}` and `canvasWorkspaceSyncMode∈{manual,realtime}` as SSOT toggles shared by Toolbar and Settings. Static+Manual is the default: force-directed layout runs to stability then freezes; rich-media/markdown overlays forward wheel/pointer to Canvas; Graph Data Table and GraphTableDb avoid position-only churn and only sync on content changes or explicit “Sync now”. Interactive+Realtime enables continuous D3 forces and fully interactive overlays/panels while still forbidding graph/layout/zoom mutations outside user actions. | `knowgrph/canvas/src/hooks/store/{canvasSlice.ts,types.ts}`, `knowgrph/canvas/src/features/settings/{registry-ui.graph-and-orchestrator.ts,useSettingsView.ts}`, `knowgrph/canvas/src/components/{Toolbar.tsx,GraphCanvasRoot/hooks/useD3GraphScene2d.ts,GraphCanvasRoot/components/RichMediaOverlayLayer2d.tsx}`, `knowgrph/canvas/src/features/{graph-table/ui/GraphTableWorkspace.tsx,graph-table/ui/GraphTableSelectionInspector.tsx,graph-data-table/ui/GraphDataTableTable.tsx}`, `knowgrph/todo-log.md`, `knowgrph/docs/documents/{knowgrph-canvas-document.md,knowgrph-renderer-document.md,knowgrph-local-storage-document.md,knowgrph-infinite-canvas-interaction-performance-document.md,knowgrph-graph-data-table-fast-grid-document.md}`, `huijoohwee.github.io/schema/AgenticRAG/{canvas.jsonld,panels.jsonld,settings.jsonld,README.md}` | Canvas interaction + sync SSOT | `setInfiniteCanvasInteractionMode`, `setCanvasWorkspaceSyncMode`, `useD3GraphScene2d`, `RichMediaOverlayLayer2d`, `ThreeGraph`, `useGraphTableDbSync`, `GraphTableWorkspace`, `GraphDataTableTable` | GraphData + Schema + workspace view + Interaction Mode + Sync Mode | Stable interaction behavior across 2D/3D renderers, Editor/Graph Data Table, and HTML Canvas exports without duplicate paths or background churn | Default to `static` + `manual`; in static mode, keep force layout bounded and frozen post-stabilization, forward overlay wheel to Canvas, and gate GraphTable sync off `graphContentRevision` to avoid position-only churn. In interactive mode, keep D3 force layout running, allow embedded overlays to be fully interactive (no wheel forwarding when safe), and key GraphTable sync off `graphDataRevision` while still deduping writes via `noteGraphWrite` gates. Workspace Sync Mode `manual` disables auto GraphTableDb sync but exposes a single “Sync now” action; `realtime` enables auto sync behind the same revision+viewKey gates. | Run `npm -C knowgrph/canvas run test:ci -- toolbarButtons settings zoomStateEq rxdbGraphTableDb graphDataTableTypography` then `npm -C knowgrph/canvas run doc:lint` and `npm -C knowgrph/canvas run doc:sanity`; finally run `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` to keep schema+docs aligned with implemented toggles and LS keys. | 2026-03-25 |

