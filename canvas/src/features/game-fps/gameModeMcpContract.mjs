export const GAME_MODE_MCP_SCHEMA = 'knowgrph-game-mode-mcp/v1'

export const GAME_MODE_WEB_MCP_TOOL_IDS = Object.freeze({
  inspect: 'inspect_local_game_mode',
  control: 'control_local_game_mode',
})

export const GAME_MODE_INVOCATION_COMMANDS = Object.freeze({
  control: '/game.mode',
})

export const GAME_MODE_INVOCATION_SEMANTICS = Object.freeze({
  gameplay: '#gameplay',
})

export const GAME_MODE_INVOCATION_BINDINGS = Object.freeze({
  canvas: '@canvas',
})
