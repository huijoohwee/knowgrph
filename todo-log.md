# todo-log

- **Canonical directive**: **STRICTLY COMPLY** with one-row-one-directive (Max 50 words)
- **Table cells**: MUST fill-up all cells in the table; FORBID empty/`-` cells.

## 2026-02-10

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Canvas 2D Interaction Speed / D3+Flow+FlowEditor | Unify drag/pan/zoom speed tuning across 2D renderers | Expose `canvasPanSpeedMultiplier` in MainPanel Settings and apply it as a bounded multiplier for wheel zoom, wheel pan, pointer pan, and touch pinch/pan so all 2D renderers share the same interaction speed knob without affecting non-2D modes. | `canvas/src/{features/settings/registry-ui.graph-and-orchestrator.ts,features/settings/settings-flow.schema.json,components/GraphCanvas/zoom.ts,components/FlowCanvas/bindNativeInteractions.ts,hooks/store/canvasSlice.ts}` | Graph store canvasSlice + 2D interaction handlers | `setCanvasPanSpeedMultiplier`, `createZoom`, `bindFlowCanvasNativeInteractions` | wheel/pointer/touch events + schema panSpeed/zoomSpeed + multiplier | Consistent 2D interaction speed across D3/Flow/FlowEditor | Multiply effective pan/zoom deltas by `clampCanvasPanSpeedMultiplier(store.canvasPanSpeedMultiplier)`; keep renderer-specific zoom tuning intact; apply only inside 2D handlers. | In MainPanel Settings, tune `canvasPanSpeedMultiplier` and verify D3↔Flow↔FlowEditor parity for wheel pan/zoom and drag-pan in map/design presets. | 2026-02-10 |
| Canvas 2D Interaction Speed / unified multiplier | Add one shared speed control for all 2D interactions | Add `canvasInteractionSpeedMultiplier` unified speed knob; multiply wheel/pinch zoom and pan deltas across D3/Flow/FlowEditor. Keep `canvasPanSpeedMultiplier` pan-only. Preserve existing `flowWheelZoom*` and schema `panSpeed/zoomSpeed` overrides. | `canvas/src/{lib/config.ls.ts,lib/canvas/camera-options-2d.ts,hooks/store/{types.ts,canvasSlice.ts},features/settings/registry-ui.graph-and-orchestrator.ts,components/{GraphCanvas/zoom.ts,FlowCanvas/bindNativeInteractions.ts}}` | Graph store canvasSlice + 2D interaction handlers | `setCanvasInteractionSpeedMultiplier`, `setCanvasPanSpeedMultiplier`, `createZoom`, `bindFlowCanvasNativeInteractions` | wheel/pointer/touch events + schema panSpeed/zoomSpeed + multipliers | Unified speed slider + pan-only knob retained | Zoom uses `flowWheelZoom* * clampCanvasInteractionSpeedMultiplier`; pan uses `panSpeed * clampCanvasPanSpeedMultiplier * clampCanvasInteractionSpeedMultiplier`; apply only when `canvasRenderMode=2d`. | Tune `canvasInteractionSpeedMultiplier` first; use `canvasPanSpeedMultiplier` only for pan feel; re-run parity checks across D3/Flow/FlowEditor. | 2026-02-10 |
| Import→Render / PDF→Markdown | Improve local PDF conversion fidelity and persistence | Refactor `/__convert_pdf` + `/__pdf_assets` into a reusable server module and add bounded `data:` URI image embedding plus config-driven provider routing (`native` default; optional `docling-remote` + OCR enhancement). | `canvas/{vite.config.ts,src/lib/pdf/{server/pdfConvertServer.ts,embedPdfAssetsInMarkdown.ts,native/*},src/cli/convert-pdf-to-md.ts}` | PDF converter server + embed utility | `createPdfConvertHandler`, `createPdfAssetsHandler`, `convertPdfToMarkdown`, `embedPdfAssetsInMarkdown` | PDF bytes/URL + env/query overrides | Markdown with improved image persistence + optional OCR enrichment | Default local path runs fully in-repo; embedding rewrites eligible `/__pdf_assets/<token>/<file>` links to `data:` URIs until size caps; online provider is selected only when explicitly configured. | Validate with `npm run pdf:convert` against a sandbox PDF and import via Workspace/Source Files to confirm rendering in Markdown surfaces and Preview panel. | 2026-02-10 |
| MainPanel Settings / Import→Render / PDF | Expose PDF import knobs as user settings | Add `Import: PDF` settings (includeImages/embedImages, caps, provider, Docling endpoint, fallback, OCR2 toggles) persisted via LS keys and applied by appending query overrides to `/__convert_pdf` for all PDF import entrypoints. | `canvas/src/{hooks/store/{types.ts,uiSlice.ts},features/settings/registry-ui.import-pdf.ts,features/settings/registry-ui.ts,lib/pdf/pdfImportClientPrefs.ts,lib/net/remoteMarkdownConversions.ts,lib/pdf/server/pdfConvertServer.ts}` | Store + Settings registry + PDF conversion client/server | `buildPdfConvertQueryParamsFromStore`, `convertPdfUrlToMarkdown`, `convertPdfFileToMarkdown`, `createPdfConvertHandler` | settings values + PDF bytes/URL | Configurable PDF import behavior without env changes | Client always sends settings as query params; server honors overrides and defaults to local native when provider/endpoint is unset; bounds are enforced in store setters. | Use Settings to tune embed caps for speed, switch provider to Docling when endpoint is available, and enable OCR2 only when needed. | 2026-02-10 |
| Import PDF → Convert Markdown → Render | Let users choose a conversion-mode preset | Expose `pdfImportConversionMode` in MainPanel Settings, persist it, and send `conversionMode` to `/__convert_pdf` so conversion presetting stays deterministic; keep docs + AgenticRAG pipeline schema aligned across repos. | `canvas/src/{features/settings/registry-ui.import-pdf.ts,lib/pdf/{pdfImportClientPrefs.ts,server/pdfConvertServer.ts}}`, `docs/documents/*`, `huijoohwee.github.io/schema/AgenticRAG/pipeline.jsonld` | Settings registry + PDF conversion pipeline | `setPdfImportConversionMode`, `buildPdfConvertQueryParamsFromStore`, `createPdfConvertHandler` | user mode + PDF bytes/URL | Markdown output + stable render behavior | `conversionMode` maps presets; explicit query overrides win. | Switch modes in Settings and import the same PDF to confirm render differences and persistence. | 2026-02-10 |
| PDF Workspace / Editor Workspace | Consolidate PDF import + workspace into Editor workspace | Move PDF workspace+import UI into Editor workspace Source Files (no standalone pages and no Workflow Manager duplicate), redirect legacy `/workspace` and `/import` to `/?openEditorWorkspace=1`, and expose `pdfWorkspaceOutputDirRel` as a Settings key so artifacts stay in-repo under `.knowgrph-workspace/`. | `canvas/src/{App.tsx,pages/Canvas.tsx,components/BottomPanel/markdownWorkspace/{MarkdownWorkspace.tsx,MarkdownWorkspaceExplorer.tsx,PdfWorkspaceSourceFilesAddon.tsx},lib/pdf/{pdfWorkspacePreferences.ts,server/pdfWorkspaceServer.ts}}` | MarkdownWorkspace + Explorer addon | `readPdfWorkspaceOutputDirRel`, `writePdfWorkspaceOutputDirRel`, `createPdfWorkspaceHandler` | PDF file + conversionMode + outputDirRel | `output.md` + `anchor-map.json` + workspace-openable markdown | Sanitize outputDirRel; open Editor workspace via query; on Open load markdown into workspace editor; use store `pdfImportConversionMode` as mode SSOT. | Import a PDF via Editor workspace import button, then open from PDF Workspace list and confirm it loads into editor/viewer. | 2026-02-10 |

## 2026-02-11

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Import→Render / Markdown Viewer+Presentation / PDF assets | Render PDF-extracted images/tables across Markdown surfaces | Preserve internal root routes (`/__*`, `/@*`) in href resolution, enable HTML parsing in large-doc fast mode for table/media tags, and add bounded regression tests to prevent PDF image/table regressions. | `curagrph/src/features/markdown/ui/{markdownPreviewLinks.tsx,markdownPreviewLex.ts}`, `canvas/src/{__tests__/*,tests/run.ts}` | Markdown link resolver + lexer fast-mode parser | `resolveHref`, `lexMarkdownContent`, `testMarkdownResolveHrefPreservesInternalAssetRoutes`, `testMarkdownLargeDocFastModeParsesHtmlTables` | Markdown with `(/__pdf_assets/<token>/...)` and large HTML `<table>` blocks | Images render via `/__pdf_assets`; HTML tables remain renderable; tests cover both | If href starts with `/__` or `/@`: return as-is; if large doc contains `<table|img|iframe|video|v-clicks>`: parse with HTML enabled; else use HTML-disabled fast parser. | Import a PDF from `sandbox/test-data/test-pdf` and confirm image/table rendering in Viewer and Presentation; use `docling-remote` when an endpoint is configured for structured tables. | 2026-02-11 |

## 2026-02-12

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Import→Render / PDF→Markdown / Native table reconstruction | Improve table fidelity without remote providers | Add a generic native table reconstruction pass using fragment x/y alignment to emit Markdown pipe tables; expose bounded knobs via MainPanel Settings and `/__convert_pdf` query overrides; add regression tests for reconstruction + lexing into table tokens. | `canvas/src/lib/pdf/{native/{pdfMarkdown.ts,nativePdfToMarkdownNode.ts},server/{pdfConvertServer.ts,pdfConvertRequest.ts},pdfImportClientPrefs.ts}`, `canvas/src/{hooks/store/{types.ts,uiSlice.ts},features/settings/registry-ui.import-pdf.ts,__tests__/*,tests/run.ts}`, `curagrph/src/features/markdown/ui/MarkdownInlineRenderer.tsx` | Native PDF converter + settings plumbing + markdown renderer | `buildMarkdownForPage`, `convertPdfBytesToMarkdown`, `parsePdfConvertRequest`, `buildPdfConvertQueryParamsFromStore` | PDF bytes + user `pdfImport*` settings | Markdown with reconstructed tables + bounded images in Viewer/Presentation | Detect consecutive column-aligned rows (min cols/rows, density check) then emit pipe table; preserve normal text otherwise; extraction and embedding remain bounded by existing caps. | Import sandbox PDFs and toggle `pdfImportReconstructTables`/thresholds in Settings to tune recall/precision; switch to `docling-remote` for documents with complex merged-cell tables. | 2026-02-12 |
| Import→Render / YouTube / Thumbnail + Config | Fix YouTube thumbnail display and allow output dir config | Extract `thumbnail_url` from oEmbed in `youtube_cmd.py` and pass it to graph node properties as `image`; expose `youtubeTranscriptOutputDir` in MainPanel Settings to control save path; ensure PreviewPanel renders the thumbnail via `getNodeMediaSpec`. | `knowgrph/knowgrph_parser/youtube_cmd.py`, `knowgrph/canvas/src/{features/toolbar/youtubeImportAction.ts,features/settings/registry.ts,hooks/store/{types.ts,uiSettingsSlice.ts}}`, `huijoohwee.github.io/schema/AgenticRAG/{youtube.jsonld,settings.jsonld}` | YouTube parser + import action + settings registry | `performYouTubeImport`, `buildYouTubeTranscriptGraphData`, `main` | YouTube URL + settings | Graph node with thumbnail property + transcript file in configured dir | oEmbed thumbnail fallback to maxresdefault; node property `image` drives preview; output dir setting overrides workspace root. | Import test URL `https://youtu.be/dC_3ys349bU` and verify thumbnail in Preview Panel and file creation in configured sandbox path. | 2026-02-12 |
| Import→Render / YouTube / Output Format | Allow selecting transcript output format (Markdown/JSON) | Add `youtubeTranscriptOutputFormat` setting (markdown/json) to MainPanel and store; update `youtubeImportAction.ts` to respect this setting and save the corresponding file type (md or json) to the output directory; update schema documentation. | `knowgrph/canvas/src/{features/settings/registry.ts,hooks/store/{types.ts,uiSettingsSlice.ts},features/toolbar/youtubeImportAction.ts}`, `huijoohwee.github.io/schema/AgenticRAG/settings.jsonld` | Settings + Import Action | `performYouTubeImport` | User setting `youtubeTranscriptOutputFormat` | Markdown or JSON file saved to disk | Defaults to 'markdown'; 'json' option saves the raw transcript JSON; ensures editor opens the correct file. | Toggle setting to 'json' and import URL; verify .json file is created in output dir and opened. | 2026-02-12 |
| Import→Render / YouTube / UI Format Selector | Allow selecting format (Markdown/JSON) at import time | Add format selector dropdown to `BottomPanelMarkdownViewerHeader`'s "Import URL" UI; update `MarkdownSourceFilesIngestIntegration` type and `sourceFilesIngestIntegration` implementation to accept format; refactor `remoteMarkdownConversions` to return rich JSON transcript data; update `youtube_cmd.py` to embed thumbnail in Markdown. | `curagrph/src/components/BottomPanel/BottomPanelMarkdownViewerHeader.tsx`, `curagrph/src/features/markdown/ui/MarkdownSourceFilesIngestIntegration.ts`, `knowgrph/canvas/src/{features/source-files/sourceFilesIngestIntegration.ts,lib/net/remoteMarkdownConversions.ts}`, `knowgrph/knowgrph_parser/youtube_cmd.py` | Import UI + Integration | `importUrlIntoActive`, `fetchYouTubeTranscriptMarkdown`, `_build_markdown` | URL + Format (MD/JSON) | Transcript file (MD with thumbnail or JSON) in active/output dir | UI selector overrides global default; `youtube_cmd` emits thumbnail in MD; `sourceFilesIngestIntegration` routes based on format. | Open "Import URL" panel, select JSON, import URL, verify .json file; select MD, import URL, verify .md with thumbnail. | 2026-02-12 |
| Import→Render / YouTube / Native Fetch Pipeline | Replace youtube-transcript-api with robust native fetch | Remove external dependency `youtube-transcript-api`; implement native `timedtext` API fetch with session/cookie handling + XML/JSON parsing; add resilient HTML scraper for captions; ensure backend returns JSON error payloads on failure; handle IP blocking (429) gracefully by exposing specific error messages to the UI. | `knowgrph/knowgrph_parser/youtube_cmd.py`, `knowgrph/canvas/src/lib/net/remoteMarkdownConversions.ts` | YouTube Parser | `_fetch_transcript_snippets`, `fetchYouTubeTranscriptMarkdown` | YouTube URL | Transcript JSON/Markdown or specific error JSON | Native fetch -> InnerTube API fallback -> yt-dlp -> Whisper; if all fail, return JSON error | Verify native fetch works on allowed IPs; verify robust error message display on blocked IPs. | 2026-02-12 |
| Markdown Workspace / Viewer+Presentation+Slides | Make Markdown render fidelity consistent across modes | Replace legacy markdown-it slide rendering in MarkdownWorkspace with curagrph `MarkdownPreview` so images/tables render consistently and relative asset hrefs resolve via `/@fs` while preserving internal routes (`/__*`, `/@*`); add bounded regression tests and expose Markdown defaults in MainPanel Settings. | `canvas/src/{components/BottomPanel/markdownWorkspace/MarkdownWorkspaceMain.tsx,features/settings/{registry-ui.markdown.ts,registry-ui.ts,settings-flow.schema.json},__tests__/{markdownWorkspacePresentationRelativeAssets.test.tsx,markdownPdfImportPresentationRender.test.tsx},tests/run.ts}`, `docs/documents/knowgrph-settings-document.md`, `huijoohwee.github.io/schema/AgenticRAG/settings.jsonld` | MarkdownWorkspaceMain + settings registry + markdown renderer | `MarkdownWorkspaceMain`, `resolveHref`, `uiSettingsRegistry` | Markdown text + relative asset paths + sandbox PDF fixture | Viewer/Presentation/Gallery render images and tables reliably | Presentation/gallery use token-based renderer with href resolution; settings read/write `LS_KEYS.markdown*` via localStorage helpers; tests assert `/@fs` rewrite and PDF asset image presence. | Use MainPanel Settings to set `markdownWordWrap`/`markdownTextHighlight`, then import PDFs from `sandbox/test-data/test-pdf` and verify images/tables render in Viewer, Presentation, and Slides Gallery. | 2026-02-12 |
| Markdown Workspace / Import Folder / Performance | Avoid loading/parsing all folder files eagerly | On Import Folder, create folder/file entries and lightweight pending stubs without reading contents or converting PDFs; clicking a file triggers indexing/loading/parsing/rendering (including on-demand PDF conversion) and shows an `Indexing` spinner with the shared `label • n/n • kb/kb` formatting. Support workspace file CRUD and keep the Source Files list synced (stable IDs keyed by `workspace:<path>`), reusing cached `parsedGraphData` when text hashes match. GeoJSON/JSON files with geodata must render by converting into nodes with `properties.geo.{lat,lng}`. Forbid file switching from toggling frontmatter mode or causing graph/layout reseeds. | `canvas/src/{components/{BottomPanel/markdownWorkspace/{workspaceImport.ts,MarkdownWorkspace.tsx,useWorkspaceFileActions.ts},GraphCanvas.tsx,GraphCanvas/layout/positioning.ts,Toolbar.tsx},features/{workspace-fs/syncToSourceFiles.ts},lib/graph/io/geojsonToGraphData.ts,hooks/store/graphDataSlice.ts,__tests__/{workspaceImportLocal.test.ts,workspaceSourceFilesSync.test.ts,markdownDocumentSetAutoFrontmatterGuard.test.ts},tests/run.ts}`, `docs/documents/knowgrph-canvas-document.md`, `huijoohwee.github.io/schema/AgenticRAG/panels.jsonld`, `knowgrph/todo.md` | MarkdownWorkspace + import pipeline + layout cache + sourceFiles sync + geodata | `importWorkspaceLocalFolder`, `hydrateWorkspaceFileFromPendingLocalImport`, `mergeWorkspaceEntriesIntoSourceFiles`, `buildGraphDataFromFeatureCollection`, `determineLayoutPositions` | Folder selection + file click + CRUD + file switching + editor↔canvas toggle | Fast folder import + stable per-file graph layout + synced Source Files + geo render | Folder import shows no indexing spinner; it writes pending stub text and stores ephemeral file handles; clicking a file hydrates content, syncs to Source Files, and indexes/parses into GraphData while showing `Indexing` with byte totals; GeoJSON/records JSON are converted into graph nodes with `properties.geo`; switching files captures layout positions on graph switch and allows cache reuse across dataset changes; Markdown file open uses `setMarkdownDocument(...,{autoEnableFrontmatter:false})` so `frontmatterModeEnabled` does not flip. | Import `sandbox/` via Import Folder, click `.geojson`/geo-data `.json` files and confirm points render in Geospatial Mode; create/edit/delete files and confirm Source Files list mirrors the workspace; switch between files and confirm cached parsed graphs and layout positions are reused. | 2026-02-12 |
| Markdown Viewer / GeoJSON code blocks / Geospatial | Make GeoJSON blocks usable as graph and datasets | In Markdown Viewer/Presentation, `geojson` blocks (and `json` blocks detected as GeoJSON) render an inline map preview (Render mode) and expose quick actions: (1) “Load as Graph” converts the FeatureCollection to GraphData nodes with `properties.geo.{lat,lng}` and loads the graph view; (2) “Add as Dataset” uploads the GeoJSON text to the bounded local geo cache and registers the resulting same-origin URL as a geospatial overlay dataset (no hardcoded providers, format auto-detected). Actions must be bounded, idempotent by content hash, and must not force Graph/Markdown layout reseeds on file switches. | `curagrph/src/features/markdown/ui/codeblock/GeoJsonGeoPanelRenderer.tsx`, `curagrph/src/features/markdown/ui/MarkdownCodeBlock.tsx`, `canvas/src/features/geospatial/markdownGeoDatasetIntegration.tsx`, `canvas/src/lib/graph/io/geojsonToGraphData.ts`, `gympgrph/src/features/geospatial/*` | Markdown GeoJSON renderer + geo dataset integration | `GeoJsonGeoPanelRenderer`, `isGeoJsonCodeBlock`, `registerGeoJsonFeatureCollection`, `buildGraphDataFromFeatureCollection` | Markdown fenced code blocks | Inline preview + dataset registration + optional graph load | Render-preview is always available; dataset registration is enabled only when the host provides `geoDatasetIntegration` and the local upload endpoint is available. “Load as Graph” reuses the same GeoJSON→GraphData conversion as file import (single SSOT). | Open `sandbox/demo/trip-demo.md` in Viewer, toggle Render for a GeoJSON fence, and verify both actions: dataset appears in Geo panel list; graph view loads nodes with geo properties. | 2026-02-12 |
| Dev Remote Fetch Proxy / MapLibre basemap / GeoJSON preview | Prevent blank basemap due to truncated proxy bodies | Ensure `/__fetch_remote` buffers full upstream body (JSON/binary) within bounded `maxBytes`, does not abort upstream on close events, and sets `Content-Length` from the buffered body; add a regression guard test. | `canvas/vite.config.ts`, `canvas/src/__tests__/geospatialHostIntegration.test.ts`, `canvas/src/tests/run.ts` | Vite dev server middleware | `createRemoteFetchHandler`, `testRemoteFetchProxyDoesNotAbortOnCloseOrTruncate` | `GET|HEAD` with `url=` param + upstream assets | Untruncated same-origin proxy responses for basemap assets | Validate http(s) URL; apply timeout/maxBytes; buffer response; return 413/504/502 on bounded failures. | In localhost dev, open `sandbox/demo/trip-demo.md` and confirm GeoJSON preview basemap loads; toggle Geospatial Mode and confirm the basemap remains visible. | 2026-02-12 |
| Canvas Preview (Editor workspace) / Layout Stability | Stop preview flicker and prevent layout recomputation loops | De-dupe preview sync by hashing graphData while ignoring store-injected `metadata.graphDataRevision`/`metadata.hash`; ignore `kg-preview-graph` echoes in host; remove presentation JSON from layout/zoom view keys and prevent simulation restarts or scene rebuilds on presentation-only changes. | `canvas/src/{pages/Canvas.tsx,components/{EmbeddedCanvasPreviewFrame.tsx,GraphCanvas.tsx,FlowCanvas.tsx},components/GraphCanvas/{zoomViewKey.ts,layout/positioning.ts},lib/canvas/active-2d-zoom-view-key.ts,hooks/store/graphDataSliceUtils.ts,__tests__/*,tests/run.ts}`, docs | Preview sync + viewKey SSOT | `hashGraphDataForPreviewSync`, preview message handlers, `buildZoomViewKey`, `buildLayoutViewKey` | kg-preview postMessage payloads + store graphData | Stable preview rendering + stable graph layout positions | Treat preview sync equality on sanitized metadata; apply presentation via dedicated presentation effects only; caches and zoom keys must not change on style-only updates. | Open Editor workspace and verify preview no longer flickers when selection/style toggles change; confirm layout caches survive hover/presentation tweaks and graphs stop “jumping”. | 2026-02-12 |
| Import→Render / Webpage URL | Generic webpage parsing with fidelity and settings | Implement generic `webpage_cmd.py` using `html.parser` for 100% fidelity (images preserved) and `urllib`; add `/__webpage_convert` vite proxy; integrate into `sourceFilesIngestIntegration.ts` with fallback logic; expose `webpageImportIncludeImages` setting in MainPanel. | `knowgrph/knowgrph_parser/webpage_cmd.py`, `knowgrph/canvas/vite.config.ts`, `knowgrph/canvas/src/{features/source-files/sourceFilesIngestIntegration.ts,lib/net/remoteMarkdownConversions.ts,features/settings/registry.ts}`, `huijoohwee.github.io/schema/AgenticRAG/settings.jsonld` | `SimpleMarkdownParser` | `fetchWebpageMarkdown`, `importUrlIntoActive`, `runKnowgrphParserConvertWebpageToPayload` | URL + settings | Markdown with images | If not known ext/YouTube/PDF: try webpage convert; if succeeds, use it; else fallback. | Verify `test-url` import and toggle `includeImages` setting. | 2026-02-12 |
| Import→Render / Remote Fetch | Robustify remote fetch headers and retries | Fix `webpage_cmd.py` to handle 403/429 errors via retry loop and `requests` fallback; use `Accept: */*` in `/__fetch_remote` proxy and Python parser to support raw files (e.g. GitHub raw content) without 406/403 errors; ensure generic imports work for strict sites (e.g. GitHub). | `knowgrph/knowgrph_parser/webpage_cmd.py`, `knowgrph/canvas/vite.config.ts` | `webpage_cmd.py`, `createRemoteFetchHandler` | `_fetch_url`, `fetch` | URL + headers | Successful content fetch or retried error | Use `requests` with retry/backoff for robust fetch; proxy must mimic browser headers to pass WAFs but use generic Accept for raw data. | Verified with `curl` against `raw.githubusercontent.com`. | 2026-02-12 |
| Import→Render / Async Pipeline | Prevent dev server blocking during imports | Replace blocking `spawnSync` with non-blocking `spawn` in `runKnowgrphParserConvertWebpageToPayload`, `runKnowgrphParserConvertYoutubeToPayload`, and `runMarkdownPipelineOnce` to prevent Node event loop starvation and `net::ERR_ABORTED` on parallel requests; ensure robust timeout/cleanup. | `knowgrph/canvas/vite.config.ts` | Vite middleware | `runKnowgrphParserConvert*`, `runMarkdownPipelineOnce` | Import request | Async process execution | Blocking calls freeze the dev server; async `spawn` allows concurrent requests (HMR, source fetch) to proceed. | Verify imports no longer stall the UI or cause other network errors. | 2026-02-12 |
| Import→Render / Webpage URL / Viewer+Presentation | Render webpages with higher visual fidelity | Keep URL import generic: parse webpages to Markdown for graph alignment; switch `Markdown|JSON|HTML|Wireframe` via `kgWebpageView` frontmatter + default `webpageImportView`; sanitize embedded base64 for editor safety; forbid graph/layout/zoom/layers recomputation on view switches. | `knowgrph/canvas/{vite.config.ts,src/components/BottomPanel/markdownWorkspace/{MarkdownWorkspace.tsx,MarkdownWorkspaceMain.tsx,workspaceImport.ts},src/features/source-files/sourceFilesIngestIntegration.ts,src/lib/{markdown/sanitizeImportedMarkdown.ts,markdown/frontmatter.ts,net/remoteMarkdownConversions.ts},src/features/settings/registry-ui.import-webpage.ts,src/hooks/store/{types.ts,uiSettingsSlice.ts,graphDataSlice.ts}}` | Vite middleware + MarkdownWorkspace + settings store | `createWebpageProxyHandler`, `fetchWebpageMarkdown`, `sanitizeImportedMarkdownText`, `parseWebpageFrontmatterMeta`, `upsertWebpageFrontmatterMeta`, `buildWireframeMarkdownFromMarkdown` | Webpage URL + view mode | Markdown parse + JSON/Wireframe editor view + iframe HTML render | Editor: `markdown→markdown`, `json→conversion payload JSON (read-only)`, `wireframe→ASCII wireframe (read-only)`, `html→markdown`. Viewer/Presentation/Slides: render HTML via `/__webpage_proxy` when `kgWebpageView∈{json,html,wireframe}`, else render Markdown. View switching is strictly view-only; sanitization replaces huge base64 with `<omitted>`/`data:,`. | Import a webpage URL, switch `Markdown/JSON/HTML/Wireframe`, and confirm graph/layout/zoom do not change while Viewer/Presentation updates. | 2026-02-12 |
| Import→Render / Website URL / Sitemap tree | Import a whole website into workspace pages and keep per-page view switching deterministic | Implement website import (sitemap/tree) that writes per-page workspace stubs with `kgWebsiteImportId/kgWebsiteNodeId`. Active-row mode dropdown supports `Markdown|JSON|HTML|Wireframe`; Viewer/Presentation/Slides render 100% fidelity HTML via iframe proxy for JSON/HTML/Wireframe. Forbid view switches from mutating graph/layout/zoom. | `canvas/src/{lib/websites/server/websiteImportServer.ts,components/BottomPanel/markdownWorkspace/{useWorkspaceFileActions.ts,MarkdownWorkspace.tsx,MarkdownWorkspaceMain.tsx},lib/websites/{wireframe.ts,websiteImportArtifactKind.ts}}` | Website import server + MarkdownWorkspace + Wireframe builder | `createWebsiteImportHandler`, `handleImportWebsite`, `switchActiveWebpageWorkspaceView`, `websiteImportArtifactKindForWebpageView`, `buildWireframeMarkdownFromMarkdown` | Root URL + settings (maxPages, concurrency, includeImages, view) | Workspace tree + cached artifacts + dropdown views | View→artifact kind mapping (editor): `json→conversionJson`, `wireframe→wireframeMarkdown`, else `markdown`; Viewer/Presentation/Slides render HTML via `/__webpage_proxy` when `kgWebpageView∈{json,html,wireframe}`. | Import `https://www.remotion.dev/` as website, open a page in the tree, and switch Markdown/JSON/HTML/Wireframe to confirm fidelity and stability. | 2026-02-12 |
| Import→Render / Webpage iframe fidelity | Preserve rich media + animations in same-origin proxy | Strip upstream CSP/XFO meta and conflicting `<base>`; rewrite media asset URLs to `/__webpage_asset_proxy`; expand iframe sandbox to allow modals/pointer-lock/presentation. Keep view switching strictly view-only and expose the same `Markdown|JSON|HTML|Wireframe` selector in Graph Table Inspector. | `canvas/{vite.config.ts,src/features/graph-table/ui/GraphTableInspector.tsx,src/components/BottomPanel/markdownWorkspace/MarkdownWorkspaceMain.tsx,src/lib/websites/wireframe.ts}` | Vite webpage proxy + Graph Table Inspector + MarkdownWorkspaceMain | `createWebpageProxyHandler`, `createWebpageAssetProxyHandler`, `injectWebpageProxyHtml`, `switchWebpageView` | Imported webpage URL + selection + view mode | Higher-fidelity iframe render + deterministic per-file view mode | Apply bounded fetch (timeout/maxBytes); never hardcode domains; only rewrite `img/video/audio/source/track` assets; switching `kgWebpageView` must not affect graph/layout/zoom/layers. | Import `https://www.remotion.dev/` and verify animations/media in Viewer/Presentation; switch view from both workspace tree and table inspector and confirm no graph/layout churn. | 2026-02-12 |

## 2026-02-09

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Import→Render / Zoom / Mode switches | Prevent zoom drift across renderers and baseline lock | Include 2D renderer in zoomViewKey, apply immediate fit-to-screen initial transforms, and persist zoom-mode flags in the Document Structure baseline snapshot so renderer/mode switches do not leak or drift across surfaces. | `canvas/src/{components/GraphCanvas/{zoomViewKey.ts,scene.ts},components/{GraphCanvas.tsx,FlowCanvas.tsx},hooks/store/{uiSlice.ts,types.ts},features/toolbar/ui/FitToViewButton.tsx}`, tests | Zoom pipeline + baseline snapshot | `buildZoomViewKey`, `setupGraphScene`, FlowCanvas initial zoom effect, `setDocumentStructureBaselineLock` | renderer switches + baseline lock toggle + fit-to-screen auto mode | Stable zoom semantics across D3/Flow/FlowEditor + baseline | Renderer segment gates zoom SSOT; baseline lock forces `{pinned=false, fitToScreen=true, zoomToSelection=false}` and restores prior; pinned disables Fit-to-View; commit initial fit in Flow renderer to avoid missed auto-fit commits. | Manually switch D3↔Flow↔FlowEditor with Pin/Fit/Selection modes and verify Help zoom behaviors; keep AgenticRAG canvas contract in sync. | 2026-02-09 |
| Viewport Controls / 2D+Geospatial | Align pan/zoom/select presets without duplicating logic | Centralize viewport preset semantics (map vs design) and wheel normalization in `grph-shared` and reuse the same helpers across D3/Flow/FlowEditor and Geospatial (MapLibre/Cesium). Pass `viewportControlsPreset` across the host↔gympgrph boundary and implement design-preset wheel-pan + modifier-zoom with restricted drag-pan in geospatial view. | `grph-shared/src/viewport/*`, `canvas/src/lib/canvas/{viewport-controls.ts,zoom-input.ts}`, `canvas/src/pages/Canvas.tsx`, `gympgrph/src/{hostBridge.ts,hooks/useGraphStore.ts,features/geospatial/{GeospatialOverlay.tsx,CesiumOverlay.tsx,mapLibreViewportControls.ts}}` | Viewport preset SSOT | `shouldWheelZoomForPreset`, `shouldAllowPanDragForPreset`, `computeWheelPanDeltaPx`, `computeWheelZoomLog2Delta` | pointer/wheel + preset + host snapshot | Consistent pan/zoom/select semantics across 2D + Geospatial | map preset enables slippy-map defaults; design preset treats wheel as pan unless Ctrl/Cmd, and restricts drag-pan to middle/right or Space+drag; Cesium interactions are gated by Space in design to reduce accidental camera drift. | Sanity-check MapLibre wheel pan + Cmd/Ctrl zoom and Space/middle/right drag pan; ensure no cross-surface interaction when inactive. | 2026-02-09 |
| Canvas 2D Renderer / Flow visibility | Fix Flow renderer blank while D3/FlowEditor visible | Reuse the shared node/edge display filter across 2D renderers and guard Flow initial transform when graph bounds are unknown, so renderer switches cannot hide content or apply stale offscreen pans. | `canvas/src/components/{FlowCanvas.tsx,FlowCanvas/transformGuards.ts,GraphCanvas/{displayFilter.ts,scene.ts}}`, `canvas/src/{__tests__/*,tests/run.ts}` | Display filter SSOT + Flow transform guard | `getGraphDataForDisplay`, `isFlowTransformShowingGraph`, FlowCanvas sceneGraphData memo | GraphData with mixed node types + pinned/unpinned zoomState | Flow nodes/edges visible and initial view fits graph | Always derive `graphDataForDisplay` first; if no finite node positions exist, reject stored transform and prefer fit/identity; otherwise validate candidate transform intersects viewport bounds. | Switch D3↔Flow↔FlowEditor on the same dataset with and without Pin-to-View and confirm no blank canvas; keep AgenticRAG canvas contract aligned. | 2026-02-09 |
| Flow 2D Wheel Zoom | Make wheel zoom feel smooth/continuous | Apply shared continuous wheel factor across all 2D renderers (D3, Flow, Flow Editor), add ctrl/meta pinch boost multiplier, and keep Flow/FlowEditor wheel smoothing animation. Cancel on pan/drag and cleanup RAF on unmount to avoid leaks and cross-surface interference. | `canvas/src/{lib/canvas/flow-zoom-tuning.ts,lib/zoom/actions.ts,hooks/store/{canvasSlice.ts,uiSlice.ts},components/FlowCanvas/{bindNativeInteractions.ts,wheelZoomSmoothing.ts,applyZoomRequestNative.ts},components/GraphCanvas/{zoom.ts,zoomController.ts},features/settings/registry-ui.graph-and-orchestrator.ts}`, docs + schema + tests | FlowCanvas + GraphCanvas zoom | wheel + zoom requests | wheel delta px + anchor + scale extent + tuning + durations + modes | aligned 2D zoom behavior + committed zoomStateByKey | Continuous wheel factor shared; ctrl/meta boost is configurable and bounded; clamp scale; wheel-guard at min/max; cubic-out easing; cancel on pointerdown/pan; zoom requests animate in Flow too; settings are persisted + bounded and shared across 2D renderers. | Tune `wheelZoomCtrlMetaBoostMultiplier`, `flowWheelZoom*`, `zoomDuration*`, and zoom modes (`viewPinned`, `fitToScreenMode`, `zoomToSelectionMode`) in MainPanel Settings; enforce parity across D3/Flow/FlowEditor; keep AgenticRAG canvas contract aligned. | 2026-02-09 |
| Flow 2D Wheel Zoom Defaults | Improve Flow Editor wheel/pinch zoom response | Bump shared wheel speed and ctrl/meta pinch boost defaults and migrate existing default values once via a versioned LocalStorage key; never override custom user-tuned values. | `canvas/src/{lib/canvas/{flow-zoom-tuning.ts,zoom-input.ts},hooks/store/canvasSlice.ts,lib/config.ls.ts,__tests__/flowZoomDefaultsMigration.test.ts,tests/run.ts}`, docs + schema | Zoom tuning + settings migration | `createCanvasSlice`, `computeZoomWheelDeltaYpx` | stored LS values + wheel/pinch events | Faster default zoom response + opt-in user tuning | If defaultsVersion<1 and stored value missing or equals old default: write new defaults and set version=1; else noop. | Validate Flow Editor pinch + wheel zoom; adjust zoom tuning keys in MainPanel Settings if needed. | 2026-02-09 |

## 2026-02-06

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Canvas 2D Interactions / Zoom Step SSOT | Align stepped wheel zoom across 2D renderers | Centralize wheel-step accumulator and constants in zoom-steps SSOT and reuse in D3/Flow handlers to prevent drift. | `canvas/src/{lib/zoom/steps.ts,components/GraphCanvas/zoom.ts,components/FlowCanvas/bindNativeInteractions.ts}` | Zoom step helpers | `applyWheelStepAccumulator`, `computeSteppedZoomTarget` | wheel delta px + accumulator + schema steps | step count + next zoom target | Reset accumulator on intent flip; clamp step burst; use schema steps | Manually verify D3/Flow wheel zoom parity in map/design presets. | 2026-02-08 |
| Floating Panel / Inspector + Node Quick Editor | Keep inspector visible and confirm mapping changes | Ensure Flow Editor inspector portal host resolves even if the slot mounts later; keep Inspector layout visible without selection; keep Node Quick Editor mapping selector visible even when fields are hidden; add registry mapping selector feedback toast on change/clear; update docs and AgenticRAG panels contract. | `canvas/src/components/{FlowEditorCanvas.tsx,FlowEditor/NodeOverlayEditor.tsx,FlowEditor/NodeOverlayEditorForm.tsx}`, `canvas/src/lib/config-copy/uiCopy.ts`, docs + schema | - | `resolveInspectorPortalHost`, `handleRegistrySelectionChange` | portal slot mount + registry selection | Stable Inspector surface + mapping confirmation toast | Observe portal host via MutationObserver; toast on selection/clear; render Inspector layout even with no selection; keep mapping selector visible in collapsed field mode | Verify empty-state Inspector visibility and mapping selection toast in Flow Editor. | 2026-02-06 |
| Flow Editor / Node Quick Editor | Align overlay panel with FloatingPanel SSOT | Refactor Flow Editor Node Quick Editor to reuse `FloatingPanel` shell and add pin/unpin (detach), drag-when-unpinned, minimize/opacity plus AI-Flow-style toolbar + More menu; persist overlay state via new `LS_KEYS.flowNodeQuickEditor*` keys and central `UI_LABELS/UI_COPY`. | `canvas/src/components/FlowEditor/{NodeOverlayEditor.tsx,NodeOverlayEditorForm.tsx}`, `canvas/src/lib/config.ls.ts`, `canvas/src/lib/config-copy/{uiMeta.ts,uiCopy.ts}` | - | - | node selection + toolbar clicks | Stable overlay UX | Use memoized panel controller; avoid hidden background work; semantic HTML only in new overlay. | Update `docs/documents` + `schema/AgenticRAG/panels.jsonld` contract. | 2026-02-06 |
| Flow Editor / Flow / D3 | Improve drag/pan/zoom perf | Remove forced layout reads and rerender storms in 2D interactions: prefer `offsetX/offsetY` over repeated `getBoundingClientRect()` in canvas handlers; throttle pinned quick editor drag updates to rAF; avoid subscribing imperative canvas renderers to high-frequency zoom state commits. | `canvas/src/{lib/canvas/canvas-event-coords.ts,components/FlowCanvas/bindNativeInteractions.ts,components/FlowCanvas.tsx,components/FlowEditor/NodeOverlayEditor.tsx}`, tests + docs + schema | - | `readCanvasLocalPoint` | pointer/wheel events + zoom commits | Smoother interactions | Use offset coords when available; only fallback to rect; batch React state updates to rAF; keep overlays on SSOT wheel-ignore guard. | Update docs + AgenticRAG `canvas.jsonld` perf contract. | 2026-02-06 |
| Flow Editor / Flow | Reduce drag-time collision cost | Throttle collision relax during drag via rAF + min-interval and compute steps with a size-aware SSOT policy; disable drag-time relax for very large graphs; use the same SSOT policy to bound commit-time relax steps. | `canvas/src/components/FlowCanvas/{bindNativeInteractions.ts,useFlowRequestCommit.ts,relaxStepPolicy.ts,relaxScenePositions.ts}`, tests + docs + schema | - | `computeFlowDragRelaxPolicy`, `computeFlowCommitRelaxSteps` | drag + commit | Smoother drag and bounded settle | Avoid per-pointermove full relax; reuse one policy across Flow/FlowEditor; keep behavior bounded by node/group counts. | Add focused regression checks + keep AgenticRAG contracts aligned. | 2026-02-06 |
| Flow Editor / Node Quick Editor | Isolate overlay rerenders on zoom | Ensure zoom/pan updates do not trigger React rerenders of the editor form; update overlay position/scale via store subscription + rAF and apply via DOM style mutation only. | `canvas/src/components/FlowEditor/NodeOverlayEditor.tsx`, docs + schema | - | - | zoom state updates | Smoother zoom with overlay open | Subscribe to `zoomState` and update `aside.style.{left,top,transform}` in rAF; keep `FloatingPanel` body memoized and independent of zoom commits. | Keep docs/schema perf invariants aligned. | 2026-02-06 |
| Canvas 2D Interactions / Overlay Sync | Reduce viewport-edge bounce and pan snap | Clamp wheel anchor near edges, commit pan each move via rAF, keep subpixel anchors, and tighten translate quantization to reduce snap; add regression and update docs/schema. | `canvas/src/lib/canvas/wheel-anchor.ts`, `canvas/src/components/FlowCanvas/bindNativeInteractions.ts`, `canvas/src/components/FlowEditor/NodeOverlayEditor.tsx`, `canvas/src/lib/zoom/zoomStateQuantize.ts`, `canvas/src/__tests__/canvasWheelAnchor.test.ts`, `canvas/src/tests/run.ts`, docs + schema | FlowCanvasRuntime + overlay anchor | `resolveWheelAnchor`, `bindFlowCanvasNativeInteractions`, `quantizeZoomStateForCommit` | pointer/wheel + zoomState | Stable anchor + smoother pan end | Clamp edge within margin; rAF commit on move; avoid anchor rounding; quantize translate to 0.1 | Monitor Safari pinch drift and adjust edge margin if needed. | 2026-02-06 |
| Flow Editor / 2D Interactions | Keep overlays in sync during pan | Commit zoomState during Flow panning (rAF-throttled and interval-gated) so node-attached overlays track without end-of-pan snap; quantize committed zoom values to reduce float-noise churn across Flow + D3. | `canvas/src/components/{FlowCanvas/bindNativeInteractions.ts,FlowCanvas/useFlowRequestCommit.ts,GraphCanvas.tsx}`, `canvas/src/lib/{canvas/interactionTiming.ts,zoom/zoomStateQuantize.ts}` | FlowCanvasNativeInteractions + ZoomCommit | `bindFlowCanvasNativeInteractions`, `useFlowRequestCommit`, GraphCanvas `onZoomTransform` | pointermove pan + wheel zoom | Stable overlay tracking + fewer store commits | rAF gate + interval gate; quantize `{k,x,y}` before compare/write; update overlay using compositor transform only. | Keep AgenticRAG canvas contract + quick-editor perf doc aligned. | 2026-02-06 |
| Flow Editor / Node Quick Editor | Prevent scroll from zooming canvas | Mark Node Quick Editor as a wheel-ignore zone; wheel-zoom handlers early-return when pointer is inside ignore zone to preserve overlay scrolling; only non-ignored wheel paths call `preventDefault()` and apply zoom. | `canvas/src/{lib/canvas/wheel-target-guard.ts,lib/config-copy/uiMeta.ts,components/FlowEditor/NodeOverlayEditor.tsx,components/FlowCanvas/bindNativeInteractions.ts,components/GraphCanvas/zoom.ts}`, tests + docs + schema | - | `shouldIgnoreCanvasWheelEvent` | wheel events | Scroll-only overlay UX | If `elementFromPoint`/rect-hit matches `UI_SELECTORS.canvasWheelIgnore`: return (no zoom, no preventDefault); else compute zoom and `preventDefault()`. | Update docs + AgenticRAG `canvas.jsonld` overlay scroll isolation contract. | 2026-02-06 |
| Canvas 2D Renderer (D3/Flow) | Remove duplicated relax/text logic | Centralize collision relax step runner + cached label wrap/truncate helpers; refactor both Graph structured relax and Flow post-layout relax to reuse SSOT runner; replace ad-hoc `slice()` label clipping with width-based `truncateTextWithEllipsis` using shared char-width estimation. | `canvas/src/lib/{graph/collision/relaxRunner.ts,ui/text/labelText.ts}`, `canvas/src/components/{GraphCanvas/layout/relax.ts,GraphCanvas/layout/utils.ts,FlowCanvas/relaxPositions.ts,FlowCanvas/nativeRuntime.ts}` | - | `runRelaxSteps`, `truncateTextWithEllipsis` | nodes + schema collision knobs + label width | Stable overlap settling + consistent labels | One alpha schedule + damping loop; cached truncation prevents per-frame recalculation; forbid per-surface copy/paste of relax loops and label clipping constants. | Update docs (`knowgrph-graph-canvas-layout-ssot.md`, `knowgrph-native-overlap-resolution-document.md`) + AgenticRAG `canvas.jsonld` label SSOT note. | 2026-02-06 |
| Canvas 2D Interactions | Align Flow Editor with D3/Flow zoom UX | Reuse SSOT wheel delta normalization + zoom factor; ignore wheel zoom during node/group drag; bound fallback anchor age but refresh fallback while wheeling to prevent viewport-edge bounce; add min-zoom bounce guard. | `canvas/src/{lib/canvas/{zoom-input.ts,wheel-anchor.ts,zoom-wheel-guard.ts},components/FlowCanvas.tsx,__tests__/*,tests/run.ts}` | - | - | wheel + drag + scaleExtent | Stable zoom/drag | Block drift at minScale; anchor uses recent pointer only; no opposite-direction jump while dragging. | Document viewport contract in renderer doc + AgenticRAG README. | 2026-02-06 |
| Flow Editor / Node Quick Editor / Zoom | Preserve macro view at zoom extremes | Scale Flow Node Quick Editor overlay via SSOT helper using `{minK,maxK}` so it stays small at max zoom-out and max zoom-in; clamp, avoid rerenders (CSS transform only), and add bounded regression test. | `canvas/src/components/FlowEditor/{NodeOverlayEditor.tsx,nodeQuickEditorZoom.ts}`, `canvas/src/__tests__/nodeQuickEditorZoom.test.ts`, `canvas/src/tests/run.ts` | - | - | zoom k + schema extent | Stable overlay scale | Use extent-normalized curve; keep anchored placement clamped by scaled size. | Document overlay zoom contract in docs + AgenticRAG README. | 2026-02-06 |
| Canvas UI Chrome | Remove duplicate mode chrome | Move Navigate button into Toolbar; delete left mode rail; surface zoom/pan/selection/renderer metrics inside Status Panel; add shared viewport format helper. | `canvas/src/{components/Toolbar.tsx,features/spotlight/LaunchSpotlightStatusCard.tsx,lib/canvas/viewport-format.ts,pages/Canvas.tsx}` | - | - | toolbar clicks | Unified chrome + status | Toolbar is SSOT for mode controls; Status Panel owns viewport metrics. | Add bounded smoke for Navigate clears selection if drift appears. | 2026-02-06 |
| Canvas 2D Interactions | Align Flow Editor pan/zoom with D3/Flow | Centralize wheel delta normalization + zoom sensitivity SSOT; apply same wheel scaling to D3 Zoom and FlowCanvas (used by Flow Editor); prevent default scroll/zoom while interacting. | `canvas/src/{components/FlowCanvas.tsx,components/GraphCanvas/zoom.ts,lib/canvas/zoom-input.ts,__tests__/canvasZoomWheelParity.test.ts,tests/run.ts}` | - | - | wheel + trackpad pinch | Consistent 2D zoom | D3 `wheelDelta` uses SSOT mapping so `pow(2,delta)` matches native `exp(-dy*s)`; FlowCanvas uses non-passive wheel + preventDefault. | Extend to touch pinch if mobile parity becomes required. | 2026-02-06 |
| Canvas 2D Interactions | Fix Flow drag/zoom UX glitches | Prevent text selection/dragstart while interacting with Flow/Flow Editor canvas; stabilize wheel zoom anchor when WheelEvent coords are outside canvas; suppress min-zoom bounce from inertial wheel jitter. | `canvas/src/{components/FlowCanvas.tsx,lib/canvas/{interaction-user-select.ts,wheel-anchor.ts},__tests__/canvasWheelAnchor.test.ts,tests/run.ts}` | - | - | pointer+wheel | Stable interaction | Lock global user-select during pointer capture; resolve wheel anchor by rect containment + fallback; ignore jitter deltas at min/max. | Add pinch-specific anchor tests if Safari pinch drift reappears. | 2026-02-06 |
| Canvas 2D Interactions / Zoom Requests | Fix zoom-out viewport-edge bounce | Change zoom-in/out requests to scale about current viewport center (not graph centroid) so panned views remain stable; keep fit/reset centering; add bounded regression test. | `canvas/src/lib/zoom/{actions.ts,viewport.ts}`, `canvas/src/__tests__/zoomInOutViewportCenterNoBounce.test.ts`, `canvas/src/tests/run.ts` | - | `computeZoomTransformFromRequest`, `computeTransformScaleAboutViewportCenter` | zoomRequest + currentTransform + viewport dims | Stable zoom-in/out without recenter jump | Preserve world center; clamp scale by schema/autoMinScale | Update renderer doc + AgenticRAG `canvas.jsonld` pan/zoom contract. | 2026-02-06 |
| Flow Editor / Node Quick Editor / Viewport Clamp | Fix quick editor border bounce on zoom-out | Clamp detached Node Quick Editor using Flow viewport dims (not window) and run clamp in layout effect (no post-paint snap); centralize clamp helper and add bounded tests. | `canvas/src/{components/FlowEditor/NodeOverlayEditor.tsx,lib/ui/overlayClamp.ts,lib/react/useIsomorphicLayoutEffect.ts,features/panels/ui/Tooltip.tsx,features/toolbar/useToolMenuState.ts,__tests__/overlayClamp.test.ts,tests/run.ts}` | - | `clampOverlayTopLeftToViewport`, `useIsomorphicLayoutEffect` | zoom k + viewport dims + panel size | Stable overlay at border | Snap to integer px; avoid re-render loops | Update docs + AgenticRAG `canvas.jsonld` overlay clamp note. | 2026-02-06 |
| Flow Editor / Node Quick Editor / Node Parity | Make quick editor behave like node | When attached to node, Node Quick Editor should track world→screen without viewport clamping (can leave viewport like nodes); when detached, clamp to Flow viewport; forbid circle nodes in Flow Editor render. | `canvas/src/{components/FlowEditor/NodeOverlayEditor.tsx,components/FlowCanvas.tsx,components/FlowCanvas/shape.ts,components/FlowEditorCanvas.tsx,__tests__/flowNodeShapeForbidCircle.test.ts,tests/run.ts}` | - | `coerceFlowNativeNodeShape` | selection + zoom + schema shapes | Node-like editor behavior | Attached mode has no clamp; detached clamps; coerce `circle→rect` | Update renderer doc + AgenticRAG canvas contract. | 2026-02-06 |
| Flow Editor / Node Quick Editor / Port Handles | Implement More-menu actions without drift | Implement Node Quick Editor More actions: enable Port Handles for all nodes by setting `schema.behavior.portHandles.{enabled,showAllInputs}`; convert selected node to Loop by setting `node.type='Loop'` and `node.properties['workflow:kind']='loop'`; centralize form copy into `UI_LABELS/UI_COPY` and keep actions bounded + tested. | `canvas/src/{components/FlowEditorCanvas.tsx,components/FlowCanvas/{handles.ts,buildNativeScene.ts},components/FlowEditor/{NodeOverlayEditor.tsx,NodeOverlayEditorForm.tsx},lib/{config.flow-editor.ts,config-copy/{uiMeta.ts,uiCopy.ts},graph/schemaTypes.ts},__tests__/flowElkMultipleHandles.test.ts}` | FlowEditorCanvas + FlowCanvas scene builder | `enableHandlesForAllInputs`, `convertSelectedNodeToLoop`, `ensureFlowHandlesHaveDefaults` | selection + schema + draft graph | Deterministic handles + loop semantics | Set schema flags only when baseline unlocked; inject default handles only when enabled+showAllInputs; keep UI copy SSOT and avoid rerender churn. | Update docs + AgenticRAG `canvas.jsonld`/`panels.jsonld` semantics; add optional UI smoke when harness exists. | 2026-02-06 |
| Flow Editor / Node Quick Editor / Schema Ports | Support field-level port handles like schema nodes | Render schema field rows from `node.properties['schema:fields']` with per-row in/out ports and bind edges via `edge.properties['flow:sourcePortKey'|'flow:targetPortKey']` (`field:<id>`); validate schema ports + endpointMatrix in Flow add-edge mode; render field mapping via `edge.properties['flow:displayLabel']`; hide native selected-node ports while quick editor is open to prevent duplicates; add bounded tests + docs. | `canvas/src/{components/FlowEditorCanvas.tsx,components/FlowCanvas/{handles.ts,buildNativeScene.ts,nativeRuntime.ts},components/FlowEditor/{NodeOverlayEditorPanel.tsx,NodeOverlayEditorForm.tsx,FlowEditorInspector.tsx},components/GraphCanvas/layers/edgeLabels.ts,components/GraphHoverTooltip.tsx,features/schema/validation.ts,lib/graph/flowPorts.ts,__tests__/{flowSchemaFieldPortKeys.test.ts,flowNodeQuickEditorSchemaFieldPorts.test.ts,flowSchemaPortEdgeValidation.test.ts}}` | FlowEditorCanvas + FlowCanvas scene builder + Node Quick Editor + Schema validation | `computeFlowHandlesByNode`, `canAddEdge`, `readSchemaFieldSpecs`, `buildFlowEdgeDisplayLabelFromPorts` | schema field list + edge port keys + selection + endpointMatrix | Row-aligned ports + stable attachments + schema-aware validation + edge label override | Prefer schema-derived stable ports; avoid DOM measurement by co-locating handles in rows; keep changes additive and cached; forbid ReactFlow. | Update quick-editor + port-handles docs and AgenticRAG canvas/panels contracts. | 2026-02-06 |
| MainPanel / Flow Editor Manager | Make Node Quick Editor registry configurable and persistent | Rename MainPanel `Workflow` tab to `Workflow Manager`; add `Flow Editor Manager` tab that renders a registry table to create/edit/delete Node Quick Editor mappings (nodeTypeId/editorType/formId → fields/ports/schemaPaths) with conflict gating and local persistence (`LS_KEYS.flowEditorManagerNodeQuickEditorRegistry`); keep UI copy SSOT and add bounded tests. | `canvas/src/{features/panels/MainPanel.tsx,features/panels/views/FlowEditorManagerView.tsx,features/flow-editor-manager/*,hooks/store/flowEditorManagerSlice.ts,lib/{config.ls.ts,config-copy/{uiMeta.ts,uiCopy.ts}},__tests__/flowEditorManagerRegistry.test.ts,tests/run.ts}`, docs + AgenticRAG schema | MainPanel + store slice + registry UI | `upsertNodeQuickEditorRegistryEntry`, `validateNodeQuickEditorRegistryEntry` | registry edits + search filter | Registry-managed Node Quick Editor creation/update | Enforce one enabled mapping per (nodeTypeId/editorType/formId); require at least one field or port; persist via centralized LS key; avoid rerender churn via memoized filtering and split editor components. | Extend registry to drive Quick Editor rendering (field types) and add UI smoke once harness exists. | 2026-02-06 |

## 2026-02-07

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Live Sync (Canvas ↔ Editor Workspace ↔ Graph Data Table) | Keep Node Quick Editor visible across surfaces and mode switches | Centralize `openQuickEditorNodeIds` in graph store; gate visibility in Canvas and Graph Table Inspector; reuse Graph Table inspector overlay in Editor Workspace; persist pin/minimize/hide-fields via LS keys; update docs + AgenticRAG panels contract. | `canvas/src/hooks/store/{graphViewSlice.ts,graphDataSlice.ts}`, `canvas/src/components/FlowEditorCanvas.tsx`, `canvas/src/features/graph-table/ui/GraphTableInspector.tsx`, docs + AgenticRAG | Graph store + FlowEditorCanvas + GraphTableInspector | `setOpenQuickEditorNodeIds`, `resolveNodeQuickEditorRegistryEntry` | selection/create/drag + workspace switch | Stable quick editor overlay visibility across Canvas/Table/Editor | Use shared store list for open overlays; render NodeOverlayEditorPanel in table inspector when nodeId in open list; persist LS keys for overlay UI state; document SSOT contracts. | Add bounded integration test for Table↔Canvas overlay sync; extend AgenticRAG panels.jsonld with live-sync contract. | 2026-02-07 |
| Live Sync (Canvas ↔ Editor Workspace ↔ Graph Data Table) | Remove Editor inspector and show Quick Editor codes inline | De-dupe preview sync payloads to prevent update-depth loops; keep `openQuickEditorNodeIds` stable across remounts; forbid Editor Workspace selection inspector dock; auto-show Node Quick Editor bundle codes in Markdown editor/viewer (JSON/MD + copy) using the open list and selection. | `canvas/src/pages/Canvas.tsx`, `canvas/src/components/{EmbeddedEditorShell.tsx,FlowEditorCanvas.tsx}`, `canvas/src/components/BottomPanel/markdownWorkspace/MarkdownWorkspace.tsx`, `canvas/src/features/graph-table/ui/GraphTableSelectionInspector.tsx`, docs + AgenticRAG | Editor Workspace + Preview sync + Markdown Workspace | `onMessage`, `updateOpenQuickEditorNodeIds`, `setContentModeSafe` | kg-preview-sync messages + node selection + open ids | Stable preview + inline code surface | If incoming schema/graph hash unchanged: ignore; if quick-editor node exists: auto-switch Markdown surface; if user forces Document: do not auto-switch. | Add bounded UI test for Editor inline quick-editor codes and preview-sync de-dupe. | 2026-02-07 |

## 2026-02-08

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Floating Panel + MainPanel + Node Quick Editor | Align pin/unpin semantics across all panels | Enforce SSOT pin semantics: pinned disables drag (blue pin icon), unpinned enables drag (floating). Centralize pin-toggle styling helper; fix Floating Panel drag gating; add bounded regression test and update docs + AgenticRAG panels contract. | `canvas/src/features/{toolbar/ToolbarToolMenu.tsx,panels/ui/HeaderActions.tsx}`, `canvas/src/components/FlowEditor/NodeOverlayEditorPanel.tsx`, `canvas/src/lib/ui/pinToggle.ts`, tests + docs + schema | - | - | pin toggle clicks + header pointerdown | Consistent panel drag behavior | If pinned: ignore header pointerdown; if unpinned: allow header drag unless interactive element. | Sanity-check pin drag behavior in MainPanel/FloatingPanel/Quick Editor; keep schema/docs aligned. | 2026-02-08 |
| Flow Editor / Node Quick Editor Toolbar | Document selection-driven toolbar visibility and action relocation | Update Node Quick Editor docs to note toolbar hidden by default, shown on selection, and actions moved to toolbar with More/Port Handles removed; align AgenticRAG panels contract accordingly. | `docs/documents/knowgrph-flow-editor-node-quick-editor-document.md`, `schema/AgenticRAG/panels.jsonld` | Node Quick Editor toolbar contract | Docs/schema update | toolbar visibility + actions change | Aligned docs + schema | Prefer toolbar-only actions; hide on unselect/outside click to avoid idle UI clutter. | Validate toolbar visibility behavior in Flow Editor and keep cross-repo docs aligned. | 2026-02-08 |
| Canvas 2D Interactions / Viewport Controls Preset | Align D3/Flow/FlowEditor pan drag mapping | Centralize pan-drag gating in viewport-controls SSOT; D3 zoom filter uses preset + space-pan to allow map:left-drag, design:middle/right or space+drag; prevent design context menu; forbid per-surface drift. | `canvas/src/lib/canvas/viewport-controls.ts`, `canvas/src/components/GraphCanvas/zoom.ts`, `canvas/src/__tests__/viewportControlsPanDragPreset.test.ts` | ViewportControlsPreset | `shouldAllowPanDragForPreset`, D3 zoom filter | pointer events + preset + space-pan | Consistent 2D pan/zoom controls | Map preset pans via left-drag; design preset pans via middle/right or space+left; touch always pans. | Extend with D3 marquee selection parity only if required; keep schema/docs aligned. | 2026-02-08 |
| Flow Editor / Node Quick Editor / Dataflow | Support connected-value transforms + auto-apply | Add native multi-hop connected-value propagation and registry schemaMappings with optional reduceId/transformId; expose apply-all + auto-apply to fill empty fields without mutating GraphData during compute. | `canvas/src/lib/flowEditor/{flowDataflow.ts,flowDataflowTransforms.ts}`, `canvas/src/components/FlowEditor/NodeOverlayEditorRegistrySection.tsx`, `canvas/src/features/flow-editor-manager/nodeQuickEditorRegistryTypes.ts`, docs + schema | FlowConnectedValuesBySchemaPath + NodeQuickEditorRegistrySchemaMapping | `computeFlowConnectedValuesBySchemaPath`, applyConnectedToEmptyFields | edges + ports + schemaMappings | connectedValuesBySchemaPath + field updates | Iterate until stable or bounded; apply reducer→transform; only write when field empty and value defined. | Document transform/reduce ids in mapping docs + AgenticRAG canvas contract. | 2026-02-08 |
| Graph Data Table Inspector / Node Quick Editor | Keep connected-value semantics consistent across modes | Compute and pass `connectedValuesBySchemaPath` to the shared Node Quick Editor panel in Graph Table Inspector; add native color helper transforms (`rgb_css`, `rgb_hex`, `contrast_text`) and bounded tests to validate compute stability. | `canvas/src/features/graph-table/ui/GraphTableInspector.tsx`, `canvas/src/lib/flowEditor/flowDataflowTransforms.ts`, `canvas/src/__tests__/flowDataflowConnectedValues.test.ts`, `canvas/src/tests/run.ts`, docs + schema | GraphTableInspector + Flow dataflow transforms | `computeFlowConnectedValuesBySchemaPath`, `applyFlowDataflowTransform` | GraphData + registry + selected node id | Connected-value hints + computed outputs in table mode | Compute only when Quick Editor is visible; treat unknown transform ids as no-op; keep compute pure and memoized. | Add a small UI harness test for table-mode Connected hints if DOM harness expands. | 2026-02-08 |
| MainPanel / Dashboard | Expose Stats/Dashboard outside Graph Fields | Add `dashboard` tab beside `graphFields` in MainPanel and render the Stats/Dashboard panel there; remove the Stats panel from Graph Fields grid to prevent mixed ownership and reduce re-render churn. | `canvas/src/features/panels/{MainPanel.tsx,views/{DashboardView.tsx,GraphFieldsView.tsx},ui/MainPanelDashboardHeader.tsx}`, `canvas/src/features/toolbar/hooks/useMainPanelDrag.ts`, `canvas/src/lib/config-copy/uiMeta.ts` | MainPanel tab system | `DashboardView` | MainPanel tab click | Stats/Dashboard view reachable via MainPanel | `graphFields` no longer mounts Stats; `dashboard` mounts `BottomPanelStatsTab`. | Smoke-check MainPanel Dashboard tab and update docs/schema semantics. | 2026-02-07 |
| Canvas 2D Interactions / Flow Editor Viewport Preset | Design preset selects on drag in Flow Editor | Ensure Flow Editor honors viewport preset selection semantics: in `design` preset, pointer drag performs lasso selection (pan via scroll/middle/right or space+drag); in `map`, selection drag requires shift. Centralize decision helpers; add bounded tests and update docs/schema. | `canvas/src/components/FlowCanvas/{bindNativeInteractions.ts,FlowCanvas.tsx}`, `canvas/src/lib/canvas/viewport-controls.ts`, `canvas/src/__tests__/{viewportControlsPanDragPreset.test.ts,viewportControlsSelectionDragPreset.test.ts}`, docs + schema | FlowCanvasNativeInteractions | `shouldStartSelectionDragForPreset`, `bindFlowCanvasNativeInteractions` | pointer events + preset + renderer variant | Consistent design-tool selection and pan | If flowEditor+design: selectionOnDrag=true; if map: shiftKey gates selection drag; forbid left-drag panning in design unless space. | Sanity-check Flow Editor drag-select and pan in both presets; keep AgenticRAG canvas contract aligned. | 2026-02-08 |
| Flow Editor / Node Quick Editor / Schema Ports | Make schema fields editable and edge-safe | Render `schema:fields` as an editable schema table (add/remove/name/type) in Node Quick Editor; on field rename, rewrite port-bound edges and recompute `flow:displayLabel`; use SSOT UI labels/tokens and avoid re-render churn. | `canvas/src/components/{FlowEditorCanvas.tsx,FlowEditor/NodeOverlayEditor.tsx,FlowEditor/NodeOverlayEditorPanel.tsx,FlowEditor/NodeOverlayEditorForm.tsx,FlowEditor/NodeOverlayEditorSchemaTable.tsx,FlowEditor/NodeOverlayEditorRegistrySection.tsx}`, `canvas/src/lib/config-copy/uiMeta.ts`, docs + schema | FlowEditorCanvas + NodeOverlayEditorSchemaTable | `renameSchemaFieldIdByNodeId` | schema field edits + rename | Consistent port keys + display labels | If `field:<prev>` is present on edge source/target: rewrite to `field:<next>` and recompute display label using patched schema fields; normalize graph and keep updates idempotent. | Add bounded regression for schema field delete + dangling edge behavior; keep AgenticRAG canvas contract aligned. | 2026-02-07 |
| Flow Editor / Node Quick Editor / Key-Value UX | Make all node fields schema-like | Render Node, Smart fields, Mapping, and Registry fields in schema-like Key/Type/Value tables; reuse SSOT typography/tokens and forbid per-field bespoke spacing/layout; keep semantic HTML only and avoid rerender churn. | `canvas/src/components/FlowEditor/{NodeOverlayEditorForm.tsx,NodeOverlayEditorRegistrySection.tsx,NodeOverlayEditorKvTable.tsx}`, `canvas/src/lib/config-copy/uiMeta.ts`, docs + AgenticRAG | Node Quick Editor | `NodeOverlayEditorKvTable` | field edit | Consistent schema-style Key/Type/Value editing | Prefer a shared table component; keep label→input association via ids; ensure registry fields preserve schemaPath mapping logic. | Add a bounded UI harness test for schema-like KV table rendering if DOM harness expands. | 2026-02-07 |
| Import→Render / Flow Editor / Registry | Add neutral JSON bundle for Quick Editor mappings | Add a project-agnostic `kg:flow:nodeQuickEditorBundle` JSON envelope to ingest/export Node Quick Editor registry entries; on graph import, emit mappings into `GraphData.metadata['flow:nodeQuickEditorRegistry']` and auto-apply to the store; add Manager import/export buttons and bounded parser tests. | `canvas/src/{lib/graph/io/{adapter.ts,quickEditorImport.ts,nodeQuickEditorBundle.ts},hooks/store/{graphDataSlice.ts,graphDataSliceUtils.ts},features/panels/views/FlowEditorManagerView.tsx,lib/{config.flow-editor.ts,config-copy/{uiMeta.ts,uiCopy.ts}},__tests__/quickEditorBundleImport.test.ts}`, docs + AgenticRAG schema | Import adapter + store applier + Manager UI | `tryParseQuickEditorImportGraphData`, `applyNodeQuickEditorRegistryFromMetadata` | bundle JSON + workflow JSON | Importable mappings + immediate Quick Editor render | Detect bundle kind/version first; validate+normalize before persisting; avoid redundant registry writes by signature compare. | Update docs and add AgenticRAG contract file for the bundle. | 2026-02-06 |
| MainPanel / Flow Editor Manager | Add Mapping/Specification tabs and neutral specs | Refactor Flow Editor Manager to provide Mapping and Specification tabs; implement row-based mapping editor (Key/Type/Value/Required/Direction), plus neutral Node/Workflow spec schemas with persistence; reuse panel typography and SSOT copy/tokens; keep changes bounded with CI tests. | `canvas/src/{features/panels/views/FlowEditorManagerView.tsx,features/flow-editor-manager/*,lib/config.ls.ts,lib/config-copy/uiMeta.ts}`, tests + docs + schema | FlowEditorManager UI + spec schema helpers | `buildMappingRowsFromRegistryEntry`, `applyMappingRowsToRegistryEntry`, `validateFlowNodeSpec`, `validateFlowWorkflowSpec` | registry entries + spec JSON + search query | Tabbed manager + persisted specs + editable mappings | Validate unique keys and (direction,key) ports; gate spec parsing by kind/version; persist to namespaced LS keys; avoid rerender churn by splitting tab panels. | Update `docs/documents` and AgenticRAG panels/schema contracts to reflect new tabs and LS keys. | 2026-02-06 |
| Import→Render / Flow Editor | Add Workflow Editor-like DnD palette + auto-open on import | Add Node Quick Editor drag payload SSOT and Flow Editor Manager palette; allow dropping palette items onto Flow Editor canvas to create typed nodes and set quick editor override keys; when imported JSON contains quick-editor registry metadata, auto-switch Canvas to 2D Flow Editor. | `canvas/src/{features/flow-editor-manager/NodeQuickEditorPalette.tsx,components/FlowEditorCanvas.tsx,features/toolbar/jsonImportAction.ts,lib/{config.flow-editor.ts,flowEditor/nodeQuickEditorDrag.ts},__tests__/*}`, docs + AgenticRAG schema | Palette + FlowEditorCanvas DnD | `setFlowNodeQuickEditorDragDataTransfer`, `readFlowNodeQuickEditorDragPayloadFromDataTransfer`, FlowEditorCanvas drop handler | drag events + JSON import result | Node creation + Flow Editor activation | Detect drag payload by kind/version; resolve enabled registry entry; compute world coords via `screenToWorld`; switch view on registry-metadata presence only. | Extend AgenticRAG schema with video-generation quick editor contract and keep docs sync updated. | 2026-02-06 |
| Flow Editor Manager / Registry Rows | Reuse Graph Fields drag-reorder + visibility UX | Add GraphFields-style drag reorder handle and show/hide control for mapping rows; persist `isHidden` on registry fields/ports; ensure import-from-metadata, Node Quick Editor UI, and Flow port handles ignore hidden items; keep semantic HTML and SSOT typography/tokens; avoid rerender churn. | `canvas/src/{features/flow-editor-manager/{FlowMappingRowsTable.tsx,FlowMappingRowsEditor.tsx,NodeQuickEditorRegistryDrawer.tsx,mappingRows.ts,nodeQuickEditorRegistryTypes.ts},hooks/store/flowEditorManagerSlice.ts,components/FlowEditor/NodeOverlayEditorRegistrySection.tsx,components/FlowCanvas/handles.ts}`, docs + AgenticRAG schema | `NodeQuickEditorRegistryField/Port`, `FlowEditorMappingRow` | `validateNodeQuickEditorRegistryEntry`, `computeFlowHandlesByNode` | mapping row edits + bundle JSON + GraphData.metadata registry | Hidden-aware registry + draggable mapping rows + suppressed hidden handles | If `isHidden=true`: hide in Node Quick Editor and skip derived handle generation; else render/apply. | Add a bounded unit test for hidden registry ports/fields and keep AgenticRAG bundle/panels contracts aligned. | 2026-02-07 |

## 2026-02-07

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Flow Editor / Node Quick Editor / Bundle Export | Show multiple node editors and enable roundtrip export | Support concurrent Node Quick Editors: scope DOM ids per node, auto-stack overlays, add workflow+node bundle export actions. | `canvas/src/components/{FlowEditorCanvas.tsx,FlowEditor/FlowEditorInspector.tsx,FlowEditor/NodeOverlayEditor.tsx,FlowEditor/NodeOverlayEditorForm.tsx}`, `canvas/src/lib/{graph/file.ts,config-copy/uiCopy.ts}`, tests + docs + schema | NodeOverlayEditor + NodeOverlayEditorForm + FlowEditorCanvas | `exportWorkflowBundle`, `runWorkflowNode`, `exportNodeQuickEditorBundleAsJson` | palette drop + workflow actions | Visible multi-node editors + exported bundle JSON | Apply deterministic stack offsets; generate per-node `idBase`; export graph+registry (match node types, fallback to resolved entry). | Run bounded CI filter for bundle roundtrip; keep AgenticRAG bundle contract updated. | 2026-02-07 |

## 2026-02-08

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Flow Editor / Node Quick Editor / Dataflow | Compute connected inputs and surface in Quick Editor | Compute connected values by schemaPath from port-bound edges and registry ports/schemaMappings; surface Connected hints + Apply action; keep compute pure (no hidden GraphData mutation) and memoized; update SSOT UI copy, tests, and docs/schema contracts. | `canvas/src/lib/flowEditor/flowDataflow.ts`, `canvas/src/components/FlowEditorCanvas.tsx`, `canvas/src/components/FlowEditor/NodeOverlayEditorRegistrySection.tsx`, `canvas/src/lib/config-copy/uiCopy.ts`, `canvas/src/__tests__/flowDataflowConnectedValues.test.ts`, `canvas/src/tests/run.ts`, docs + AgenticRAG schema | FlowConnectedValuesBySchemaPath map | `computeFlowConnectedValuesBySchemaPath` | GraphData + Node Quick Editor Registry + port-bound edges | Connected-value UI hints + explicit apply-to-properties writes | Resolve per-node registry entry; read `flow:sourcePortKey`/`flow:targetPortKey`; map source output schemaPath value into target input schemaPath; apply `schemaMappings` over `{in,node}`; compute only for open overlay node ids. | Extend to schema-field ports and add a deterministic multi-source reduction policy per port. | 2026-02-08 |
| MainPanel / Flow Editor Manager | Align mapping editor documentation with UI behavior | Document mapping editor header ownership, JSON Key column, and direction semantics (Default=input+output, input-only, output-only) and state that Apply/Reset in MainPanel header gates persistence and enables only on dirty edits. | `docs/documents/knowgrph-flow-editor-node-quick-editor-document.md`, `schema/AgenticRAG/panels.jsonld` | docs:Mapping Editor (Flow Editor Manager) | n/a | mapping editor UI changes | Updated docs + schema contract | Capture SSOT rules to prevent Flow Editor Manager drift. | Verify Flow Editor Manager Apply/Reset gating and mapping row direction behavior. | 2026-02-08 |
| Flow Editor / Node Quick Editor / KV Layout | Standardize quick editor column grid and port dots | Document the 5-column In Port/Key/Type/Value/Out Port layout (1%/29%/10%/59%/1%), render port dots for every key row, align value inputs/selects via SSOT classes, and forbid horizontal scrolling; keep docs and AgenticRAG canvas contract aligned. | `docs/documents/knowgrph-flow-editor-node-quick-editor-document.md`, `schema/AgenticRAG/canvas.jsonld`, `todo-log.md` | Node Quick Editor layout contract | n/a | quick editor KV rows | Stable KV grid + port-dot continuity | Use SSOT keyValueInputClass for consistent borders; keep dots centered on the border line. | Re-validate quick editor grid visually and keep schema strings aligned if layout changes. | 2026-02-08 |
| Docs / Flow Editor / Edge Creation Paths | Keep edge creation rules aligned across surfaces | Document edge-creation sources (Flow handles + Quick Editor port dots), port-key binding, and displayLabel usage; align AgenticRAG panels contract with the same edge-creation path. | `docs/documents/knowgrph-flow-editor-node-quick-editor-document.md`, `docs/documents/knowgrph-port-handles-and-subgraph-layout-document.md`, `schema/AgenticRAG/panels.jsonld` | Docs + panel contract | n/a | edge drag from handles/port dots | Aligned edge-creation semantics | When port dots are used, set flow:sourcePortKey/flow:targetPortKey; otherwise fall back to edge.id; Quick Editor open suppresses native handles for the selected node. | Verify edge drag from canvas vs quick editor writes correct port keys and labels. | 2026-02-08 |
| Docs / Import→Render / Computing Data Flows | Document end-to-end bundle import→render behavior | Add an end-to-end doc for importing a `kg:flow:nodeQuickEditorBundle` computing-data-flows fixture and rendering it in Flow Editor with native Flow edge drawing and multi-node Node Quick Editor layout; update LocalStorage keys and pin semantics; align AgenticRAG pipeline/panels contracts. | `docs/documents/{knowgrph-computing-data-flows-import-render-pipeline-document.md,knowgrph-pipeline-document.md,knowgrph-flow-editor-node-quick-editor-document.md,knowgrph-local-storage-document.md}`, `schema/AgenticRAG/{pipeline.jsonld,panels.jsonld}` | Docs + schema contracts | n/a | bundle JSON + Flow Editor render | Updated docs + schema alignment | Reuse parser adapter bundle detection and native Flow renderer edges; persist detached positions per node id to avoid overlap. | Run the fixture import manually and verify edges + multi-editor layout remain stable across reload. | 2026-02-08 |
## 2026-02-05

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Collision / Verification | Verify no-stick behavior | Add regression test `groupBoxBorder.test.ts` to verify inner/outer border constraints and sum-of-gaps logic; ensure packed R-tree broadphase works correctly for nested/border cases. | `canvas/src/__tests__/groupBoxBorder.test.ts` | - | - | groups + nodes | Pass/Fail | Verify `vx` push direction and magnitude | Merge test into CI suite | 2026-02-05 |
| Collision        | No-Stick Z Gating            | Forbid inner border (x2..x4/y2..y4/z2..z4) snap/touch/stick to other outer envelope (x1..x5/y1..y5/z1..z5); enable Z only when explicitly used (cz/halfD/gapZ) to avoid accidental Z pushes | `grph-shared/src/collision/boxCollision.ts`, `canvas/src/__tests__/groupBoxNoStickRegression.test.ts`, `canvas/src/tests/run.ts` | `resolveGroupCollisions` | groups + nodes + touchEpsilon | Stable separation | 2D collisions stay 2D; Z separation works when configured; regression coverage added | 2026-02-05 |
| Collision / Nested containment | Keep child inner border away from parent outer envelope | Use indexed borders for containment: push child x2..x4 inside parent x1..x5 (axis-aware gaps + nested touch-epsilon), gating Z by explicit depth | `canvas/src/components/GraphCanvas/layout/groupOverlapByDepth.ts` | - | `createGroupBboxCollideForceByDepth` | group states | Stable nested spacing | Containment uses outer envelope indices; Z only when explicit | Add bounded nested-containment regression if drift appears. | 2026-02-05 |
| 2D Renderer / Flow Editor | Add native Flow Editor on same canvas | Add `flowEditor` 2D renderer variant with draft graph editing + explicit commit; share 2D zoom viewKey across D3/Flow/FlowEditor while isolating layout cache by renderVariant; enable bounded collision relax during drag only in Flow Editor. | `canvas/src/components/{FlowEditorCanvas.tsx,FlowCanvas.tsx}` | - | - | 2D switch | Stable edit + no-touch drag | Active gating prevents cross-mode interference; collisions use PackedRTree relaxer. | Document renderer contract + schema. | 2026-02-05 |
| Sidebar / SSOT Inspector | Consolidate Node inspector into Graph Data Table inspector | Replace Canvas sidebar "Node" inspector with shared Graph Data Table Record Inspector (dl/header SSOT); sync RxDB graph table with active render view; propagate cell edits back to graph store; remove legacy NodeEditor and duplicate logic. | `canvas/src/pages/Canvas.tsx`, `canvas/src/features/graph-table/ui/*` | - | - | node/edge selection | Unified inspector | Single inspector component used across table + sidebar; global sync guard prevents double sync work. | Keep docs/schema aligned; add bounded selection/edit smoke. | 2026-02-05 |
| Flow Editor / Sidebar Inspector | Consolidate Flow Editor Inspector into right SidePanel Inspector | Portal Flow Editor Inspector into the right SidePanel Inspector tab (no separate floating inspector); fold workflow node list into the same inspector surface; remove FlowEditorWorkflowSidebar overlay; keep panel shell reused via FloatingPanel and semantic HTML. | `canvas/src/{pages/Canvas.tsx,components/FlowEditorCanvas.tsx,components/FlowEditor/FlowEditorInspector.tsx}` | - | - | flowEditor selection | Single inspector surface | Sidebar provides a stable portal slot id; FlowEditorCanvas renders inspector into that slot to avoid cross-mode duplicate panels. | Keep schema/docs aligned; add bounded UI smoke for tab switching + portal presence. | 2026-02-05 |
| UI Shell / FloatingPanel | Consolidate right SidePanel into FloatingPanel SSOT shell | Replace ad-hoc SidePanel containers with a single `FloatingPanel` SSOT shell (`as='div'` + `role='complementary'`), remove legacy duplicate panel wrappers (nested asides), and mount only the active SidePanel tab to prevent hidden background work and re-render churn. | `canvas/src/{components/ui/FloatingPanel.tsx,pages/Canvas.tsx,features/canvas/utils.ts}` | - | - | SidePanel open/tab events | Drift-resistant panel shell | FloatingPanel is the only shell primitive for floating/side panels; SidePanel tab events support `inspector` with legacy `node` compatibility. | Keep contracts/docs aligned; add a bounded portal+tab smoke if drift appears. | 2026-02-05 |
| UI Shell / Panel Consolidation | Remove legacy right Sidebar | Move Inspector/Chat/Geo into the Floating Panel tool menu header; delete Canvas right SidePanel and sidebar state/LS key; route `SIDE_PANEL_OPEN_EVENT` to open Floating Panel views; remove panel-aware zoom/toast offsets to prevent stale layout coupling. | `canvas/src/{features/toolbar/*,pages/Canvas.tsx,components/GraphCanvas/*,hooks/store/*}` | - | - | side panel open events | Single floating panel shell | Open events map to floating views; no background sidebar rerenders; zoom uses full viewport dims. | Update AgenticRAG panels contract. | 2026-02-05 |
| Orchestrator / Panel SSOT | Consolidate Orchestrator into FloatingPanel Graph Traversal | Move all Orchestrator UI into Floating Panel Graph Traversal; delete BottomPanel Orchestrator UI + Text editor; route legacy open actions to graphTraversal event; edit workflow config only in Editor workspace via `orchestrator/graphrag-workflow.jsonld` and sync to store. | `canvas/src/{features/toolbar/ToolbarToolMenu.tsx,features/panels/views/*,components/BottomPanel/markdownWorkspace/*}` | - | - | open events + workspace FS saves | Single traversal surface + file-based editing | Avoid duplicate editors; mount active view only; sync file→store on autosave. | Update docs/schema; add bounded traversal smoke if drift appears. | 2026-02-05 |
| Parser+Schema / Panel SSOT | Move Parser/Schema UI into Workflow; file-based text editing only | Move Parser and Schema Configurator UI surfaces into MainPanel Workflow; remove BottomPanel parser/schema tabs and embedded Monaco text editors; edit parser script only via Editor workspace file `/parser/parser.py` and schema config via `/schema/schema.json`, syncing autosave into stores with bounded parse+status. | `canvas/src/{features/panels/views/WorkflowSection.tsx,features/panels/views/WorkflowSteps.tsx,components/BottomPanel.tsx,components/BottomPanel/markdownWorkspace/MarkdownWorkspace.tsx}`, `curagrph/src/components/BottomPanel/*` | - | - | workflow clicks + workspace autosave | Single workflow surface + no duplicate editors | Forbid duplicate Orchestrator/Parser/Schema text editors; keep UI-only panels; apply schema from workspace file only when JSON parses. | Update AgenticRAG panels contract + docs; bounded smoke. | 2026-02-05 |
| 2D Flow Editor / Node UI + Routing Perf | Add in-canvas node quick edit and remove obstacle recompute | Add semantic Node Quick Editor overlay for selected Flow Editor node; map form fields to `node.properties`; validate required keys; optimize Flow edge routing by building obstacle rects once per draw and ignoring endpoint-contained obstacles; add bounded routing regression. | `canvas/src/{components/FlowEditor/NodeOverlayEditor.tsx,components/FlowEditorCanvas.tsx,components/FlowCanvas/{nativeRuntime.ts,edgeRouting.ts}}`, `canvas/src/__tests__/flowEdgeRouting.test.ts`, `canvas/src/tests/run.ts`, `docs/documents/knowgrph-renderer-document.md`, `huijoohwee.github.io/schema/AgenticRAG/panels.jsonld` | - | - | Flow selection + validate click | Quick edit + stable routing | Single overlay and contract-aligned routing; no E×N obstacle recompute per edge. | Keep schema/docs aligned; add bounded UI smoke once harness exists. | 2026-02-05 |
| UI / Typography SSOT | Align non-markdown panel typography with FloatingPanel baseline | Apply `uiPanelTextFontClass` + `uiPanelKeyValueTextSizeClass` at FloatingPanel content root; remove hardcoded `text-*`/`font-*` inside Inspector components; expose shared `usePanelTypography` helper; add bounded inspector typography test; document contract in SSOT UI doc. | `canvas/src/{features/toolbar/ToolbarToolMenu.tsx,features/graph-table/ui/{GraphTableInspector.tsx,GraphTableSelectionInspector.tsx},components/FlowEditor/FlowEditorInspector.tsx,lib/ui/panelTypography.ts,__tests__/floatingPanelInspectorTypography.test.tsx,tests/run.ts}`, `docs/documents/knowgrph-markdown-ssot-ui-contract-document.md` | - | - | UI settings | Typography-aligned inspector | FloatingPanel inspector and inner components inherit shared typography; bounded regression test guards drift. | Extend token use to remaining panels to avoid `text-xs` drift. | 2026-02-05 |
| UI / Typography SSOT / Cross-repo | Enforce panel typography in gympgrph + curagrph | Define `PanelTypography` SSOT type in `grph-shared/ui/panelTypography`; pass `panelTypography` into `gympgrph` GeospatialPanelHost; remove hardcoded `text-*` from geospatial panel + datasets manager; align curagrph slide chrome (header/footer) to micro-label + monospace classes; add bounded quick editor + geospatial typings verification via typecheck. | `grph-shared/src/ui/panelTypography.ts`, `canvas/src/features/toolbar/ToolbarToolMenu.tsx`, `gympgrph/src/{GeospatialHost.tsx,features/geospatial/{GeospatialPanel.tsx,GeospatialDatasetsManager.tsx}}`, `curagrph/src/features/markdown/ui/{SlideParts.tsx,markdownPresentationSlides.tsx,MarkdownPreviewPresentation.tsx}`, `canvas/src/__tests__/flowNodeQuickEditorTypography.test.tsx`, `canvas/src/tests/run.ts`, `docs/documents/knowgrph-markdown-ssot-ui-contract-document.md`, `huijoohwee.github.io/schema/AgenticRAG/panels.jsonld` | - | - | host UI settings | Drift-resistant typography inheritance | Cross-repo panels inside FloatingPanel inherit the same ladder via typed prop; no hardcoded text sizing in panel bodies. | Add a bounded geospatial DOM smoke once harness exists. | 2026-02-05 |
| UI / Typography SSOT / Workspace + Tables | Align MainPanel, Editor workspace, Graph Data Tables to SSOT ladder | Replace local `text-*`/`font-*` hardcodes in MainPanel + EmbeddedEditorShell + Markdown Workspace + GraphTable; pass `panelTypography` into table/toolbar components; remove text sizing hardcodes from `curagrph` GraphDataTable UI; add shared `grph-shared/ui/tailwindTextSize` for px-only consumers (AG Grid); add bounded host tests for MainPanel + GraphTable typography. | `canvas/src/{features/panels/MainPanel.tsx,components/EmbeddedEditorShell.tsx,components/BottomPanel/{MarkdownWorkspaceToolbar.tsx,markdownWorkspace/MarkdownWorkspaceMain.tsx},features/graph-table/ui/{GraphTableWorkspace.tsx,GraphTableToolbar.tsx,GraphTableSemanticTable.tsx,GraphTableCanvasPreviewDock.tsx,GraphTableGrid.tsx},__tests__/{mainPanelTypography.test.tsx,graphTableTypography.test.tsx},tests/run.ts}`, `curagrph/src/features/graph-data-table/ui/*`, `grph-shared/src/ui/tailwindTextSize.ts`, `docs/documents/knowgrph-markdown-ssot-ui-contract-document.md`, `huijoohwee.github.io/schema/AgenticRAG/panels.jsonld` | - | - | UI settings | Typography-consistent panels + bounded regression guards | All major panel/workspace/table surfaces inherit the SSOT typography ladder; portals apply micro label sizing; AG Grid font px derives from SSOT. | Add a bounded portal typography smoke for GraphDataTable menus once harness is stable. | 2026-02-05 |
| UI / Typography SSOT / Inspector | Align Record Inspector key labels with value inputs | Use panel key/value text size for inspector `dt` labels (no micro-label), keep inputs inheriting the same ladder; update bounded typography test and SSOT docs/schema. | `canvas/src/features/graph-table/ui/GraphTableInspector.tsx`, `canvas/src/__tests__/floatingPanelInspectorTypography.test.tsx`, `docs/documents/knowgrph-markdown-ssot-ui-contract-document.md`, `huijoohwee.github.io/schema/AgenticRAG/panels.jsonld` | - | - | Inspector field labels | Key/value font alignment | FloatingPanel Inspector key labels match input font size; docs and schema reinforce the rule. | Add portal label checks if menu harness adds key/value fields. | 2026-02-05 |
| UI / Typography Token | Align Inspector Key label with Value input | Add `PanelTypography.keyLabelClass` + `PANEL_TYPOGRAPHY_DEFAULTS`; switch Inspector key `<label>` typography to `keyLabelClass` while inputs stay on `textSizeClass`; propagate into `gympgrph` geospatial panel and `curagrph` frozen resize handle; fix toast kind drift. | `grph-shared/src/ui/panelTypography.ts`, `canvas/src/lib/ui/panelTypography.ts`, `canvas/src/components/FlowEditor/{FlowEditorInspector.tsx,NodeOverlayEditor.tsx}`, `gympgrph/src/features/geospatial/{GeospatialPanel.tsx,GeospatialDatasetsManager.tsx}`, `curagrph/src/features/graph-data-table/ui/*` | - | - | UI settings | Typography-aligned labels/inputs | Key labels and value inputs share the same typography ladder across repos; defaults are centralized to avoid drift. | Keep remaining label usages on `microLabelClass` only when truly micro. | 2026-02-05 |

## 2026-02-04

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Import→Render SSOT / Cross-repo alignment | Remove GraphData shape drift and keep Geospatial/Mermaid surfaces consistent | Move `GraphData/GraphNode/GraphEdge/JSONValue` SSOT into `grph-shared/graph/types`; normalize incoming GraphData before commit; pass `useActiveGraphRenderData()` into geospatial snapshots; enable Mermaid ELK layouts by registering `@mermaid-js/layout-elk` loaders before Mermaid init. | `knowgrph/grph-shared/{src,dist}/graph/types.*`, `knowgrph/canvas/src/{lib/graph/normalize.ts,hooks/store/graphDataSlice.ts,pages/Canvas.tsx}`, `curagrph/src/features/panels/views/preview-panel/ui/MermaidDiagram.tsx` | - | `normalizeGraphData`, `setGraphData*`, `MermaidDiagram` | Parser outputs, store commits, geospatial snapshot, Mermaid config | Drift-resistant cross-surface rendering + ELK-capable flowcharts | Shared type export is the contract boundary; normalization is shallow and clone-on-change; geospatial/mermaid consume the derived active view to avoid panel drift. | Add a bounded render smoke test that uses `config.layout: elk` and verifies SVG renders without errors. | 2026-02-04 |
| Mode/layout switching / Cache isolation | Prevent D3/Flow/Keyword/Frontmatter/Stratify switching from reusing stale positions/zoom | Gate “effective frontmatter” by existence of frontmatter Mermaid seeds to prevent cache churn; key Flow rebuild by `layoutVariant`; seed Flow from D3 using matching stratify `layoutVariant`; include `layout.stratify` + `layout.flow` in Flow zoom view key; add bounded regression test. | `canvas/src/{hooks/useActiveGraphData.ts,lib/graph/{frontmatterMode.ts,layerDerivation.ts}}`, `canvas/src/components/{GraphCanvas.tsx,FlowCanvas.tsx,FlowCanvas/seed.ts}`, `canvas/src/components/GraphCanvas/layout/{positioning.ts,stratifyVariant.ts}` | - | `computeEffectiveFrontmatterMode`, `filterGraphToFrontmatterMermaid` | mode/renderer/layout toggles | Stable mode switching without layout bleed | Frontmatter toggle is a no-op when no seeds; cache keys must not change in that case. | Add an interaction smoke test that toggles frontmatter ON/OFF on a non-mermaid doc and asserts viewKey/cacheKey stability. | 2026-02-04 |
| Edge routing + layer visibility (2D D3/Flow) | Reduce spaghetti and align edge underlay behavior across renderers | Implement schema-driven Flow obstacle-avoiding edge routing and group underlay fading; align group depth visibility via schema.layout.groups.depthStyle and honor Mermaid subgraph visual fill/stroke; forbid hardcoded routing constants. | `canvas/src/components/{FlowCanvas.tsx,FlowCanvas/nativeRuntime.ts,FlowCanvas/edgeRouting.ts,GraphCanvas/layers/groups.ts}`, `canvas/src/lib/graph/{schemaTypes.ts,schema.ts,groupDepthStyle.ts}` | - | `routeFlowEdgeOrtho`, `drawEdge`, `fadeEdgesUnderGeometry`, `createGroupsLayer` | schema.layout.flow.edges + schema.layout.groups | Routed edges + consistent z/opacity across 2D renderers | Routing + underlay + depth styling are schema-driven; group obstacles exclude endpoint-containing groups. | Add a bounded visual snapshot test once the screenshot harness is stable. | 2026-02-04 |
| Flow zoom + layout cache-key correctness | Fix FlowCanvas auto-fit/selection timeouts and prevent stale layout reuse | Make auto-fit/selection zoom react to viewport changes; isolate layout position cache by `mediaPanelDensity` + `renderMediaAsNodes` + `renderVariant`; pass `prevRenderVariant` into positioning to avoid incorrect skip-initial-layout across renderer toggles; add bounded regression tests. | `canvas/src/{features/zoom/useAutoZoomModes2d.ts,components/GraphCanvas.tsx,__tests__/layoutPositioning.test.ts,__tests__/flowCanvasIntegration.test.ts,tests/run.ts}` | - | `useAutoZoomModes2d`, `determineLayoutPositions` | viewport resize, renderer/mode toggles | Reliable auto-fit/selection + drift-free mode switching | Cache keys must include all render-affecting toggles; zoom triggers when viewport updates. | Add a bounded UI smoke test that toggles sidebar width and asserts fit-to-screen re-applies. | 2026-02-04 |
| Semantic mode isolation (Document ↔ Keyword) | Prevent Keyword/Document toggles from mutating the canonical document schema | Maintain `schemaBySemanticMode` in the store; switching semantic mode restores its schema snapshot and clears selection/collapsed groups; persist schema edits only in Document Structure Mode. | `canvas/src/hooks/{useGraphStore.ts,store/{types.ts,uiSettingsSlice.ts,schemaSlice.ts}}` | - | `setDocumentSemanticMode`, `setSchema` | mode toggle + schema edits | Stable mode switching without schema bleed | Schema writes are gated by `documentSemanticMode==='document'`; other modes are kept in-memory only. | Add policy docs linking semantic mode rules to SSOT. | 2026-02-04 |
| Groups + layers | Improve nested group spacing and ensure edges render beneath groups | Use deepest Mermaid/Markdown subgraph group key for collision/anchoring; adjust default Canvas z-order to place `links` behind `groups` so group underlays remain readable. | `canvas/src/components/GraphCanvas/{layout/layoutGroupKey.ts,zOrder.ts}`, tests | - | `createLayoutGroupKeyOfNode`, `applyGraphCanvasZOrder` | graph groups + schema layout | Cleaner group separation + reduced edge clutter | GroupKey prefers deepest group per node; z-order ranks `links < groups < nodes`. | Add a bounded 2D screenshot diff when harness exists. | 2026-02-04 |
| Collision SSOT (2D/3D) | Forbid group inner-border snap/stick to other boxes | Treat group collision gap as per-side indices (x1..x5/y1..y5/z1..z5), sum gaps in overlap checks, and use touch-epsilon to avoid exact-contact sticking; keep PackedRTree (Morton) broadphase in `grph-shared` and reuse from D3/Flow. | `grph-shared/src/collision/*`, `canvas/src/components/GraphCanvas/layout/{groupOverlap*,overlap.ts}` | - | `resolveGroupCollisions`, `PackedRTree`, `applyAabbOverlapPush` | schema.layout.forces group/node bbox knobs | O(n log n) collision broadphase + stable separation | Gap is axis-aware; per-side gaps prevent outer border entering another box’s inner border region. | Keep docs/schema aligned and add a focused collision regression if drift reappears. | 2026-02-04 |
| Policy / Fixtures | Forbid absolute sandbox demo paths in code/tests | Add a bounded policy guard that rejects hardcoded `/Users/.../GitHub/sandbox/...` paths; use sandbox-root helpers + basenames or repo-local fixtures. | `canvas/src/__tests__/crossRepoBoundaryGuards.test.ts`, `canvas/src/tests/run.ts` | - | `testForbidHardcodedSandboxAbsolutePaths` | source scan | CI fails fast on path leakage | Absolute paths are not portable and break neutrality; prefer `readSandboxDemoText` or `src/__tests__/fixtures/*`. | Keep docs/schema aligned and add more forbidden path patterns if drift appears. | 2026-02-04 |
| Settings UX | Allow tuning group nesting + edge opacity from MainPanel Settings | Expose `schema.layout.groups.nestedPaddingStep` and `schema.layout.edges.{opacity,opacityUnderGroups}` as store-backed Settings keys (bounded clamps). | `canvas/src/features/settings/registry-ui.graph-and-orchestrator.ts`, `canvas/src/features/panels/views/SettingsFallbackDetails.ts` | - | settings registry entries | user edits | Tunable separation + readable edges without schema editor | Settings write through `setSchema` and still respect Document-mode persistence gating. | Add flow-details entries later if the schema extractor is extended. | 2026-02-04 |
| Document Structure baseline / Mode lock | Prevent mode switching drift across surfaces | Remove Stratify (coerce legacy to Force); add persisted baseline lock default-on to block Keyword/Frontmatter/Renderer/3D/Select/Create switches; keep `useActiveGraphRenderData()` as SSOT. | `canvas/src/{lib/config.ls.ts,hooks/store/{uiSlice.ts,uiSettingsSlice.ts,canvasSlice.ts,schemaSlice.ts},hooks/useActiveGraphData.ts,components/{Toolbar.tsx,GraphCanvas.tsx,FlowCanvas.tsx}}` | - | - | user toggles + legacy schema | Stable baseline render pipeline | Default-on lock gates switch paths; legacy `layout.mode: stratify` coerces to `force`. | Update docs + schema contracts for lock + layout modes. | 2026-02-04 |
| Cross-Repo       | Shared Geospatial Events     | Unify geospatial mode event name + payload + subscribe/emit helpers to prevent cross-repo drift | `grph-shared/geospatial/{constants.ts,events.ts}`, `gympgrph/src/hooks/store/geospatialSlice.ts`, `canvas/src/{pages/Canvas.tsx,components/Toolbar.tsx,features/geospatial/events.ts}` | `GEOSPATIAL_MODE_CHANGED_EVENT`, `emitGeospatialModeChanged`, `onGeospatialModeChanged` | enabled/viewMode payload | SSOT compliance | Shared helpers replace raw `window.dispatchEvent` and raw listeners; host no longer resets persisted geospatial state on mount | 2026-02-04 |

## 2026-02-03

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| SSOT / Cross-surface sync | Make Table/Markdown/Slides/Canvas/Map re-render deterministically on edits/imports | Bump `graphDataRevision` (and stamp `GraphData.metadata.graphDataRevision/hash`) on every graph mutation + history restore; emit shared SSOT focus/change events via `grph-shared/ssot/*`; keep Graph Data Table UI NocoDB-like without theme drift. | `knowgrph/canvas/src/hooks/store/{graphDataSlice.ts,historySlice.ts}`, `knowgrph/canvas/src/features/ssot/SsotEventBridge.tsx`, `knowgrph/grph-shared/src/ssot/{types.ts,events.ts}`, `curagrph/src/features/graph-data-table/ui/{GraphDataTableHeader.tsx,GraphDataTableTable.tsx}` | `SsotEventBridge` | `setGraphData*`, `updateNode`, `updateEdge`, `restore/undo/redo`, `scheduleHistory`, `emitSsotChanged`, `emitSsotFocusChanged` | Table edits, Canvas edits, Markdown-driven imports, Undo/Redo | Consistent derived renders across surfaces + stable event stream | `graphDataRevision` is the canonical invalidation token; persistence is debounced via history scheduling to avoid per-keystroke churn; extracted surfaces share `SsotSurface` vocabulary to prevent string drift. | Subscribe extracted surfaces to SSOT events where store coupling is undesired; add a small doc snippet showing how to consume `SSOT_FOCUS_CHANGED_EVENT` for deep linking. | 2026-02-03 |
| Spreadsheet Storage / Grid | Add a local-first, multi-table spreadsheet backend without auth or hosted DB | Add RxDB+Dexie spreadsheet DB with SSOT types in `grph-shared`; render a new `/spreadsheet` UI via AG Grid; stream row/column changes from RxDB; forbid Supabase/registration; add bounded CI test. | `knowgrph/grph-shared/src/spreadsheet/types.ts`, `knowgrph/canvas/src/features/spreadsheet-db/spreadsheetDb.ts`, `knowgrph/canvas/src/features/spreadsheet/ui/SpreadsheetPage.tsx`, `knowgrph/canvas/src/App.tsx`, `knowgrph/canvas/src/__tests__/rxdbSpreadsheetDb.test.ts` | - | `createSpreadsheetDb`, `ensureSpreadsheetSeed`, `updateRowCell`, `addRow` | User edits + table CRUD | Persisted spreadsheet tables + editable grid | Use RxDB change streams for incremental UI updates; keep schemas index-safe (bounded integer ordering). | Add SSOT event bridge hooks so spreadsheet edits can feed Graph/Markdown/Map sync when needed. | 2026-02-03 |
| Docs / Cross-repo alignment | Prevent Editor/Graph Table/Preview docs drift | Update docs + schema to reflect Editor workspace sections (Markdown vs Graph Table), preview iframe contract, selection sync, TOC focus, and persisted LS keys; clearly separate host Graph Table vs curagrph Graph Data Table to avoid duplicated ownership. | `knowgrph/docs/documents/{knowgrph-canvas-document.md,knowgrph-ui-ux-design-document.md,knowgrph-markdown-ssot-ui-contract-document.md,knowgrph-local-storage-document.md,knowgrph-graph-data-curation-document.md,curagrph-graph-data-curation-document.md}`, `huijoohwee.github.io/schema/AgenticRAG/{panels.jsonld,markdown.jsonld}` | - | - | New Editor workspace + Graph Table behaviors | Drift-resistant docs + schema contracts | Contracts live in one place; LS keys referenced only from `config.ls.ts`; avoid naming collisions (`graphDataTable*` vs `graphTable*`). | Re-run docs site sync map; add a bounded doc lint check if schema or UI contracts change. | 2026-02-03 |
| SSOT / Active graph render view | Align derived GraphData across Table/Props/Canvas/Flow/Stats | Use `useActiveGraphRenderData()` as SSOT: keyword-mode base → optional frontmatter filter → optional group collapse; forbid per-surface derivation. All UI surfaces must render nodes/edges from this derived view to prevent drift. | `knowgrph/canvas/src/hooks/useActiveGraphData.ts`, `knowgrph/canvas/src/components/NodeEditor.tsx`, `curagrph/src/features/graph-data-table/ui/GraphDataTableTable.tsx`, `curagrph/src/components/BottomPanel/hooks/useStatsSelection.ts` | - | `useActiveGraphRenderData`, `deriveGraphDataForActiveView` | mode toggles + collapsed groups | Consistent records across surfaces | Derivation is pure and memoized by semantic/frontmatter/collapse keys; no duplication. | DONE: migrated remaining panels to `useActiveGraphRenderData()`; added bounded table-vs-collapsed-view test. | 2026-02-03 |
| Flow Renderer / Dependency | Replace Flow renderer without semantic drift | Implement native Canvas2D Flow mode and remove React Flow entirely; reuse `useActiveGraphRenderData()` plus layout/zoom cache keys; forbid React Flow dependencies, CSS, and imports. | `knowgrph/canvas/src/components/FlowCanvas.tsx`, `knowgrph/canvas/package.json`, `knowgrph/canvas/vite.config.ts`, `knowgrph/canvas/src/index.css` | `FlowCanvas` | - | GraphData + schema + zoomRequest | Native Canvas2D Flow mode | GraphKey stays topology-based; position updates remain incremental and cached per mode. | Add a policy guard if React Flow reappears; keep canvas docs aligned. | 2026-02-03 |
| Workspace         | Graph Data Table (Curation Tab) | Keep Graph Data Table inside BottomPanel `curation` (Graph Data) tab; remove full-page table workspace mode. | `canvas/src/components/BottomPanel.tsx`, `curagrph/src/components/BottomPanel/*`, `canvas/src/pages/Canvas.tsx`, `canvas/src/components/Toolbar.tsx` | - | GraphData + selection | Unified curation table surface | Single curation surface; Editor workspace remains Markdown SSOT + preview `kgPreview=1` contract | 2026-02-03 |
| Perf             | Warm-Mount Inactive Renderer | Enable background layout calculation for inactive renderer to prevent switch lag; forbid active-only data derivation | `Canvas.tsx`, `FlowCanvas.tsx`, `GraphCanvas.tsx` | `useActiveGraphRenderData`, `useEffect` | canvas2dRenderer | Instant switch | Inactive renderer warm-mounts (hidden) and pre-calculates layout/data; only drawing/simulation is paused | 2026-02-03 |
| Flow Renderer    | Parity: Groups + Handles     | Reuse D3 Cluster Shape (Rect/Polygon) + Port Handles toggles in Flow; draw groups/port handles; route endpoints by axis in TB/LR; avoid store-wide re-renders | `canvas/src/components/FlowCanvas.tsx`, `canvas/src/components/FlowCanvas/nativeRuntime.ts`, `canvas/src/components/GraphCanvas/portHandlesConfig.ts`, `canvas/src/components/GraphCanvas/layout/graphGroups.ts`, `canvas/src/lib/geometry/convexRing.ts` | `setFlowNativePresentation`, `drawGroups`, `drawPortHandles` | schema.behavior.portHandles + schema.layout.groups.shape | Consistent 2D parity | Canvas-native renderer honors schema toggles with shared SSOT utilities and bounded work | 2026-02-03 |

## 2026-02-02

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Markdown Workspace Import → Render | Prevent content disappearing/flicker on imports + view switches | Batch workspace FS change events during bulk imports; guard import focus via jobId so late async completions never steal focus; make workspace seed demos repo-local; narrow file-load effect deps to avoid redundant reloads. | `knowgrph/canvas/src/{features/workspace-fs/workspaceFsEvents.ts,components/BottomPanel/markdownWorkspace/useWorkspaceFileActions.ts,components/BottomPanel/markdownWorkspace/MarkdownWorkspace.tsx,features/workspace-fs/workspaceFs.ts}`, `knowgrph/canvas/vite.config.ts` | `MarkdownWorkspace`, `WorkspaceFs` | `runWorkspaceFsChangedBatch`, `notifyWorkspaceFsChanged`, import handlers | local files/folder/URL imports | Stable file list + stable active document | Refresh once per import; ignore stale import completions; avoid sibling repo coupling | Add a bounded UI integration test for large folder import with rapid mode switching (Editor/Viewer/Presentation/Table). | 2026-02-02 |
| Toolbar Workspace Mode | Add VS Code-like Editor workspace without breaking Canvas | Add persisted `workspaceViewMode` and an `Editor` toolbar button left of Status; Editor mode renders the Markdown Workspace SSOT plus a resizable Canvas preview `iframe` (`/?kgPreview=1`) that forces preview-only rendering; avoid mode bleed and background canvas work. | `knowgrph/canvas/src/{components/Toolbar.tsx,pages/Canvas.tsx,components/EmbeddedEditorShell.tsx,lib/config.ls.ts,hooks/store/uiSlice.ts}`, tests | `Toolbar`, `CanvasPage`, `EmbeddedEditorShell` | toggle handlers + preview gating | toolbar click; `kgPreview=1` query | Editor layout + iframe preview | `kgPreview=1` forces preview-only canvas render; Editor mode unmounts BottomPanel/side panels; view mode persisted via `LS_KEYS` | Keep `knowgrph-canvas-document.md` + `schema/AgenticRAG/panels.jsonld` aligned with the Editor/Preview contract and add policy coverage if new UI chrome is introduced. | 2026-02-02 |

## 2026-02-01

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Layout (2D D3/Flow) | Align Stratify with Flow layout | Centralize Flow layout knobs in layoutDefaults; Stratify defaults reuse Flow spacing; isolate layout position caches by renderVariant and optional layoutVariant; modularize Stratify grid constraints to prevent drift and cross-mode interference. | `knowgrph/canvas/src/lib/graph/layoutDefaults.ts`, `knowgrph/canvas/src/components/{FlowCanvas/config.ts,GraphCanvas/layout/{stratify.ts,stratifyGrid.ts},GraphCanvas/layout/positioning.ts}`, tests | `GraphCanvas`, `FlowCanvas` | `readFlowLayoutKnobs`, `applyStratifyLayout`, `determineLayoutPositions` | schema.layout + nodes/edges | Stable, consistent 2D layouts | Cache key = semantic/frontmatter/layout/render + renderer + layout variant | Keep docs/schema aligned and expand layout-only regression coverage when knobs change. | 2026-02-01 |
| Geospatial Mode | Support sandbox geospatial fixtures | Make local geo upload size bounded + configurable; derive GeoJSON points from record-style JSON; derive itinerary POIs from Markdown tokens using a bounded in-memory geo index; render graph edges on basemap; compute fit padding from viewport for 80/20 fill; forbid dataset-specific logic. | `knowgrph/canvas/src/{components/BottomPanel/BottomPanelMarkdownSection.tsx,features/source-files/sourceFilesIngestIntegration.ts,features/geospatial/*}`, `knowgrph/canvas/vite.config.ts`, `gympgrph/src/features/geospatial/*`, `huijoohwee.github.io/schema/AgenticRAG/geospatial.jsonld` | `GeospatialOverlay`, `BottomPanelMarkdownSection` | `uploadGeoJsonTextToLocalStore`, `recordsToPointFeatureCollection`, `extractMarkdownItineraryPois`, `computeViewportFitPaddingPx` | local `.geojson/.json/.md` + GraphData | Basemap + layers + POI details | Prefer GeoJSON parse; else derive points; resolve POIs via token index; fit uses viewport-derived padding | Add a bounded integration test that loads cities+airports+itinerary and verifies panel opens on POI click. | 2026-02-01 |
| Markdown Content Sync (Cross-View) | Prevent content disappearing on view switches | Key markdown preview caches by activeDocumentPath; return cached tokens only when storedTokensPath matches; loader uses basename identity fallback so Editor/Viewer/Presentation/Slides/GraphData/Canvas share text SSOT; add bounded regression tests for doc switches and view-mode switches with Canvas mounted. | `curagrph/src/features/markdown/ui/useMarkdownPreviewTokens.ts`, `knowgrph/canvas/src/__tests__/markdownPreviewShowOnCanvas.test.ts`, `knowgrph/canvas/src/tests/run.ts` | `MarkdownPreview` | `useMarkdownPreviewLexedMarkdown` | markdownText + activeDocumentPath + viewMode | Stable cross-view markdown rendering | Cache hit requires same doc path; view switches must not reuse prior doc tokens | Keep SSOT docs + AgenticRAG markdown schema notes aligned with cache isolation rule. | 2026-02-01 |
| Markdown Workspace Explorer | Prevent markdown content clearing when expanding folders | In the Markdown Workspace file tree, folder clicks must only toggle expand/collapse; do not set activePath to folders and do not clear the active document text when navigating. Add a bounded UI regression test for folder click behavior. | `knowgrph/canvas/src/components/BottomPanel/MarkdownFileTree.tsx`, `knowgrph/canvas/src/components/BottomPanel/markdownWorkspace/{MarkdownWorkspace.tsx,MarkdownWorkspaceExplorer.tsx}`, `knowgrph/canvas/src/__tests__/markdownFileTreeFolderClick.test.tsx` | `MarkdownWorkspace`, `MarkdownFileTree` | click handlers | folder tree node clicks | Active document and viewer content remain stable | Folder click = expand/collapse only; file click = set active document | Consider adding keyboard affordances (Enter vs ArrowRight) for folder expand without selection. | 2026-02-01 |
| Markdown → Geospatial Layer | Render GeoJSON from Markdown Workspace into geospatial datasets | Provide `geoDatasetIntegration` to Markdown Workspace previews; detect GeoJSON inside `json` fences; auto-register GeoJSON code blocks by uploading to the local geo dataset endpoint and adding URLs to gympgrph datasets list. Add a bounded regression test using `sandbox/demo/trip-demo.md`. | `knowgrph/canvas/src/{components/BottomPanel/markdownWorkspace/MarkdownWorkspaceMain.tsx,features/geospatial/markdownGeoDatasetIntegration.tsx}`, `knowgrph/canvas/src/__tests__/markdownGeoIntegrationTripDemo.test.ts`, `gympgrph/src/index.ts` | `MarkdownPreview`, `GeoJsonGeoPanelRenderer` | `registerGeoJsonFeatureCollection` | Markdown code fences | GeoJSON visible as overlay dataset when Geospatial Mode is enabled | Auto-register uses content hash + bounded cache; datasets are URL-backed via `/__geo_upload`→`/__geo_local/*` | Consider extending extractor to cover itinerary POI derivation once datasets are present. | 2026-02-01 |
| Markdown Sidebar | Explorer-like Sidebar + FS DB | Align Source Files/Outline/Backlinks with VS Code Explorer; persist Source Files + workspace state | `MarkdownPanelLayout.tsx`, `MarkdownSourceFilesPanel.tsx`, `useMarkdownExplorerControls.ts`, `config.ls.ts`, `sourceFilesDb.ts`, `SourceFilesPersistenceBootstrap.tsx`, `localMarkdownFolderSlice.ts` | - | sourceFiles, LS keys | Explorer sidebar + persisted workspace | Dedup sidebar state; Dexie persists SourceFile + workspace meta; LS persists expanded folders; cached fallback for local files | 2026-02-01 |
