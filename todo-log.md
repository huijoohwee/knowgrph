# todo-log

- **Canonical directive**: **STRICTLY COMPLY** with one-row-one-directive (Max 50 words)
- **Table cells**: MUST fill-up all cells in the table; FORBID empty/`-` cells.

## 2026-02-15

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Import→Render / Local In-Repo URL / Performance | Support local file imports and optimize rendering | Support local in-repo Import URL + assets via `/__webpage_proxy`, `/__webpage_asset_proxy`, `/__repo_file/*`; render HTML/JSON via sandboxed `srcdoc`; cap HTML size and idle-yield sanitization; keep progress for large imports. | `canvas/vite.config.ts`, `canvas/src/lib/websites/webpageIframeSrcdoc.ts`, `docs/documents/knowgrph-webpage-proxy-and-srcdoc-iframe-document.md` | Vite middleware + Srcdoc builder | `createWebpageProxyHandler`, `createRepoFileHandler`, `fetchWebpageHtmlViaProxy` | Local file path + View mode | Sandboxed `srcdoc` rendering of local files + smooth UI | Resolve paths relative to repo root; block traversal; use `__repo_file` baseHref for relative assets; yield during sanitization. | Import `sandbox/test-data/my-site/index.html` and confirm CSS/images load in iframe without freezes. | 2026-02-15 |
| Import→Render / Webpage JS-render fidelity (native) | Remove headless browser dependencies while preserving JS-rendered content capture | Replace Playwright-based rendering with a browser-native, sandboxed iframe DOM export bridge (`kg-export-dom`) and use it to upgrade webpage Markdown when static conversion is incomplete; keep the approach domain-neutral and bounded, and forbid external headless browser dependencies. | `canvas/src/lib/websites/{webpageDomExport.ts,webpageClientConvert.ts,webpageMarkdownQuality.ts,webpageIframeSrcdoc.ts}`, `canvas/src/components/BottomPanel/markdownWorkspace/workspaceImport.ts`, `canvas/vite.config.ts` | DOM export bridge + client convert fallback | `exportWebpageDomViaHiddenIframe`, `convertWebpageUrlToMarkdownViaBrowser`, `isLowQualityWebpageMarkdown`, `fetchWorkspaceUrlContent` | URL + bounded export options (maxChars/timeout/scrollCrawl/expandFaq) | Upgraded Markdown for JS-heavy pages + unchanged behavior for static pages | If the initial conversion is low quality, export rendered DOM text/HTML via hidden iframe → convert to Markdown → keep the better result; otherwise keep the first-pass conversion; sandbox and bounds must always apply. | Import a JS-heavy FAQ page and confirm (1) the saved Markdown includes FAQ content, (2) the HTML viewer still renders via sandboxed iframe, and (3) no Playwright install step exists in the repo or docs. | 2026-02-15 |
| Import→Render / Webpage HTML/JSON view | Enforce sandboxed iframe rendering and reduce freezes | Render `kgWebpageView∈{html,json}` via sandboxed `srcdoc`; treat HTML/JSON as view-only overlays; idle-defer `srcdoc` build; cap HTML size; sanitize by default (`webpageViewerScriptPolicy=strip`); ignore abort errors; reuse fetch caches and parse workers to prevent freezes. | `canvas/src/components/BottomPanel/markdownWorkspace/{MarkdownWorkspaceMain.tsx,MarkdownWorkspace.tsx}`, `canvas/src/features/panels/utils/idle.ts`, `canvas/src/lib/websites/{webpageIframeSrcdoc.ts,webpageHtmlToMarkdownArtifact.ts}`, `canvas/src/lib/graph/{parseWorker.ts,parseWorker.worker.ts}`, `docs/documents/knowgrph-webpage-proxy-and-srcdoc-iframe-document.md` | Workspace iframe viewer + conversion + parse worker | `useWebpageIframeSrcdoc`, `buildWebpageHtmlSrcdocAsync`, `fetchWebpageHtmlAuto`, `convertWebpageHtmlToMarkdownArtifactAsync`, `parseGraphInWorker` | URL + view + settings | Stable iframe render + responsive UI | Prefer stored `raw.html` when present; otherwise fetch once; conversion yields to main; abort shows Cancelled; singleton worker reduces parse stalls. | Toggle Markdown→HTML→JSON rapidly during import/convert and confirm no freezes, no “Fetch is aborted”. | 2026-02-15 |
| Import→Render / Mode contract / Doc-scoped collapse | Make view switching deterministic and prevent “missing blocks” | Enforce mode contract: (1) Markdown SSOT default, (2) HTML view-only iframe, (3) JSON view-only iframe; scope collapsed headings by `activeDocumentKey`; default `websiteImportGenerateWebpageArtifactDocs=false`; generate artifact markdown only from stored `raw.html` when enabled. | `canvas/src/components/BottomPanel/markdownWorkspace/{MarkdownWorkspaceMain.tsx,useWorkspaceFileActions.ts,workspaceImport.ts}`, `curagrph/src/features/markdown/ui/useMarkdownExplorerControls.ts` | Workspace + viewer controls | `useWebpageIframeSrcdoc`, `handleImportWebsite`, `useMarkdownExplorerControls` | Active doc + view toggle + import settings | Stable view switching + visible wireframes | Never write graph/layout/zoom on view change; HTML/JSON render `srcdoc`; doc-scoped LS key avoids cross-doc hidden sections. | Import ≤10 pages and confirm view toggles keep graph/zoom unchanged and wireframes visible. | 2026-02-15 |
| Import→Render / Webpage URL | Generic webpage conversion with fidelity and settings | Convert URL→Markdown using a browser-native, sandboxed iframe DOM export bridge with bounded proxy-fetch fallback; integrate into `sourceFilesIngestIntegration.ts` import routing; expose `webpageImportIncludeImages` + `webpageImportView` settings and keep view switching strictly view-only. | `canvas/src/lib/websites/{webpageDomExport.ts,webpageClientConvert.ts}`, `canvas/src/lib/net/remoteMarkdownConversions.ts`, `canvas/src/features/source-files/sourceFilesIngestIntegration.ts`, `canvas/src/features/settings/registry-ui.import-webpage.ts` | Webpage client converter + DOM export bridge | `convertWebpageUrlToMarkdownViaBrowser`, `exportWebpageDomViaHiddenIframe`, `fetchWebpageMarkdown` | URL + settings | Markdown (graph SSOT) + iframe HTML/JSON views | If not known ext/YouTube/PDF: convert via DOM export; if it fails, fall back to same-origin proxy fetch; apply `webpageImportIncludeImages` and persist `kgWebpageUrl/kgWebpageView` frontmatter without mutating graph/layout/zoom. | Verify Import URL for a JS-heavy page and a static page; toggle `Markdown/HTML/JSON`; confirm `[NAV]/[CTA]/[PRICE]/[TIME]` tokens remain visible. | 2026-02-15 |
| Import→Render / Local repo-relative HTML | Make local in-repo webpages render reliably | Fetch repo-relative `kgWebpageUrl` via `/__repo_file/*` in `fetchWebpageHtmlAuto`; prefer website-import `rawHtml` artifacts for both HTTP and local nodes; surface byte progress; keep HTML/JSON as sandboxed `srcdoc` view-only overlays. | `canvas/src/lib/url.ts`, `canvas/src/lib/websites/webpageIframeSrcdoc.ts`, `canvas/src/components/BottomPanel/markdownWorkspace/MarkdownWorkspaceMain.tsx` | Repo-file URL utils + iframe srcdoc fetch | `buildRepoFilePath`, `fetchWebpageHtmlFromRepoFile`, `fetchWebpageHtmlAuto`, `useWebpageIframeSrcdoc` | Repo-relative path or HTTP URL | Sandboxed iframe render + progress | If `kgWebsiteImportId/nodeId` exists: fetch artifact first; else if HTTP: use proxy; else: `/__repo_file`; sanitize+idle-yield; forbid path traversal. | Import `canvas/index.html` via Import URL and confirm iframe loads without freezes. | 2026-02-15 |
| Canvas Preview / Flicker / Preview Sync | Stop preview flicker and reduce render churn | Prevent no-op `setGraphData` commits from bumping `graphDataRevision`; memoize `hashGraphDataForPreviewSync`; make preview iframes load with `?kgPreview=1` to avoid initial UI flash; RAF-throttle preview pane resize state updates; add no-op guard to `setCanvasRenderMode`. | `canvas/src/hooks/store/{graphDataSlice.ts,graphDataSliceUtils.ts,canvasSlice.ts}`, `canvas/src/pages/Canvas.tsx`, `canvas/src/components/EmbeddedEditorShell.tsx`, `canvas/src/features/graph-table/ui/GraphTableCanvasPreviewDock.tsx` | Store + preview iframe host | `setGraphData`, `hashGraphDataForPreviewSync`, `setCanvasRenderMode`, `EmbeddedCanvasPreviewFrame` | GraphData + preview sync messages | Stable preview without flicker | Only commit state when effective graph/mode changed; avoid per-pointermove reflow; ensure embedded preview mode is detected at load. | Type in MarkdownWorkspace with preview open and confirm no visible flicker. | 2026-02-15 |
| Webpage Artifact / Main Content / Pricing-like cards | Remove duplicate synthetic “Content” and render card blocks as grid | Mark seeded “Content” main sections as synthetic and avoid emitting the redundant fenced heading echo; coalesce consecutive plain-text card-like blocks into a Markdown table (grid-like) using neutral heuristics (short lines, no markup). | `canvas/src/lib/websites/{webpageHtmlToMarkdownArtifact.ts,webpageHtmlToMarkdownArtifactPrivate.ts}`, `canvas/src/__tests__/webpageHtmlToMarkdownArtifact.test.ts` | HTML→Markdown artifact converter | `extractMainSections`, `convertWebpageHtmlToMarkdownArtifact` | HTML page content | Cleaner artifact + table grid blocks | If section is synthetic `Content`, skip the repeated `## Content` echo; if 2–4 adjacent card blocks detected, render as Markdown table with inferred card titles. | Import `https://www.remotion.dev/` and confirm Pricing cards render as a table without duplicate Content headings. | 2026-02-15 |
| Code Maintenance / Repo size / History purge | Shrink repo size and prevent artifact churn | Purge large artifacts from git history using `git-filter-repo`: remove `.knowgrph-workspace/**`, `data/knowgrph-workflow-preview/**`, `node_modules/**`, `backups/**`, and `git-error-*`; run aggressive GC; restore `origin` and force-push rewritten `main` + tags; require fresh re-clone for collaborators. Applies across `/GitHub/{knowgrph,curagrph,gympgrph,chatgrph,sandbox}`. | `.gitignore`, `.git/filter-repo/*`, `docs/documents/knowgrph-repo-hygiene-document.md` | Git history rewrite + artifact policy | `git rm --cached`, `git filter-repo --invert-paths`, `git gc --aggressive` | Repo history + artifact paths | Small `.git` + clean status + faster clone/push | First stop tracking artifacts; then rewrite history and garbage-collect; never commit `.knowgrph-workspace` (stored under `sandbox` via symlink) and never commit `node_modules/**`. | Verify `.git` size drops and `git log` still contains feature commits; then `git push --force-with-lease`. | 2026-02-15 |
| Import→Render / Webpage HTML view / Large payloads | Prevent srcdoc failures for very large pages | Remove hard size-fail; sanitize first then apply domain-neutral shrink (strip scripts/handlers/comments; collapse whitespace; replace base64 images; drop oversized inline svg/style); only fall back to text if still too large. | `canvas/src/lib/websites/webpageIframeSrcdoc.ts`, `canvas/src/__tests__/webpageIframeSrcdocLargeHtml.test.ts` | Srcdoc builder | `buildWebpageHtmlSrcdocAsync`, `shrinkOversizedHtml` | Raw HTML + URL | Stable sandboxed iframe render | If sanitized output exceeds budget, shrink; never hardcode domains; avoid freezes via yielding. | Import a large JS app shell and confirm iframe renders (no “too large for srcdoc”). | 2026-02-15 |
| Canvas / Layout positions / Renderer toggles | Keep layout stable across D3, Flow, Flow Editor, editor ON/OFF | Make layout-position caching renderer-agnostic for `renderMode=2d` and treat 2D renderer toggles as no-op for seeding; preserve 3D variant isolation. | `canvas/src/components/GraphCanvas/layout/positioning.ts`, `canvas/src/__tests__/layoutPositioning.test.ts` | Layout cache key + seed policy | `buildLayoutPositionCacheKey`, `determineLayoutPositions` | Graph nodes with positions | No reseed on 2D renderer switch | Ignore `renderVariant` for 2D cache keys; only treat 3D variant changes as engine changes. | Toggle D3↔Flow↔Flow Editor repeatedly and confirm positions persist. | 2026-02-15 |
| Canvas / Flow Editor / Viewport controls | Restore Flow Editor pan/zoom and align gestures | Stop forcing `design` viewport preset in Flow Editor; add `flowEditorSelectionOnDrag` setting + LS key; use Shift-drag selection when disabled; keep zoom modes and wheel parity consistent across 2D renderers. | `canvas/src/components/FlowCanvas.tsx`, `canvas/src/hooks/store/canvasSlice.ts`, `canvas/src/features/settings/registry-ui.graph-and-orchestrator.ts` | FlowCanvas + GraphState | `bindFlowCanvasNativeInteractions`, `setFlowEditorSelectionOnDrag` | `canvas2dRenderer=flowEditor`, `viewportControlsPreset`, selection drag | Usable pan/zoom + optional selection-on-drag | Never override stored preset; selection-on-drag is opt-in via `flowEditorSelectionOnDrag`. | Enter Flow Editor, confirm left-drag pans, wheel zoom works; enable selection-on-drag only if desired. | 2026-02-15 |
| Markdown / Source fidelity / Round-trip | Enforce lossless MD→HTML→MD | Embed the exact Markdown source in rendered preview DOM (base64, non-executing script) and restore it during HTML→Markdown import before heuristic conversion; mark derived UI blocks as non-source. | `curagrph/src/features/markdown/ui/{MarkdownPreview.tsx,MarkdownPreviewViewer.tsx}`, `canvas/src/features/parsers/html-parser.ts` | Preview DOM payload + import guard | `encodeUtf8ToBase64`, `tryExtractLosslessMarkdownFromHtml` | Markdown text or imported HTML | Exact Markdown recovery | If embedded payload exists, return it exactly; otherwise fall back to HTML→Markdown traversal. | Copy rendered HTML and re-import; confirm Markdown matches byte-for-byte. | 2026-02-15 |
| Import→Render / Local In-Repo URL / Sitemap + Rich Media | Make local repo imports and rich embeds reliable | Treat `file://` as repo-relative in Import URL; generate `website.sitemap.md` with doc links to created page docs; promote standalone inline HTML (`svg/iframe/video/audio/img`) into HTML blocks for safe rich-media rendering; use shared progress ticker for long conversions. | `canvas/src/components/BottomPanel/markdownWorkspace/{workspaceImport.ts,useWorkspaceFileActions.ts}`, `canvas/src/lib/websites/websiteSitemapMarkdown.ts`, `canvas/src/lib/progress/progressTicker.ts`, `curagrph/src/features/markdown/ui/markdownPreviewLexBlock.ts` | Workspace import + sitemap + markdown lex | `fetchWorkspaceUrlContent`, `handleImportWebsite`, `buildWebsiteSitemapMarkdown`, `createProgressTicker`, `buildBlockTokens` | `file://<repoRel>` + website root + markdown with `<svg>/<iframe>` | Stable local import + clickable sitemap docs + rendered embeds | Strip `file://` then enforce repo-root bounds; sitemap links use created doc paths (post-write); treat single-inline HTML blocks as block HTML for allowlisted rendering. | Import a local folder website, open `website.sitemap.md`, click `Doc` links, and verify `<svg>/<iframe>` blocks render in Markdown preview. | 2026-02-15 |
| Import→Parse / HTML↔Markdown / Unified fallback | Improve general-purpose HTML→Markdown without replacing markdown UI renderer | Add unified+rehype+remark HTML→Markdown conversion as a fallback path (prefer lossless embedded Markdown payload when present); keep Markdown UI rendering on existing markdown-it/custom token renderer (no duplication). | `canvas/src/features/parsers/html-parser.ts`, `canvas/src/lib/markdown/htmlToMarkdownUnified.ts`, `docs/documents/knowgrph-webpage-proxy-and-srcdoc-iframe-document.md` | HTML import parser + unified converter | `parseHtmlToMarkdownAllTextAsync`, `convertHtmlToMarkdownUnified` | Large HTML + optional baseUrl | Faster, more robust HTML import with idle scheduling | If payload script exists: restore exactly; else if large HTML: run unified pipeline in idle; otherwise use DOMParser traversal; always keep site-agnostic extraction. | Import a large HTML page and confirm conversion completes without UI freeze and links resolve against baseUrl. | 2026-02-15 |
| Markdown Workspace / Webpage View Modes / Fidelity | Increase HTML↔Markdown fidelity and per-doc granularity | Add per-doc webpage controls (View/Script/Imgs/Fid + Sync) in the Markdown toolbar; extend frontmatter to include script policy, include-images, and fidelity level; preserve rich media via safe HTML blocks; keep view switching and sync bounded and view-only. | `canvas/src/components/BottomPanel/{MarkdownWorkspaceToolbar.tsx,markdownWorkspace/*}`, `canvas/src/lib/markdown/{frontmatter.ts,htmlToMarkdownUnified.ts}`, `curagrph/src/features/markdown/ui/markdownPreviewLinks.tsx` | Markdown toolbar + webpage meta | `upsertWebpageFrontmatterMeta`, `convertHtmlToMarkdownUnified` | Webpage-backed Markdown doc | Stable UI controls + richer preview | Prefer frontmatter overrides when present; apply selection-free, view-only switches; regenerate Markdown only on explicit Sync; preserve media as allowlisted HTML when fidelity is high. | Import a JS-heavy page, toggle `DOM/Raw/HTML/Markdown/JSON`, hit Sync, and confirm media blocks render and no graph/layout/zoom mutates. | 2026-02-15 |

| Import→Render / Geospatial Mode / Large record-map JSON | Enable geo-capable JSON import without breaking other modes | Detect geo-capable JSON/GeoJSON imports, build sampled geo nodes for very large record-map datasets, optionally auto-enable Geospatial Mode, and snapshot/restore host zoom/fit state across Geospatial Mode toggles; forbid absolute sandbox paths and avoid full JSON.parse for huge fixtures. | `canvas/src/lib/graph/io/{adapter.ts,geodataJson.ts}`, `canvas/src/features/{toolbar/importFlow.ts,geospatial/autoEnable.ts}`, `canvas/src/pages/Canvas.tsx`, `canvas/src/components/BottomPanel/markdownWorkspace/MarkdownWorkspace.tsx`, `canvas/src/features/settings/registry-ui.import-geo.ts` | Geodata sampler + auto-enable + viewport snapshot | `tryBuildGeodataGraphDataFromJsonText`, `maybeAutoEnableGeospatialModeForGraphData`, `runImportFlow`, `onGeospatialModeChanged` | `sandbox/test-data/airports.json`, `sandbox/demo/trip-demo*.md` | Sampled GraphData with `properties.geo` + geospatial overlay enabled (optional) | If geo fields detected and payload is large: sample first N records; enable overlay only when setting is true; restore zoom/pin/fit state on disable to prevent cross-mode interference. | Import `airports.json`, confirm map shows POIs; toggle Geospatial Mode on/off and verify graph zoom/pin/fit settings are unchanged. | 2026-02-15 |

## 2026-02-14

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Import→Render / Webpage Markdown Artifact / Mode Contract | Align artifact doc structure while preserving view-switch invariants | Generate a fixture-like, site-agnostic Webpage Markdown Artifact (TOC + Page Structure Overview + Document Structure + Asset Catalog) with bounded fidelity via `webpageArtifactFidelityMaxLevel`, and ensure `kgWebpageView` switches preserve existing frontmatter keys (incl. `kgWebsiteImportId/kgWebsiteNodeId/kgWebsiteOutputDirRel`) so artifact-backed HTML/JSON rendering remains stable; forbid any graph/layout/zoom mutations on view switches. | `canvas/src/lib/websites/{webpageMarkdownArtifactGenerator.ts,webpageMarkdownArtifact.ts}`, `canvas/src/lib/markdown/frontmatter.ts`, `canvas/src/features/settings/registry-ui.import-webpage.ts`, `canvas/src/components/BottomPanel/markdownWorkspace/{MarkdownWorkspace.tsx,workspaceImport.ts}` | Webpage artifact generator + frontmatter upsert + settings registry | `buildWebpageMarkdownArtifactFromMarkdown`, `buildWebpageMarkdownArtifactDoc`, `upsertWebpageFrontmatterMeta` | Converted webpage Markdown + URL + store settings | Deterministic artifact Markdown + stable view switching | Infer fidelity from extracted signals/structure then clamp to `webpageArtifactFidelityMaxLevel`; update only `kgWebpageUrl/kgWebpageView` lines in frontmatter and preserve the rest; render HTML/JSON as sandboxed iframe overlays using proxy-src (highest fidelity) or sanitized srcdoc snapshot when available. | Import a JS-heavy page and a static page, toggle `Markdown/HTML/JSON`, and confirm (1) website-import ids remain intact, (2) HTML works in proxy-src and srcdoc snapshot paths, and (3) switching views does not change graph/layout/zoom/table selection. | 2026-02-14 |
| Website Import / Status Progress / Workspace Write Perf | Prevent freezes and redundant re-computation during website imports | Expose lightweight `progress` snapshot in `/__website_import/status` and avoid per-page manifest `nodes/errors` array cloning; use bounded backoff polling and concurrency-limited workspace writes with yielding; keep iframe rendering sandboxed and view-only; forbid domain branching, infinite loops, re-index churn, or view-switch graph/layout/zoom mutations. | `canvas/src/lib/websites/{server/websiteImportServer.ts,webpageIframeSrcdoc.ts}`, `canvas/src/components/BottomPanel/markdownWorkspace/useWorkspaceFileActions.ts`, `docs/documents/knowgrph-webpage-proxy-and-srcdoc-iframe-document.md`, `huijoohwee.github.io/schema/AgenticRAG/pipeline.jsonld` | WebsiteImportServer + Workspace file actions + iframe proxy/srcdoc contract | `createWebsiteImportHandler`, `crawlInternalUrls`, `handleImportWebsite`, `buildWebpageHtmlSrcdoc` | Website root URL + import options + workspace FS + frontmatter | Progress UI + website tree + stable sandboxed iframe render | During run: update manifest `progress` only; after done: fetch manifest nodes once; polling backs off when progress stalls; writing uses concurrency limit and yields to avoid blocking; HTML view uses proxy-src by default and may use sanitized srcdoc when a safe snapshot is available. | Manual smoke import a small site (≤10 pages) and confirm progress updates, no UI freeze, and HTML view works for both stored-artifact srcdoc and proxy-src paths without view-switch side effects. | 2026-02-14 |
| Editor Workspace / Monaco / Webpage view switching | Prevent empty editor during async hydration and mode toggles | Ensure Monaco initializes with the latest text value (not a stale first-render snapshot) and ensure `kgWebpageView` switches hydrate from disk before writing; forbid any path where switching `Markdown/HTML/JSON` can result in an empty editor buffer when the on-disk file is non-empty. | `canvas/src/features/monaco/MonacoTextEditor.tsx`, `canvas/src/components/BottomPanel/markdownWorkspace/MarkdownWorkspace.tsx`, `docs/documents/knowgrph-webpage-proxy-and-srcdoc-iframe-document.md` | MonacoTextEditor + MarkdownWorkspace | `MonacoTextEditor`, `switchActiveWebpageWorkspaceView` | Workspace file path + view mode + hydrated file text | Non-empty editor content when file content exists | Monaco must seed model value from a `latestValue` ref at create-time; view switching must read from FS/lastLoaded when editor is not dirty; markdown switches must clear HTML/JSON overrides before async conversions complete. | Import a large webpage (≥50k chars), immediately toggle `kgWebpageView: html → markdown`, and confirm the editor shows content without requiring a reload or extra toggle. | 2026-02-14 |

## 2026-02-13
