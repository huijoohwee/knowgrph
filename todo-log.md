# todo-log

**Canonical directive**: **STRICTLY COMPLY** with one-row-one-directive (Max 50 words)

## 2026-01-29

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Markdown Viewer/Editor/Presentation + Local Source Files | Fix local Markdown CRUD UX + keep text SSOT | Keep Source Files actions clear and capability-aware; require opening a writable folder before create actions; selecting a local source file must update both store doc and editor text so Viewer/Editor/Presentation always render the same content. | `curagrph/src/features/markdown/ui/MarkdownPanelLayout.tsx`, `curagrph/src/components/BottomPanel/BottomPanelMarkdownSection.tsx`, `knowgrph/canvas/src/features/source-files/localMarkdownFolder.ts` | `MarkdownPanelLayout`, `BottomPanelMarkdownSection`, `localMarkdownFolder` | `openLocalMarkdownFolder`, `createLocalMarkdownFolder`, `createNewMarkdownSourceFileAndOpenViewer`, `readLocalMarkdownFileText` | Click Open folder/New folder/New file; select local file; switch Viewer↔Editor↔Presentation | Clickable actions; created local items; Editor no longer blank after Viewer/Presentation | Create buttons disabled unless a writable folder handle exists; selection sets `setMarkdownText(resolvedText)` | Add a bounded host-level UI smoke test for the Viewer→Editor text sync on local files. | 2026-01-29 |
| Markdown Viewer/Presentation + Markdown Parser | Support slide-demo Mermaid↔Markdown interop | Preserve HTML anchors; emit heading + block-id targets; render callouts (foldable); convert in-doc wikilinks (`[[#...]]`, `[[#^...]]`) to safe hash links; centralize `slugify` in `grph-shared`; add bounded slide-demo interop tests. | `curagrph/src/features/markdown/ui/{markdownPreviewLex.ts,markdownPreviewLexBlock.ts,markdownPreviewLexInline.ts,MarkdownCalloutBlock.tsx,MarkdownInlineRenderer.tsx,markdownPreviewLinks.tsx,MarkdownPreviewViewer.tsx}`, `knowgrph/canvas/src/features/parsers/markdownJsonLd.ts`, `knowgrph/grph-shared/src/markdown/slugify.ts` | `MarkdownTokenRenderer`, `MarkdownJsonLdParser`, `slugify` | `renderSafeHtmlBlock`, `buildMarkdownJsonLd` | `sandbox/demo/*.md` | Mermaid click jump targets + callouts + in-doc anchors/wikilinks work end-to-end (render + parse) | Hash links remain same-tab; slugify SSOT keeps UI+parser aligned; block-id anchors use `^id` form | If needed, add file-level `[[Note]]` resolution + backlink indexing as a separate pass (avoid coupling it to the renderer). | 2026-01-29 |
| Markdown Viewer / Mermaid Render Mode | Enable interactive Mermaid inspection | In Mermaid Render mode, enable pan/zoom drag within the code block and keep fullscreen overlay; treat in-diagram `#hash` links as in-doc navigation and scroll within preview container only. | `curagrph/src/features/panels/views/preview-panel/ui/MermaidDiagram.tsx`, `curagrph/src/features/markdown/ui/MarkdownCodeBlock.tsx`, `curagrph/src/features/markdown/ui/MarkdownPreviewViewer.tsx` | `MermaidDiagram`, `MarkdownCodeBlock`, `MarkdownPreviewViewer` | `ZoomPanViewport`, `scrollRootRef` | Mermaid code block (Render); Mermaid click `"#anchor"` | Draggable diagram; hash click scrolls to target | Allow only same-doc `#...`; persist pan/zoom via canonical LS key | Add a bounded UI smoke test for Mermaid click→scroll and pan drag. | 2026-01-29 |
| Markdown Viewer (Contents Sidebar) | Add file-level wikilinks + backlinks view | Convert file wikilinks (`[[Note]]`, `[[Note#Heading]]`, `[[Note#^block]]`) into safe internal hrefs and resolve targets against Source Files via loose normalization; add a Backlinks section (linked + heuristic unlinked mentions) in the left sidebar; keep semantic tags and avoid hardcoded paths. | `knowgrph/grph-shared/src/markdown/wikiLinks.ts`, `curagrph/src/features/markdown/ui/{markdownPreviewLexInline.ts,MarkdownPreviewViewer.tsx,MarkdownPanelLayout.tsx}`, `knowgrph/canvas/src/lib/config-copy/uiCopy.ts` | `MarkdownPreviewViewer`, `MarkdownPanelLayout`, `wikiLinks` | `parseWikiLinkInner`, `buildMarkdownWikiHref`, `parseMarkdownWikiHref` | `sandbox/demo/markdown-slide-demo.md` | Clickable wikilinks across files + sidebar backlinks list | Match by `normalizeLooseKey`; set hash for anchor scroll after file switch; memoize backlinks from source file texts | Consider upgrading unlinked-mentions to token-aware matching (skip code blocks) if false positives become noisy. | 2026-01-29 |
| Canvas Initialization + Frontmatter Mode | Enforce init state for slide-like docs | Default to Document Structure + Document Mode; enable Frontmatter Mode by default but never blank: if no frontmatter mermaid, fall back to full graph. In Frontmatter Mode, include frontmatter Mermaid nodes/subgraphs plus tied anchors/internal links/callouts via `pointsTo` (forbid hardcoded demo paths). | `knowgrph/canvas/src/hooks/store/uiSettingsSlice.ts`, `knowgrph/canvas/src/lib/graph/layerDerivation.ts`, `knowgrph/canvas/src/features/parsers/markdownJsonLd.ts` | `createUiSettingsSlice`, `filterGraphToFrontmatterMermaid`, `buildMarkdownJsonLd` | `setFrontmatterModeEnabled`, `pointsTo` | Markdown with YAML frontmatter mermaid + in-doc anchors/links/callouts | Frontmatter-focused graph view with linked doc targets (bounded) | If no frontmatter nodes → return original graph; derive reachability by `pointsTo` only | Keep behavior documented in Canvas init + Mermaid frontmatter docs and schema. | 2026-01-29 |
| Import→Parse→Render (Markdown) | Keep provenance portable + speed up frontmatter filtering | Preserve relative `metadata.documentPath`; strip absolute paths to basenames; avoid O(E×V) scans in frontmatter filtering; replace non-semantic toolbar separators. | `knowgrph/canvas/src/features/parsers/markdownJsonLd.ts`, `knowgrph/canvas/src/lib/graph/layerDerivation.ts`, `knowgrph/canvas/src/components/Toolbar.tsx` | `buildMarkdownJsonLd`, `filterGraphToFrontmatterMermaid`, `Toolbar` | `documentPath`, `pointsTo` BFS | Markdown source names (local/URL) | Consistent selection/highlight across Viewer↔Canvas; faster filtering | documentPath: url/relative preserved, absolute→basename; pointsTo adjacency cache; divider `<hr>` | Add a CI regression test for documentPath normalization (bounded filter run). | 2026-01-29 |
| Canvas Init + Source Files Shortcuts | Prevent empty canvas/editor via conflicting modes/actions | Force Document Mode on init (disable Geospatial Mode); keep Frontmatter Mode + Document Structure Mode default on; keyboard “New Source File” must call the canonical helper (no direct store mutation). | `knowgrph/canvas/src/pages/Canvas.tsx`, `knowgrph/canvas/src/components/Toolbar.tsx`, `knowgrph/canvas/src/features/toolbar/ToolbarMenuLauncher.tsx` | `CanvasPage`, `Toolbar`, `ToolbarMenuLauncher` | init effects + `createNewMarkdownSourceFileAndOpenViewer` | persisted LS + shortcuts | Stable startup; no blank graph; no markdown desync | init writes `geospatialOverlayEnabled=false`; reset keeps frontmatter/document; shortcut uses helper only | Keep a bounded regression test for “Open in editor” honoring selected markdown doc. | 2026-01-29 |
| Markdown Viewer/Presentation SSOT + JSON consolidation | Remove duplicate sidebar/header shapes; eliminate JSON editor | Extract a shared Markdown sidebar frame SSOT; reuse it across Viewer/Presentation (only `aside` vs `section` differs); remove graph JSON “code” tab and `JsonEditor`; route JSON/YAML edits via `MarkdownStructuredTextEditor`; delete stale aliases/tests. | `curagrph/src/features/markdown/ui/MarkdownSidebarFrame.tsx`, `curagrph/src/features/markdown/ui/SlidesSidebar.tsx`, `curagrph/src/features/markdown/ui/MarkdownPanelLayout.tsx`, `curagrph/src/features/markdown/ui/MarkdownStructuredTextEditor.tsx`, `knowgrph/canvas/src/components/BottomPanel.tsx` | `MarkdownSidebarFrame`, `MarkdownPanelLayout`, `SlidesSidebar`, `MarkdownStructuredTextEditor`, `BottomPanel` | - | - | Unified sidebar markup + no dedicated JSON editor surface | Avoid `tab==='code'`; remove `@/features/json/*` alias; keep bounded `npm run typecheck` verification only | Extend conversion policies (GeoJSON/TopoJSON) inside Markdown renderer only if needed, not via new editors. | 2026-01-29 |
| Markdown SSOT Header/Sidebar + Shared Utils | Update cross-repo SSOT docs/schema | Enforce single SSOT Markdown Section Header + Sidebar structure across Editor/Viewer/Presentation; place Source Files actions in the header (left of Apply changes) and keep sidebar section headers typography-only; compute Backlinks via shared utils; remove legacy duplicates. | `curagrph/src/components/BottomPanel/BottomPanelMarkdownViewerHeader.tsx`, `curagrph/src/features/markdown/ui/{MarkdownPanelLayout.tsx,MarkdownSidebarFrame.tsx,MarkdownSidebarSection.tsx}`, `knowgrph/grph-shared/src/markdown/{backlinks.ts,wikiLinks.ts,slugify.ts}` | `ViewerHeaderRow`, `MarkdownPanelLayout`, `MarkdownSidebarFrame`, `computeMarkdownBacklinks` | - | Unified header/sidebar DOM + drift-resistant markdown utilities | SSOT rule: one implementation per surface; Viewer/Presentation reuse Editor-based UI blocks | Keep schema + docs updated in lockstep with UI contract changes. | 2026-01-29 |


## 2026-01-28

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Markdown Viewer / TOC + Persistence | Reduce duplicated persistence and reorder logic | Centralize Markdown sidebar + collapsed-heading persistence via `LS_KEYS`; forbid magic keys; enable TOC reorder only from the grip handle; reuse `grph-shared` reorderList; remove duplicate reorder implementations. | `knowgrph/canvas/src/lib/config.ls.ts`, `curagrph/src/features/markdown/ui/{MarkdownPreviewViewer.tsx,MarkdownTableOfContents.tsx}`, `curagrph/src/components/BottomPanel/hooks/useMarkdownSectionLogic.ts`, `knowgrph/grph-shared/src/array/reorderList.ts`, `knowgrph/canvas/src/hooks/store/sourceFilesSlice.ts` | `LS_KEYS`, `MarkdownPreviewViewer`, `MarkdownTableOfContents`, `useMarkdownSectionLogic`, `reorderList` | `lsBool/lsSetBool`, `lsJson/lsSetJson`, `reorderSourceFiles` | Toggle sidebar; collapse headings; drag TOC reorder; reorder Source Files | Persisted sidebar/collapse state; safer reorder UX; shared reorder util SSOT | Persist: `kg:ui:markdown:{sidebarOpen,collapsedHeadingIds}`; Drag start bound to grip element; reorderList handles index moves | Add a one-time migration adapter for legacy non-namespaced keys if required for backward compatibility. | 2026-01-28 |
| Tests / CI | Prevent duplicated markdown scroll-sync runs | Remove duplicate scroll-sync test execution from the global runner; forbid running the same test in both modular and “remaining tests” sections. | `knowgrph/canvas/src/tests/run.ts`, `knowgrph/canvas/src/tests/runners/runMarkdownTests.ts` | `runAllTests`, `runMarkdownTests` | `exec`, `execTest` | `KG_TEST_TIMEOUT_MS`, filter args | Stable suite progress; no repeated `markdown.scrollSync.viewerToEditor` | Scroll-sync tests run only in the Markdown runner; CI filter runs targeted cases via argv | Keep CI filter usage documented and prefer `ci.ts <filter>` for bounded runs. | 2026-01-28 |
| Policy / Markdown | Forbid Editor.js usage | Enforce native Markdown Editor/Viewer/Presentation; forbid importing or copying Editor.js (block-style WYSIWYG) and related packages/URLs. | `knowgrph/canvas/src/__tests__/crossRepoBoundaryGuards.test.ts`, `knowgrph/canvas/src/tests/run.ts` | `testForbidEditorJsDependencies` | `policy.markdown.forbidEditorJs` | TS/JS/JSON source files | CI fails fast when Editor.js is referenced | Regex guard blocks `@editorjs/*`, `editorjs` strings, and `codex-team/editor.js` URLs | Keep Markdown feature work based on Monaco + token renderer, not WYSIWYG blocks. | 2026-01-28 |
| Markdown Editor / WYSIWYG-like | Add native formatting toolbar | Provide Editor.js-like affordances without dependency: formatting buttons apply Markdown syntax to Monaco selection and keep live preview. Centralize text transforms in `grph-shared` for reuse. | `curagrph/src/components/BottomPanel/{BottomPanelMarkdownSectionView.tsx,BottomPanelMarkdownViewerHeader.tsx}`, `knowgrph/grph-shared/src/markdown/formatting.ts`, `knowgrph/canvas/src/__tests__/markdown/markdownFormatting.test.ts` | `ViewerHeaderRow`, `applyMarkdownFormatAction` | `onFormatAction`, `applyMarkdownFormatAction` | Selection offsets + toolbar click | Updated Markdown text + restored selection | Transform is pure; UI schedules selection restore via `requestAnimationFrame` to match controlled editor updates | Add additional actions (code block, table, checklist) only if needed, keeping transforms pure + testable. | 2026-01-28 |
| Markdown Viewer / Block-style editing | Enable line-level block ops in Viewer | Implement native Viewer block controls (no Editor.js): keep heading grip drag reorder among same-parent siblings; remove Move Up/Down buttons; add “Add line” control on blocks to insert a new blank line after the block. Centralize pure transforms in `grph-shared` (`computeMarkdownTocReorder`, `insertMarkdownLineAfter`). | `curagrph/src/features/markdown/ui/{MarkdownPreview.tsx,MarkdownPreviewViewer.tsx,MarkdownHeadingBlock.tsx,MarkdownParagraphBlock.tsx,MarkdownListBlock.tsx,MarkdownBlockquoteBlock.tsx,MarkdownCodeBlock.tsx,MarkdownTableOfContents.tsx}`, `curagrph/src/components/BottomPanel/{BottomPanelMarkdownSectionView.tsx,MarkdownViewerPane.tsx,hooks/useMarkdownSectionLogic.ts}`, `knowgrph/grph-shared/src/markdown/{toc.ts,lineEditing.ts}`, `knowgrph/canvas/src/__tests__/markdown/markdownLineEditing.test.ts` | `MarkdownHeadingBlock`, `insertMarkdownLineAfter`, `computeMarkdownTocReorder` | `onReorderHeadingSection`, `onInsertLineAfter` | Hover grip drag; click Add line | Updated Markdown text with inserted/reordered blocks | Enable only when callbacks provided; keep transforms pure + bounded; maintain font/spacing consistency | Extend with delete/duplicate only via pure transforms + tests. | 2026-01-28 |
| Markdown Viewer / Source Files | Open new file in Viewer (not Editor) | Route “New Source File” (Workflow + Tool Menu) through a shared helper: create an empty `source-N.md`, select it, open BottomPanel Curation→Markdown, and force Viewer mode via a dedicated event. | `knowgrph/canvas/src/features/{bottom-panel/constants.ts,source-files/createNewMarkdownSourceFile.ts,workspace-actions/WorkspaceActionsPanel.tsx,toolbar/useToolbarMenuAction.ts}`, `curagrph/src/components/BottomPanel/BottomPanelMarkdownSection.tsx`, `knowgrph/canvas/src/__tests__/markdown/newSourceFileOpensViewer.test.ts` | `createNewMarkdownSourceFileAndOpenViewer`, `BottomPanelMarkdownSection` | `createNewMarkdownSourceFileAndOpenViewer`, `handleNewSourceFile` | Header plus icon click | New Source File + BottomPanel Markdown Viewer focused on it | Next index = max(`source-*.md`) + 1; dispatch `kg:bottomPanelMarkdownOpenViewer` using `window.CustomEvent` | Consider adding a Viewer-side “New Source File” affordance only if users request it. | 2026-01-28 |
| Markdown Viewer / Contents Panel | Add left-side Source Files TOC tree | Render BottomPanel Markdown “Contents” as a left-side panel and show Source Files as a folder/file tree derived from `/` in names; add icon-only “New folder” and “New file” actions and keep file drag reorder working. | `curagrph/src/features/markdown/ui/{MarkdownPanelLayout.tsx,MarkdownPreview.tsx,MarkdownPreviewViewer.tsx}`, `curagrph/src/components/BottomPanel/{BottomPanelMarkdownSectionView.tsx,MarkdownViewerPane.tsx}`, `knowgrph/canvas/src/features/source-files/createNewMarkdownSourceFile.ts`, `knowgrph/canvas/src/__tests__/markdown/markdownSourceFilesTreeNewFile.test.ts` | `MarkdownPanelLayout`, `createNewMarkdownSourceFileAndOpenViewer` | `sidebarPosition`, `New folder/New file` | Source Files present + sidebar open | Tree navigation + create actions | Folder nodes are virtual; order follows store order; create uses optional parentPath to prefix file name | Add rename/move only if users ask; keep folder modeling virtual (name-based). | 2026-01-28 |
| Markdown UX / Selection + Context Menu + Theme | Prevent gesture regressions and stale theme | Preserve native click/double/triple selection in Viewer/Presentation; show “Show on/in …” only on Right Click at pointer position; apply Monaco theme on editor create; disable nested block gutters/controls; update docs + schema to match. | `knowgrph/canvas/src/features/monaco/MonacoTextEditor.tsx`, `curagrph/src/features/markdown/ui/{useMarkdownPreviewEvents.ts,MarkdownBlockGutter.tsx,MarkdownTokenRenderer.tsx}`, `knowgrph/docs/documents/{knowgrph-frontend-document.md,knowgrph-markdown-processing-document.md,curagrph-graph-data-curation-document.md,knowgrph-ui-ux-design-document.md}`, `huijoohwee.github.io/schema/AgenticRAG/{markdown.jsonld,curation.jsonld,panels.jsonld}` | `MonacoTextEditor`, `useMarkdownPreviewEvents`, `MarkdownBlockGutter`, `MarkdownTokenRenderer` | `monaco.editor.setTheme`, `handleContextMenu` | Click/double/triple; right click; theme toggle | Native selection + anchored toolbar + correct theme | Scope handlers to Markdown root; disable Monaco context menu; avoid hijacking click events | Add a regression test for right-click toolbar anchoring if drift is observed. | 2026-01-28 |

## 2026-01-27

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------|
| Markdown UI / Geospatial Mode | Render embedded GeoJSON as MapLibre layers with basemap | Reuse shared geospatial basemap/style loading; bound map init and uploads; forbid host calling gympgrph React hooks; remove hashing drift | `knowgrph/grph-shared/src/hash/stringHash.ts`, `gympgrph/src/features/geospatial/mapLibreFactory.ts`, `knowgrph/canvas/src/features/geospatial/InlineMarkdownGeoJsonLayerMap.tsx`, `curagrph/src/features/markdown/ui/MarkdownCodeBlock.tsx`, `knowgrph/canvas/src/lib/gympgrph/externalStore.ts` | ... | `createMapLibreMapWithBasemap`, `InlineMarkdownGeoJsonLayerMap`, `useGympgrphExternalStore` | Fenced `geojson` blocks in Markdown; `sandbox/demo/markdown-slide-demo.md` | Inline basemap + GeoJSON layer preview; GeoJSON blocks can register as overlay datasets | Inline maps load MapLibre only when in-view; basemap style URL reads from `LS_KEYS.geospatialStyleUrl` with `DEFAULT_GEOSPATIAL_STYLE_URL` fallback; MapLibre requests are normalized/proxied via `applyMediaProxySrc`; GeoJSON upload promises are LRU+TTL-bounded | Open the sandbox demo and confirm: Viewer defaults to Inline (code), Presentation defaults to Render (map), and failures show a visible “Map preview unavailable” overlay (no silent blanks). | 2026-01-27 |
| Markdown UI / GeoJSON Preview | Fix GeoJSON map requiring Beside→Render remount | Keep a stable MapLibre container across deferred-load states; init only when visible; keep bounded observers; forbid hardcoded demo paths. | `knowgrph/canvas/src/features/geospatial/InlineMarkdownGeoJsonLayerMap.tsx` | `InlineMarkdownGeoJsonLayerMap` | `useEffect` (shouldLoadMap + map init) | GeoJSON code blocks in Viewer/Presentation | Map renders on first Render without toggling | If initial rect is 0: bounded poll + IntersectionObserver + ResizeObserver; init once with in-flight guard; do not init against a detached container; resize on mount window frames; show non-silent overlay state | Open `sandbox/demo/markdown-slide-demo.md` and confirm GeoJSON shows immediately | 2026-01-27 |
| Markdown UI / Mermaid | Render per-block Mermaid frontmatter | Parse YAML `---...---` inside Mermaid code fences; merge into Mermaid init config; forbid hardcoded themes; keep doc config as default; verify with bounded test | `curagrph/src/features/markdown/ui/{MarkdownCodeBlock.tsx,mermaidBlockFrontmatter.ts}`, `knowgrph/canvas/src/__tests__/markdown/markdownMermaidCodeBlockFrontmatter.test.ts` | `MarkdownCodeBlock` | `splitMermaidBlockFrontmatter`, `mergeMermaidInitConfig` | Fenced `mermaid`/`mmd` code blocks | Mermaid SVG preview with block-scoped config | If YAML exists at top of block, strip it from diagram text and merge config (themeVariables deep-merge) | Add docs snippet showing per-block theme override in Markdown Viewer | 2026-01-27 |
| Markdown Viewer / Render Toggle | Make Render button cover frontmatter + gate GeoJSON in `json` fences | On Render click: if first line is `---`, auto-enable Frontmatter Mode; frontmatter Mermaid follows global mode. Reuse Gympgrph `parseGeoJsonFromText` for inline+register; add `isGeoJsonCodeBlock` to skip non-GeoJSON `json` fences. Verify with bounded smoke tests. | `curagrph/src/components/BottomPanel/BottomPanelMarkdownViewerHeader.tsx`, `curagrph/src/features/markdown/ui/{MarkdownPreviewViewer.tsx,MarkdownRendererTypes.ts,MarkdownCodeBlock.tsx}`, `knowgrph/canvas/src/components/BottomPanel/BottomPanelMarkdownSection.tsx`, `knowgrph/canvas/src/features/geospatial/InlineMarkdownGeoJsonLayerMap.tsx` | `ViewerHeaderRow`, `MarkdownPreviewViewer`, `MarkdownGeoDatasetIntegration`, `InlineMarkdownGeoJsonLayerMap` | `annotate-display=render` click, `frontmatterBlocks`, `isGeoJsonCodeBlock` | Markdown with YAML frontmatter + fenced `geojson`/`json` blocks | Render mode shows Mermaid/GeoJSON outputs and frontmatter without extra toggles | `firstLine==='---'` → enable frontmatter on Render; `parseGeoJsonFromText==null` → no map/register for `json` blocks | Keep `sandbox/demo/markdown-slide-demo.md` as the canonical smoke fixture for UI validation | 2026-01-27 |
| Markdown Presentation / GeoJSON | Prevent per-slide code-block mode leakage | Use stable React keys (document path + line range) so per-block Inline/Beside overrides don’t leak across slides; GeoJSON remains Render-by-default. Remove duplicate slide demo fixture; use `sandbox/demo/markdown-slide-demo.md` in tests. | `curagrph/src/features/markdown/ui/MarkdownTokenRenderer.tsx`, `knowgrph/canvas/src/__tests__/markdown/{markdownGeoJsonPresentationRender.test.ts,markdownSlideStylingReferenceRender.test.ts,markdownGuidelinesIngestion.test.ts}` | `MarkdownTokenRenderer` | `renderBlockTokens` | Presentation slides + per-block toggles | GeoJSON renders as map (not source) across slide navigation | Key = `${documentPath}:${type}:${startLine}:${endLine}` avoids state reuse across slides | Toggle Inline on one slide, Next to GeoJSON slide; map still renders | 2026-01-27 |
| Markdown Viewer / Code Block UI | Synchronize global and per-block display modes | Global Beside/Inline/Render sets the default; per-block toggles create local overrides that auto-clear when matching the global mode so future global changes apply. Add a bounded regression test using the sandbox slide demo. | `curagrph/src/features/markdown/ui/MarkdownCodeBlock.tsx`, `knowgrph/canvas/src/__tests__/markdown/markdownCodeBlockModeSync.test.ts` | `MarkdownCodeBlock` | `AnnotateDisplayModeToggle` | Global mode changes + per-block toggles | Consistent code block display and predictable overrides | If `localOverride && localMode===baseMode` → clear override; otherwise keep explicit per-block choice | Override Beside on block, set global Beside then Render; block follows global Render | 2026-01-27 |
| Markdown Viewer / Presentation | GeoJSON map render from `geojson` and detected `json` blocks | Render mode uses Geospatial Mode basemap helpers; Viewer defaults to global annotate mode; Presentation defaults GeoJSON blocks to Render. `json` blocks render as GeoJSON only when `isGeoJsonCodeBlock` identifies GeoJSON content. Add regression tests for defaults across Viewer/Presentation. | `curagrph/src/features/markdown/ui/MarkdownCodeBlock.tsx`, `knowgrph/canvas/src/__tests__/markdown/markdownGeoJsonJsonTagDefaultRender.test.ts` | `MarkdownCodeBlock` | `GeoJsonGeoPanelRenderer` | Viewer + Presentation code block rendering | GeoJSON maps render predictably without manual toggle in Presentation | `isGeoJsonRenderable` for `json` uses `geoDatasetIntegration.isGeoJsonCodeBlock` (otherwise `json` stays source) | Render Markdown with ```json + FeatureCollection; Presentation shows map by default | 2026-01-27 |
| Markdown Viewer / Presentation | GeoJSON map interaction parity with Mermaid | GeoJSON map previews follow Mermaid overlay conventions: in container-scope they open fullscreen `PreviewOverlay`; in viewport-scope they open the Geo panel via `requestOpenGeoPanel`. | `curagrph/src/features/markdown/ui/MarkdownCodeBlock.tsx` | `MarkdownCodeBlock` | `GeoJsonGeoPanelRenderer` | GeoJSON map preview click | Consistent interaction model across renderable blocks | Click map: container-scope opens overlay; viewport-scope opens Geo panel | Click map in Viewer/Presentation and confirm expected open behavior | 2026-01-27 |
| Markdown UI / GeoJSON Errors | Fix “GeoJSON renderer unavailable” + shrink error panel | Ensure Viewer memo deps include `geoDatasetIntegration` so map renderer updates when injected; render GeoJSON error feedback as a compact bar (~18px) instead of a large panel. | `curagrph/src/features/markdown/ui/{MarkdownPreviewViewer.tsx,MarkdownCodeBlock.tsx}`, `knowgrph/canvas/src/components/ui/ErrorFeedback.tsx` | `MarkdownPreviewViewer`, `GeoJsonGeoPanelRenderer`, `ErrorFeedback` | `useMemo` deps, `ErrorFeedback(variant=compact)` | GeoJSON code blocks in Viewer/Presentation | No stale “renderer unavailable”; compact error UI | If integration becomes available later, Viewer must re-render renderers; compact error uses fixed height | Open `sandbox/demo/markdown-slide-demo.md` and confirm no GeoJSON renderer unavailable | 2026-01-27 |
| Markdown Viewer/Presentation / GeoJSON | Fix GeoJSON render defaults + basemap race | In Viewer, default to global annotate display mode (inline); in Presentation, default GeoJSON blocks to Render. After basemap setStyle, wait briefly for style load before adding sources/layers. | `curagrph/src/features/markdown/ui/MarkdownCodeBlock.tsx`, `gympgrph/src/features/geospatial/mapLibreFactory.ts`, `knowgrph/canvas/src/__tests__/markdown/*GeoJson*.test.ts` | `MarkdownCodeBlock`, `createMapLibreMapWithBasemap` | baseMode selection; style-load wait | GeoJSON `geojson` fences + GeoJSON-like `json` fences | Viewer shows code by default; presentation shows map by default | `isGeoJsonRenderable && markdownPresentationMode` → Render else respect annotateDisplayMode; wait style load (bounded) after setStyle | Run filtered CI: `npm run test:ci -- geojson` and `-- maplibre` | 2026-01-27 |
| Document Mode ↔ Geospatial Mode | Align mode rendering rules across Canvas + Markdown | Geospatial Mode: Canvas renders GeoJSON/geo-JSON datasets and forbids knowledge-graph rendering (nodes/edges/layers/rich media). Document Mode: Canvas renders the knowledge graph and forbids full-screen geospatial overlay rendering; Markdown Viewer/Presentation code blocks can still render fitted GeoJSON MapLibre previews. | `knowgrph/canvas/src/pages/Canvas.tsx`, `curagrph/src/features/markdown/ui/MarkdownCodeBlock.tsx`, `knowgrph/canvas/src/components/BottomPanel/BottomPanelMarkdownSection.tsx` | `CanvasPage`, `MarkdownCodeBlock`, `BottomPanelMarkdownSection` | - | Mode toggle + Markdown GeoJSON blocks | Predictable separation of canvas mode and markdown previews | Geospatial Mode gates graph canvases/minimap/node editor; overlay host active only when enabled; Markdown previews stay mode-independent | Update docs + schema to reflect mode rules | 2026-01-27 |

## 2026-01-26

| Context          | Intent                     | Directive                                                                                   | Module                     | Function/Method | Input                                          | Output                      | Decision Logic                                    | Updated Date |
|------------------|----------------------------|---------------------------------------------------------------------------------------------|----------------------------|-----------------|------------------------------------------------|-----------------------------|---------------------------------------------------|--------------|
| Settings UI | MainPanel Settings alignment + consistency | Enforce one-row Key/Type/Value settings layout with right-edge-aligned value controls; enforce consistent row/control heights across inputs/selects/checkboxes/pills; keep status and action pills inline; preserve clean interactions (value control clicks do not toggle expand). | `knowgrph/canvas/src/features/panels/ui/KeyTypeValueRow.tsx`, `knowgrph/canvas/src/features/panels/views/SettingsView.tsx`, `knowgrph/canvas/src/features/settings/ui.tsx` | `KeyTypeValueRow`, `SettingsView`, `renderSettingInput` | Settings edits + hover tooltips + Check Health | Stable, aligned Settings rows with consistent control heights | Use non-stacking grid templates; ensure value wrappers are `w-full min-w-0` and inputs use `min-w-0` so right borders align; normalize h-6 for controls and preview chips; stopPropagation on value controls and action pills to avoid accidental expand/collapse. | 2026-01-26 |
| Workflow UI | MainPanel Workflow layout alignment + low-verbosity guidance | Enforce left/right edge alignment and consistent spacing across Workflow step sections; move step descriptions into Role→Actions→Outcome tooltips to reduce inline verbosity while keeping guidance accessible. | `knowgrph/canvas/src/features/panels/views/WorkflowSteps.tsx`, `knowgrph/canvas/src/features/panels/config.ts`, `knowgrph/docs/documents/knowgrph-frontend-document.md` | `WorkflowSteps`, `WORKFLOW_STEP_COPY` | Workflow tab usage + collapse/expand + status chips | Aligned Workflow surface with tooltip-based step guidance | Remove visible step description blocks; wrap step headers in tooltips using RAO wording; normalize status display as h-6 pills; remove redundant top border/padding on the first step section to avoid double separators. | 2026-01-26 |
| Workflow UI | Workspace Actions tool area alignment | Normalize Source Files / Parser / Validation headers and controls so sections align left/right edges and keep consistent control heights in Workflow → Workspace Actions. | `knowgrph/canvas/src/features/toolbar/{ToolbarToolMenuAreas.tsx,ToolbarSourceFilesArea.tsx,ToolbarParserArea.tsx,ToolbarValidationArea.tsx}`, `knowgrph/canvas/src/features/panels/ui/StatusBadge.tsx`, `gympgrph/src/{lib/ui/theme-tokens.ts,features/panels/ui/StatusBadge.tsx}` | `ToolbarToolMenuAreas`, `ToolbarSourceFilesArea` | Import/export menus + status badges | Edge-aligned tool areas with consistent row heights | Use px-2 section padding and remove nested px wrappers that drift; standardize controls to h-7 (inputs/selects/textarea rows=1) and status pills to h-6; keep Source Files as a single table with a header New Source File icon + per-row actions; avoid extra nested Workspace Actions headers. | 2026-01-26 |
| Workflow UI | Source Files header action | Add a right-aligned icon-only "New Source File" control in the Source Files section header that inserts a new Source File row. | `knowgrph/canvas/src/features/workspace-actions/WorkspaceActionsPanel.tsx`, `huijoohwee.github.io/schema/AgenticRAG/workflow.jsonld` | `WorkspaceActionsPanel` | Header click | New empty Source File row | Control writes directly to the canonical Source Files store (`addSourceFile`) so a new row appears immediately in the Source Files table and can be imported/exported/cleared. | 2026-01-26 |
| Workflow UI | Source Files management table | Render one source file (Local/URL) per row in Source Files with fixed column widths and minimal row DOM. | `knowgrph/canvas/src/features/toolbar/ToolbarSourceFilesArea.tsx`, `knowgrph/canvas/src/hooks/store/{types.ts,sourceFilesSlice.ts}`, `knowgrph/canvas/src/features/graph-fields/ui/graphFieldIcons.tsx` | `ToolbarSourceFilesArea`, `createSourceFilesSlice` | Source files list + drag reorder + toggles | Table-driven source file management UX | Widths: 2%/2%/22%/28%/32%/6%/10% (Geo column is only shown when Geospatial Mode is enabled). Columns: Move, Show, Label, Local/URL, Action, Geo, Status. Per-row actions are icon-only square buttons. | 2026-01-26 |
| Workflow UI | Source Files per-row import + stale action cleanup | Remove legacy Source Files tool-menu Import/Clear/Export and use per-row actions with local-file pick and URL import; generate stable ids without relying on crypto-only APIs. | `knowgrph/canvas/src/features/toolbar/ToolbarSourceFilesArea.tsx`, `knowgrph/canvas/src/features/toolbar/ToolbarToolMenuAreas.tsx`, `knowgrph/canvas/src/features/toolbar/useToolbarMenuAction.ts`, `knowgrph/canvas/src/lib/id.ts` | `ToolbarSourceFilesArea`, `importSourceFileToCanvas`, `handleImportSourceFile`, `createId` | Local import, URL import (inline), export menu, clear | Parsed graph renders on Canvas; source rows stay deterministic | Local Import uses the file picker and derives format internally; URL Import uses a per-row inline URL input (hidden by default) that expands leftwards from a URL icon; Clear resets the row’s text + parsed caches (does not delete the row) and recomposes the Canvas if enabled. Row action buttons are borderless to avoid visual noise. | 2026-01-26 |
| Geospatial Mode | MapLibre dependency boundary + dev-server interop | Treat `maplibre-gl` as a host-level dependency and prebundle it for ESM dev servers; enforce gympgrph host integration via package root exports only. | `knowgrph/canvas/{vite.config.ts,package.json}`, `gympgrph/{package.json,src/index.ts}`, `huijoohwee.github.io/schema/AgenticRAG/geospatial.jsonld` | `optimizeDeps.include`, `resolve.dedupe`, `addGeospatialDatasetUrls` | Geospatial overlay load + dataset registration | No MapLibre default-export crash; root-only gympgrph imports | Host installs + dedupes `maplibre-gl` and includes it in dependency optimization; gympgrph exposes dataset helpers via the root entrypoint so hosts avoid `gympgrph/*` subpath imports while preserving boundary guard tests. | 2026-01-26 |
| Geospatial Mode | Dataset fetch limits relocation | Move Dataset fetch limits controls from Geo side panel into MainPanel Workflow → Step 3 Ingest as its own collapsible subsection; keep the Geo panel focused and non-duplicative. | `knowgrph/canvas/src/features/workspace-actions/WorkspaceActionsPanel.tsx`, `knowgrph/canvas/src/features/panels/ui/KeyTypeValueRow.tsx`, `gympgrph/src/hooks/store/geospatialSlice.ts` | `WorkspaceActionsPanel`, `SimpleKeyValueRow`, `RightAlignedTooltipInput` | Timeout/max-bytes edits | Same bounded fetch behavior, configured from one surface | Render one key/value row per limit using the MainPanel Settings Key/Value row primitives; remove the legacy Geo side-panel fetch-limit UI and keep only one configuration surface. | 2026-01-26 |
| Workflow UI | Step 3 Ingest subsections | Consolidate Step 3 (Ingest) into collapsible subsections: Sample Dataset + Dataset fetch limits + Source Files; remove legacy workspace-actions menu state and stale keyboard shortcut actions. | `knowgrph/canvas/src/features/workspace-actions/{WorkspaceActionsStep.tsx,WorkspaceActionsPanel.tsx}`, `knowgrph/canvas/src/features/panels/views/WorkflowSteps.tsx`, `knowgrph/canvas/src/features/toolbar/{ToolbarSourceFilesArea.tsx,useToolMenuShortcuts.ts,ToolbarMenuLauncher.tsx}` | `WorkspaceActionsPanel`, `ToolbarSourceFilesArea`, `useToolMenuShortcuts` | Workflow Step 3 usage + Cmd/Ctrl+Alt+N | Single ingest surface with clean collapse + no stale actions | Step 3 renders Sample Dataset inside Workspace Actions so all Step 3 subsections share the same padding/alignment; Source Files remains the canonical ingest table, and tool-menu shortcuts only create a new Source File row (no legacy import/export/clear). | 2026-01-26 |
| Workflow UI | URL+Local fetch bounds wiring | Ensure Step 3 Dataset fetch limits govern Source Files URL import, editor fetch, and local file reads, preventing failures caused by small shared defaults and keeping ingest consistent. | `knowgrph/canvas/src/features/toolbar/ToolbarSourceFilesArea.tsx`, `knowgrph/grph-shared/src/net/fetchRemoteText.ts`, `knowgrph/canvas/src/features/workspace-actions/WorkspaceActionsPanel.tsx` | `handleImportSourceFile`, `fetchRemoteTextDetailed` | Large URL sources + large local files | No false \"Too large\" on valid datasets; actionable status text | Source Files passes `timeoutMs/maxBytes` from the geospatial fetch limits store into `fetchRemoteTextDetailed`, bounds local file reads by the same max-bytes, and Geo-layer URL imports register datasets without forcing a graph parse. | 2026-01-26 |
| MainPanel UI | Minimize header always visible | Ensure minimizing the MainPanel preserves a visible header so the panel can always be restored. | `knowgrph/canvas/src/features/panels/ui/{MainPanelContainer.tsx,MainPanelFrame.tsx}` | `MainPanelContainer`, `MainPanelFrame` | Minimize/Restore | Always-visible header when collapsed | When collapsed, the MainPanel container uses auto height (instead of h-full) so the TabHeader remains visible even when the outer wrapper uses h-fit. | 2026-01-26 |
| Geospatial Mode | Geo panel dedupe + hint icon | Remove duplicate Dataset fetch limits UI from the Geo side panel and replace the “Add dataset layers…” inline copy with an icon-only hint (tooltip). | `gympgrph/src/features/geospatial/GeospatialPanel.tsx`, `gympgrph/src/lib/config.ts`, `gympgrph/src/index.ts` | `GeospatialPanel` | Host hides dataset manager | No duplicate controls; cleaner Geo panel | Geo panel no longer renders Dataset fetch limits controls; hosts configure fetch limits from Workflow Step 3 and Geo panel shows a single icon hint pointing users to Source Files import. | 2026-01-26 |
| Geospatial Mode | Mode naming clarity | Change the Toolbar map icon button title to explicitly reflect the active mode: `Document Mode (Geospatial Mode Off)` vs `Geospatial Mode (On)`. | `knowgrph/canvas/src/components/Toolbar.tsx`, `knowgrph/canvas/src/lib/config-copy/uiCopy.ts` | `Toolbar` | Toolbar map icon hover/aria-label | Clear mode status + accessible labeling | Avoid ambiguous “Geo” copy; surface the user-facing mode names directly in the button title/tooltip so screen readers and hover tooltips match runtime state. | 2026-01-26 |
| Geospatial Mode | Embedded GeoJSON in Markdown | Allow local Markdown Source Files to register embedded fenced `geojson` as overlay datasets by extracting blocks and uploading them to the bounded local dataset cache; dedupe dataset registration by URL. | `knowgrph/canvas/src/features/toolbar/ToolbarSourceFilesArea.tsx`, `knowgrph/canvas/src/lib/markdown/{extractFencedCodeBlocks.ts,embeddedGeoJson.ts}`, `gympgrph/src/hooks/store/geospatialSlice.ts`, `knowgrph/docs/documents/{knowgrph-source-files-import-document.md,knowgrph-geospatial-mode-document.md}` | `handleToggleFileGeoLayer`, `addGeospatialDatasetUrl` | Local Markdown with embedded GeoJSON blocks | Embedded GeoJSON renders as MapLibre layers when Geospatial Mode is active | Extract fenced `geojson` blocks that `parseGeoJsonFromText(...)` accepts (FeatureCollection/Feature/Geometry); upload via `/__geo_upload` (bounded) and register returned `/__geo_local/...` URLs; prevent duplicates by re-enabling existing datasets when the URL already exists in the registry. | 2026-01-26 |
| Markdown UI | Header declutter | Remove the redundant Markdown status pill (already shown in Toolbar “Open Data”), and show Markdown Viewer/Editor as icon-only with hover tooltip (no inline label spans). | `curagrph/src/components/BottomPanel/{BottomPanelMarkdownViewerHeader.tsx,BottomPanelMarkdownStatusHeader.tsx,BottomPanelMarkdownSectionView.tsx}` | `ViewerHeaderRow`, `HeaderStatusRow` | BottomPanel Markdown header | Cleaner header; fewer duplicated labels | Viewer/editor title is conveyed via the SVG `title` tooltip; the status pill is removed to avoid duplicating the current document status/name already visible in the host toolbar. | 2026-01-26 |
| Markdown UI | Code block render mode | Keep a main “Beside / Inline / Render” toggle in the Markdown Viewer header, and add per-code-block icon toggles to override the mode locally for that block. In Render mode, supported blocks render fitted previews (Mermaid diagrams, GeoJSON via MapLibre). | `curagrph/src/components/BottomPanel/BottomPanelMarkdownViewerHeader.tsx`, `curagrph/src/features/markdown/ui/{MarkdownCodeBlock.tsx,MarkdownPreviewViewer.tsx}`, `knowgrph/canvas/src/features/geospatial/InlineMarkdownGeoJsonLayerMap.tsx`, `curagrph/src/features/panels/views/preview-panel/ui/MermaidDiagram.tsx`, `knowgrph/docs/documents/knowgrph-markdown-processing-document.md` | `ViewerHeaderRow`, `MarkdownCodeBlock`, `MermaidDiagram`, `InlineMarkdownGeoJsonLayerMap` | Markdown Viewer | Main mode + per-block override controls; consistent previews | Viewer header sets the default mode; each code block header exposes icon-only Beside/Inline/Render controls that override locally without mutating the global preference; Mermaid uses SVG output and GeoJSON uses a bounded MapLibre layer preview; other languages fall back to source rendering. | 2026-01-26 |
| Cross-repo Build | Shared utils packaging + conflict removal | Migrate `grph-shared` into `knowgrph/grph-shared` and ship JS `dist/` exports so Node (Vite config) never loads TS from `node_modules`; remove legacy top-level `../grph-shared` repo to prevent cross-repo drift. | `knowgrph/grph-shared/{package.json,tsconfig.json,src/*}`, `knowgrph/canvas/{package.json,vite.config.ts}`, `gympgrph/package.json` | `build:grph-shared` | `npm run dev` (predev), Vite config import graph | Vite dev starts cleanly; shared utils stay SSOT | `grph-shared` exports map to `dist/*.js` + `dist/*.d.ts` only; canvas predev/prebuild/pretest compile the package; boundary guard test forbids `grph-shared/src` imports so runtime cannot bypass the export contract. | 2026-01-26 |
| Markdown UI | Embedded GeoJSON Rendering | Implement inline rendering of embedded GeoJSON blocks in Markdown Viewer using MapLibre layers; reuse `gympgrph` utilities (`ensureDatasetLayer`, `setGeoJsonSourceData`) to avoid duplication; support Rich Media (YouTube/iframe) without hardcoded paths; remove legacy inline GeoJSON preview implementation. | `knowgrph/canvas/src/components/BottomPanel/BottomPanelMarkdownSection.tsx`, `knowgrph/canvas/src/features/geospatial/InlineMarkdownGeoJsonLayerMap.tsx`, `curagrph/src/features/markdown/ui/MarkdownCodeBlock.tsx` | `createGeoDatasetIntegration`, `InlineMarkdownGeoJsonLayerMap` | Markdown with `geojson` blocks | MapLibre layer preview | Reuses `gympgrph` shared utils for consistent rendering; passes rendering logic via `geoDatasetIntegration` prop to `curagrph`; legacy code deleted to prevent drift. | 2026-01-26 |

## 2026-01-25

| Context          | Intent                     | Directive                                                                                   | Module                     | Function/Method | Input                                          | Output                      | Decision Logic                                    | Updated Date |
|------------------|----------------------------|---------------------------------------------------------------------------------------------|----------------------------|-----------------|------------------------------------------------|-----------------------------|---------------------------------------------------|--------------|
| Workspace Actions | Multi-source file imports | Support multi-source Source Files imports via MainPanel Workflow → Workspace Actions; normalize GitHub blob-like URLs; ensure imported source files appear in Markdown "Contents" nav; optionally register URL sources as geospatial datasets. | `knowgrph/canvas/src/features/toolbar/ToolbarSourceFilesArea.tsx`, `knowgrph/canvas/src/lib/{url.ts,net/fetchRemoteText.ts}`, `gympgrph/{package.json,src/geospatialDatasets.ts}`, `curagrph/src/features/markdown/ui/MarkdownPanelLayout.tsx`, `curagrph/src/components/BottomPanel/BottomPanelMarkdownSection.tsx` | `splitUserProvidedTextList`, `normalizeGitHubBlobLikeUrl`, `fetchRemoteText`, `addSourceFile`, `addGeospatialDatasetUrls` | Local files (multi) + URL list | Populated `sourceFiles` store + selectable Source Files list | Parse URL list by whitespace/commas; normalize GitHub blob URLs to raw; fetch text with bounded limits; add each as `{id,name,text,...}`; per-row Geo Layer checkbox triggers dataset URL registration; per-row Import loads file into Markdown; per-row Export downloads file text; per-row Clear resets row text + parsed caches (does not delete the row). | 2026-01-26 |
| Geospatial Mode | Dataset UI relocation | Move dataset add/list controls out of the hosted Geo side panel and into Source Files (Workspace Actions); forbid duplicated dataset UI across surfaces; keep gympgrph standalone behavior; verify with bounded typecheck only. | `gympgrph/src/{GeospatialHost.tsx,features/geospatial/{GeospatialPanel.tsx,GeospatialDatasetsManager.tsx},geospatialDatasetsUi.ts,package.json}`, `knowgrph/canvas/src/{pages/Canvas.tsx,features/toolbar/ToolbarSourceFilesArea.tsx}` | `GeospatialPanelHost`, `showDatasetsManager` | Host flag + dataset list | Geo panel hides dataset manager; datasets are registered from Source Files | Host passes `showDatasetsManager=false` so the Geo panel does not render dataset add/list; Source Files registers URL datasets via Geo Layer tick actions in a single surface. | 2026-01-26 |
| Workspace Actions | UI Finetuning | Streamline MainPanel Workflow → Workspace Actions → Source Files layout; keep a single table-driven surface with a New Source Files row and per-row actions; avoid extra menu toggles. | `knowgrph/canvas/src/features/toolbar/{ToolbarToolMenuAreas.tsx,ToolbarSourceFilesArea.tsx}` | `ToolbarToolMenuAreas`, `ToolbarSourceFilesArea` | Workspace action click + URL list + local files | Cleaner, conflict-free Source Files UX | Keep Source Files as one table and avoid separate import panels; per-row actions handle import/export/clear and the Geo Layer checkbox is textless for clean alignment. | 2026-01-26 |
| Workspace Actions | Surface consolidation | Consolidate Workspace Actions controls into MainPanel Workflow Step 3 (Ingest); reuse MainPanel Settings header + scrolling conventions; wire Toolbar "Open Data" button to open MainPanel Workflow; consolidate Workspace Actions filtering into the MainPanel header Graph Search toggle; forbid duplicated action surfaces in FloatingPanel; centralize Workspace Actions state + handlers behind a single model hook; verify with bounded, filtered UI tests only. | `knowgrph/canvas/src/features/panels/{MainPanel.tsx,views/WorkflowSection.tsx,views/WorkflowSteps.tsx,ui/MainPanelWorkflowHeader.tsx,ui/MainPanelSettingsHeader.tsx,ui/ExpandCollapseAllButton.tsx}`, `knowgrph/canvas/src/features/workspace-actions/{useWorkspaceActionsModel.ts,useWorkspaceActionsMenuState.ts,WorkspaceActionsPanel.tsx,WorkspaceActionsStep.tsx}`, `knowgrph/canvas/src/features/toolbar/{ToolbarToolMenu.tsx,ToolbarToolMenuTypes.ts,ToolbarMenuLauncher.tsx,ToolbarToolMenuAreas.tsx,ToolbarToolMenuAreas.registry.tsx,useToolMenuState.ts}` | `useWorkspaceActionsModel`, `WorkspaceActionsStep`, `MainPanelWorkflowHeader`, `ExpandCollapseAllButton`, `ToolbarMenuLauncher` | Toolbar Open Data + MainPanel Workflow Step 3 interactions | Single source of truth for import/export actions + unified search affordance | Place Workspace Actions inside the canonical Workflow step flow (Step 3 Ingest) instead of a separate details block; keep the floating tool menu focused on transient panels (Props/Renderer/Traversal); reuse the shared MainPanel `<aside>` wrapper and Expand/Collapse All header control; route Workspace Actions filtering through the MainPanel header Graph Search UI to avoid duplicated search inputs. | 2026-01-25 |
| Graph Data | Extract to curagrph + integrate as plugin | Move Graph Data curation/presentation implementation out of Knowgrph into `curagrph` (BottomPanel curator + Graph Data Table + Markdown editor/viewer/presentation/gallery + JSON editor + preview-panel UI); keep Knowgrph as host; forbid duplicate/conflicting implementations; verify with bounded typecheck + filtered CI only. | `curagrph/src/components/BottomPanel/*`, `curagrph/src/features/graph-data-table/*`, `curagrph/src/features/markdown/*`, `curagrph/src/features/json/JsonEditor.tsx`, `curagrph/src/features/panels/views/preview-panel/ui/*`, `knowgrph/docs/documents/curagrph-graph-data-curation-document.md`, `knowgrph/canvas/tsconfig.json`, `knowgrph/canvas/package.json`, `knowgrph/canvas/tailwind.config.js`, `knowgrph/canvas/vite.config.ts` | - | GraphData + markdown/json source docs + panel state | Same curation/presentation UI with extracted ownership | Keep UI unchanged while enforcing single-source-of-truth module ownership in `curagrph`; hosts resolve extracted modules via installed `node_modules/curagrph/src` (not sibling src paths), preserve symlink resolution in both Vite and TypeScript, and dedupe+prebundle dual ESM/CJS deps (e.g. `highlight.js`) to prevent runtime default-export mismatches. | 2026-01-25 |
| Cross-repo Pipeline | Modularity + conflict hardening | Unify import→parse→render routing into host-owned helpers; centralize URL-derived name normalization and error/status shaping; enforce host↔plugin boundaries with automated checks (no sibling source-relative imports; gympgrph imported only via package root in production); isolate persistence keys via namespacing + centralized constants; remove duplicated cross-repo types (start with GraphDataTableScope). Verify with bounded typecheck + filtered CI only. | `knowgrph/canvas/src/features/toolbar/{importFlow.ts,importUi.ts,ingestUtils.ts,htmlImportAction.ts,jsonImportAction.ts,pdfImportAction.ts,youtubeImportAction.ts}`, `knowgrph/canvas/src/__tests__/crossRepoBoundaryGuards.test.ts`, `knowgrph/canvas/src/__tests__/{gympgrphUrlInterop.test.ts,gympgrphMapLibreBehaviors.test.ts}`, `knowgrph/canvas/src/hooks/store/graphDataTableUiSlice.ts`, `curagrph/src/components/BottomPanel/{BottomPanelCuratorToolbarModel.tsx,BottomPanelCuratorContent.tsx}`, `curagrph/src/components/BottomPanel/hooks/useBottomPanelCuratorState.ts`, `gympgrph/{package.json,src/testkit.ts}`, `knowgrph/docs/documents/{knowgrph-graph-data-curation-document.md,curagrph-graph-data-curation-document.md}`, `huijoohwee.github.io/schema/AgenticRAG/{pipeline.jsonld,canvas.jsonld,curation.jsonld,geospatial.jsonld,knowgrph-documents-map.graph.jsonld}` | `runImportFlow`, `applyLoaderResultToParserUi`, parser registry routing, boundary guard tests | URLs/files/YouTube/PDF inputs; parser-selected text/JSON; persisted UI settings | Deterministic import status + drift-resistant boundaries + fewer conflicts | Policy: single owner per concern (host orchestration; curagrph curation UI; gympgrph geospatial UI). Guards: forbid `*/{curagrph,gympgrph}/src/*` imports; forbid `gympgrph/*` deep imports in production; forbid new `\"kg:\"` LS key literals outside `config.ls.ts`. Verification (bounded): `npm --prefix knowgrph/canvas run typecheck`; `npm --prefix knowgrph/canvas run test:ci policy.boundary`; `npm --prefix knowgrph/canvas run test:ci geospatial.gympgrphUrl`; `npm --prefix curagrph run typecheck`; `npm --prefix gympgrph run typecheck`. | 2026-01-25 |

| Settings UI | Tooltip semantics + details declutter | Standardize Settings tooltips with Role→Actions→Outcome (key) and Default/Min/Max/Interval/Impact (value); reduce expanded details table to Modules/Classes/Functions only; forbid stray inline copy and duplicated bounds. | `knowgrph/canvas/src/features/panels/views/{SettingsView.tsx,SettingsFallbackDetails.ts}`, `knowgrph/canvas/src/lib/config-copy/tooltips.ts`, `knowgrph/docs/documents/knowgrph-settings-document.md`, `huijoohwee.github.io/schema/AgenticRAG/roles-actions-outcomes-schema.jsonld` | `SettingsView`, `buildSettingsKeyTooltip`, `buildSettingsValueTooltip` | Hover key/value; click expand | Consistent, bounded tooltips + simplified details rendering | Key tooltip derives RAO string and truncates to 50 words; value tooltip shows on hover of the value control (no icon), derives bounds+impact, and truncates to 15 words; expanded row renders only modules/classes/functions. | 2026-01-25 |
| Settings UI | MainPanel layout cleanup + flow provenance | Remove duplicate Settings header presets and inline store writes that bypass Apply/Reset; refine Key/Type/Value grid layout; generate responsibility-flow metadata from code and keep flow doc + JSON artifacts in sync. | `knowgrph/canvas/src/features/panels/ui/{MainPanelSettingsHeader.tsx,KeyTypeValueRow.tsx}`, `knowgrph/canvas/src/features/panels/views/{SettingsView.tsx,useSettingsView.ts}`, `knowgrph/canvas/src/cli/extract-settings-schema.ts`, `knowgrph/knowgrph-codebase-responsibility-flow.md`, `huijoohwee.github.io/schema/AgenticRAG/system-design-pattern.jsonld` | `SettingsView`, `useSettingsView`, `build:settings` | Settings edits + Apply/Reset + build:settings | Drift-resistant Settings surface + inspectable module/function provenance | Settings renders only writable keys, batches edits via Apply/Reset, and regenerates flow metadata to populate Modules/Classes/Functions without manual sync. | 2026-01-25 |
| Workflow UI | GraphRAG workflow editor de-dup + tooltip alignment | Remove duplicated GraphRAG workflow JSON editor from MainPanel Workflow Step 6; keep Orchestrator as the canonical workflow editor; enforce Role→Actions→Outcome key tooltips and bounded Default/Min/Max/Interval value tooltips for GraphRAG workflow fields. | `knowgrph/canvas/src/features/panels/{MainPanel.tsx,views/{WorkflowSteps.tsx,WorkflowSection.tsx,GraphRagWorkflowSection.tsx,GraphRagWorkflowIndexingSection.tsx}}`, `knowgrph/canvas/src/lib/config-copy/{tooltips.ts,uiMeta.ts}`, `knowgrph/docs/documents/knowgrph-source-files-import-document.md`, `huijoohwee.github.io/schema/AgenticRAG/system-design-pattern.jsonld` | `WorkflowSteps`, `GraphRagWorkflowSection`, `GraphRagWorkflowIndexingSection`, `buildNumericTooltip`, `buildDefaultTooltip` | Workflow tab step interactions + Orchestrator workflow edits | Single canonical GraphRAG workflow editor + consistent tooltips | Workflow Step 6 links to Orchestrator; indexing fields use RAO key tooltips and bounded value tooltips; avoid duplicated anchors and stale inline copy. | 2026-01-25 |
| Renderer UI | FloatingPanel Renderer cleanup + tooltip alignment | Remove duplicate renderer layout-mode quick control; keep FloatingPanel Renderer as canonical surface; enforce Role→Actions→Outcome key tooltips and bounded Default/Min/Max/Interval value tooltips for palette and hover content controls. | `knowgrph/canvas/src/features/toolbar/{ToolbarToolMenuRendererView.tsx,ui/{RendererPaletteSettings.tsx,RendererHoverSettings.tsx}}`, `knowgrph/canvas/src/features/panels/views/RenderSettingsSection.tsx`, `knowgrph/canvas/src/lib/config-copy/tooltips.ts`, `knowgrph/docs/documents/knowgrph-renderer-document.md`, `curagrph/src/components/BottomPanel/BottomPanelMarkdownStatusHeader.tsx`, `huijoohwee.github.io/schema/AgenticRAG/system-design-pattern.jsonld` | `ToolbarToolMenuRendererView`, `RendererPaletteSettings`, `RendererHoverSettings`, `buildDefaultTooltip`, `buildNumericTooltip` | FloatingPanel Renderer interactions | Single renderer settings surface + consistent tooltip semantics | Remove duplicated layout-mode row and stale tooltip copy; add RAO key tooltips and bounded value tooltips for palette + hover settings; align tooltip theme tokens cross-repo. | 2026-01-25 |
| Orchestrator UI | BottomPanel Orchestrator cleanup + tooltip alignment | Deduplicate traversalDelayMs control across GraphRAG workflow and AI-KG sections; remove duplicated hover-tooltip content toggles from AI-KG; align Orchestrator key/value tooltips to Role→Actions→Outcome and bounded Default/Min/Max/Interval value semantics; fix stale AI-KG anchor. | `knowgrph/canvas/src/features/panels/{views/{OrchestratorSettingsSection.tsx,OrchestratorTraversalStackSection.tsx,OrchestratorTraversalPanels.tsx,OrchestratorTraversalSequenceSection.tsx,AgenticRagNodeInspectorSection.tsx,AgenticRagContextSection.tsx,AiKgLayersSection.tsx,AiKgLayersSectionControls.tsx,AiKgLayers/AiKg{ForceControls,OpacityControls,VisualControls}.tsx,AiKgLayersSectionTooltips.ts},ui/OrchestratorTraversalDelayRow.tsx}`, `knowgrph/canvas/src/lib/config-copy/{tooltips.ts,uiMeta.ts,uiCopy.ts}`, `curagrph/src/components/BottomPanel/BottomPanelOrchestratorToolbar.tsx`, `knowgrph/docs/documents/knowgrph-frontend-document.md`, `huijoohwee.github.io/schema/AgenticRAG/system-design-pattern.jsonld` | `OrchestratorSettingsSection`, `OrchestratorTraversalDelayRow`, `AI_KG_*_TOOLTIP`, `buildNumericTooltip`, `buildDefaultTooltip` | BottomPanel Orchestrator interactions + AI-KG tuning | Single canonical Orchestrator surface + consistent tooltip semantics | Remove stale/duplicate controls; standardize tooltip styling/tokens; keep traversal + AI-KG tuning coherent across panels and repos. | 2026-01-25 |

## 2026-01-24

Note: As of 2026-01-24, Knowgrph hosts Geospatial Mode via the sibling repo `gympgrph` (implementation lives in `gympgrph/src/`; Knowgrph keeps only a minimal integration bridge).

| Context          | Intent                     | Directive                                                                                   | Module                     | Function/Method | Input                                          | Output                      | Decision Logic                                    | Updated Date |
|------------------|----------------------------|---------------------------------------------------------------------------------------------|----------------------------|-----------------|------------------------------------------------|-----------------------------|---------------------------------------------------|--------------|
| Geospatial Mode  | Extract to gympgrph + integrate as plugin | Move all Geospatial Mode runtime/render/ingest code out of Knowgrph into `gympgrph`; keep Knowgrph geospatial-free except for a minimal toolbar entrypoint + state handoff; add Toolbar Geospatial Mode button (right of 3D Mode) that opens the Geo side-panel tab and activates the gympgrph overlay; verify with bounded typecheck + filtered CI cases only. | `canvas/src/components/Toolbar.tsx`, `canvas/src/features/toolbar/hooks/useToolbarActions.ts`, `canvas/src/pages/Canvas.tsx`, `canvas/src/features/canvas/utils.ts`, `canvas/package.json`, `gympgrph/src/*` | `emitSidePanelOpen`, `handleOpenGeospatialMode`, `GeospatialOverlayHost`, `GeospatialPanelHost` | GraphData + selection + zoom state | Gympgrph overlay/panel mounted and synced from Knowgrph | Knowgrph stays free of MapLibre/Turf logic; gympgrph owns all geospatial behavior and persistence keys; Knowgrph only passes graph/selection/zoom + delegates selection/toasts/camera handlers to host. | 2026-01-24 |
| Toast System     | Stacking + auto-dismiss correctness | Implement native toast stacking that forbids overlap; newest stays at default Y and pushes older downward; auto-dismiss by TTL with prune loop; forbid upsert-refresh that prevents dismissal; upsert moves toast to top when updated. | `components/ui/ToastHost.tsx`, `hooks/store/uiToastSlice.ts` | `pushUiToast`, `upsertUiToast`, `pruneUiToasts` | Toast id/kind/message/ttlMs | Non-overlapping stacked notifications | ToastHost renders by store order (index 0 is top); upsert preserves createdAt-based expiry; prune removes expired; dedupe by id to prevent collisions. | 2026-01-24 |
| UI Cleanup        | Consolidate legacy banners into toast | Replace inline “banner” alerts with toast notifications where appropriate; reuse shared sanitizers/utilities; forbid duplicate/legacy alert implementations drifting from toast behavior. | `features/panels/views/GraphFieldsView.tsx`, `lib/config-copy/uiCopy.ts` | `upsertUiToast`, `dismissUiToast` | GraphFields view + GraphData context | Toasted warning instead of inline banner | Show a warning toast once per graph load for `context="raw-nodes-edges"`; auto-dismiss; dismiss when context changes; remove banner copy keys. | 2026-01-24 |
| Geospatial Mode  | Vector tiles + interactive navigation | Default basemap uses a vector-tile style (OpenFreeMap Liberty) and supports drag/pan + smooth zoom, while remaining configurable via interaction mode (off/hold-space/always). | `gympgrph/src/lib/geospatial/styles.ts`, `gympgrph/src/hooks/store/geospatialSlice.ts`, `gympgrph/src/features/geospatial/mapLibreInteractions.ts`, `gympgrph/src/features/geospatial/GeospatialOverlay.tsx` | `DEFAULT_GEOSPATIAL_STYLE_URL`, `setGeospatialInteractionMode`, `setMapInteractionEnabled` | Style URL + interaction mode | Interactive vector basemap overlay | Default interactionMode is `always`; users can switch to hold-space/off as needed. | 2026-01-24 |
| Geospatial Mode  | Toolbar toggle + input hardening | Ensure the Toolbar Geospatial Mode entrypoint toggles enable/disable and makes the map immediately interactive on enable; ensure Hold Space doesn’t scroll the page or interfere with text inputs. | `gympgrph/src/hostBridge.ts`, `gympgrph/src/features/geospatial/useHeldKey.ts`, `knowgrph/canvas/src/features/toolbar/hooks/useToolbarActions.ts` | `isGeospatialModeEnabled`, `setGeospatialModeEnabled`, `useHeldKey` | Geospatial Mode button click + keyboard input | Reliable enable/disable + predictable Hold Space behavior | On enable, force `interactionMode=always` for immediate navigation; prevent Space default scrolling and ignore key holds while editing text inputs. | 2026-01-24 |
| Geospatial Mode  | SidePanel-independent overlay lifecycle | Keep Geospatial Mode ON/OFF independent of SidePanel expand/collapse while avoiding non-map UI conflicts by mounting the overlay only for the Geo tab selection. | `gympgrph/src/GeospatialHost.tsx`, `knowgrph/canvas/src/pages/Canvas.tsx` | `GeospatialOverlayHost` | SidePanel tab selection + persisted geospatial enabled flag | Stable overlay visibility + bounded UI collision surface | Mount the overlay host after first Geo activation; set `active` by `sidePanelTab==='geo'` (not by `isSidebarOpen`) so collapsing the SidePanel does not disable the overlay. | 2026-01-24 |
| Geospatial Mode  | POI click selection + graph sync | Enable clicking/selecting POIs on the MapLibre overlay and sync selection back to the graph selection model; add a neutral selection marker for dataset POIs; verify selection mapping with bounded verification (typecheck + focused CI filters) without MapLibre runtime. | `gympgrph/src/features/geospatial/GeospatialOverlay.tsx`, `gympgrph/src/features/geospatial/geospatialPoiSelection.ts` | `pickPoiSelection`, `selectNode`, `selectEdge`, `setSelectionSource` | Rendered map features + dataset registry + selection state | Map POI selection that preserves graph-first affordances | When map interaction is active, click queries rendered features from overlay layers; graph POIs select corresponding graph nodes; dataset POIs show a temporary marker + toast; selection mapping is verified without requiring a live basemap. | 2026-01-24 |
| Geospatial Mode  | Interaction + persistence hardening | Ensure interactive map gestures work reliably in host runtimes and avoid cross-app collisions by bundling required MapLibre CSS from gympgrph and using namespaced Geospatial Mode LocalStorage keys. | `gympgrph/src/index.ts`, `gympgrph/src/lib/config.ts`, `gympgrph/src/hooks/store/geospatialSlice.ts`, `gympgrph/src/features/geospatial/GeospatialPanel.tsx`, `knowgrph/canvas/tailwind.config.js`, `gympgrph/src/features/geospatial/GeospatialOverlay.tsx` | - | Host + user enables Geospatial Mode | Reliable drag/pan/zoom + isolated persisted settings | Import `maplibre-gl/dist/maplibre-gl.css` once in the gympgrph public entrypoint; use LS keys `kg:ui:geospatial:*` only; remove generic legacy key fallbacks to prevent cross-app collisions; ensure the host Tailwind build scans `gympgrph/src` so overlay stacking/pointer-capture classes are generated; harden the overlay root with inline z-index + pointer-events so interactions don’t silently break on stale CSS. | 2026-01-24 |
| Geospatial Mode  | Dataset URL normalization + bounded fetch compatibility | Support multi-dataset GeoJSON/records loading via http(s) URLs and same-origin absolute paths; normalize GitHub blob-like URLs; proxy cross-origin fetches via `/__fetch_remote` on localhost only; remove unused legacy geo→graph converters to avoid drift. | `gympgrph/src/lib/url.ts`, `gympgrph/src/lib/net/fetchRemoteText.ts`, `gympgrph/src/lib/graph/geo/*`, `knowgrph/canvas/src/__tests__/gympgrphUrlInterop.test.ts` | `applyMediaProxySrc`, `coerceFetchUrl`, `fetchRemoteTextDetailed` | Dataset/style URLs (http(s) or `/path`) | Deterministic, bounded dataset/style fetch with dev proxy support | Coerce and validate fetch targets; proxy only on localhost; preserve same-origin paths; normalize GitHub blob URLs to raw; delete unreferenced converters to keep single source of truth for geo parsing/rendering. Verified by filtered CI cases (`geospatial.gympgrphUrl.*`) and typecheck. | 2026-01-24 |
| Geospatial Mode  | MapLibre popups + hover + clustering | Support hover/click feedback and large point-layer usability using MapLibre feature-state + popups and optional GeoJSON source clustering; keep behavior config-driven and dataset-agnostic; align 3D auto-fit to selection bounds when selection exists. | `gympgrph/src/features/geospatial/GeospatialOverlay.tsx`, `gympgrph/src/features/geospatial/geospatialOverlayUtils.ts`, `gympgrph/src/features/geospatial/geospatialPoiSelection.ts`, `gympgrph/src/hooks/store/geospatialSlice.ts`, `gympgrph/src/features/geospatial/GeospatialPanel.tsx`, `knowgrph/canvas/src/__tests__/gympgrphMapLibreBehaviors.test.ts` | `ensureDatasetLayer`, `pickPoiSelection`, `coerceFeatureCollectionIds` | Map features + dataset layers + selection state | Popups on hover/click, cluster zoom expansion, and selection-aligned 3D map fit | Skip cluster features for POI picking; assign feature ids when missing; cluster point-only datasets above a bounded threshold; on cluster click zoom to expansion; on hover set feature-state hover; in 3D prefer selection bounds when selection contains geo nodes. Verified with bounded filtered CI cases (`geospatial.gympgrphMapLibre.*`) and typecheck. | 2026-01-24 |


## 2026-01-23

| Context          | Intent                     | Directive                                                                                   | Module                     | Function/Method | Input                                          | Output                      | Decision Logic                                    | Updated Date |
|------------------|----------------------------|---------------------------------------------------------------------------------------------|----------------------------|-----------------|------------------------------------------------|-----------------------------|---------------------------------------------------|--------------|
| Geospatial Mode  | Archived legacy notes      | Rows below are historical (pre-extraction) and may reference old Knowgrph geospatial module paths; current Geospatial Mode implementation lives in `gympgrph/src/` and Knowgrph remains host-only. | `gympgrph/src/*` | - | - | - | Treat entries as archival; do not reintroduce duplicate geospatial modules into Knowgrph. | 2026-01-23 |
| Geospatial Mode  | Pipeline Verification      | Verify geospatial integration with filtered CI tests; ensure no legacy code overlap; clean up test harness registration; verify Airports/Countries/Cities data loading without external deps in CI | `tests/run.ts`, `geospatialIntegration.test.ts`, `geospatialOverlayUtils.ts`, `geospatialIngest.test.ts` | `runAllTests`, `testGeospatialIntegration*`, `loadDatasetFeatureCollection` | `npm run test:ci geospatial` | 100% pass (9/9 tests) | Refactored `geospatialIntegration.test.ts` to match custom runner (removed `vitest` dependency); injected mock fetcher into `loadDatasetFeatureCollection` for deterministic testing; verified legacy `geospatialIngest.test.ts` remains valid | 2026-01-23 |
| Geospatial Mode  | Map overlay visibility + multi-dataset layers | Overlay a non-interactive MapLibre basemap and dataset layers on top of the canvas; support 2D map and 3D globe-style projection when supported; support multi-dataset URL layers (GeoJSON + record→point) without hardcoded datasets; bound all fetch/parse work (timeout/bytes); preserve graph-first affordances; verify with bounded, filtered tests. | `Canvas.tsx`, `GeospatialOverlay.tsx`, `geospatialOverlayUtils.ts`, `GeospatialPanel.tsx`, `geospatialOverlaySlice.ts`, `geospatialDatasetsSlice.ts`, `lib/geospatial/config.ts`, `lib/geospatial/geojson.ts`, `lib/url.ts`, `lib/net/fetchRemoteText.ts`, `tests/run.ts` | `setGeospatialOverlayEnabled`, `parseGeospatialDatasetsFromEnv`, `recordsToPointFeatureCollection`, `requestGeospatialFitToData` | Map style URL + datasets (URL + format), GraphData nodes with `properties.geo.{lat,lng}` | Visible basemap overlay + dataset layers + bounded fit-to-data | Default OFF. Toggle ON renders a translucent overlay (opacity clamped to [0,1], default 0.65). Dataset parsing supports GeoJSON and generic record datasets as arrays or object maps (key → record). In 3D render mode, overlay selects globe-style projection when supported and auto-fits to active geo bounds to avoid “blank” defaults. | 2026-01-23 |
| Geospatial Mode  | Fix non-visibility + abort noise | Prevent Geospatial Mode from appearing blank in 2D/3D by avoiding MapLibre re-creation on data changes, deferring map init in dev StrictMode to prevent abort noise, restoring safe opacity when enabling/hydrating, applying a vector-preferred style on boot, proxying cross-origin map assets, and surfacing non-silent status/errors. | `GeospatialOverlay.tsx`, `geospatialOverlaySlice.ts`, `geospatialDatasetsSlice.ts`, `geospatialOverlayUtils.ts`, `components/ui/ToastHost.tsx`, `hooks/store/uiToastSlice.ts`, `lib/url.ts`, `lib/geospatial/styleUrl.ts`, `lib/geospatial/config.ts`, `docs/documents/knowgrph-geospatial-mode-document.md` | `GeospatialOverlay`, `setGeospatialOverlayEnabled`, `applyPreferredStyle`, `applyMediaProxySrc`, `upsertUiToast`, `normalizeGeospatialStyleUrl`, `coerceGeospatialOverlayOpacity` | Style URL, persisted opacity, datasets | Stable visible overlay + actionable status/error feedback | Map is created once per enable-cycle; StrictMode probe-mount cancels timer instead of aborting network; enabling/hydration restore opacity when it would be 0; initial boot applies a fetched style object with raster sources removed when possible (but never stripping everything); cross-origin tiles/sprites/glyphs go through `/__fetch_remote` to avoid CORS blank-map behavior; geospatial loading/opacity/error status is surfaced via a native toast (top-right, below toolbar) that stays visible above the SidePanel; bounded basemap-load timeout reports the last request URL when available. Verified via `npm run test:ci -- geospatial` and `npm run typecheck`. | 2026-01-23 |
| Geospatial Mode  | Map overlay + geo ingest    | Implement a non-interactive MapLibre basemap overlay on top of the 2D canvas with Turf-bounded fit-to-data; support multi-dataset URL layers (GeoJSON + record→point); derive `node.properties.geo.{lat,lng}` during ingest for GeoJSON and record datasets; forbid hardcoded datasets/providers and unbounded fetch/parse loops; verify with bounded typecheck + filtered geospatial tests. | `Canvas.tsx`, `GeospatialOverlay.tsx`, `GeospatialPanel.tsx`, `useGraphStore.ts`, `geospatialOverlaySlice.ts`, `geospatialDatasetsSlice.ts`, `config.ls.ts`, `uiCopy.ts`, `main.tsx`, `lib/net/fetchRemoteText.ts`, `features/toolbar/ingestUtils.ts`, `lib/graph/io/adapter.ts`, `lib/graph/geo/geojsonToGraphData.ts`, `lib/graph/geo/arrayRecordsToGraph.ts`, `lib/graph/geo/recordHeuristics.ts`, `lib/geospatial/geojson.ts`, `lib/graph/jsonld/utils.ts`, `tests/run.ts`, `geospatialIngest.test.ts`, `uiMapTabUiOnly.test.tsx`, `package.json` | `parseGraph`, `geojsonToGraphData`, `arrayRecordsToGraphData`, `deriveGeoFromRecord`, `fetchRemoteText`, `setGeospatialOverlayEnabled`, `addGeospatialDatasetUrl`, `requestGeospatialFitToData` | GeoJSON (`FeatureCollection|Feature|Geometry`), record-style JSON with coordinate fields, dataset URL layers, graph nodes with `properties.geo` | Basemap overlay + dataset layers; derived geo fields; Map tab config UI; bounded fit-to-data | Default OFF. Toggle ON opens Map tab and renders a translucent overlay (non-interactive). Basemap style URL + opacity + datasets persist to local storage. | 2026-01-23 |
| Geospatial Mode  | Map view integration        | Integrate MapLibre GL + configurable vector style URL (runtime override) + Turf proximity + translucent Map Layer overlay on main canvas; embed a non-interactive MapLibre basemap preview inside the minimap card and keep it synced to the shared proximity center/radius; forbid non-http(s) style URLs, credentialed URLs, relative proxy URLs in worker contexts, hardcoded providers/datasets, and unbounded map/query loops | `Canvas.tsx`, `useToolbarActions.ts`, `geospatialOverlaySlice.ts`, `constants.ts`, `openFreeMap.ts`, `mapLibreMap.ts`, `MapOverlayLayer.tsx`, `geojsonCoerce.ts`, `geospatialOpenFreeMap.test.ts`, `geospatialOverlayDefaults.test.ts`, `geospatialOverlayDatasetLoader.test.ts`, `geospatialMapProxyUrl.test.ts`, `features/minimap/Minimap.tsx`, `features/geospatial/zoomUtils.ts`, `tests/run.ts`, `SettingsView.tsx`, `registry-ui.graph-and-orchestrator.ts`, `SettingsFallbackDetails.ts`, `knowgrph-geospatial-mode-document.md`, `refactor-pipeline-cleanup.md` | `handleToggleGeospatialMode`, `normalizeGeospatialMapStyleUrl`, `buildMapRequestProxyUrl`, `createGeospatialOverlaySlice`, `initMapLibreMap`, `coerceGeoJsonFeatureCollection`, `computeZoomFromRadiusKm` | `LS_KEYS.geospatialMapStyleUrl` and `VITE_GEOSPATIAL_MAP_STYLE_URL` (fallback); GraphData nodes with `properties.geo.{lat,lng}`; GeoJSON `FeatureCollection|Feature|Geometry`; record-style JSON with coordinate fields | Map tab + basemap overlay + minimap basemap preview + 2D map / 3D globe projection (when supported) | Start OFF on load. Toggle ON opens Map tab, enables a translucent overlay (non-interactive by default) and clamps opacity to a non-zero default (current default 0.45). Projection follows 3D Mode (2D map vs globe-style projection when supported). Style URL can be set in Map panel and persists to local storage; minimap and overlay both reuse the same style+proxy MapLibre basemap and apply the shared proximity center/radius (radius→zoom mapping) wh[... 34 chars omitted ...]
| Geospatial Mode  | Basemap defaults            | Default basemap style uses the OpenFreeMap Liberty style URL while remaining configurable via the Map panel style URL input; persist style URL and overlay opacity to local storage; forbid hardcoded dataset dependencies. | `docs/documents/knowgrph-geospatial-mode-document.md`, `config.ls.ts`, `geospatialDatasetsSlice.ts` | - | - | Updated docs/schema for basemap default behavior | Style URL and opacity load from local storage with safe defaults and [0,1] clamping | 2026-01-23 |

## 2026-01-22

| Context          | Intent                     | Directive                                                                                   | Module                     | Function/Method | Input                                          | Output                      | Decision Logic                                    | Updated Date |
|------------------|----------------------------|---------------------------------------------------------------------------------------------|----------------------------|-----------------|------------------------------------------------|-----------------------------|---------------------------------------------------|--------------|
| Ingestor/Parser  | GraphRAG text-to-graph demo | Implement sequential, inspectable text→entities→triples→GraphData pipeline with FOSS attribution; forbid test-only demo components and hardcoded storage keys | `graphragTextPipeline.ts`, `default.ts`, `GraphRagTextPipelineSection.tsx`, `panelLayoutUiSlice.ts`, `jsonImportAction.ts` | `runGraphRagTextPipeline`, `graphrag-text.parse`, `GraphRagTextPipelineSection`, `setBottomPanelCollapsed`, `performJsonImport` | Plain text (`.txt` / freeform), parser selection, UI state | Canonical `GraphData` + stage outputs + metrics | Heuristic pipeline emits `metadata.graphragTextPipeline.stages[]` (SVO stages + latency/output size) and builds nodes from entities + edges from extracted triples; persist bottom-panel collapse via `LS_KEYS.bottomPanelCollapsed` | 2026-01-22 |
| Ingestor/Parser  | Demo fidelity + import routing | Ensure markdown import routes plain-text `.md` to GraphRAG pipeline (no hardcoded paths); add bounded demo-fidelity tests; remove unused legacy import/export helpers | `graphragTextPipeline.ts`, `default.ts`, `graphragTextPipeline.test.ts`, `runParserTests.ts`, `file.ts` | `isLikelyPlainTextMarkdown`, `bestMatch`, `extractTriplesHeuristic`, `hfToySubwordsFromText` | Plain-text `.md` + `.txt` inputs; demo fixture markdown; export actions | Deterministic demo graph + stable stage metadata; fewer duplicate paths | Markdown parser match short-circuits for plain-text markdown; GraphRAG parser accepts plain markdown and resolves pronoun subjects; tests load fixture by relative path; remove unused `loadGraphFile` and unused CSV exporters | 2026-01-22 |
| Ingestor/Parser  | Rich semantics + summarization | Add bounded extractive summarization and sentence-level evidence to GraphRAG pipeline; forbid date-as-location false positives and unbounded scoring loops | `extractiveSummarization.ts`, `graphragTextPipeline.ts`, `textAnalysis.ts`, `graphragTextPipeline.test.ts`, `aieBookGraphRagTextPipeline.test.ts` | `buildExtractiveSummary`, `extractTriplesHeuristic`, `runGraphRagTextPipeline` | Plain text (`.txt` / plain `.md`) | Stage `extractiveSummarize` output + edges with evidence spans | Extractive summary selects top sentences by token+entity scoring; triple extractor emits `evidenceText` + `sentenceIndex/start/end`; skip `located-in` for date-like objects; handle pronoun `is-a/located-in` | 2026-01-22 |
| Cleanup          | Remove demo coupling         | Remove production imports from `src/__tests__` and delete legacy demo wrappers; forbid test-only demo components and hardcoded demo paths | `ToolbarMenuLauncher.tsx`, `tsconfig.json`, `src/__tests__/fixtures/*` | - | - | Cleaner import→render pipeline + neutral fixtures | Demo-only runner removed; fixtures moved to `__tests__/fixtures`; graphrag tests use neutral fixture instead of demo markdown path | 2026-01-22 |
| Semantics        | Context-Aware Analytics    | Enhance GraphRAG entity/node analytics with HITS (Hubs/Authorities), Centrality (Betweenness/Closeness), and PageRank; update schema and docs to reflect new metrics | `graphragTextAnalytics.ts`, `graphAlgorithms.ts`, `graphMetrics.ts`, `graphrag-pipeline.jsonld`, `node-schema.jsonld`, `knowgrph-graphrag-pipeline-in-action-prd-tad.md` | `applyGraphRagTextAnalytics`, `computeHITS`, `computeClosenessCentrality` | Graph nodes/edges | Enhanced node properties with centrality scores | Compute iterative HITS and path-based Centrality metrics during pipeline execution; normalized scores [0,1]; updated schema definitions | 2026-01-22 |
| UI/Stats         | Analytics configurability   | Allow users to toggle Context-Aware Analytics (PageRank/HITS/Betweenness/Closeness) from BottomPanel Parser/Stats and apply config during parsing | `GraphRagCentralityStatsSection.tsx`, `GraphRagTextPipelineSection.tsx`, `default.ts`, `config.ls.ts`, `graphragTextConfig.ts` | `useGraphRagTextCentralityConfig`, `graphrag-text.parse`, `runGraphRagTextPipeline` | GraphRAG text documents | Configurable analytics + updated GraphData | Persist config in local storage and attach effective config under `metadata.graphragTextPipeline.config`; recompute pipeline graph when settings change | 2026-01-22 |
| Renderer/Graph Layers | Native expand/collapse for clusters | Implement native expand/collapse for Mermaid subgraphs, markdown heading layers, keyword layers, and semantic communities; forbid ReactFlow copy patterns | `GraphCanvas.tsx`, `viewDerivation.ts`, `graphViewSlice.ts`, `groups.ts`, `nodes.ts` | `deriveGraphDataWithGroupCollapse`, `toggleGroupCollapsed`, `createGroupsLayer` | Group label and group node interactions | Derived group nodes + aggregated edges; stable interactions | Chevron click toggles collapse on group labels (expanded groups) and on collapsed group nodes; collapse replaces member nodes with a derived group node (`kg:groupId`, `kg:groupMemberCount`) and aggregates cross edges (`kg:edgeCount`); Alt+double-click keeps expand-select | 2026-01-22 |
| Renderer/Camera | Pin to View stability | Prevent pinned view jump when collapsing/expanding groups, toggling panels, or switching presentation modes; reuse existing zoom state and keep world center stable | `GraphCanvas.tsx`, `zoomState.ts`, `canvasSlice.ts` | `adjustPinnedZoomForViewportChange`, `pickInitialZoomTransform`, `setZoomState` | Pin button + chevron + view-only/presentation toggles | Stable pinned transform across viewport changes | Persist viewport dims with zoomState and adjust x/y when pinned so the same world center stays at the new viewport center; reuse zoomState across presentation rebuilds when graph revision matches | 2026-01-22 |
| Renderer/Rich Media | Rich Media visibility | Fix Rich Media ON visibility for relative media URLs and GitHub blob URLs produced by ingest; reuse shared URL sanitizer and keep media rendering view-only | `url.ts`, `helpers.ts`, `nodes.ts`, `NodeMesh.tsx` | `coerceMediaUrl`, `normalizeGitHubBlobLikeUrl`, `applyMediaProxySrc`, `getNodeMediaSpec` | Markdown ingest with local/relative asset links | Media overlays render reliably in 2D/3D | Accept safe relative URLs (reject explicit schemes) and normalize GitHub blob links to raw before proxying to avoid broken media placeholders | 2026-01-22 |
| Renderer/Rich Media | Layer-aligned opacity + bounded worker parse | Align Rich Media panel background and opacity across 2D/3D; ensure worker parsing always resolves; remove duplicate URL filename derivation; forbid hardcoded external example URLs | `highlight.ts`, `nodes.ts`, `NodeMesh.tsx`, `parseWorker.ts`, `jsonImportAction.ts`, `mediaProxySrc.test.ts` | `computeNodeVisual`, `createNodesLayer`, `NodeMesh`, `parseGraphInWorker`, `performJsonImport` | Rich Media toggle + `mediaNodeOpacity` + `visual:layer` | Consistent Rich Media rendering + no indefinite worker hangs | Media opacity applies only when Rich Media is enabled and composes with `visual:layer` opacity; media panels tint using node base fill; worker parse has hard timeout and terminates cleanly; URL import uses shared `deriveFilenameFromUrl` | 2026-01-22 |
| Renderer/Nodes | Native node shapes (diamond/hex) | Support node shapes beyond circle/rect via schema + toolbar; forbid ReactFlow copy patterns | `nodeSizing2d.ts`, `nodes.ts`, `sceneHandlers.ts`, `Toolbar.tsx`, `useToolbarActions.ts`, `utils.ts`, `nodeShapes2d.test.ts` | `getNodeRenderShape2d`, `createNodesLayer`, `attachSimulationTick`, `handleToggleNodeShapeMode`, `isNodePointerTarget` | Schema `nodeShapes` + `behavior.nodeShapeMode` + `visual:shape` | Correct 2D shape rendering + bounded hit-testing | Render `diamond`/`hex` as SVG paths positioned by tick; toolbar cycles `circle→rect→diamond→hex`; pointer hit-testing is SSR-safe by guarding missing DOM globals; tests validate rendered paths and pointer target acceptance | 2026-01-22 |
| Renderer/Labels | Label SSOT + node-shape sizing | Reuse shared label wrapping/ellipsis logic across render/collision/fit; forbid drift | `labelLayout2d.ts`, `labels.ts`, `overlap.ts`, `fit.ts` | `computeVisibleLabelLines2d`, `estimateNodeLabelAabbHalfExtents2d` | Node labels + schema label styles | Center-aligned labels + bounded ellipsis | Non-circle labels are centered (`dx=0`, `anchor=middle`) and wrapped within node bounds; collision and fit bounds use the same visible lines/ellipsis logic to prevent spacing drift | 2026-01-23 |
| Renderer/Graph | Break import cycle | Extract adjacency map utilities to a leaf module; forbid renderer↔simulation cycles | `adjacency.ts`, `simulation.ts`, `highlight.ts`, `selectionZoom.ts`, `zoomController.ts` | `getAdjacencyMap`, `buildAdjacencyMap` | GraphData nodes/edges | Cleaner boundaries + safer imports | Centralize adjacency building/caching in `adjacency.ts`; remove duplicate adjacency exports from `simulation.ts`; update dependents to import from the leaf module | 2026-01-23 |
| Parser | Restore base parser matchers | Ensure JSON/CSV/JSON-LD base parsers match by extension; forbid “dead” parser specs | `default.ts` | `jsonSpec.match`, `csvSpec.match`, `jsonLdSpec.match` | File name + URL name | Predictable parser selection | Match by file extension for both local names and URLs; `parse` delegates to the auto parser so behavior stays SSOT | 2026-01-23 |

## 2026-01-21

| Context          | Intent                     | Directive                                                                                   | Module                     | Function/Method | Input                                          | Output                      | Decision Logic                                    | Updated Date |
|------------------|----------------------------|---------------------------------------------------------------------------------------------|----------------------------|-----------------|------------------------------------------------|-----------------------------|---------------------------------------------------|--------------|
| Typography       | Font/Text/Icon Alignment   | Enforce consistent baseline and anchor alignment; forbid per-component offsets and overlap  | `labels.ts`, `index.css`, `GraphDataTableUiPrimitives.tsx` | `createLabelsLayer`, `.lucide`, `FilterCombobox` | SVG labels, UI controls | Visually aligned UI + canvas labels | Base anchor by dx sign + `dominant-baseline: middle` + global Lucide baseline | 2026-01-21 |
| Performance      | End-to-end pipeline speed  | Preserve inactive 2D/3D canvases; cache tick geometry; gate expensive forces; cache ignore matches; forbid unnecessary recompute | `Canvas.tsx`, `GraphCanvas.tsx`, `sceneHandlers.ts`, `highlight.ts`, `simulation.ts`, `semantic_processor.py`, `codebase_index_config.py` | `GraphCanvas` scene lifecycle, `attachSimulationTick`, `applySelectionHighlight`, `buildSimulation`, `process_semantics`, `build_ignore_matcher` | graph nodes/edges, schema, markdown text, ignore patterns | Stable UI + bounded CPU and memory | Keep renderers mounted; cache per-node metrics by schema ref; run heavy forces only at high alpha; cap semantic pair growth; cache ignore decisions | 2026-01-21 |
| Semantics        | AgenticRAG context alignment + pipeline map | Materialize canonical AgenticRAG context; normalize `kg:` labels/types; publish loadable import→parse→derive→layout→render pipeline map; extend schema context terms; forbid semantic drift | `parse.ts`, `utils.ts`, `context.jsonld`, `knowgrph-pipeline-deep-dive-document.md`, pipeline map JSON‑LD | `parseJsonLd`, `AGENTIC_RAG_MINIMAL_CONTEXT` | JSON‑LD graphs (`@context`, `@graph`) | Canonical `GraphData` + correct context match diagnostics | If context IRI matches: inject `@vocab` + minimal `@type:@id` terms; strip `kg:` prefixes for readability | 2026-01-21 |
| Ingestor/Parser  | URL name + JSON-LD edge alignment | Canonicalize imported document names for URL-based HTML/Markdown/PDF; gate JSON-LD inferred edges via explicit allow-list; forbid docPath drift and surprise edges | `markdownImportAction.ts`, `htmlImportAction.ts`, `pdfImportAction.ts`, `parse.ts`, `jsonldInferredEdges.test.ts`, pipeline map JSON‑LD | `perform*Import`, `parseJsonLd` | URL/local sources; JSON‑LD context + metadata | Stable markdown viewer/presentation selection + predictable edges | Use derived `.md` name for URL docs; persist `markdownDocumentSourceUrl`; infer edges only when `@context` has `@type:@id` or `metadata.jsonLdMapping.contextEdgeProperties` includes key | 2026-01-21 |
| Semantics/UI     | Document Structure ↔ Keyword mode | Add mode toggle; derive stopword-filtered keyword graph from document text; infer Verb/Predicate/Relationship edges from between-mention text; compute PageRank for keyword entity importance; sync Keyword Graph across Canvas + tables + panels; size nodes by keyword frequency and thickness edges by strength; preserve media overlays; align X/Y/Z layering via community→layer mapping; align palette; make keyword sizing/width user-tunable; forbid stopword noise and selection-only dashboards | `Toolbar.tsx`, `GraphCanvas.tsx`, `useActiveGraphData.ts`, `keywordGraph.ts`, `graphAlgorithms.ts`, `keywordStopwords.ts`, `positioning.ts`, `graphGroups.ts`, `helpers.ts`, `BottomPanel.tsx`, `BottomPanelStatsTab.tsx`, `KeywordEntitiesSection.tsx`, `CommunitiesStatsSection.tsx`, `EdgesStatsSection.tsx`, `GraphDataTableTable.tsx`, `GraphDataTableRows.tsx`, `three/layout.ts`, `three/NodeMesh.tsx` | `useActiveGraphData`, `deriveKeywordGraphFromText`, `mergeKeywordGraphWithMediaNodes`, `determineLayoutPositions`, `deriveGraphGroups`, `getRenderNodeRadius2d`, `getEdgeStrokeWidth` | graphData, markdown text, semantic mode | Derived Keyword GraphData + synced UI surfaces | Cache derived graphs by text hash (LRU); cache positions by `{semantic}:{frontmatter}:{layout}:{render}`; use NLTK English stopwords to filter entity and predicate tokens; infer directionality by mention order (subject→object); set `visual:nodeSize` from mention count, `keyword:pagerank` for centrality, and `visual:importance` from count+centrality; set `visual:fill`/`fill` from shared palette; set `visual:width` from edge strength; set `keyword:kind` for keyword nodes/edges; map `visual:community`→`visual:layer`; merge media-capable base nodes so the media overlay toggle remains effective | 2026-01-22 |
| Semantics/Parser | Community detection cleanup | Remove Louvain terminology and legacy config fields; compute communities using NetworkX only; keep community ids deterministic and dataset-agnostic | `semantic_processor.py`, `schema_config.py`, schema-config JSON‑LD, docs + workflow-preview JSON‑LD | `_run_networkx_connected_components`, `build_schema_config_jsonld` | semantic edges (`coOccursWith`), schema layers config | `communityId` / `visual:community` for layers/subgraphs | Build undirected graph from `coOccursWith`; run NetworkX connected components; assign stable ids by sorted community min-node; expose config as `{enabled, algorithm, weightProperty}` | 2026-01-21 15:30 |
| Semantics/Parser | NetworkX PageRank centrality | Prefer NetworkX PageRank for corpus centrality; add semanticRole (subject/object/entity) counts from semanticRelation direction; forbid unstable ids and unbounded iterations | `semantic_processor.py`, `stopwords.py` | `process_semantics`, `stopwords_en` | semantic edges (`semanticRelation`, `coOccursWith`), entity nodes, sem_defaults | entity properties: centrality, semanticRole, communityId | PageRank via NetworkX over weighted undirected graph; community via NetworkX connected components over `coOccursWith`; role from directed semanticRelation counts; NLTK stopwords with hardcoded fallback | 2026-01-21 16:05 |
| Palette          | Lean Startup color sync      | Synchronize Lean Startup palette across UI + schema-config + preview fixtures; forbid hardcoded legacy hex colors | `schema.ts`, schema-config JSON‑LD, `MarkdownTableOfContents.tsx`, workflow preview JSON‑LD | `MVP_COLOR_PALETTE`, TOC drag indicators | UI components, schema configs, docs fixtures | Consistent colors everywhere | Replace legacy Tailwind hex codes with palette hex codes; replace inline color constants with UI_THEME_TOKENS; keep docs-derived preview JSON‑LD aligned with source docs | 2026-01-21 16:20 |
| Layout/Renderer  | 2D/3D toggle stability       | Prevent layout drift when switching render mode; separate caches by renderMode; forbid hidden simulations running | `GraphCanvas.tsx`, `positioning.ts`, schema docs | `determineLayoutPositions` | nodes, schema, canvasRenderMode | Stable layout across toggles | Stop 2D simulation on deactivate and persist layout; key layout caches by semantic/frontmatter/layout/render; restore cached positions when returning to 2D | 2026-01-21 16:30 |
| UI Copy          | Geo → Polygon naming         | Rename Geo group-shape label to Polygon; centralize copy; forbid inline strings | `Toolbar.tsx`, `uiMeta.ts`, `uiCopy.ts` | - | toolbar button labels/tooltips | Consistent terminology | Display Polygon for geo group outlines while preserving schema value `geo`; use UI_LABELS/UI_COPY for titles and tooltips | 2026-01-21 16:30 |
| Ingestor/Parser  | YouTube Import | Fix non-functioning YouTube transcript/captions import across environments; support youtube-transcript-api API variants; return Markdown + transcript JSON; open Markdown Editor/Preview/Slides by default; allow JSON Editor and UI Editor to inspect full transcript metadata + segments; parse requested `t=` start time; forbid external copy-paste | `youtube_cmd.py`, `vite.config.ts`, `youtubeImportAction.ts`, `uiCopy.ts`, docs | `performYouTubeImport`, `/__youtube_transcript`, `knowgrph_parser youtube` | YouTube URL/ID | Markdown doc + transcript JSON source | Normalize user-provided URL/ID; compute `requested_start_s`; emit transcript payload with `title`, `video_id`, `source_url`, requested/selected language fields, `is_generated`, `is_translatable`, `translation_languages`, `oembed`, `start_s/end_s/duration_s`, `segment_count`, `generated_at_ms`, and `{text,start,duration}` `segments`; store `jsonSourceDocumentText` and `markdownDocumentText`; open curation markdown view | 2026-01-22 00:00 |
| Terminology      | Cluster SSOT disambiguation  | Treat Cluster as SSOT across graph layers/subgraphs/communities/groups; rename UI to “Clusters” and “Cluster Shape”; forbid conflicting terminology | UI copy + docs + AgenticRAG schema | - | user-facing labels/tooltips/docs | Reduced ambiguity | Keep internal schema keys (graphLayers/community) stable but present “Cluster/Cluster layer” in UI and docs; map Mermaid subgraph + semantic community to cluster concept | 2026-01-21 16:45 |



## 2026-01-20

| Context          | Intent                     | Directive                                                                                   | Module                     | Function/Method | Input                                          | Output                      | Decision Logic                                    | Updated Date |
|------------------|----------------------------|---------------------------------------------------------------------------------------------|----------------------------|-----------------|------------------------------------------------|-----------------------------|---------------------------------------------------|--------------|
| Graph Layout     | Layered Layout (Native)    | Implement native layered layout; enforce 16:9 spread + centering for subgraphs               | `simulation.ts`, `mermaidSeed.ts`, `disjoint.ts` | `buildSimulation` | nodes, edges, dims, schema | Stable positions | Seed + anchor targets + cache per mode | 2026-01-20 21:00 |
| Port Handles     | Input/Output Sides         | Restrict port handles: Input=Left/Top, Output=Right/Bottom based on graph topology | `portHandles.ts` | `listPortHandlesForNodes` | nodes, edges | Filtered ports | In-degree=0 -> Input; Out-degree=0 -> Output | 2026-01-20 21:00 |
| Parsing          | Worker Fallback            | Prefer worker parsing when no parser matches; fallback to sync parse                         | `loader.ts`, `parseWorker.ts` | `loadGraphDataFromTextViaParser` | name, text | GraphData | Worker-first on unmatched parser id | 2026-01-20 22:50 |
| Cleanup          | Remove Duplicate Wrapper    | Remove redundant parse wrapper; standardize on `parseGraph`                                 | `roundtrip.test.ts`, `jsonldRoundtrip.test.ts` | - | - | - | Use `parseGraph(...).data` directly | 2026-01-20 22:50 |
| Labels           | Toggle-safe Visibility      | Keep node labels visible when toggling node shape/port handles/group shape                   | `labels.ts` | `createLabelsLayer` | node label lines | Anchored labels | No per-line x override that breaks tick positioning | 2026-01-20 23:10 |
| Layout           | Radial Toggle Stability     | Prevent radial layout from inheriting other mode positions; recompute on entry unless cached | `positioning.ts` | `determineLayoutPositions` | nodes, cache | Correct radial | Skip initial layout only for cached radial          | 2026-01-20 23:10 |
| Edge Rendering   | Stable Z-Level             | Keep edge arrows always visible without blocking node clicks                                 | `links.ts`, `scene.ts`, `sceneHandlers.ts` | `createLinksHitLayer`, `createLinksLayer` | edges, schema | Two-layer edges | Hit targets below nodes; visuals above nodes | 2026-01-20 22:10 |
| Graph Layers     | Label Interactions          | Enable group label select/expand-select/drag without rebuilding simulation                   | `groups.ts`, `selectionSlice.ts`, `useGroupSelectionHighlight.ts` | `createGroupsLayer` | group id + members | Group selection | Click timer disambiguation + bulk member shift | 2026-01-20 22:10 |
| Typography       | Shared Markdown Sizing      | Reuse markdown heading/body typography logic for canvas labels                               | `markdownTypography.ts`, `MarkdownHeadingBlock.tsx`, `groups.ts`, `schema.ts` | - | heading depth | Consistent sizing | Shared mapping for hn/body sizes | 2026-01-20 22:10 |
| Graph Shapes     | Geo Outline Ring (Native)  | Render geo group outlines via geometry ring computed from member extents                      | `groups.ts`, `convexRing.ts` | `computeConvexRing` | points | Ring points | Monotone Chain convex hull (native, no-copy); tight fit around members | 2026-01-20 23:00 |
| Renderer         | Fix Toggle Visibility      | Ensure groups layer updates on node shape/port toggle to prevent stale tick crashes          | `GraphCanvas.tsx`          | `schemaGroupsPresentationJson` | schema behavior | Updated dependency key | Include nodeShapeMode/portHandles in dependency | 2026-01-20 23:00 |
| Renderer         | Decouple Media Sizes       | Size Rich Media panels independently of rect node sizing; forbid minimap-coupled sizing rules | `nodes.ts`                 | `MEDIA_PANEL_BODY_MINIMAP_MULTIPLIER_DEFAULT` | media panel density | Stable media sizing | Panel sizing uses media-specific constants; rect node sizing remains schema-driven | 2026-01-20 23:30 |
| Cleanup          | Prevent Text Selection     | Forbid accidental text selection on nodes, edges, and labels                                 | `nodes.ts`, `labels.ts`, `edgeLabels.ts` | - | - | - | `user-select: none` | CSS style application | 2026-01-20 22:30 |
| Labels           | Anti Fly-away Anchoring    | Prevent viewport clamping from pushing labels far from their nodes                            | `sceneHandlers.ts` | `attachSimulationTick` | labels + zoom | Stable offsets | Bounded shifts + near-viewport gating | 2026-01-20 23:50 |
| Labels           | Ports-aware Edge Labels     | Place edge labels using the same endpoints as rendered edges (port handles supported)         | `sceneHandlers.ts` | `attachSimulationTick` | edges + schema | Aligned labels | Midpoint from endpoints + normal offset | 2026-01-20 23:50 |
| Labels           | Group Label Alignment       | Anchor group labels inside group containers with per-group font sizing                        | `groups.ts` | `createGroupsLayer` | groups | Aligned labels | Per-group font size + hanging baseline | 2026-01-20 23:50 |
| Labels           | LOD Hide Group/Edge Labels  | Reduce clutter by hiding group/edge labels below the same zoom threshold                       | `zoom.ts` | `createZoom` | schema lod | Less clutter | Toggle display on zoom | 2026-01-20 23:50 |
| Labels           | Ellipsis + Tooltip Labels   | Clamp long node/group labels in-canvas and expand via shared hover tooltip                     | `labels.ts`, `groups.ts`, `GraphHoverTooltip.tsx` | `createLabelsLayer`, `createGroupsLayer` | label text | Clean canvas | Ellipsis + tooltip for full text | 2026-01-21 00:15 |
| Documentation    | Sync Media Proxy Notes      | Document proxy usage for markdown backgrounds; forbid domain rewrites                        | `knowgrph-backend-document.md`, `knowgrph-markdown-processing-document.md` | - | - | Updated docs | Background URLs always go through applyMediaProxySrc and /__fetch_remote | 2026-01-20 20:10 |
| Renderer         | Toggle-Safe Presentation    | Forbid layout/simulation rebuild when switching node shape/ports/groups/media; preserve positions | `GraphCanvas.tsx`, `scene.ts`, `sceneHandlers.ts` | `updateGraphSceneNodesPresentation`, `updateGraphSceneGroupsPresentation`, `attachSimulationTick` | schema presentation toggles | In-place layer updates | Rebuild only on graph revision+dims+layout(mode/forces); update layers otherwise | 2026-01-20 19:35 |
| Dependencies     | Break Circular Imports      | Forbid circular import between helpers/utils/highlight; use direct leaf-module imports       | `helpers.ts`               | -               | -                                              | -                           | Replace barrel import with direct `simulation.ts` import | 2026-01-20 19:35 |
| Markdown Media   | Remove Deprecated Polyfill  | Remove deprecated Unsplash rewrite; proxy remote backgrounds via same-origin media endpoint | `markdownSlideVisuals.ts`, `url.ts` | `buildBackgroundStyle`, `applyMediaProxySrc` | background URL | Proxied background image | Treat all remote URLs uniformly via proxy; no hardcoded domains | 2026-01-20 19:35 |
| Build            | Unblock docs:update         | Add missing design document required by docs:update/build pre-step                           | `docs/documents/knowgrph-design-document.md` | - | - | - | - | Single entry-point doc to satisfy parser inputs   | 2026-01-20 18:45 |
| Graph Layers     | Rect/Geo Toggle            | Add toolbar toggle to switch group rendering between rect and geo shapes                    | `Toolbar.tsx`, `useToolbarActions.ts` | `handleToggleGroupShapeMode` | schema.layout.groups.shape                     | Updated schema              | Schema-driven mode                                | 2026-01-20 18:30 |
| Graph Layers     | 16:9 Disjoint Layout       | Spread disconnected components in 16:9 aspect ratio elliptical spiral                       | `disjoint.ts`                | `computeDisjointComponentTargets` | nodes, width, height                           | Target positions            | Elliptical spiral scaleX/scaleY                   | 2026-01-20 18:30 |
| Port Handles     | Render + Endpoint Routing  | Render port handles and route edge endpoints via nearest cardinal side; avoid extra ports   | `portHandles.ts`           | `getEdgeEndpointFromPorts` | nodes, schema                                  | Edge endpoints              | Nearest-side projection to node boundary          | 2026-01-20 16:10 |
| Graph Layers     | 16:9 Spread + No Overlap   | Spread top-level subgraphs across 16:9 space; avoid band-only layouts and overlap           | `mermaidSeed.ts`           | `applyMermaidSeedLayout` | nodes, edges, dims, schema                     | Seeded positions            | Subgraph grid targets + centroid recenter         | 2026-01-20 16:10 |
| Graph Layers     | Geo Outlines (Native)      | Add `layout.groups.shape="geo"` to render geometry-based group outlines without external libs | `groups.ts`, `convexRing.ts` | `createGroupsLayer`, `computeConvexRing` | group member extents | SVG path `d`            | Divide-and-conquer convex ring over member extents | 2026-01-20 18:05 |
| Layout Stability | Toggle-safe Caching        | Prevent mode/toggle switches from reseeding; cache positions per view+mode                  | `positioning.ts`, `scene.ts` | `determineLayoutPositions`, `storeLayoutPositions` | nodes, cache         | Stable positions         | Cache key = `{view}:{mode}` + skip initial seed when positioned | 2026-01-20 18:05 |
| Node Shape       | Toggle Circle/Rect         | Add toolbar toggle to switch node rendering between circle and rectangular shapes           | `Toolbar.tsx`, `nodeSizing2d.ts` | `handleToggleNodeShapeMode`, `getNodeRenderShape2d` | schema.behavior.nodeShapeMode | Updated scene render key | Schema-driven mode, single rebuild trigger        | 2026-01-20 17:25 |
| Markdown Layers  | Headings as Graph Layers   | Render markdown headings (Hn) as nested graph layers; avoid drawing heading nodes as nodes  | `markdownHeadingGroups.ts`, `scene.ts`, `groups.ts` | `deriveMarkdownHeadingGroups` | GraphData | Group rectangles | Structure edges → membership; depth by section nesting | 2026-01-20 17:25 |
| Frontmatter Mode | Strict Filtering           | Filter graph to show only Mermaid frontmatter nodes when enabled; forbid mixed content      | `layerDerivation.ts`         | `filterGraphToFrontmatterMermaid` | GraphData | Filtered GraphData | Check `isMermaidFrontmatter` OR `mermaidScope==='frontmatter'` | 2026-01-20 11:30 |
| Port Layout      | Topological Handles        | Pin inputs/outputs to border by Mermaid direction when enabled; forbid hardcoded axis        | `simulation.ts`              | `buildSimulation` | nodes, edges, schema                           | Simulation with forceX/Y    | In/out-degree → role; direction (LR/RL/TB/BT) → axis + side | 2026-01-20 12:20 |
| Mermaid Layout   | Balanced 16:9 Seeding      | Spread top-level subgraphs; center centroid; forbid clustered bands/overlaps                | `mermaidSeed.ts`             | `applyMermaidSeedLayout` | nodes, edges, dims, schema                  | Seeded node positions       | Top-level grouping + DAG ranks → banded placement + recenter | 2026-01-20 12:10 |
| Mermaid Subgraph | Parse + Group Render       | Preserve nesting + layering; forbid flattened hierarchies                                  | `markdownJsonLdMermaidParser.ts`, `groups.ts` | parser + groups  | Mermaid code + GraphData                        | Group boxes + stable nesting | `visual:parentId` + `visual:topParentId` + depth-derived `visual:layer` | 2026-01-20 12:05 |
| Documentation    | Update Specs               | Updated frontmatter & overlap docs; forbade outdated info                              | `docs/documents/*`           | -               | Markdown content                               | Updated Docs                | Aligned with refactored code                      | 2026-01-20 09:30 |
| Graph Layout     | Enhance Collision          | Verified Quadtree usage; forbade O(n²) loop                                            | `overlap.ts`                 | `createBboxCollideForce` | schema, nodes                                 | Force function              | Used `d3.quadtree` for broadphase                 | 2026-01-20 09:00 |
| Graph Simulation | Enhance Maintainability    | Extracted heuristic layout; removed hardcoded dims; forbade large file size            | `simulation.ts`              | `buildSimulation` | nodes, edges, width, height                    | Simulation instance         | Split logic to `layout/heuristic-cluster.ts`      | 2026-01-20 08:30 |
| Path Resolution  | Normalize file references  | Resolved relative to base_dir; canonicalized; forbade absolute hardcoding              | `codebase_index_cmd`         | `main`            | input, config paths                            | Resolved Path objects       | `Path.resolve()` with base_dir anchor               | 2026-01-20 08:00 |
| Rendering        | Fix Z-Ordering             | Fix Z-ordering of layers; Groups < Links < Nodes < Ports < Labels                           | `scene.ts`                   | `setupGraphScene` | -                                              | SVG Layers                  | Explicit `raise()`/`lower()` order                  | 2026-01-20 23:55 |
| Labels           | Interactive Truncation     | Enable pointer events on labels; add tooltip on hover; ensure truncation                    | `labels.ts`                  | `createLabelsLayer` | nodes                                          | Interactive labels          | `pointer-events: all`, `setHoverInfo`               | 2026-01-20 23:55 |
| Simulation       | Shape-Aware Collision      | Update force simulation when node shape mode changes to prevent drift/overlap               | `simulation.ts`, `GraphCanvas.tsx` | `updateForceSimulationPresentation` | schema.behavior.nodeShapeMode | In-place force update      | Rebind collide/bbox/box forces + alpha restart      | 2026-01-21 00:40 |
| Labels           | Word Ellipsis              | Clamp labels with >20 words (excluding markdown heading layers `hn`) and use tooltip for full text | `labels.ts`, `groups.ts`, `layout/utils.ts` | `truncateTextWithWordEllipsis` | label text | Clean canvas labels        | Word cap then char cap; tooltip reveals full        | 2026-01-21 00:40 |
| Cleanup          | Remove Build Artifacts      | Remove committed build output and generated settings-flow file to avoid drift/conflicts     | `canvas/dist`, `canvas/public/settings-flow.json` | - | - | - | Single source of truth     | Build outputs untracked; generated via build steps  | 2026-01-21 00:40 |
| Dependencies     | Break Store Type Cycle      | Avoid import-cycle risk by importing GraphState from store types instead of store factory   | `hooks/store/*`              | - | -                                              | No runtime cycle risk       | Type imports from leaf `types.ts`                   | 2026-01-21 00:40 |
| Config           | Single Source UI Copy       | Standardize UI_COPY imports through `lib/config` to prevent copy drift                      | `lib/config.ts` + consumers  | - | -                                              | Consistent copy access      | Prefer `@/lib/config` over `@/lib/config-copy/*`    | 2026-01-21 00:40 |
| Parsing          | Unify Auto Parser           | Remove duplicate built-in parser specs; use adapter-based auto parse with worker fast-path  | `features/parsers/default.ts`, `features/parsers/loader.ts` | `autoGraphSpec` | name, text                                 | Single parsing pathway      | Registry always selects; parseAsync uses worker when available | 2026-01-21 01:20 |
| Layout           | Radial Toggle Stability     | Avoid using radial positions as “valid cache” for force mode seeding on mode switch         | `layout/positioning.ts`      | `determineLayoutPositions` | mode changes                               | Stable mode switching       | Only skip initial layout when same mode or cached    | 2026-01-21 01:20 |
| Theming          | Theme-Safe Label Halo       | Align label/heading-layer fill+halo with Theme; strip legacy default schema label colors     | `useGraphCanvasStyles.ts`, `schema.ts`, `schemaSlice.ts`, `schema-editor/utils.ts` | `useGraphCanvasStyles`, `readSchemaFromStorage` | themeMode, schema | Visible labels in dark mode | Theme tokens by default; migration removes `#111111/#111` + `#ffffff/#fff` | 2026-01-21 01:20 |
| Markdown Layers  | Heading Labels Ellipsis     | Clamp markdown heading group labels to prevent overflow and visual overlap                   | `groups.ts`, `GraphHoverTooltip.tsx` | group label render  | group label                                 | Clean canvas labels         | Word cap then char cap; hover tooltip preserves full label | 2026-01-23 |
| Layout           | Mode Toggle Refits View      | When switching Force↔Radial and view not pinned, refit to prevent offscreen/blank toggles   | `GraphCanvas.tsx`, `scene.ts` | `GraphCanvas` effect | zoomState, layout mode | Visible layout on toggle | Reuse zoom only if mode+frontmatter unchanged or pinned | 2026-01-21 01:20 |
| Theming          | System-Light-Dark Unification | Align CSS vars + Tailwind tokens + renderer colors; forbid token drift and toolbar-coupled init | `index.css`, `theme.ts`, `theme-tokens.ts`, `uiSettingsSlice.ts`, `App.tsx`, `GraphCanvas/*`, `three/Scene.tsx` | `applyThemeMode`, `subscribeToSystemThemeChanges`, `setThemeMode` | themeMode, OS scheme | Unified theme across UI/canvas/3D | CSS vars are source-of-truth; JS uses CSS var refs/resolution; system changes handled via matchMedia subscription | 2026-01-20 |
| Markdown         | Editor→Viewer DblClick Sync  | Restore editor double-click to scroll preview; forbid JSDOM fallback regressions             | `MonacoTextEditor.tsx`, `MarkdownEditorPane.tsx`, `BottomPanelMarkdownSectionView.tsx`, `markdownScrollUtils.ts` | `onDoubleClickSelectionOffsets`, `scrollToLineInViewer` | selection offsets | Viewer scroll to block start | Textarea fallback emits offsets; viewer scroll uses raf; scrollIntoView guarded for non-HTMLElement elements | 2026-01-20 |
| Edges            | Theme-Safe Edge Labels       | Apply theme-safe label paint and reuse shared edge handlers; forbid hardcoded fills          | `edgeLabels.ts`, `links.ts`, `edgeInteractions.ts`, `useGraphCanvasStyles.ts` | `createEdgeLabelsLayer`, `createLinksHitLayer` | edge label text | Consistent edge labels | Edge labels inherit halo/fill via canvas styles; shared handler centralizes hover/click logic | 2026-01-23 |
| Edges            | Bounded Endpoint Avoidance   | Prevent edge labels from overlapping their endpoint nodes; forbid messy midline labels       | `sceneHandlers.ts`, `overlap.ts` | `attachSimulationTick`, `getNodeAabbHalfExtentsWithLabel` | edge label positions | Cleaner canvas | Bounded normal-offset nudging avoids endpoint AABBs; viewport gating remains bounded | 2026-01-23 |
| Graph Layers     | Theme-Safe Group Outlines    | Remove palette hardcoding for group outlines; align with canvas edge stroke token            | `groups.ts`, `theme-tokens.ts` | `createGroupsLayer` | group style | Unified theming | Group stroke/fill fall back to `--kg-canvas-edge-stroke` unless overridden by `visual:stroke/fill` | 2026-01-23 |
| Graph Layers     | Label-Contained Bounds       | Ensure rect group bounds expand to contain label width; forbid label overflow                | `groups.ts` | `computeBoundsAndLabel` | group label text | Clean group headers | Rect width expands to include estimated label width (after truncation), keeping label inside group container | 2026-01-23 |
| Port Handles     | Leaf Config Extraction       | Extract port-handle config to a leaf module; forbid import cycles across sizing/layout       | `portHandlesConfig.ts`, `portHandles.ts` | `getPortHandlesConfig` | schema.behavior.portHandles | Safer imports | Config resolver no longer depends on node sizing, enabling reuse in collision and sizing paths | 2026-01-23 |
| Collision        | Port Handle Clearance        | Include port handle offset/size in collision extents; forbid handle overlap                  | `overlap.ts` | `getNodeAabbHalfExtentsWithLabel` | portHandles config | Cleaner handles | Node AABB extents incorporate port clearance while port rendering keeps node half-extents unchanged | 2026-01-23 |
| Layout           | Radial Bounded Relaxation    | Reduce overlaps after radial placement using bounded AABB-collide steps; forbid indefinite runs | `radial.ts`, `overlap.ts` | `applyRadialClusterLayout`, `createBboxCollideForce` | radial positions | Less overlap | Apply 6 bounded relax steps with bbox-collide; integrates velocities into positions and dampens | 2026-01-23 |
| Node Shapes      | Remove Minimap-Driven Sizing | Remove minimap-coupled rect sizing policy; derive rect dimensions from node render radius    | `nodeSizing2d.ts` | `getNodeRectDimensions2d` | node sizes | Predictable sizing | Rect fallback uses a 2:1 ratio based on schema render radius; width/height remain overrideable via `visual:width/height` | 2026-01-23 |
| Theme            | Canvas Accent Token          | Add `--kg-canvas-accent` and use it for temp link styling; forbid hardcoded accent strokes   | `index.css`, `links.ts` | `createTempLink` | edge creation | Theme-safe accent | Temp link stroke follows the active theme via a dedicated CSS variable | 2026-01-23 |
