---
title: "todo-log"
doc_type: "Planning Ledger"
status: "active"
lang: "en-US"
frontmatter_contract: "required"
---

# todo-log

## Planning Ledger Contract

- The opening YAML frontmatter block remains the first-block machine SSOT for this planning ledger's metadata, status, and scope.
- This document is a canonical authored planning/work log, not a generated registry surface or typed validation fixture.
- Frontmatter stays in plain YAML so the file demonstrates the default authoring path for repo-level logs, directives, and implementation history.
- New rows and dated sections belong to the live ledger body; do not duplicate ledger ownership in ad hoc metadata tables or downstream mirrors.
- If typed `{key, type, value}` envelopes are needed for ingest -> parse -> render validation, that coverage should live in a dedicated fixture doc rather than replacing canonical planning prose.
- Planning history, next steps, and canonical directives must be derived from parsed frontmatter and document content only, never from file path assumptions or downstream mirrors.

- **Canonical directive**: **STRICTLY COMPLY** with one-row-one-directive (Max 50 words)
- **Table cells**: MUST fill-up all cells in the table; FORBID empty/`-` cells.

## 2026-06-15

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Chat skill prompt modularization release follow-up | Record the Dev -> Prod -> Cloudflare release trail and keep downstream docs-map indexing canonical | Keep chat skill prompt modularization source-owned in `knowgrph`, publish only through generated mirror sync, log paired source/publish commits plus live route proof in canonical docs, and regenerate the AgenticRAG docs map from authored release-note sources only. | `knowgrph/{canvas/src/features/chat,docs/documents,todo-log.md}`, `huijoohwee/{content/knowgrph,knowgrph}`, `huijoohwee.github.io/schema/AgenticRAG/{sync_map.py,knowgrph-documents-map.graph.jsonld}` | Chat skill registry + release ledger | `pages:build-sync`, `pages:deploy-cloudflare`, `sync_map.py --mode write` | Source commit `fcd0ea5f`, publish commit `f0422135`, deployed route `https://airvio.co/knowgrph/`, preview deploy `https://84f45986.joohwee.pages.dev` | Added release note, updated topology/ledger trail, regenerated docs map entry for the new release document | Release evidence belongs in canonical upstream docs and generated schema mirrors only; do not hand-edit publish artifacts or downstream graph nodes when the authored release note is the real source. | Re-run `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` after future release-note additions, publish-topology changes, or companion-doc sharding. | 2026-06-15 |

## 2026-06-13

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Nested documentation sync into AgenticRAG map | Keep newly added API-reference and companion docs visible in the cross-repo docs graph | Make the AgenticRAG docs-map sync recurse through `docs/documents/**`, preserve relative document paths, regenerate the map, and mirror the recursive-source rule in shared schema notes instead of hand-patching generated graph artifacts. | `knowgrph/{todo-log.md,docs/documents/knowgrph-api-reference/knowgrph-ai-agents-memory-layer-prd-tad.md}`, `huijoohwee.github.io/schema/AgenticRAG/{sync_map.py,README.md,knowgrph-documents-map.graph.jsonld}`, `huijoohwee.github.io/docs/documents/hjh-workspace-todo-log.md` | Docs sync map contract | `iter_doc_rel_paths`, `get_node_doc_rel_path`, `sync_map.py --mode write` | Nested docs under `docs/documents/**` including `knowgrph-api-reference/*` and future sharded companions | Refreshed docs map with nested relative-path document nodes plus updated schema guidance | Fix missing nested docs at the sync root once; generated map artifacts stay derived from recursive canonical docs, and hand-added downstream nodes or filename-only assumptions remain forbidden. | Re-run `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` after future nested doc additions, sharding moves, or companion-folder growth. | 2026-06-13 |

## 2026-06-05

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| MiroMind Pages server-managed key readiness | Prove MainPanel MiroMind API key setup without browser secrets | Keep `miromindApi.api_key` server-managed by default and gate live Pages with a non-deploy readiness command. | `knowgrph/{canvas/src/__tests__,scripts/check-miromind-readiness.mjs,docs/documents,todo-log.md}` | MainPanel Integrations + Pages chat proxy | `testMainPanelMiroMindApiKeyUsesServerManagedPagesSecretContract`, `miromind:readiness:check` | Toolbar -> MainPanel Integrations -> `miromindApi.api_key`, `joohwee` Pages `MIROMIND_API_KEY` | Focused local proof, secret-list proof, live no-BYOK proxy smoke | Pages secrets must be present before the deployment that reads them; local UI and proxy code can pass while the live route still needs a fresh Pages deploy. | After deploy approval, run `npm run pages:deploy-cloudflare`, then rerun `npm run miromind:readiness:check` against `joohwee.pages.dev` and `airvio.co/__chat_proxy`. | 2026-06-05 |
| Toolbar Theme and renderer palette consistency | Keep one compact Theme button and palette defaults source-owned | Restore the single Toolbar Theme cycle button while keeping System/Light/Dark order centralized; route renderer palette reads through schema helper. | `knowgrph/canvas/src/{components/Toolbar.tsx,lib/ui/theme.ts,features/toolbar/hooks/{useToolbarActions,useRendererPalette}.ts}` | Toolbar Theme + renderer palette SSOT | `THEME_MODE_OPTIONS`, `getNextThemeMode`, `handleToggleTheme`, `getRendererPalette` | Toolbar theme click + `schema.metadata['renderer:palette']` | One compact Theme button cycles System -> Light -> Dark -> System; shared palette merge path remains schema-owned | Theme option order is centralized in `THEME_MODE_OPTIONS`; palette defaults are read by `getRendererPalette(schema)`; segmented Toolbar groups and local mode maps are forbidden. | Mirror the theme/palette contract into AgenticRAG docs after the next cross-repo schema-doc batch; keep Prod/Cloudflare deploy out of scope until requested. | 2026-06-05 |

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| 2D Dashboard renderer | Render graph-derived dashboard through existing D3 | Register canonical Dashboard surface, derive cards from active graph, and let Display Controls Grid own chart gridlines. | `knowgrph/canvas/src/{components/DashboardCanvas,lib/config.render.ts,components/toolbar,__tests__}` | Dashboard renderer | `buildDashboardCanvasModel`, `DashboardCanvas`, `getCanvasViewRendererOptions` | Active `GraphData`, `GraphSchema.behavior.canvasGrid` | D3 dashboard canvas, generic cards, Grid On/Off marker | Dashboard is graph-derived and file-agnostic; no alternate chart runtime, fixture backfill, or external-demo data; minimap stays disabled. | Mirror AgenticRAG schema only when cross-repo doc batch is authorized; Dev→Prod/Cloudflare deploy remains blocked until instructed. | 2026-06-05 |
| 2D renderer centroid SSOT | Center graph elements consistently across 2D surfaces | Reuse one shared graph-element centroid/viewport helper for D3, Flow Canvas, Flow Editor, Design, SVG export, layout seed, post-fit, and arrange owners. | `knowgrph/canvas/src/{lib/canvas/graph-elements,components/{GraphCanvas,FlowCanvas,DesignCanvas},lib/graph,__tests__,tests/registry}` | Graph element centroid utility | `computeViewportCenteredTransformForGraphElements`, `measureGraphElementCenterSet`, `computeLayoutRectSetViewportCenterShift` | Node/group/edge points, visual rects, viewport bounds, zoom transforms | Shared centroid metrics, viewport-centered transforms, layout rect recenter shifts | Graph-element centering is renderer-neutral; 2D renderers import the helper and no longer own duplicate local centroid sums or switching-specific center patches. | Re-smoke live Source Files switches across D3, Flow, Design, and Flow Editor before deploy approval. | 2026-06-05 |
| Source Files Share URL context menu | Make right-click Share URL publish before copy and fail visibly | Hydrate metadata-only workspace entries through the shared active-document resolver before storage publish, reuse FS reads for path publishes, and make D1 document writes converge by canonical workspace/path. | `knowgrph/{canvas/src/{features/source-files,features/markdown-workspace,__tests__,tests/registry},cloudflare/workers/knowgrph-storage}` | Source Files Share URL | `publishWorkspaceEntryShareUrl`, `publishWorkspaceEntriesToKnowgrphStorage`, `processDocumentMutation`, `buildMarkdownFileTreeContextMenuItems` | Metadata-only `WorkspaceEntry`, storage worker publish, seeded D1 document row, clipboard fallback | Published storage document plus copied/prompted URL or visible publish error | Storage publication owns public URL validity; D1 writes resolve `(workspace_id, canonical_path)` before insert so seeded docs and Share URL records do not create duplicate rows or UNIQUE failures. | Re-smoke live Source Files right-click Share URL when production storage configuration is authorized; no Dev->Prod or Cloudflare deploy was run. | 2026-06-05 |
| Generated chat artifact storage | Make New Chat KGC Markdown and binary outputs durable without a FloatingPanel uploader | Promote generated `/chat-log/{session}` Markdown to the server-owned GitHub write route first, then mirror to Source Files storage; upload generated binary bytes to R2 and promote a sibling Markdown manifest. | `knowgrph/{canvas/src/{features/chat,features/source-files,__tests__,tests/registry},cloudflare/{pages,workers/knowgrph-storage},scripts}` | Chat artifact storage promotion | `persistChatStreamArtifacts`, `writeKgcCompanionOutputBlob`, `publishGeneratedWorkspacePathsToGitHub`, `publishGeneratedWorkspacePathsToKnowgrphStorage`, `handleGitHubWorkspaceWrite`, `configure-pages-github-write.mjs`, `readKnowgrphStorageRuntimeSyncEnabled`, `e2e-github-canonical-storage-sync.mjs` | Canonical KGC path, trace/report paths, generated PNG/video Blob, GitHub write env, runtime sync env | GitHub repository file under `chat-log/{session}/...` as canonical write target; D1/R2 as secondary read/share/cache targets when runtime storage is enabled | Workspace-backed files remain skipped by background sync; generated chat artifacts opt into GitHub-first promotion once through the shared generated-artifact path, using canonical `chat-log/...` repository paths; binary bytes stay out of D1 and D1 stores only the searchable `.manifest.md`; if GitHub write fails, Cloudflare cache promotion is skipped. | Production Pages has repo, branch, and token bindings; `npm run e2e:github-canonical-storage:dev` and `npm run e2e:github-canonical-storage:prod -- --json` validate GitHub-before-Cloudflare ordering, custom-domain route aliasing, GitHub Contents readback, D1 doc/pull, and Share URL readback. Latest production proof wrote commit `e750ca7e1afa8bddc6b64fb28ed5d16060f8d99a`. Re-smoke with an actual FloatingPanel Chat -> New Chat turn when validating full assistant output UX. | 2026-06-06 |
| Source Files cache and switching performance | Reduce repeated active-document hydration during Editor Workspace Source Files switching and startup | Share same-key resolved-text promises, reuse active-path startup snapshots, and settle completed document switches without retry churn; keep ownership upstream. | `knowgrph/canvas/src/{lib/markdown-workspace-runtime,features/source-files,__tests__,tests/registry}` | Source Files selection/startup cache | `readCachedWorkspaceSelectionResolvedTextForActivePath`, `isWorkspaceDocumentSwitchApplySettled`, `createWorkspaceStartupSourceRootEntriesReader` | Same active workspace path, duplicate switch/startup hydration, completed switch retry tick | One FS/storage read per same-key handoff, retained active text, settled retry timers, registered focused guards | Cache keys include active path, canonical-read intent, entry timestamp, and text hash; switch settlement requires selected name/text plus Canvas graph source convergence; no renderer-local fallback or document hardcode. | Mirror AgenticRAG schema note after the next cross-repo docs batch; re-smoke live Source Files reload on Flow Editor before deploy approval. | 2026-06-05 |
| Source Files 2D renderer switching | Keep selected-file renderer presets authoritative during Source Files switches | Include current 2D renderer in switch settlement and gate Flow Editor frontmatter handoff to the Flow Editor renderer only. | `knowgrph/canvas/src/{lib/markdown-workspace-runtime,components,__tests__}` | Source Files renderer switch authority | `isWorkspace2dRendererPresetStaleForDocument`, `isWorkspaceDocumentSwitchApplySettled`, `CanvasViewport` | Selected Markdown `kgCanvas2dRenderer`, current Canvas renderer, graph source | Matching active Design, GitGraph, or Flow Editor surface with no stale Flow Editor override | Frontmatter renderer preset must converge before switch settles; frontmatter-flow graph shape cannot override non-Flow-Editor 2D renderers while Editor Workspace is open. | Re-smoke Design, GitGraph, and Flow Editor file switches before deploy approval. | 2026-06-05 |
| KGC session artifact consolidation | Keep canonical run and trace documents as the retained Markdown outputs | Write final KGC to `kgc_*`, keep `kgc-trace_*`, fold stream-log content into trace, fold markdown run/manifests into canonical, and repair invalid inline compute YAML at source. | `knowgrph/canvas/src/{features/chat,lib/markdown,features/parsers,__tests__}`, `knowgrph/docs/documents` | KGC workspace artifact policy | `appendChatHistoryWorkspaceFile`, `persistChatStreamArtifacts`, `persistKnowgrphVdeoxplnRunManifestForChat`, `emitKgcRunOutput`, `repairFrontmatterYamlSyntax` | KGC finalization, stream SSE metadata, markdown run output, vdeoxpln manifests, malformed inline compute envelopes | `kgc_YYYYMMDDTHHmmssZ.md` plus `kgc-trace_YYYYMMDDTHHmmssZ.md` with consolidated sections and parseable frontmatter | Canonical KGC remains the graph/run source; trace owns observability; markdown companions become idempotent marked sections; syntax repair is generic and in-memory, not file backfill. | Re-smoke one live FloatingPanel Chat KGC turn and confirm no `chat-stream-log_*` or markdown `kgc-output_*` sidecar appears before deploy approval. | 2026-06-05 |
| Root config folder cleanup | Keep config artifacts under one data-owned SSOT | Move root config folders to `data/config/*`, remove duplicate shell delete shims, and keep parser, canvas, docs, and generated previews on canonical config paths. | `knowgrph/{data/config,canvas/src,knowgrph_parser,docs,CodeWiki.md,todo-log.md}` | Config path SSOT | `DEFAULT_SCHEMA_CONFIG_PATH`, `DEFAULT_GRAPHRAG_CONFIG_PATH`, `repo_path` | Former root-level config folders and duplicate shell delete shims | `data/config/{graphrag,schema,orchestrator,llm-chat}` plus clean consumers | Config artifacts are data-owned repo inputs; parser and canvas consumers derive default paths from shared constants; unreferenced shell `rm` overrides are removed instead of preserved as compatibility shims. | Mirror the `data/config` path in AgenticRAG schema docs during the next schema/API doc batch and run `sync_map.py --mode check` when that cross-repo update is authorized. | 2026-06-05 |
| Chat structured-content neutral dataflow | Make card-to-panel render payloads dynamic by default | When structured responses include source cards/widgets and panel targets without authored edges, synthesize one neutral inline compute widget and explicit handle edges; preserve authored `edges[]` and `flow:compute` authority. | `knowgrph/canvas/src/features/chat`, `knowgrph/canvas/src/__tests__`, `knowgrph/docs/documents/knowgrph-renderer-document.md` | Chat structured-content projection | `extractChatResponseStructuredSurface`, `ensureStructuredSurfaceDataflow`, `computeFlowConnectedValuesBySchemaPath` | MCP-style `structuredContent.cards[]`, `widgets[]`, `panels[]`, `media[]`, authored edges, or safe inline compute | Dynamic frontmatter-flow compute node, connected Rich Media Panel values, and Storyboard card recompute path | Explicit assistant dataflow wins; missing dataflow is repaired once upstream in the extractor, not by fixture backfill, renderer aliases, or downstream panel patches. | Re-smoke FloatingPanel Chat with a real neutral card-plus-panel structured response before any Prod or Cloudflare deploy. | 2026-06-05 |

## 2026-06-04

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Shared response card/audio rendering | Keep AI response text and audio media renderer-neutral | Project generated `output` text into shared editable Card/Storyboard rows; route `audioUrl`/audio media through shared card, panel, overlay, preview, inventory, importer, and HTML-export owners. | `knowgrph/canvas/src/{lib/{cards,canvas,render,graph,websites},components,features/flow-editor-manager,__tests__,tests/registry}` | Shared media/card response owners | `GRAPH_NODE_CARD_OUTPUT_PROPERTY_KEYS`, `buildStoryboardBoardModel`, `getNodeMediaSpec`, `buildRichMediaPanelPreviewSpec`, `CardMediaPreview`, `resolveWidgetCompactPreview` | Headless response fields plus `audioUrl`/audio media nodes | Editable output rows, audio cards/widgets/panels/overlays, audio inventory counts, audio HTML export | Audio is a normal media kind and generated text is a normal card field; shared schema paths and media-kind inference own projection. | Re-smoke FloatingPanel Chat -> Storyboard/Flow Editor with an audio URL response before deploy approval. | 2026-06-04 |
| MCP-shaped chat structured content | Make FloatingPanel Chat responses materialize like MCP tool results | Extract `response.structuredContent`, literal MCP `result.structuredContent`, and structured `result.content[]` text blocks from assistant YAML/JSON or accepted KGC frontmatter, normalize plain fields plus typed KTV envelopes, preserve declared widget-form records as Flow Editor widget nodes with document-scoped registry entries, preserve safe `flow:compute`, and project panels, cards, media, nodes, generated edges, authored edges, and overlay refs into canonical KGC flow data before workspace apply. | `knowgrph/canvas/src/features/chat/{chatResponseStructuredContent.ts,chatResponseStructuredContentProjector.ts,chatHistoryWorkspace.kgc.baseFallback.ts,chatHistoryWorkspace.kgc.build.ts,chatResponseBaseContract.ts}` | Chat structured-content projection | `extractChatResponseStructuredSurface`, `projectChatResponseStructuredSurfaceIntoKgcFrontmatter`, `normalizeKgcAssistantBodyForStorage`, `unwrapFlowEnvelopeFieldValue`, `resolveWidgetRegistryEntry`, `computeFlowConnectedValuesBySchemaPath`, `buildFlowEditorInlineComputeOutputPatch`, `buildGraphNodeCanonicalTextPatch` | MCP-style structured content with declared widget form/type records, literal MCP result envelopes, safe inline compute data, text, image, audio, video, HTML, edge fields, exact `{key,type,value}` envelopes, or `properties[]` KTV rows | Parsed Flow Editor widget nodes plus document-scoped registry entries for declared forms, Rich Media Panel endpoints otherwise, `n-deliver` response edges, authored handle-bearing edges, `widget_bundle.graph.nodes_ref` overlay refs, editable shared media/card render fields, active frontmatter writeback, provider-free inline-compute run writeback, connected-value recomputation, and run-all eligible dataflow widgets | Chat persists structured tool-result semantics as normal frontmatter-flow data; typed frontmatter values unwrap upstream before parser; literal MCP envelopes and content text parts resolve upstream; declared widget forms use shared Flow Editor widget registry and connected-value owners; authored `flow:compute` nodes write computed output schema paths before provider TextGeneration dispatch; neutral render records use shared Rich Media owners; inline edits keep flattened and native `properties` mirrors aligned through shared card patch/updateNode owners. | Live-smoke a FloatingPanel Chat response that emits typed `response.structuredContent.widgets[]`, literal MCP `result.structuredContent`, safe `flow:compute`, handle-bearing `edges[]`, run-all inline compute, and inline-edited output before deploy approval. | 2026-06-04 |
| MCP submit validation acceptance | Stop valid MCP structured results from entering legacy KGC retry | Finalize literal MCP structured responses at the shared attempt resolver when a renderable structured surface exists; do not synthesize KGC or retry for a KGC block. | `knowgrph/canvas/src/features/chat/floatingPanelChat/{floatingPanelChatKgcAttempt.ts,floatingPanelChatSubmitCoordinator.ts}` | Chat submit validation owner | `resolveChatKnowgrphAttempt`, `executeFloatingPanelChatSubmitCoordinator`, `extractChatResponseStructuredSurface` | Literal MCP `result.structuredContent` with renderable widgets, panels, cards, media, or edges | One provider attempt, raw assistant response finalized, `validatedKgc=null`, structured-response validation snapshot | The root validator owns acceptance; the coordinator keeps one finalize path and existing workspace/canvas projection materializes the response later. | Re-smoke a real provider or local runtime command emitting literal MCP structured content before deploy approval. | 2026-06-04 |
| Flow Editor widget-run workspace artifacts | Make widget LLM/transcript/image/video output land in Editor Workspace without renderer forks | Persist completed Text Widget and Video Transcriber Markdown outputs as sibling workspace artifacts; persist Image Generation and Video Generation binaries plus editable Markdown manifests; passively register text artifacts and media manifests in Source Files; carry workspace paths through shared widget/Rich Media Panel patches. | `knowgrph/{canvas/src/features/chat,canvas/src/components/FlowEditorCanvas/runtime,docs/documents,todo-log.md}` | Flow Editor workflow run + widget artifact helpers | `writeTextWidgetRunOutputArtifact`, `writeRichMediaWidgetRunOutputArtifact`, `buildTextWidgetOutputPatch`, `buildRichMediaWidgetOutputPatch`, `runWorkflowNode` | Final generated text/transcript/image/video output plus active workspace path | Workspace Markdown artifact or manifest, binary media artifact path, Source Files entry, widget/Rich Media Panel output metadata | Final text writes once; media writes one binary and one editable manifest; Source Files registration uses `applyToGraph: false`; no provider, renderer, or graph-apply branch owns local artifacts. | Live-smoke Flow Editor Text/Image/Video widget runs from a workspace-backed document and confirm the sibling artifact or manifest appears in Source Files before any deploy. | 2026-06-04 |
| Chat KGC workspace landing | Make LLM output render through shared Source Files owners | Route saved `chatKnowgrph` KGC apply through workspace import before active markdown graph apply. | `knowgrph/{canvas/src/features/chat,canvas/src/features/workspace-fs,docs/documents,todo-log.md}` | Chat KGC canvas bridge | `applyChatKgcWorkspaceDocumentToCanvas` | Saved canonical KGC markdown | Parsed Source Files entry plus active canvas document | Source Files import owns renderer-neutral materialization; active markdown actions own graph apply; no provider, renderer, or downstream patch branch. | Mirror schema docs next and re-smoke Flow Editor/Storyboard on a live chatKnowgrph response before any deploy. | 2026-06-04 |
| Canvas toolbar dock SSOT | Keep editor-workspace canvas controls reachable on mobile | Move canvas toolbar dock geometry into shared responsive owners and span the mobile safe viewport while desktop keeps the editor-pane boundary. | `knowgrph/canvas/src/{pages/Canvas.tsx,lib/ui/responsiveElementClasses.ts,styles/responsive-canvas-toolbar.css,__tests__}` | Canvas toolbar dock | `workspaceToolbarBoundaryStyle` | Desktop, narrow, and coarse-pointer viewports | Bounded top desktop dock and bottom mobile dock | Focused shared CSS owns geometry and content clamps; React only suppresses the desktop pane boundary on narrow/coarse viewports; toolbar row-scroll remains the scroll owner. | Re-smoke `?openEditorWorkspace=1` after future toolbar, workspace split, or mobile breakpoint changes. | 2026-06-04 |
| Stripe Checkout payment acceptance | Accept hosted Stripe payments without browser secrets | Route checkout through the payment Worker, preserve same-window Session ID redirects, and keep Checkout URLs session-only. | `knowgrph/{grph-shared/src/payments/stripePaymentSsot.ts,cloudflare/workers/knowgrph-payment,canvas/src/features/payments,docs/documents,todo-log.md}` | Stripe payment Worker + Canvas return runtime | `resolveStripeCheckoutServerConfig`, `buildStripeCheckoutSessionCreateForm`, `createStripeHostedCheckoutSessionForWorker`, `handleStripeCheckoutStatus`, `handleStripeWebhook`, `StripeCheckoutReturnRuntime`, `PaywallOverlay` | Worker env, hosted Checkout returns, webhook events | One-time payment default, subscription mode via Price ID, visible Worker `[vars]` price/mode/return origin, Worker-secret-only Stripe credentials/webhook secret, locally-owned D1 row before live Stripe status lookup, minimal public status payload, payment-status-only return unlock, same-payload duplicate webhook acknowledgement, retryable failed/stale webhook processing state, hosted Session expiry on audit persistence failure | Server env owns price/mode/return origin; paywall always creates fresh Sessions; status reads D1 first, requires canonical `session_id`, retrieves Stripe only for existing open/unpaid rows, rejects unknown session ids and legacy `id` aliases before Stripe calls, keeps customer/metadata/url/workspace details in D1, returns only minimal payment state publicly, unlocks only paid/no-payment-required returns, clears transient URLs on return, skips same-payload duplicate side effects, retries failed processing, reclaims stale claims, and expires a hosted Stripe Session before returning if the Stripe audit row cannot be persisted. | Configure visible Worker `[vars]` checkout price authority, configure `STRIPE_WEBHOOK_SECRET` as a Worker secret, and apply the webhook processing D1 migration with `npx wrangler d1 migrations apply knowgrph-storage --remote --config cloudflare/workers/knowgrph-payment/wrangler.toml`, then deploy/payment-smoke when production checkout is authorized. | 2026-06-04 |
| Agentic Stripe Checkout ACP settlement | Accept agent-initiated fiat checkout through hosted Stripe | Let ACP create requests include `stripe_checkout`, return hosted Checkout URLs, and settle, fail, or cancel only from verified matching Stripe webhook/status refresh. | `knowgrph/{grph-shared/src/payments/{agenticCommerceSsot.ts,stripePaymentSsot.ts},cloudflare/workers/knowgrph-payment,canvas/src/features/payments,docs/documents,todo-log.md}` | Stripe payment Worker + ACP settlement | `createStripeHostedCheckoutSessionForWorker`, `expireStripeHostedCheckoutSessionForWorker`, `handleStripeCheckoutStatus`, `settleAgenticCommerceSessionFromStripeSession`, `failAgenticCommerceSessionFromStripeSession`, `cancelAgenticCommerceSessionFromExpiredStripeSession`, `handleCompleteSession` | `stripe_checkout`, `agenticCommerceSessionId`, `workspaceId`, expected amount/currency, Stripe idempotency, Stripe session metadata, delegate token, Stripe event id | `session.stripe_checkout.url`, `metadata[acp_session_id]`, `metadata[expected_amount_total]`, `metadata[expected_currency]`, `Idempotency-Key`, server-owned return origin, ACP proof settlement, `payment_failed`, `cancelled`, duplicate webhook acknowledgement, or 409 guard | ACP id owns Stripe client reference and idempotency; hosted return URLs and Stripe totals must match route origin/config and ACP amount/currency; Stripe audit or ACP persistence failures after Stripe creation expire the hosted Session before returning; Stripe settlement also requires `client_reference_id` to match `metadata[acp_session_id]`; terminal states reject later local completion. | Configure Worker checkout price and run the live create-and-expire payment smoke before Cloudflare deploy approval. | 2026-06-04 |
| Solana Pay ACP payment acceptance | Accept Solana Pay without a parallel payment service | Add Solana Pay as an ACP rail in the payment Worker, validate signatures through Solana RPC, and keep generic webhooks unable to settle unconfirmed on-chain sessions. | `knowgrph/{grph-shared/src/payments,cloudflare/workers/knowgrph-payment,canvas/src/__tests__,docs/documents,todo-log.md}` | Solana Pay payment Worker rail | `buildAgenticCommerceSolanaPayUrl`, `confirmAgenticCommerceSolanaPayTransfer`, `handleSolanaPaySettle`, `settleAgenticCommerceSession` | `payment_rail="solana_pay"`, recipient, SPL token, deterministic reference, memo, submitted signature | `session.solana_pay.url`, `pending_onchain`, Solana RPC confirmation, ACP proof, payment trace, or 409 webhook guard | Existing ACP session, D1, proof, and trace owners stay canonical; settlement validates generated reference, memo, recipient, amount, and SPL/SOL transfer deltas before proof write. | Configure `SOLANA_PAY_RECIPIENT`, `SOLANA_PAY_SPL_TOKEN`, and `SOLANA_PAY_RPC_URL`, then run a bounded live transfer smoke before deploy approval. | 2026-06-04 |
| x402 pay-to authority | Accept machine-native payments only to an operator-owned address | Keep zero-address `X402_PAY_TO_ADDRESS` as future-setup placeholder while rejecting fallback and placeholder `payTo` in x402 readiness. | `knowgrph/{cloudflare/workers/knowgrph-payment/wrangler.toml,grph-shared/src/payments/agenticCommerceSsot.ts,scripts/{agent-ready-commerce-checks,configure-x402-payment-worker}.mjs,canvas/src/__tests__,docs/documents,todo-log.md}` | x402 payment readiness | `assertAuthoritativeX402PaymentRequired`, `readAgenticCommerceX402PayToAddress`, `payment:x402:configure`, `payment:x402:readiness` | `PAYMENT-REQUIRED`, `X402_PAY_TO_ADDRESS`, fallback derived address, placeholder zero address | Pass/fail x402 readiness authority | x402 response syntax is insufficient for production if `payTo` equals the shared deterministic fallback or zero-address placeholder; readiness requires an ERC-20 asset, exact scheme, network, and configured EVM pay-to address. | Replace placeholder with `X402_PAY_TO_ADDRESS=0x... npm run payment:x402:configure -- --write-visible-vars --deploy-visible-vars --apply --yes --confirm=apply-stripe-payment-worker-config`, then rerun `payment:x402:readiness`. | 2026-06-05 |
| Combined payment readiness | Prove human and agentic payment gates together | Aggregate existing Stripe and x402 readiness scripts without moving their semantics. | `knowgrph/{scripts/check-payment-readiness.mjs,canvas/src/__tests__/paymentReadinessScript.test.ts,todo-log.md}` | Payment readiness wrapper | `payment:readiness` | Stripe readiness JSON, x402 readiness JSON | Combined pass/fail summary | The wrapper only composes focused gates and passes `--live-checkout-create` through to Stripe when explicitly requested. | After configuring Stripe price authority and `X402_PAY_TO_ADDRESS`, run `npm run payment:readiness -- --live-checkout-create`. | 2026-06-04 |
| Stripe payment readiness gate | Make production payment blockers explicit without accidental Stripe mutations | Add a non-mutating Worker config and remote D1 schema check, surface it in Commerce, then require an explicit bounded live Checkout create-and-expire smoke flag. | `knowgrph/{grph-shared/src/payments/stripePaymentSsot.ts,scripts/check-stripe-payment-readiness.mjs,canvas/src/features/panels/views/stripePaymentApiDocs.ts,docs/documents,todo-log.md}` | Payment readiness script + MainPanel Commerce Stripe rows | `payment:stripe:readiness`, `stripeApi.worker.d1_migrations`, `stripeApi.worker.readiness_gate` | Wrangler secret names, visible Worker vars, remote D1 table/column names, bounded optional live Checkout create-and-expire flag | Pass/fail readiness report and operator rows | Config check requires server key, visible Worker `[vars]` checkout price authority, webhook secret, visible checkout mode and return origin when set, payment D1 tables, webhook-processing columns, nullable webhook claim fields, and a timeout around the optional live smoke; readiness fails if Stripe credentials appear in visible Worker `[vars]`, fails if checkout price authority, checkout mode, or return origin is hidden as a Worker secret, and Commerce exposes the same readiness scope. | Apply pending payment D1 migrations, keep non-secret checkout price/mode/return origin in Worker `[vars]`, keep Stripe credentials as Worker secrets, then after Stripe webhook config run `npm run payment:stripe:readiness -- --live-checkout-create` for a bounded create-and-expire smoke; use `-- --live-checkout-timeout-ms=<ms>` only when intentionally adjusting the smoke timeout. | 2026-06-04 |
| Stripe payment Worker config helper | Apply operator-supplied Stripe config without source churn | Add a dry-run-first helper that validates env values and requires explicit confirmation before `wrangler secret put`. | `knowgrph/{grph-shared/src/payments/stripePaymentSsot.ts,scripts/{configure-stripe-payment-worker.mjs,stripe-payment-script-runtime.mjs},package.json,canvas/src/__tests__,docs/documents,todo-log.md}` | Payment config script + Commerce settings docs | `payment:stripe:configure` | Process env, visible Worker `[vars]`, shared Stripe payment SSOT, Wrangler secret put | Secret-name apply plan or guarded apply | The helper reads visible Worker `[vars]` checkout price authority, rejects missing webhook/price authority, conflicting price sources, subscription inline pricing, and checkout price/mode/return-origin process input that would hide non-secret Worker `[vars]` config as secrets, and apply attempts missing confirmation. | Keep checkout price authority, `STRIPE_CHECKOUT_MODE`, and `STRIPE_CHECKOUT_RETURN_ORIGIN` in Worker `[vars]`, supply the real Stripe webhook secret through process env, dry-run, then apply with the confirmation flag when production config is authorized. | 2026-06-04 |

## 2026-05-22

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Agent-ready MCP and WebMCP rollout | Ship implementation-accurate MCP docs plus hardened deployed WebMCP lifecycle | Reuse shared tool contracts, harden browser and HTML fallback WebMCP lifecycle upstream, verify Dev->Preview->Live with the focused smoke, and keep publish artifacts mirror-generated only. | `knowgrph/{canvas/src/features/agent-ready,cloudflare/pages,scripts,docs/documents/knowgrph-mcp,mcp/README.md,todo-log.md}` + `huijoohwee/{content/knowgrph,knowgrph,functions/knowgrph/[[path]].js}` | Agent-ready Pages surface + browser WebMCP runtime + publish mirror | `installKnowgrphWebMcpRuntime`, exported `webMcpScript`, `check-agent-ready.mjs`, `pages:build-sync`, `pages:check-sync` | Shared tool contract, late `navigator.modelContext`, localhost/preview/prod storage reads, preview and live Pages deploys | Shipped late-binding `registerTool(tool, { signal })` WebMCP lifecycle, aligned MCP docs, clean source and mirror commits, preview/live smoke `27/27`, pushed commits `d666208d` and `321e4b4d` | Keep browser and HTML fallback lifecycle contract-equal, add future tools only as thin adapters over MainPanel -> FloatingPanel Chat -> KGC -> Canvas owners, and rerun preview/live smoke after future agent-ready changes. | 2026-05-22 |

## 2026-05-27

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Agnes production Pages proxy parity | Keep deployed Cloudflare Pages chat proxy aligned with the shared Agnes rollout | Add Agnes and MiroMind host/provider support to the publish-repo `__chat_proxy` owner plus shared integration helper, validate missing-key handling with the focused publish smoke, and document the production proxy owner upstream so Dev and deployed behavior cannot drift apart. | `knowgrph/{docs/documents/knowgrph-cross-repo-publish-topology.md,todo-log.md}` + `huijoohwee/{functions/{__chat_proxy/[[path]].js,api/_integrationHub.js},scripts/smoke-test-integrations.mjs}` | Shared Pages chat proxy + publish validation | `normalizeProviderId`, `pickUpstreamBase`, `parseAllowedHosts`, `smoke-test-integrations.mjs` | Shared provider ids, upstream hosts, publish-repo env keys, focused smoke requests | Production proxy accepts Agnes/MiroMind as shared providers and returns provider-specific `401` missing-key responses under smoke validation | Production chat proxy behavior lives in publish-repo Pages Functions, not the Vite dev proxy; fix provider drift there once, then keep the canonical topology doc and ledger aligned with the validated publish owner. | Push the updated `huijoohwee` repo, deploy Pages, then run a real Agnes BYOK smoke against `/knowgrph` when an API key is available. | 2026-05-27 |
| Agnes shared chat provider integration | Add Agnes AI API without forking the chat-to-canvas runtime | Register Agnes only at the shared provider boundary, document MainPanel Integrations readiness plus SSE JSON chunk expectations upstream, and keep publish sync generated-only with no provider-specific finalize, renderer, or graph-mutation branches. | `knowgrph/{canvas/src/{lib/chatEndpoint.ts,features/{chat/FloatingPanelChat.helpers.ts,panels/views/{agnesApiDocs.ts,settingsView.constants.ts,useSettingsChatAssist.tsx,useSettingsView*.ts}},__tests__/*},canvas/vite.config.ts,docs/documents/{knowgrph-agnes-ai-api-prd-tad.md,knowgrph-chat-ai-markdown-pipeline-document.md},todo-log.md}` + `huijoohwee/{content/knowgrph,knowgrph}` | Shared chat provider + Integrations readiness + publish sync | `normalizeChatProviderId`, `buildChatProxyHeaders`, `buildProviderChatRequestOptions`, `pages:build-sync`, `pages:check-sync` | Agnes endpoint/auth/model defaults, shared chat settings, SSE JSON delta stream, generated publish mirrors | Agnes on shared chat-completions transport, clean Integrations visibility, proxy auth support, generated prod mirror with sync parity | Fix provider onboarding once in shared chat/proxy/settings owners, validate with focused provider and Integrations tests, then regenerate publish mirrors instead of hand-editing downstream artifacts. | Run a live Agnes BYOK smoke on deployed `/knowgrph` and mirror any later contract wording into AgenticRAG schema docs from these canonical upstream docs only. | 2026-05-27 |
| Streaming docs sync after raw SSE session-artifact and URL-dereference rollout | Keep Dev and schema mirrors aligned with the shared streaming pipeline | Document raw SSE -> workspace session artifacts -> dereferenced markdown on the shared path, keep canonical source docs sub-600 with continuation links, and mirror only validated upstream streaming rules cross-repo. | `knowgrph/{todo-log.md,docs/documents/{knowgrph-llm-prompt-contract-prd-tad*.md,knowgrph-chat-ai-markdown-pipeline-document.md}}`, `huijoohwee.github.io/schema/AgenticRAG/{README.md,knowgrph-documents-map.graph.jsonld}` | Streaming docs + schema mirror | doc split, continuation links, `sync_map.py --mode write` | Shared chat streaming/runtime owners and updated canonical docs | Sub-600 canonical source doc, new companion, updated pipeline note, refreshed schema mirror references | Keep streaming ownership upstream in shared chat/workspace helpers; treat stream logs, reports, and share/report dereference as additive workspace artifacts, never a second runtime or graph-apply source. | Re-run `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` after the next streaming-doc batch or doc sharding change. | 2026-05-27 |

## 2026-05-24

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Animatic renderer + Flow Editor frontmatter harmonization | Keep animatic and Flow Editor on one canonical renderer/frontmatter contract | Centralize 2D renderer normalization, toolbar/menu labels, shared `flow:` frontmatter ownership, runtime command usage, and validator entry wording upstream; treat `timeline.beats.*` as additive animatic metadata only. | `knowgrph/{canvas/src/{lib/{config.render.ts,markdown/frontmatter.ts},components/{CanvasViewport.tsx,toolbar/canvasViewMenu.ts},features/agent-ready/workspaceRuntimeCommand.ts,__tests__/*},canvas/scripts/validate_animatic_timeline_interactions.py,docs/documents/knowgrph-renderer-document.md,todo-log.md}` + `huijoohwee/{docs/knowgrph-animatic-demo.md}` + `huijoohwee.github.io/{guidelines/knowgrph-animatic-prd-tad.md,schema/AgenticRAG/README.md}` | Shared renderer spec + frontmatter contract | `resolveCanvas2dRendererId`, `getCanvas2dRendererMenuLabel`, `getCanvasViewRendererOptions`, `createWorkspaceRuntimeCommand`, `parseCanvasWorkspaceFrontmatterPreset` | `kgCanvas2dRenderer`, shared `flow:` YAML, optional `timeline.beats.*`, mounted runtime command, validator command | One SSOT renderer descriptor and one canonical live-mutation/validation path for parsing, toolbar menu, and docs/schema wording | Keep ids/order/labels/frontmatter ownership in the renderer spec, keep live mutation on `window.knowgrphWorkspaceCommand.applyMarkdownDocument(...)`, and forbid legacy `animation` aliases, animatic-only markdown dialects, or duplicated alias tables. | Mirror the updated animatic contract into any remaining renderer-selection or schema docs and run `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` after the next schema-doc batch. | 2026-05-25 |

## 2026-05-20

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Source Files bootstrap scheduling | Reduce Source Files switching overhead and drift | Centralize seed-sync and rematerialize scheduling behind request-owned helpers; reuse caller-owned sourceFiles snapshots; forbid duplicate request shaping across mount/poll/wake/mutation bursts. | `knowgrph/canvas/src/features/source-files/SourceFilesPersistenceBootstrap.tsx` | Source Files bootstrap runtime | seed-sync lifecycle helpers + prepared requests | Workspace FS events + seed poll | Coalesced seed + rematerialize scheduling | Capture one prepared seed-sync payload per run; schedule rematerialize from prepared request when available, else resolve once. | Mirror the updated runtime patterns into `docs/documents` + AgenticRAG schema notes. | 2026-05-20 |

## 2026-05-19

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Canvas toolbar mobile dock | Improve touch-first canvas ergonomics | Dock primary canvas toolbar in the mobile safe-area thumb zone through shared CSS. | `knowgrph/canvas/src/{pages/Canvas.tsx,styles/responsive-toolbar.css}` | Canvas toolbar dock | `kg-canvas-toolbar-dock` | Narrow/coarse canvas viewport | Reachable one-row toolbar without theme changes | Reuse toolbar component and shared row-scroll owner. | Browser-smoke mobile canvas after publish sync. | 2026-05-19 |
| Editor Workspace pane toggles | Improve Markdown edit and Viewer preview reachability | Put Explorer, Markdown, and Viewer first; keep Viewer touch-sized and paired with an editable source pane. | `knowgrph/canvas/src/features/markdown-workspace/MarkdownWorkspaceToolbar.tsx` + `responsive-toolbar.css` | Workspace pane toggles | `kg-workspace-pane-toggle--viewer`, `resolveViewerEditPaneVisibility`, `Show Viewer preview pane` | Collapsed mobile toolbar | Readable/touchable Viewer toggle with editable Markdown or JSON pane | Toolbar owns Viewer edit-pane pairing; shared responsive CSS owns sizing and row overflow. | Re-smoke mobile overflow after publish sync. | 2026-05-19 |
| Editor Workspace mobile width | Prioritize editability on compact screens | Centralize overlay canvas gutter and compact width defaults in workspace defaults. | `knowgrph/canvas/src/features/workspace-table/workspaceViewCanvasDefaults.ts` | Workspace width defaults | `WORKSPACE_EDITOR_CANVAS_GUTTER_PX`, `resolveWorkspaceEditorPaneDefaultWidthPx` | 320px editor workspace | 272px overlay, focused Monaco, bounded body width | Compact defaults open at max editable pane while preserving one gutter for resize/canvas reachability. | Re-smoke after publish sync. | 2026-05-19 |
| Editor Workspace editability | Keep mobile editing reachable | Route workspace editor panes, pane dividers, Monaco host/root, and textarea fallback through shared responsive editability classes. | `knowgrph/canvas/src/features/markdown-workspace/main/{layout,editor}` + `knowgrph/canvas/src/lib/monaco` | Editor pane primitives | `kg-markdown-workspace-editor-panes`, `kg-markdown-workspace-pane`, `kg-monaco-editor-root`, `kg-monaco-textarea-fallback` | Mobile editor workspace | Stacked editable panes, bounded scroll, touch-safe textarea fallback | Layout owns semantic pane classes; responsive CSS owns mobile stacking, editable height, overscroll containment, and divider orientation. | Continue with focused browser edit smoke after publish sync. | 2026-05-19 |
| Responsive element-row SSOT | Centralize clipped mobile element rows | Route icon buttons, Explorer controls, graph-table labels, design controls, graph-editor rail buttons, Settings previews, inline markdown menus, launch/dropdown menu rows, data-view actions, chips, add-column controls, and kanban rows through shared responsive element classes. | `knowgrph/canvas/src/lib/ui/responsiveElementClasses.ts` | Responsive element primitives | `UI_RESPONSIVE_ELEMENT_ROW_CLASSNAME`, `UI_RESPONSIVE_MENU_ROW_CLASSNAME`, `UI_RESPONSIVE_ACTION_ROW_CLASSNAME`, `UI_RESPONSIVE_TOUCH_MENU_ROW_CLASSNAME` | One-row UI elements | Clipped icons/text, ellipsis-ready rows | CSS owns overflow and nowrap; components own only spacing/intent. | Continue with deploy parity after publish sync. | 2026-05-19 |
| Toolbar row-scroll SSOT | Centralize mobile same-row scrolling | Route toolbar, panel, workspace, data-view, and table rows through toolbar row-scroll helpers plus shared CSS primitives. | `knowgrph/canvas/src/{features/toolbar/ui/toolbarStyles.ts,styles/responsive-toolbar.css}` | Toolbar row-scroll primitives | `uiToolbarRowScrollClassName`, `uiToolbarResponsiveRowScrollClassName` | Mobile toolbar/panel rows | One-row horizontal scroll, no viewport overflow | Helper owns class identity; responsive CSS owns scroll behavior. | Continue with Cloudflare parity after publish sync. | 2026-05-19 |

## 2026-05-18

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Markdown graph apply cache | Stop duplicate mobile Source Files graph churn | Deduplicate same semantic apply before pending graph install. | `knowgrph/canvas/src/hooks/store/graph-data-slice/graphDataDocumentActions.ts` | Markdown graph apply | `setActiveMarkdownDocument`, `applyMarkdownDocumentToGraph` | Same markdown+preset | No revision churn | Shared semantic key + revision guard | Smoke Cloudflare after sync. | 2026-05-18 |
| Markdown workspace indexing and media wrapper clean targets | Continue near-threshold runtime/UI closures | Move empty-selection-cache policy and media wrapper/gutter controls into focused owners while preserving public exports. | `knowgrph/canvas/src/lib/{markdown-workspace-runtime/{useMarkdownWorkspaceIndexing,markdownWorkspaceSelectionCache}.ts*,markdown-core/ui/{MarkdownMediaUi.impl,MarkdownMediaWrapper}.tsx}` | Markdown workspace runtime and media UI owners | `shouldTrustEmptyWorkspaceSelectionCache`, `MediaWrapper` | Full `hygiene:audit` near-threshold backlog | Two more source files below 600 lines | Keep compatibility re-exports from original modules; split stable policy and wrapper UI from runtime/render facades. | Continue with the next smallest full-audit owners after refreshing `hygiene:audit`. | 2026-05-18 |
| Lazy loading, Flow Editor registry, and 3D SVG clean targets | Continue full-audit test/source closures | Move Monaco settings assertions and Flow Editor registry link checks into focused test owners; fold the 3D SVG schema type import into its value import. | `knowgrph/canvas/src/{__tests__/{lazyLoadingGatesRegression*,flowEditorManagerRegistry*},lib/graph/graphCenteredSvg3d.ts}` | Hygiene regression and graph export owners | `assertMonacoSettingsRegistryKeys`, `testFlowEditorManagerResolvesWidgetRegistry*`, `exportGraphAsCentered3dSvgMarkup` | Full `hygiene:audit` near-threshold backlog | Three more files below 600 lines | Extract stable assertion/link owners; preserve test discovery exports and 3D SVG runtime behavior. | Continue with `useMarkdownWorkspaceIndexing.tsx` and `MarkdownMediaUi.impl.tsx` next. | 2026-05-18 |
| Config, mapping, graph selection, and URL import clean targets | Continue full-audit near-threshold closures | Move key-owner registry, mapping JSON/smart-media/row helpers, selection-subgraph cache, and URL import heuristics into focused owners without changing public facades. | `knowgrph/canvas/src/{lib/{config.ls*,graph/{file,selectionSubgraph}.ts},features/{flow-editor-manager/{FlowEditorMapping*,useFlowEditorMappingRowsEditor}.ts*,markdown-workspace/workspaceImport/urlContent*}}` | Hygiene source owners | `LS_KEY_OWNERS`, `importWidgetRegistryFromJson`, `buildSelectionSubgraph`, `autoTuneFromHtml` | Full `hygiene:audit` near-threshold backlog | Four more source files below 600 lines | Split stable helper ownership while preserving export surfaces and cache semantics. | Continue with `graphCenteredSvg3d.ts`, `FlowEditorManagerRegistry.test.ts`, and `lazyLoadingGatesRegression.test.ts` next. | 2026-05-18 |
| Workspace FS shadow owner split | Continue next near-threshold storage closure | Move resilient shadow entries, merge, delete, and read helpers into a focused shadow owner while preserving the public workspace FS facade. | `knowgrph/canvas/src/features/workspace-fs/{workspaceFs.ts,workspaceFsShadow.ts}` | Workspace FS persistence | `snapshotShadowEntries`, `mergeEntriesWithShadow`, `buildShadowFileEntry` | Full `hygiene:audit` near-threshold backlog | Workspace FS facade below 600 lines | Keep fallback/shadow continuity in one owner; leave exported resilient filesystem API unchanged. | Continue with `config.ls.ts`, `FlowEditorMappingTab.tsx`, and `graph/file.ts` next. | 2026-05-18 |
| Flowchart renderer settings control split | Continue next near-threshold closure | Move compact row controls and breakpoint handling into a focused Flowchart controls owner without changing rendered labels, classes, or store bindings. | `knowgrph/canvas/src/features/toolbar/ui/{FlowchartRendererSettings.tsx,FlowchartRendererControls.tsx}` | Flowchart toolbar settings | `ToggleRow`, `NumberRow`, `SelectRow`, `useCompactControls` | Full `hygiene:audit` near-threshold backlog | Flowchart settings below 600 lines | Extract repeated control UI while keeping runtime preset/build logic in the settings facade. | Continue with `config.ls.ts`, `workspaceFs.ts`, and `FlowEditorMappingTab.tsx` next. | 2026-05-18 |
| Flow Editor inspector, zoom, and registry clean targets | Continue next full-audit hygiene closures | Move inspector tabs/buttons, Flow Editor overlay recentering, and registry JSON value editing into focused owners while preserving public exports and UI behavior. | `knowgrph/canvas/src/components/{FlowEditor/{FlowEditorInspector*,NodeOverlayEditorRegistrySection.tsx,NodeOverlayEditorJsonLikeValueEditor.tsx},FlowCanvas/{applyZoomRequestNative.ts,flowEditorOverlayRecenter.ts}}` | Flow Editor hygiene owners | `FlowEditorInspectorTabs`, `FlowEditorInspectorJsonButton`, `recenterFlowEditorOverlayWidgetPositions`, `JsonLikeValueEditor` | Full `hygiene:audit` near-threshold backlog | Three more source files below 600 lines | Move stable UI/control helpers out of broad components; preserve exported zoom helpers and registry editing behavior. | Continue with `FlowchartRendererSettings.tsx`, `config.ls.ts`, and `workspaceFs.ts` next. | 2026-05-18 |
| Parser, copy, and graph-view next clean targets | Continue full-audit near-threshold closures | Move frontmatter node placement, Graph Fields copy, and graph-view ID filtering into focused owners while preserving public keys and store semantics. | `knowgrph/canvas/src/{features/parsers/markdownFrontmatterFlowGraph.{nodes,nodePositions}.ts,lib/config-copy/{uiCopy,graphFieldsUiCopy}.ts,hooks/store/{graphViewSlice,graphViewIds}.ts}` | Hygiene source owners | `spreadMissingNodePositions`, `createGraphFieldsUiCopy`, `normalizeOpenWidgetNodeIds` | Full `hygiene:audit` near-threshold backlog | Three more source files below 600 lines | Extract stable helper ownership; preserve copy keys, graph-view filtering, and frontmatter placement behavior. | Continue with `FlowEditorInspector.tsx`, `config.ls.ts`, and `FlowchartRendererSettings.tsx` next. | 2026-05-18 |
| Store, toolbar, parser, and import-test clean targets | Continue next full-audit hygiene closures | Move PDF defaults, URL/export controls, Mermaid parsing helpers, and the large import regression into focused owners; keep changed-file gates green while reducing full-audit backlog. | `knowgrph/canvas/src/{hooks/store/{uiSliceInitialState.ts,uiSlicePdfImportInitialState.ts},lib/toolbar/{LaunchDropdown*,ImportUrlRendererSelect.tsx},features/parsers/markdownJsonLdMermaidParser*,__tests__/workspaceImport{Local,LargeCanvasPreset}.test.ts}` | Hygiene source owners | `createPdfImportInitialState`, `LaunchDropdownExportMenu`, `parseMermaidNodeString` | Full `hygiene:audit` and changed-file regression gate | Store, parser, and Launch dropdown files below 600; import test back under changed baseline | Split stable helper ownership without copying behavior or adding file-specific special cases. | Continue with near-threshold Markdown workspace, parser nodes, and config-copy owners from full `hygiene:audit`. | 2026-05-18 |
| Additional hygiene audit threshold closures | Fix other full-audit line failures | Split tooltip data, markdown data-view candidates, PDF observer forcing, GrabMaps settings grid, chat correction prompt, and remove stale registry helper. | `knowgrph/canvas/src/{components/GraphHoverTooltip*,features/{chat/floatingPanelChat/*CorrectionPrompt.ts,flow-editor-manager/registryTemplates.ts,markdown-workspace/main/{exports/exportPdf*,viewer/*DataViewCandidates.ts},toolbar/GrabMapsDiscovery*}}` | Source budget owners | helper extraction and stale helper deletion | Full `hygiene:audit` backlog | Six more source files below 600 lines | Move reusable data/helpers to focused owners; delete unused code instead of keeping compatibility residue. | Continue with MarkdownWorkspaceMain, parser nodes, and remaining near-threshold store/config owners. | 2026-05-18 |
| Webpage HTML view-switch hygiene preservation | Keep concurrent webpage pane fixes inside changed-file gates | Split the new HTML view-switch regression and move initial pane preset ownership into a hook without reverting the active webpage view fix. | `knowgrph/canvas/src/{__tests__/{markdownWorkspaceWebpageHtmlView.test.tsx,markdownWorkspaceWebpageHtmlViewSwitch.test.tsx,workspaceSwitchStaleGuard.test.ts},features/markdown-workspace/main/{MarkdownWorkspaceMain.tsx,useInitialWorkspacePaneVisibility.ts,types.ts,layout/MarkdownWorkspaceLayout.tsx},tests/registry/postParserCases2.ts}` | Markdown workspace webpage panes | `testMarkdownWorkspaceEditorHtmlViewSwitchShowsViewerPane`, `useInitialWorkspacePaneVisibility` | HTML webpage frontmatter view switching in editor workspace | Focused regression file and sub-baseline workspace main growth | Work with current dirty edits by preserving behavior while moving ownership to focused files; avoid reverting user/concurrent changes. | Continue splitting Markdown workspace main by viewer/export/scroll ownership until full `hygiene:audit` is green. | 2026-05-18 |
| Canvas performance panel split and docs sanity closure | Close the next `/goal` hygiene/docs slice | Recognize schema README companions and move canvas performance telemetry into a focused panel, keeping interaction controls sub-600 without behavior drift. | `knowgrph/{canvas/src/{cli/doc-sanity-check.ts,features/canvas/{InfiniteCanvasInteractionPanel.tsx,CanvasPerformancePanel.tsx}},docs/knowgrph-technical-architecture.md,todo-log.md}` | Canvas interaction/performance ownership | `listSchemaFilesFromDisk`, `CanvasPerformancePanel`, `InfiniteCanvasInteractionPanel` | Schema README companion files and canvas performance event state | Doc sanity green and interaction panel 454 lines with perf panel 164 lines | Schema companion detection belongs in doc sanity owner; performance readout belongs in canvas performance panel, not interaction control facade. | Continue full `hygiene:audit` backlog with Markdown workspace and other over-budget owners after preserving concurrent edits. | 2026-05-18 |
| Super-agent mobile-first responsive proof | Close the next `/goal` implementation gap after responsive goal wording | Emit shared responsive proof metadata, workspace frontmatter, verifier checks, and `responsive-proof.json`; keep proof classes source-owned and deterministic. | `knowgrph/{knowgrph_parser/superagent_{responsive,renderers,verifier,proof_manifest}.py,knowgrph_parser/superagent_harness_test.py,README.md,docs/documents/knowgrph-superagent-harness.md,todo-log.md}` | Responsive harness proof contract | `build_responsive_layout_metadata`, `build_responsive_verification_checks`, `responsive_evidence_from_checks` | Mobile-first goal contract and neutral harness run | 12-artifact goal run with responsive checks passing for mobile/tablet/desktop/wide classes plus AgenticRAG README mirror note. | Static proof belongs in the harness graph/workspace metadata before browser smoke checks; avoid route-specific mobile patches or duplicate component trees. | Continue with live browser responsive smoke checks against generated `rich-media-flow.md`, then Cloudflare route parity after publish sync. | 2026-05-18 |
| Flow Editor overlay surface split | Remove active hook line-budget debt without behavior drift | Extract overlay JSX composition, overlay-only/frontmatter graph exclusion, and runtime store selection into focused helpers; update source assertions to canonical owners. | `knowgrph/canvas/src/{components/FlowEditorCanvas{.runtime.tsx,/runtime/{useFlowEditorOverlaySurface.tsx,flowEditorOverlaySurfaceElements.tsx,flowEditorOverlaySurfaceVisibility.ts,useFlowEditorRuntimeStoreState.ts}},__tests__/{richMediaPanelWidget.test.ts,flowEditorOverlayLayeringRegression.test.ts}}`, `docs/documents/knowgrph-renderer-document.md`, `todo-log.md` | Flow Editor overlay surface runtime | `buildOverlayEditorElements`, `buildFrontmatterOverlayHideSafety`, `resolveOverlayOnlyActive`, `buildFlowCanvasGraphDataOverride`, `useFlowEditorRuntimeStoreState` | Runtime render graph, overlay ids, rich-media connected values, store selectors | Sub-600 runtime facade and overlay hook plus small policy/render/store helpers with focused regression coverage | Keep overlay policy in one helper and JSX composition outside the hook; remove stale coverage ref/key and unused hook parameter instead of preserving no-op state. | Continue next with `useFlowEditorOverlayEdges.ts`, then `useFlowEditorOverlayCollision.ts` and `useFlowEditorRuntimeScene.ts`. | 2026-05-18 |
| Super-agent harness ownership split | Remove active Python harness line-budget debt without changing CLI/API behavior | Split contracts, plan, tools, verifier, renderers, and utilities into focused modules; keep `superagent_harness.py` as the run-loop facade. | `knowgrph/knowgrph_parser/{superagent_harness.py,superagent_{contracts,plan,tools,verifier,renderers,utils}.py}`, `docs/documents/knowgrph-superagent-harness.md`, `todo-log.md` | Super-agent harness modules | `SuperAgentHarness`, `build_default_tool_registry`, `tool_judge_verify`, `render_final_report` | Existing goal run, neutral fixture, state/trace artifacts | Sub-600 harness modules with unchanged public imports and green deterministic run | Preserve public imports from `superagent_harness.py`; split by ownership instead of adding aliases or downstream wrappers. | Continue with Flow Editor runtime files: `useFlowEditorOverlayEdges.ts`, `useFlowEditorOverlayCollision.ts`, then `useFlowEditorRuntimeScene.ts`. | 2026-05-18 |
| Goal harness run and JSON writer repair | Make `/goal` runnable through the Codex-compatible harness | Import the shared `write_json` helper, run `run-goal` with deterministic mock providers, and document the CLI alias. | `knowgrph/{knowgrph_parser/superagent_harness.py,docs/documents/{knowgrph-api-document.md,knowgrph-superagent-harness.md},todo-log.md,data/outputs/goal-run-current}` | Super-agent harness | `_persist_goal`, `_save_state`, `run-goal` | `/Users/huijoohwee/Documents/GitHub/knowgrph/goal` | Completed run artifacts, trace, state, report, proof manifest | The source-owned harness failed before writing `goal.json`; importing the existing common JSON writer fixes all state/goal/proof persistence paths without a new helper. | Continue with the next over-budget source owner, starting from Flow Editor runtime or storage sync files surfaced by `wc -l`. | 2026-05-18 |
| Goal operating contract and cross-repo docs sync | Keep the rich-media refactor goal aligned with Dev->Prod->Cloudflare ownership and hygiene gates | Add execution topology, hygiene, architecture, pipeline, validation, and post-task docs rules to `goal`; mirror the contract in README, API, topology, and AgenticRAG schema notes. | `knowgrph/{goal,README.md,docs/documents/{knowgrph-api-document.md,knowgrph-cross-repo-publish-topology.md},todo-log.md}`, `huijoohwee.github.io/schema/AgenticRAG/README.md` | Goal contract docs | goal update, docs mirror | User-supplied Dev/Prod/Cloudflare and hygiene directives | Canonical upstream goal plus focused cross-repo/API docs notes | Source-owned goal is the authority; downstream publish/schema docs only mirror the validated upstream contract. | Continue next in `/GitHub/knowgrph/{todo-log.md,docs/}` and `/GitHub/huijoohwee.github.io/schema/AgenticRAG`; run focused hygiene/docs sync checks after related source changes. | 2026-05-18 |
| Flow Editor runtime refactor slice | Reduce code-budget debt without behavior drift | Extract repeated runtime LRU cache ownership into one helper; update stale regression assertions to canonical source files. | `knowgrph/canvas/src/{components/{FlowCanvas/useFlowComputedPositions.ts,FlowEditorCanvas/runtime/{flowEditorRenderGraph.ts,flowEditorRuntimeCache.ts}},__tests__/flowEditorFrontmatterFlowWidgetAndTypedHandlesRegression.test.ts}` | Flow Editor render graph cache | `readFlowEditorRuntimeCacheEntry`, `writeFlowEditorRuntimeCacheEntry` | Render graph cache maps | Shared bounded cache helper and sub-600 render graph and FlowCanvas computed-position files | Reuse one cache primitive instead of four duplicate read/write implementations; tests inspect upstream owners after refactors. | Continue with `useFlowEditorOverlayCollision.ts`, `useFlowEditorOverlayEdges.ts`, then `useFlowEditorRuntimeScene.ts`. | 2026-05-18 |

## 2026-05-09

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| DeerFlow super-agent harness integration for Canvas rich media pipeline | Enable DeerFlow as first-class optional provider for text/image/video generation on Canvas Flow Editor, with Dev+Prod support and Cloudflare Tunnel for prod gateway | Remove BytePlus hard-force in runtime dispatch, add DeerFlow API reference index, create setup guide (v4.0) with Dev/Prod/Cloudflare Tunnel instructions, add `dev:all` script, eliminate repo-wide hardcoded paths, build-sync prod artifacts | `knowgrph/{canvas/src/{components/FlowEditorCanvas/runtime/useFlowEditorWorkflowActions.ts,features/panels/views/deerflowApiDocs.ts,lib/chatEndpoint.ts,features/flow-editor-manager/registryTemplates.ts,features/chat/richMediaRun.ts,hooks/store/uiSliceChat.ts},scripts/dev-all.sh,docs/documents/{knowgrph-deerflow/knowgrph-deerflow-setup-guide.md,knowgrph-api-reference/api-reference-codebase-index_202604261230/knowgrph-deerflow-gateway-api-reference-codebase-index.md},knowgrph-api-reference/api-reference-codebase-index_202604261230/knowgrph-byteplus-modelark-chat-api-reference-codebase-index.md},package.json,todo-log.md}` | Runtime dispatch + DeerFlow API docs + setup guide + dev:all script | `normalizeChatProviderId`, `buildChatProxyHeaders`, `DEERFLOW_API_REQUEST_DOC_ENTRIES`, `dev:all` npm script | Active provider from store, proxy headers, DeerFlow SSOT rows | Provider-normalized dispatch, no BytePlus hard-force, 15-row DeerFlow API index, v4.0 setup guide | Dispatcher derives provider from `normalizeChatProviderId(store.chatProvider)` with provider-scoped endpoint fallback; DeerFlow is optional first-class provider; default experience is OpenAI-powered; no DeerFlow = no impact on any feature | Run `npm run pages:check-sync` after future source changes; update setup guide if DeerFlow image/video adapter is implemented (PRD-E003-S001). | 2026-05-09 |
| CLI generation script for API reference codebase index files | Enable deterministic regeneration of all API reference index markdown files from TypeScript SSOT source, with drift detection and provider-agnostic pipeline | Create `generateApiCodebaseIndex.ts` CLI with 8 INDEX_SPECS, `--check` drift mode, `--verbose` logging, filename/provider filtering; add `api-index:generate` and `api-index:check` npm scripts; generate `api-reference-update-guidelines.md` following PRD-TAD CID framework; regenerate all 8 index files (173 total rows) | `knowgrph/{canvas/src/cli/generateApiCodebaseIndex.ts,package.json,docs/documents/knowgrph-api-reference/api-reference-codebase-index_202604261230/*.md}`, `huijoohwee.github.io/guidelines/api-reference-update-guidelines.md` | CLI script + npm scripts + guidelines doc | `generateIndex`, `buildRow`, `normalizeRow`, `npm run api-index:generate`, `npm run api-index:check` | TypeScript SSOT source files (7 providers) | 8 markdown index files with 18-column tables | Script imports SSOT exports via `pathToFileURL`, normalizes flat and VirtualSettingsEntry row shapes, infers endpoint/kind/type/required/pattern from metadata; `--check` compares generated vs committed; guidelines doc follows CID framework with phase-gated process | Integrate `api-index:check` into CI/pre-push hook; add new provider specs to INDEX_SPECS when onboarding new integrations. | 2026-05-09 |
| Gemini Veo Video API integration for MainPanel Integrations | Enable Google Gemini Veo as first-class video generation provider with full parameter control (model, aspect ratio, resolution, duration, person generation) | Add provider registration in chatEndpoint.ts, create SSOT with 9 doc rows, add 5 settings registry entries with dropdowns, implement runtime dispatch with async polling, wire Integrations view with section/dropdown/anchor support, add CLI index spec | `knowgrph/{canvas/src/{lib/chatEndpoint.ts,features/integrations/{geminiVideoGenerationSsot.ts,geminiVideoGenerationDefaults.ts},features/chat/geminiRunGeneration.ts,features/settings/registry-ui.ui.ts,features/panels/views/{useSettingsView.ts,geminiVideoGenerationApiDocs.ts},lib/config.ls.keys.ts,cli/generateApiCodebaseIndex.ts},docs/documents/knowgrph-api-reference/api-reference-codebase-index_202604261230/knowgrph-gemini-veo-video-generation-api-reference-codebase-index.md}` | Provider registration + SSOT + runtime dispatch + Integrations wiring | `CHAT_PROVIDER_GEMINI`, `generateRunVideoWithGemini`, `GEMINI_VIDEO_GENERATION_API_DOC_ENTRIES`, `buildGeminiVideoGenerationFields` | Provider from store, prompt, widget defaults from localStorage | GeneratedBinaryAsset (blob + renderUrl) or error with failure context | Provider normalization routes `gemini`/`google-gemini`/`google` to CHAT_PROVIDER_GEMINI; runtime dispatches POST to `/v1beta/models/{model}:predictLongRunning`, polls operation status, downloads video via proxy; 5 dropdown overrides in resolveIntegrationEntryMeta | Add proxy auth header mapping for `x-goog-api-key` (Gemini uses different auth header than Bearer); add flow editor widget template using `buildGeminiVideoGenerationFields()`. | 2026-05-09 |

## 2026-05-08

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Progressive Web App (PWA) enhancement | Enable install-to-homescreen, offline shell, deferred install UX, and Web Share API | Add apple-touch-icon, manifest link, Service-Worker-Allowed headers, beforeinstallprompt capture with toolbar install button, share_target manifest entry with bootstrap handler, and six regression tests. | `knowgrph/{canvas/{index.html,public/{_headers,apple-touch-icon.png,favicon.svg},vite.config.ts,src/{lib/pwa/runtime.ts,components/Toolbar.tsx,features/canvas/CanvasQueryBootstrapRuntime.tsx,lib/routing/queryParams.ts,lib/config-copy/uiMeta.ts,__tests__/pipelinePwaEnhancementsRegression.test.ts,tests/registry/postParserCases1.ts}},docs/documents/knowgrph-pwa.md,todo-log.md}` | PWA runtime + toolbar install button + share handler | `installPwaRuntime`, `getDeferredInstallPrompt`, `promptPwaInstall`, `testPwaToolbarInstallButtonWiresDeferredPrompt`, `testPwaShareQueryParamsHandledInBootstrap` | beforeinstallprompt event, ?share=1 URL params | DOM data-kg-installable attribute, install dialog, shared-content toast | MutationObserver on data-kg-installable drives toolbar button visibility; share handler reads params, shows toast, cleans URL; all guarded by refs to prevent double-handling | Run `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` after adding PWA doc to schema map. | 2026-05-08 |
| Public document view endpoint + deep link canvas rendering | Enable sharing readable URLs for D1 documents and rendering them directly in the knowledge graph canvas | Add `GET /api/storage/doc/:workspaceId/:canonicalPath*` Worker route returning `text/markdown`, add `docPrefix` to sync contract, create `CanvasDocDeepLinkRuntime` that reads `?kgPath=` and imports via `importWorkspaceUrl()`, deploy Worker and SPA. | `knowgrph/{cloudflare/workers/knowgrph-storage/index.ts,canvas/src/{lib/storage/knowgrphStorageSyncContract.ts,features/canvas/CanvasDocDeepLinkRuntime.tsx,pages/Canvas.tsx},docs/documents/knowgrph-storage-sync-document.md,todo-log.md}` | Worker route handler + deep link runtime | `handleDocView`, `CanvasDocDeepLinkRuntime`, `KNOWGRPH_STORAGE_ROUTE_PATHS.docPrefix` | URL path with workspaceId and canonicalPath | `text/markdown` response or 404; canvas-rendered document via SPA deep link | Worker splits URL on first `/` after prefix, queries D1 with `deleted = 0` filter; SPA normalizes `/doc/` paths to `?kgPath=`, runtime constructs API URL and calls import pipeline | Add D1→filesystem export script for git-backed backup; update workspace creation flow for multi-member SSOT transition. | 2026-05-08 |
| Default workspace import source URL setting | Allow users to configure a URL as the default workspace seed source via Settings | Add `workspace.import.defaultSourceUrl` to LS keys, settings registry, and seed provider priority chain; reuse `fetchWorkspaceUrlContent()` for URL fetch step. | `knowgrph/{canvas/src/{lib/config.ls.keys.ts,lib/workspace/workspaceStoreSyncSettings.ts,features/settings/registry-ui.workspace.ts,features/workspace-fs/workspaceSeedProvider.ts},docs/documents/knowgrph-storage-sync-document.md,todo-log.md}` | Settings registry + seed provider | `readWorkspaceImportDefaultSourceUrlSetting`, `readWorkspaceDocsMirrorEntriesFromDefaultSourceUrl` | URL string from localStorage | WorkspaceDocsMirrorEntry[] from fetched content | Read/write helpers use `getLocalStorage()` directly (no `lsString` helper exists); seed provider dynamically imports `fetchWorkspaceUrlContent` and converts result to mirror entry | Document the setting in user-facing settings documentation; consider adding placeholder/hint text to the SettingMeta entry. | 2026-05-08 |

## 2026-05-04

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Broader repo-note archival cleanup for panel ownership wording | Remove stale bottom-panel and curator ownership language from repo-level notes while preserving truthful history | Normalize broader markdown notes and generated previews to canonical markdown workspace / panel wording, and add explicit historical labels where old file paths remain only as archive context. | Historical path snapshot: `knowgrph/{CodeWiki.md,knowgrph-codebase-responsibility-flow.md,docs/documents/knowgrph-local-storage-document.md,data/knowgrph-workflow-preview/knowgrph-local-storage-document-document.md,todo-log.md}` | Broader repo narrative + archival note hygiene | wording cleanup, historical-context labels | Repo-level markdown notes, generated preview outline, archival wording residue | Current notes match canonical panel/runtime ownership and archival references no longer read like active file contracts | Update current-state narrative at the source documents first, then align generated/preview notes; keep historical rows truthful but explicitly archival instead of silently preserving stale ownership language. | Re-sweep broader repo markdown after future panel/runtime renames and regenerate preview artifacts from canonical docs if the local-storage source outline changes again. | 2026-05-04 |
| Markdown workspace wrapper/root cleanup + curator residue removal | Remove markdown-cluster bottom-panel wrapper churn so the runtime, types, and lazy load path resolve from canonical upstream modules only | Repoint markdown workspace imports to `lib/markdown-workspace-runtime`, source layout/types from `workspaceUi` and `markdownUtils`, delete dead wrapper/model/curator files, remove the stale Vite alias, and document the runtime from the canonical workspace source only. | Historical path snapshot: `knowgrph/{canvas/src/{components/{EmbeddedEditorShell.tsx,BottomPanel/*,__tests__/*},lib/markdown-workspace-runtime/MarkdownWorkspaceRuntime.impl.tsx},canvas/vite.config.ts,docs/documents/{knowgrph-markdown-processing-document.md,knowgrph-frontend-document-interactions-and-operations.md,knowgrph-source-files-import-document.md},todo-log.md}` | Markdown workspace runtime SSOT + markdown shared types | direct runtime imports, `MarkdownWorkspaceLayoutMode`, `MarkdownSelectionInfo`, doc contract updates | Markdown workspace lazy loads, markdown layout type usage, selection-info typing, legacy wrapper paths, stale docs | Canonical runtime imports, shared workspace/layout types, removed dead wrappers and curator residue, updated docs/config | Fix markdown workspace ownership at the root runtime and shared helpers, never by keeping wrapper aliases alive; remove dead markdown/curator surfaces entirely once direct imports and shared types cover all active call sites. | Re-run focused markdown workspace tests after future runtime/path refactors and keep any downstream schema/doc mirrors sourced from these canonical Knowgrph runtime documents only. | 2026-05-04 |
| Panel SSOT cleanup + widget-mode semantic cache docs sync | Keep panel ownership and widget bundle hot-path behavior documented from the canonical upstream sources only | Move shared panel events/default sizing into `features/panels`, remove legacy `features/bottom-panel` helpers, document widget bundle gating + semantic subgraph caching once, and forbid duplicate bottom-panel helper namespaces or raw-identity subgraph rebuilds. | Historical path snapshot: `knowgrph/{canvas/src/{features/panels/*,lib/{graph/lookupCache.ts,markdown-workspace-runtime/useMarkdownWorkspaceWidgetMode.ts},components/BottomPanel/*,__tests__/*},docs/documents/{knowgrph-frontend-document.md,knowgrph-markdown-processing-document.md},todo-log.md}` | Panel surface SSOT + widget-mode graph cache contract | `getCachedGraphSubsetByNodeIds`, historical event name `openBottomPanel`, doc contract updates | Panel activation events, widget node-id subsets, graph revision, semantic graph key, canonical docs | Shared panel surface owner, removed legacy helper namespace, widget-mode subgraph cache reuse, updated canonical docs | Fix ownership at the root `features/panels` surface, key widget subgraphs by semantic signatures plus revision, and document the runtime once in canonical docs instead of preserving legacy aliases or downstream copy drift. | Re-run focused panel/widget regressions after future panel-surface or widget-mode edits and mirror any later contract wording changes into AgenticRAG schema docs only from these canonical Knowgrph docs. | 2026-05-04 |
| Storage/Worker/client sync/runtime/conflict UX docs sync | Keep implemented local-first storage, Cloudflare sync, inbound pull-apply, and shared conflict UX documented across repos without drift | Keep `knowgrph-storage-document.md` as the sub-600 canonical index, move detail into companions, mirror shared toast/log/runtime conflict actions in AgenticRAG docs, and refresh the docs map from canonical sources only. | `knowgrph/{todo-log.md,docs/documents/knowgrph-storage-document*.md}`, `huijoohwee.github.io/schema/AgenticRAG/{README.md,documentation.jsonld,markdown.jsonld,panels.jsonld,knowgrph-documents-map.graph.jsonld}` | Storage docs + AgenticRAG sync contract | continuation-link sharding, `sync_map.py --mode write`, schema note updates | Implemented storage/sync/conflict UX files plus canonical storage docs and schema guidance | Sub-600 canonical storage index, two continuation companions, aligned schema guidance, refreshed docs map | Keep one canonical storage index, shard only at stable section boundaries, describe conflict actions once through the shared toast/log/runtime path, and keep schema artifacts generated from the canonical doc set. | Run `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` and re-check edited doc line counts after future storage-doc updates. | 2026-05-04 |
| Knowgrph + Singabldr publish topology contract | Keep Knowgrph source isolated while sharing one Cloudflare Pages publish boundary with Singabldr | Keep `knowgrph` source/build/release logic in its own repo, sync only Knowgrph publish artifacts into `huijoohwee`, and keep `airvio.co/knowgrph` route ownership fully separated from `airvio.co/singabldr`. | `knowgrph/{todo-log.md,docs/documents/knowgrph-cross-repo-publish-topology.md,docs/documents/knowgrph-integrations-ssot-sync-directives.md}`, `huijoohwee.github.io/{docs/documents/{hjh-topology-document.md,hjh-workspace-todo-log.md},schema/AgenticRAG/{README.md,documentation.jsonld,knowgrph-documents-map.graph.jsonld}}` | Cross-repo topology + docs sync boundary | `sync_map.py --mode write`, docs updates | Existing Knowgrph Dev repo, shared publish repo, and public route contract | Canonical topology companion doc, shared HJH topology doc, updated sync directives, and refreshed schema/docs map | Keep Knowgrph upstream-only, keep publish artifacts generated, and forbid cross-app source trees, route shims, or publish-repo hand edits. | Re-run `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` after future topology, route, or publish-boundary changes. | 2026-05-04 |


## 2026-05-03

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Initialization-file UI import activation + cross-repo doc sync | Formalize frontmatter-driven landing so imported preset docs cannot be overwritten by stale selected-doc replay | Promote the first imported preset document to active raw-frontmatter authority before composed replay; document canonical landings for `README.md`, `knowgrph-video-demo.md`, and `knowgrph-maps-grabmap-multim-demo.md` across Knowgrph docs and AgenticRAG schema references. | Historical path snapshot: `knowgrph/{canvas/src/components/BottomPanel/markdownWorkspace/useWorkspaceFileActions/importActions.ts,canvas/src/features/workspace-fs/applyWorkspaceImportToCanvas.ts,docs/documents/*,todo-log.md}`, `huijoohwee.github.io/schema/AgenticRAG/{README.md,canvas.jsonld,markdown.jsonld,pipeline.jsonld}` | Workspace import activation + frontmatter landing contract | `activateFirstImportedWorkspaceFile`, `applyWorkspaceImportToCanvas`, doc/schema contract updates | UI local-file imports of frontmatter-preset Markdown plus initialization-file seed sources | Deterministic imported-document activation and cross-repo documentation of D3, Flow Editor, and Geospatial frontmatter landings | Keep active-path promotion upstream in the exact UI import flow, preserve frontmatter as the landing SSOT, and forbid stale composed replay or helper logic from overwriting the imported preset document. | Run `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` and re-smoke UI imports for the canonical three-file family after the next doc/schema batch. | 2026-05-03 |
| Flow Editor import flash root fix + seed-order centralization | Stop post-import layout mutation where Flow Editor lands correctly then snaps to other-renderer cached positions | Centralize layout-seed ordering in shared helpers and make Flow Editor frontmatter/document imports prefer source-seeded positions before cached positions from other renderers; keep other-renderer caches as fallback only when imported source coverage is insufficient. | `knowgrph/{canvas/src/lib/canvas/layoutSeed.ts,canvas/src/components/FlowCanvas/{seed.ts,useFlowComputedPositions.ts},canvas/src/__tests__/{flowSeedOtherRendererVariant.test.ts,workspaceImportVideoDemoRendererIsolation.test.tsx},canvas/src/tests/registry/{postParserCases1.ts,postParserCases3.ts},todo-log.md}` | Shared layout seed ordering + Flow Editor import isolation | `pickPreferredLayoutSeed`, `useFlowComputedPositions`, focused runtime/import regressions | Local-file import of `knowgrph-video-demo.md` with prior non-Flow-Editor renderer cache state | Stable Flow Editor landing with no delayed Flow Canvas-style layout snap and retained widget visibility | Fix seed precedence upstream in shared helpers, never by renderer-local patching; use imported source positions first in Flow Editor frontmatter/document mode, then fall back to other seeds only when source coverage is not stable enough. | Keep re-smoke import checks focused on `knowgrph-video-demo.md` after future layout/cache changes and re-run the seed/runtime isolation regressions before widening test scope. | 2026-05-03 |
| Flowchart renderer/runtime migration + sibling fixture promotion | Finish the legacy renderer cleanup by making Flowchart the only public renderer/runtime contract and documenting the upstream fixture path | Canonicalize renderer/store/runtime ids to `flowchart`, expose `/__flowchart_fixture`, promote `hackamap-flowchart-fixture.json` into sibling content/project trees, and remove residual legacy fixture names from code, archives, and docs. | `knowgrph/{canvas/src,canvas/vite.config.ts,todo-log.md,docs/{documents/knowgrph-renderer-document.md,documents/knowgrph-pipeline-document.md,knowgrph-technical-architecture.md}}`, `huijoohwee/content/hackamap`, `project/prjt4000-hackamap/{site,_archive}` | Flowchart renderer + Hackamap fixture contract | `normalizeFlowchartApiGraphData`, `buildFlowchartSourceMeta`, `resolveHackamapFlowchartFixturePath`, doc contract updates | Legacy renderer ids, fixture route names, sibling Hackamap payload files, renderer/pipeline docs | One Flowchart SSOT across runtime code, upstream fixture files, archive naming, and nearby documentation surfaces | Keep sibling fixture data promoted under Flowchart basenames, keep canvas resolver source-owned with `KNOWGRPH_FLOWCHART_FIXTURE_PATH`, and treat any remaining legacy renderer text in docs or runtime paths as stale residue to remove at the root. | Run `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` after the next cross-repo docs batch and smoke-check `http://localhost:5181/__flowchart_fixture` when the dev server is up. | 2026-05-03 |
| Flowchart migration final wrap-up + cross-repo archive labeling | Close the migration by aligning schema companions, frontmatter templates, and historical content handling with the Flowchart SSOT | Keep live cross-repo docs/templates canonical on `flowchart`, mark dated bipartite-era content as historical archive, and validate sync/JSON outputs without rewriting truthful build snapshots. | `knowgrph/{todo-log.md,docs/documents/*}`, `huijoohwee.github.io/{schema/AgenticRAG/{README.md,README.pipeline-and-renderer-guidelines.md,canvas.jsonld,knowgrph-documents-map.graph.jsonld},template/{yaml-frontmatter-guidelines.md,knowgrph-yaml-frontmatter-template.md},content/{pmf/PMF.{md,json},full-stack/full-stack-foss.{md,json}}}` | Flowchart docs/template/archive cleanup | `sync_map.py --mode {check,write}`, schema/template text normalization, archive label insertions | Flowchart migration residue in AgenticRAG docs, frontmatter templates, and historical content metadata surfaces | Cross-repo Flowchart terminology normalized in live contract surfaces, docs-map clean, and archive artifacts explicitly labeled instead of silently carrying stale names | Normalize live contract surfaces at the root, but preserve dated product artifacts by labeling them as archive snapshots when historical bipartite wording remains contextually true. | Re-run `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` after future doc batches; only rename archive content if a page is promoted from historical snapshot to current-facing product copy. | 2026-05-03 |
