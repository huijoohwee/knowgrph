# todo-log

- **Canonical directive**: **STRICTLY COMPLY** with one-row-one-directive (Max 50 words)
- **Table cells**: MUST fill-up all cells in the table; FORBID empty/`-` cells.

## 2026-02-19

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Canvas / 2D renderer initialization | Keep 2D initialization consistent across variants | Update cross-repo docs: 2D init is bounds-guarded + idempotent; skip invalid positions; stable node dims for fit; warm-mount effect gating. | `knowgrph/todo-log.md`, `knowgrph/docs/documents/{knowgrph-renderer-document.md,knowgrph-canvas-document.md,knowgrph-canvas-default-initialization-document.md,knowgrph-flow-editor-pan-zoom-overlay-failsafe-document.md}`, `huijoohwee.github.io/schema/AgenticRAG/{canvas.jsonld,README.md}` | Cross-repo documentation | Update Markdown + JSON-LD contract text | Renderer init invariants | Updated docs/contract text | Enforce SSOT parity across D3/Flow/Design/Flow Editor; forbid double-fit jumps; guard bounds before applying stored transforms; ignore invalid geometry to prevent one-long lines. | Run `python3 schema/AgenticRAG/sync_map.py --mode check` to validate docs↔schema alignment. | 2026-02-19 |
| Canvas / Flow Editor / Node Quick Editor / Layout Initialization | Prevent initial clustering and enforce centered layout | Fix Flow Editor Node Quick Editor initialization: calculate a centered grid layout for unpinned editors instead of top-left stacking; update collision relaxation to center the grid; forbid clustering/overlaying on one corner. | `canvas/src/components/FlowEditorCanvas.tsx` | FlowEditorCanvas | `scheduleOverlayCollisionResolve`, `seedGridAroundFixed` | Initial overlay node list + viewport dims | Centered, non-overlapping grid layout | Count unpinned items; compute grid cols/rows; calculate start offsets to center the grid in viewport; use centered grid for fallback positions and relaxation seeding. | Verify initial open of multiple Quick Editors shows a centered, spread-out layout. | 2026-02-19 |
| Canvas / 2D Renderer / D3 Initialization | Prevent initial clustering and off-center layout | Fix D3 renderer initialization: default to 16:9 preset when viewport is small; update centering forces on resize to respect new dimensions; strict validity check for heuristic seeding to force re-layout if nodes are clustered. | `canvas/src/components/GraphCanvas/{simulation.ts,GraphCanvas.tsx}`, `canvas/src/components/GraphCanvas/layout/heuristic-cluster.ts` | Simulation + GraphCanvas | `buildSimulation`, `updateForceSimulationPresentation`, `applyClusterAwareHeuristicSeedLayout` | Initial nodes/edges + viewport dims | Centered, well-spread layout | Use preset dims if width < 100; update x/y forces in presentation update; reject clustered positions in heuristic check. | Verify D3 graph starts centered and spread out even if initial viewport is small/zero. | 2026-02-19 |

## 2026-02-17

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Docs / Graph Data Table / Cross-repo contract | Keep Graph Data Table docs accurate across repos | Update Graph Data Table docs across repos: distinguish host fast-grid vs curagrph curation table; host fast-grid uses single scroll owner + DOM header overlay synced to body scroll; persist host column order via `kg:ui:graphTable:columnOrderByTableId`; keep frozen bands/width-sharing and empty-graph behavior specified for curation table. | `knowgrph/docs/documents/{curagrph-graph-data-curation-document.md,knowgrph-graph-data-curation-document.md}`, `huijoohwee.github.io/schema/AgenticRAG/{README.md,panels.jsonld}`, `knowgrph/todo-log.md` | Cross-repo documentation | Update Markdown + JSON-LD contract text | Graph Data Table behavior updates | Updated docs/contract text | Clarify ownership boundaries and UI behavior contracts; document drift-prone details as SSOT rules. | Run `python3 schema/AgenticRAG/sync_map.py --mode check` and ensure docs map stays consistent. | 2026-02-17 |
| Graph Data Table / Fast Grid / Column reorder + stale-cache prevention | Make Graph Data Table reorderable and consistent with Source Files state | Implement drag-to-reorder columns in host fast-grid header with drop hint; persist via sparse RxDB column ordering; fix per-table row cache isolation; ensure sourceFiles mutations recompute composed graph so empty Source Files yields empty tables (no stale rows). | `canvas/src/features/{graph-table/ui,graph-table-db,source-files}`, `canvas/src/__tests__/graphTableTypography.test.tsx` | GraphTableFastGrid + GraphTableDb + SourceFilesPersistenceBootstrap | `reorderGraphTableColumn`, `drawGrid`, `SourceFilesPersistenceBootstrap` | Header pointer drag + sourceFiles mutations | Reordered columns + cleared tables when no sources | Use pointerDrag + hitTest to compute target; patch only moved column order when possible; subscribe to sourceFiles and recompute graph on change. | Smoke: remove all Source Files, confirm graph/tables empty; drag header to reorder and reload to confirm persistence. | 2026-02-17 |
| Graph Data Table / Import→Render / DateEditor | Make date editing work end-to-end from imports | Infer ISO-like date property columns as `kind: date` (upgrade from text when safe) so DateEditor opens for imported values; key RxDB sync by revision + collapsedGroupIds; forbid mode-derived sync rewrites and no-op churn. | `canvas/src/features/{graph-table-db,graph-table/ui,graph-table/hooks}`, `docs/documents/knowgrph-graph-data-table-fast-grid-document.md` | GraphTableDb + GraphTableWorkspace | `inferKind`, `ensureColumnsForRowData`, `useGraphTableDbSync` | Imported GraphData + collapse toggles | Date columns + stable sync | Only label as date when all non-empty values are ISO-like; include viewKey in sync gate to avoid missed updates/loops. | Import a graph with `created: 2026-02-17`, open table, edit date, toggle group collapse; confirm stable table. | 2026-02-17 |

## 2026-02-18

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Workspace Layout / Editor + Graph Data Table / Canvas SSOT | Remove duplicate canvas previews and keep one live SVG | Make Editor workspace and Graph Data Table reuse a single in-app Canvas pane (`CanvasViewport`) and forbid any second “Canvas Preview” surface in these modes; reserve `?kgPreview=1`/`data-kg-preview="1"` iframe rendering for external embedded preview only. | `canvas/src/pages/Canvas.tsx`, `canvas/src/components/{EmbeddedEditorShell.tsx,CanvasViewport.tsx}`, `docs/documents/{knowgrph-canvas-document.md,knowgrph-ui-ux-design-document.md,knowgrph-local-storage-document.md}`, `huijoohwee.github.io/schema/AgenticRAG/panels.jsonld` | CanvasPage + Workspace mode docs | `CanvasPage`, `CanvasViewport` | Workspace view mode toggles | One live Canvas SVG resized into right pane | If mode is Editor or Table: render left tool surface + right canvas pane; do not mount a second Canvas or iframe preview; persist canvas pane width via `LS_KEYS.workspacePreviewWidthPx`. | Run `npm -C knowgrph/canvas test:ci` and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` to validate contract alignment. | 2026-02-18 |
| Flow Editor / Node Quick Editor / Drag behavior | Align pinned/unpinned drag behavior and docs across repos | Document pinned header drag as anchor-offset update that moves all pinned overlays together; unpinned drag moves freely in viewport and persists position; avoid bounce/pan proxy behavior. | `knowgrph/docs/documents/{knowgrph-flow-editor-node-quick-editor-document.md,knowgrph-renderer-document.md,knowgrph-canvas-document.md,knowgrph-graph-data-table-fast-grid-document.md}`, `huijoohwee.github.io/schema/AgenticRAG/{panels.jsonld,canvas.jsonld}`, `knowgrph/todo-log.md` | Node Quick Editor contract | Documentation updates | Flow Editor drag requirements | Updated docs and schema contracts | Keep pinned overlays node-anchored while allowing collective offset drag; keep unpinned overlays free and clamped. | Run `npm -C knowgrph/canvas run doc:lint` and `python3 huijoohwee.github.io/schema/AgenticRAG/sync_map.py --mode check` to validate docs sync. | 2026-02-18 |

## 2026-02-15

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Import→Render / Local In-Repo URL / Performance | Support local file imports and optimize rendering | Support local in-repo Import URL + assets via `/__webpage_proxy`, `/__webpage_asset_proxy`, `/__repo_file/*`; render HTML/JSON via sandboxed `srcdoc`; cap HTML size and idle-yield sanitization; keep progress for large imports. | `canvas/vite.config.ts`, `canvas/src/lib/websites/webpageIframeSrcdoc.ts`, `docs/documents/knowgrph-webpage-proxy-and-srcdoc-iframe-document.md` | Vite middleware + Srcdoc builder | `createWebpageProxyHandler`, `createRepoFileHandler`, `fetchWebpageHtmlViaProxy` | Local file path + View mode | Sandboxed `srcdoc` rendering of local files + smooth UI | Resolve paths relative to repo root; block traversal; use `__repo_file` baseHref for relative assets; yield during sanitization. | Import `sandbox/test-data/my-site/index.html` and confirm CSS/images load in iframe without freezes. | 2026-02-15 |
| Import→Render / Webpage JS-render fidelity (native) | Remove headless browser dependencies while preserving JS-rendered content capture | Replace Playwright-based rendering with a browser-native, sandboxed iframe DOM export bridge (`kg-export-dom`) and use it to upgrade webpage Markdown when static conversion is incomplete; keep the approach domain-neutral and bounded, and forbid external headless browser dependencies. | `canvas/src/lib/websites/{webpageDomExport.ts,webpageClientConvert.ts,webpageMarkdownQuality.ts,webpageIframeSrcdoc.ts}`, `canvas/src/components/BottomPanel/markdownWorkspace/workspaceImport.ts`, `canvas/vite.config.ts` | DOM export bridge + client convert fallback | `exportWebpageDomViaHiddenIframe`, `convertWebpageUrlToMarkdownViaBrowser`, `isLowQualityWebpageMarkdown`, `fetchWorkspaceUrlContent` | URL + bounded export options (maxChars/timeout/scrollCrawl/expandFaq) | Upgraded Markdown for JS-heavy pages + unchanged behavior for static pages | If the initial conversion is low quality, export rendered DOM text/HTML via hidden iframe → convert to Markdown → keep the better result; otherwise keep the first-pass conversion; sandbox and bounds must always apply. | Import a JS-heavy FAQ page and confirm (1) the saved Markdown includes FAQ content, (2) the HTML viewer still renders via sandboxed iframe, and (3) no Playwright install step exists in the repo or docs. | 2026-02-15 |
| Import→Render / Webpage HTML/JSON view | Enforce sandboxed iframe rendering and reduce freezes | Render `kgWebpageView∈{html,json}` via sandboxed `srcdoc`; treat HTML/JSON as view-only overlays; idle-defer `srcdoc` build; cap HTML size; sanitize by default (`webpageViewerScriptPolicy=strip`); ignore abort errors; reuse fetch caches and parse workers to prevent freezes. | `canvas/src/components/BottomPanel/markdownWorkspace/{MarkdownWorkspaceMain.tsx,MarkdownWorkspace.tsx}`, `canvas/src/features/panels/utils/idle.ts`, `canvas/src/lib/websites/{webpageIframeSrcdoc.ts,webpageHtmlToMarkdownArtifact.ts}`, `canvas/src/lib/graph/{parseWorker.ts,parseWorker.worker.ts}`, `docs/documents/knowgrph-webpage-proxy-and-srcdoc-iframe-document.md` | Workspace iframe viewer + conversion + parse worker | `useWebpageIframeSrcdoc`, `buildWebpageHtmlSrcdocAsync`, `fetchWebpageHtmlAuto`, `convertWebpageHtmlToMarkdownArtifactAsync`, `parseGraphInWorker` | URL + view + settings | Stable iframe render + responsive UI | Prefer stored `raw.html` when present; otherwise fetch once; conversion yields to main; abort shows Cancelled; singleton worker reduces parse stalls. | Toggle Markdown→HTML→JSON rapidly during import/convert and confirm no freezes, no “Fetch is aborted”. | 2026-02-15 |
| Import→Render / Mode contract / Doc-scoped collapse | Make view switching deterministic and prevent “missing blocks” | Enforce mode contract: (1) Markdown SSOT default, (2) HTML view-only iframe, (3) JSON view-only iframe; scope collapsed headings by `activeDocumentKey`; default `websiteImportGenerateWebpageArtifactDocs=false`; generate artifact markdown only from stored `raw.html` when enabled. | `canvas/src/components/BottomPanel/markdownWorkspace/{MarkdownWorkspaceMain.tsx,useWorkspaceFileActions.ts,workspaceImport.ts}`, `curagrph/src/features/markdown/ui/useMarkdownExplorerControls.ts` | Workspace + viewer controls | `useWebpageIframeSrcdoc`, `handleImportWebsite`, `useMarkdownExplorerControls` | Active doc + view toggle + import settings | Stable view switching + visible wireframes | Never write graph/layout/zoom on view change; HTML/JSON render `srcdoc`; doc-scoped LS key avoids cross-doc hidden sections. | Import ≤10 pages and confirm view toggles keep graph/zoom unchanged and wireframes visible. | 2026-02-15 |
| Import→Render / Webpage URL | Generic webpage conversion with fidelity and settings | Convert URL→Markdown using a browser-native, sandboxed iframe DOM export bridge with bounded proxy-fetch fallback; integrate into `sourceFilesIngestIntegration.ts` import routing; expose `webpageImportIncludeImages` + `webpageImportView` settings and keep view switching strictly view-only. | `canvas/src/lib/websites/{webpageDomExport.ts,webpageClientConvert.ts}`, `canvas/src/lib/net/remoteMarkdownConversions.ts`, `canvas/src/features/source-files/sourceFilesIngestIntegration.ts`, `canvas/src/features/settings/registry-ui.import-webpage.ts` | Webpage client converter + DOM export bridge | `convertWebpageUrlToMarkdownViaBrowser`, `exportWebpageDomViaHiddenIframe`, `fetchWebpageMarkdown` | URL + settings | Markdown (graph SSOT) + iframe HTML/JSON views | If not known ext/YouTube/PDF: convert via DOM export; if it fails, fall back to same-origin proxy fetch; apply `webpageImportIncludeImages` and persist `kgWebpageUrl/kgWebpageView` frontmatter without mutating graph/layout/zoom. | Verify Import URL for a JS-heavy page and a static page; toggle `Markdown/HTML/JSON`; confirm `[NAV]/[CTA]/[PRICE]/[TIME]` tokens remain visible. | 2026-02-15 |
| Import→Render / Local repo-relative HTML | Make local in-repo webpages render reliably | Fetch repo-relative `kgWebpageUrl` via `/__repo_file/*` in `fetchWebpageHtmlAuto`; prefer website-import `rawHtml` artifacts for both HTTP and local nodes; surface byte progress; keep HTML/JSON as sandboxed `srcdoc` view-only overlays. | `canvas/src/lib/url.ts`, `canvas/src/lib/websites/webpageIframeSrcdoc.ts`, `canvas/src/components/BottomPanel/markdownWorkspace/MarkdownWorkspaceMain.tsx` | Repo-file URL utils + iframe srcdoc fetch | `buildRepoFilePath`, `fetchWebpageHtmlFromRepoFile`, `fetchWebpageHtmlAuto`, `useWebpageIframeSrcdoc` | Repo-relative path or HTTP URL | Sandboxed iframe render + progress | If `kgWebsiteImportId/nodeId` exists: fetch artifact first; else if HTTP: use proxy; else: `/__repo_file`; sanitize+idle-yield; forbid path traversal. | Import `canvas/index.html` via Import URL and confirm iframe loads without freezes. | 2026-02-15 |
| Canvas Preview / Flicker / Preview Sync | Stop preview flicker and reduce render churn | Prevent no-op `setGraphData` commits from bumping `graphDataRevision`; memoize `hashGraphDataForPreviewSync`; make preview iframes load with `?kgPreview=1` to avoid initial UI flash; RAF-throttle preview pane resize state updates; add no-op guard to `setCanvasRenderMode`. | `canvas/src/hooks/store/{graphDataSlice.ts,graphDataSliceUtils.ts,canvasSlice.ts}`, `canvas/src/pages/Canvas.tsx`, `canvas/src/components/EmbeddedEditorShell.tsx`, `canvas/src/features/graph-table/ui/GraphTableCanvasPreviewDock.tsx` | Store + preview iframe host | `setGraphData`, `hashGraphDataForPreviewSync`, `setCanvasRenderMode`, `EmbeddedCanvasPreviewFrame` | GraphData + preview sync messages | Stable preview without flicker | Only commit state when effective graph/mode changed; avoid per-pointermove reflow; ensure embedded preview mode is detected at load. | Type in MarkdownWorkspace with preview open and confirm no visible flicker. | 2026-02-15 |
| Webpage Artifact / Main Content / Pricing-like cards | Remove duplicate synthetic “Content” and render card blocks as grid | Mark seeded “Content” main sections as synthetic and avoid emitting the redundant fenced heading echo; coalesce consecutive plain-text card-like blocks into a Markdown table (grid-like) using neutral heuristics (short lines, no markup). | `canvas/src/lib/websites/{webpageHtmlToMarkdownArtifact.ts,webpageHtmlToMarkdownArtifactPrivate.ts}`, `canvas/src/__tests__/webpageHtmlToMarkdownArtifact.test.ts` | HTML→Markdown artifact converter | `extractMainSections`, `convertWebpageHtmlToMarkdownArtifact` | HTML page content | Cleaner artifact + table grid blocks | If section is synthetic `Content`, skip the repeated `## Content` echo; if 2–4 adjacent card blocks detected, render as Markdown table with inferred card titles. | Import `https://www.remotion.dev/` and confirm Pricing cards render as a table without duplicate Content headings. | 2026-02-15 |
| Code Maintenance / Repo size / History purge | Shrink repo size and prevent artifact churn | Purge large artifacts from git history using `git-filter-repo`: remove `.knowgrph-workspace/**`, `data/knowgrph-workflow-preview/**`, `node_modules/**`, `backups/**`, and `git-error-*`; run aggressive GC; restore `origin` and force-push rewritten `main` + tags; require fresh re-clone for collaborators. Applies across `/GitHub/{knowgrph,curagrph,gympgrph,chatgrph,sandbox}`. | `.gitignore`, `.git/filter-repo/*`, `docs/documents/knowgrph-repo-hygiene-document.md` | Git history rewrite + artifact policy | `git rm --cached`, `git filter-repo --invert-paths`, `git gc --aggressive` | Repo history + artifact paths | Small `.git` + clean status + faster clone/push | First stop tracking artifacts; then rewrite history and garbage-collect; never commit `.knowgrph-workspace` (stored under `sandbox` via symlink) and never commit `node_modules/**`. | Verify `.git` size drops and `git log` still contains feature commits; then `git push --force-with-lease`. | 2026-02-15 |
| Import→Render / Webpage HTML view / Large payloads | Prevent srcdoc failures for very large pages | Remove hard size-fail; sanitize first then apply domain-neutral shrink (strip scripts/handlers/comments; collapse whitespace; replace base64 images; drop oversized inline svg/style); only fall back to text if still too large. | `canvas/src/lib/websites/webpageIframeSrcdoc.ts`, `canvas/src/__tests__/webpageIframeSrcdocLargeHtml.test.ts` | Srcdoc builder | `buildWebpageHtmlSrcdocAsync`, `shrinkOversizedHtml` | Raw HTML + URL | Stable sandboxed iframe render | If sanitized output exceeds budget, shrink; never hardcode domains; avoid freezes via yielding. | Import a large JS app shell and confirm iframe renders (no “too large for srcdoc”). | 2026-02-15 |
| Canvas / Layout positions / Renderer toggles | Keep layout stable across D3, Flow, Flow Editor, editor ON/OFF | Make layout-position caching renderer-agnostic for `renderMode=2d` and treat 2D renderer toggles as no-op for seeding; preserve 3D variant isolation. | `canvas/src/components/GraphCanvas/layout/positioning.ts`, `canvas/src/__tests__/layoutPositioning.test.ts` | Layout cache key + seed policy | `buildLayoutPositionCacheKey`, `determineLayoutPositions` | Graph nodes with positions | No reseed on 2D renderer switch | Ignore `renderVariant` for 2D cache keys; only treat 3D variant changes as engine changes. | Toggle D3↔Flow↔Flow Editor repeatedly and confirm positions persist. | 2026-02-15 |
| Canvas / Flow Editor / Viewport controls | Restore Flow Editor pan/zoom and align gestures | Stop forcing `design` viewport preset in Flow Editor; add `flowEditorSelectionOnDrag` setting + LS key; use Shift-drag selection when disabled; keep zoom modes and wheel parity consistent across 2D renderers. | `canvas/src/components/FlowCanvas.tsx`, `canvas/src/hooks/store/canvasSlice.ts`, `canvas/src/features/settings/registry-ui.graph-and-orchestrator.ts` | FlowCanvas + GraphState | `bindFlowCanvasNativeInteractions`, `setFlowEditorSelectionOnDrag` | `canvas2dRenderer=flowEditor`, `viewportControlsPreset`, selection drag | Usable pan/zoom + optional selection-on-drag | Never override stored preset; selection-on-drag is opt-in via `flowEditorSelectionOnDrag`. | Enter Flow Editor, confirm left-drag pans, wheel zoom works; enable selection-on-drag only if desired. | 2026-02-15 |
| Markdown / Source fidelity / Round-trip | Enforce lossless MD→HTML→MD | Embed the exact Markdown source in rendered preview DOM (base64, non-executing script) and restore it during HTML→Markdown import before heuristic conversion; mark derived UI blocks as non-source. | `curagrph/src/features/markdown/ui/{MarkdownPreview.tsx,MarkdownPreviewViewer.tsx}`, `canvas/src/features/parsers/html-parser.ts` | Preview DOM payload + import guard | `encodeUtf8ToBase64`, `tryExtractLosslessMarkdownFromHtml` | Markdown text or imported HTML | Exact Markdown recovery | If embedded payload exists, return it exactly; otherwise fall back to HTML→Markdown traversal. | Copy rendered HTML and re-import; confirm Markdown matches byte-for-byte. | 2026-02-15 |
| Import→Render / Local In-Repo URL / Sitemap + Rich Media | Make local repo imports and rich embeds reliable | Treat `file://` as repo-relative in Import URL; generate `website.sitemap.md` with doc links to created page docs; promote standalone inline HTML (`svg/iframe/video/audio/img`) into HTML blocks for safe rich-media rendering; use shared progress ticker for long conversions. | `canvas/src/components/BottomPanel/markdownWorkspace/{workspaceImport.ts,useWorkspaceFileActions.ts}`, `canvas/src/lib/websites/websiteSitemapMarkdown.ts`, `canvas/src/lib/progress/progressTicker.ts`, `curagrph/src/features/markdown/ui/markdownPreviewLexBlock.ts` | Workspace import + sitemap + markdown lex | `fetchWorkspaceUrlContent`, `handleImportWebsite`, `buildWebsiteSitemapMarkdown`, `createProgressTicker`, `buildBlockTokens` | `file://<repoRel>` + website root + markdown with `<svg>/<iframe>` | Stable local import + clickable sitemap docs + rendered embeds | Strip `file://` then enforce repo-root bounds; sitemap links use created doc paths (post-write); treat single-inline HTML blocks as block HTML for allowlisted rendering. | Import a local folder website, open `website.sitemap.md`, click `Doc` links, and verify `<svg>/<iframe>` blocks render in Markdown preview. | 2026-02-15 |
| Import→Parse / HTML↔Markdown / Unified fallback | Improve general-purpose HTML→Markdown without replacing markdown UI renderer | Add unified+rehype+remark HTML→Markdown conversion as a fallback path (prefer lossless embedded Markdown payload when present); keep Markdown UI rendering on existing markdown-it/custom token renderer (no duplication). | `canvas/src/features/parsers/html-parser.ts`, `canvas/src/lib/markdown/htmlToMarkdownUnified.ts`, `docs/documents/knowgrph-webpage-proxy-and-srcdoc-iframe-document.md` | HTML import parser + unified converter | `parseHtmlToMarkdownAllTextAsync`, `convertHtmlToMarkdownUnified` | Large HTML + optional baseUrl | Faster, more robust HTML import with idle scheduling | If payload script exists: restore exactly; else if large HTML: run unified pipeline in idle; otherwise use DOMParser traversal; always keep site-agnostic extraction. | Import a large HTML page and confirm conversion completes without UI freeze and links resolve against baseUrl. | 2026-02-15 |
| Markdown Workspace / Webpage View Modes / Fidelity | Increase HTML↔Markdown fidelity and per-doc granularity | Add per-doc webpage controls (View/Script/Imgs/Fid + Sync) in the Markdown toolbar; extend frontmatter to include script policy, include-images, and fidelity level; preserve rich media via safe HTML blocks; keep view switching and sync bounded and view-only. | `canvas/src/components/BottomPanel/{MarkdownWorkspaceToolbar.tsx,markdownWorkspace/*}`, `canvas/src/lib/markdown/{frontmatter.ts,htmlToMarkdownUnified.ts}`, `curagrph/src/features/markdown/ui/markdownPreviewLinks.tsx` | Markdown toolbar + webpage meta | `upsertWebpageFrontmatterMeta`, `convertHtmlToMarkdownUnified` | Webpage-backed Markdown doc | Stable UI controls + richer preview | Prefer frontmatter overrides when present; apply selection-free, view-only switches; regenerate Markdown only on explicit Sync; preserve media as allowlisted HTML when fidelity is high. | Import a JS-heavy page, toggle `DOM/Raw/HTML/Markdown/JSON`, hit Sync, and confirm media blocks render and no graph/layout/zoom mutates. | 2026-02-15 |


## 2026-02-16

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Canvas / Flow Editor / Pan+Zoom / Fly-Out wheel | Restore wheel pan/zoom from fly-out overlays | Proxy wheel events from `[data-kg-node-quick-editor]` to FlowCanvas wheel handler; preserve overlay scrolling by skipping proxy for scrollable regions and form inputs; guard behind `flowEditorOverlayWheelProxyEnabled`; add regression test. | `canvas/src/components/FlowCanvas/bindNativeInteractions.ts`, `canvas/src/hooks/store/{canvasSlice.ts,types.ts}`, `canvas/src/features/settings/registry-ui.graph-and-orchestrator.ts`, `canvas/src/__tests__/flowCanvasWheelOverlayProxyRegression.test.ts`, `canvas/src/tests/run.ts` | FlowCanvas native interactions | `onWindowWheelCapture`, `handleWheel` | Wheel event over fly-out | Flow canvas zoom/pan updates | If ctrl/meta: always proxy; else proxy only when overlay cannot scroll; when setting disabled: never proxy. | Run `npm run build:settings && npm run test:ci -- overlay.wheelProxy` and verify zoom works while hovering fly-out. | 2026-02-16 |
| Canvas / Flow Editor / Fly-Out / Gesture pinch zoom | Prevent browser zoom and restore pinch zoom over overlays | Handle Safari gesture pinch events in FlowCanvas: preventDefault and apply anchored zoom when event target is canvas or fly-out overlay; centralize overlay target detection; keep cleanup bounded and forbid duplicate listeners. | `canvas/src/components/FlowCanvas/bindNativeInteractions.ts`, `canvas/src/lib/canvas/flow-editor-overlay-proxy.ts`, `canvas/src/components/FlowEditor/NodeOverlayEditor.tsx`, `docs/documents/knowgrph-flow-editor-pan-zoom-overlay-failsafe-document.md` | FlowCanvas native interactions + overlay proxy helper | `onWindowGestureStartCapture`, `onWindowGestureChangeCapture`, `resolveFlowEditorOverlayProxyTarget` | Safari `gesture*` over canvas/fly-out | Flow canvas zoom updates; no page zoom | Proxy only when target is canvas or `[data-kg-node-quick-editor]` and not interactive; compute ratio from gesture scale; clamp scale extent; anchor at pointer or center fallback. | Open Flow Editor in Safari, pinch over fly-out, confirm canvas zooms and browser does not zoom. | 2026-02-16 |
| Canvas / Flow Editor / Fly-Out drag / Pan proxy freeze | Prevent canvas pan proxy from hijacking overlay drag and freezing interactions | For overlay pointerdown outside canvas, require Space-pan held before starting canvas pan proxy; this prevents competing pointer captures between overlay drag and canvas drag that can leave pan/zoom/drag unusable after a small movement. | `canvas/src/components/FlowCanvas/bindNativeInteractions.ts`, `canvas/src/lib/canvas/flow-editor-overlay-proxy.ts`, `canvas/src/__tests__/flowEditorOverlayDragDoesNotStartCanvasPanProxyRegression.test.ts`, `canvas/src/tests/run.ts` | FlowCanvas overlay pan proxy | `onWindowPointerDownCapture` | Pointerdown+drag on fly-out header | Overlay drags without canvas freeze | If target resolves to overlay and Space-pan is not held, do not start pending pan proxy; Space-pan still works from overlays. | Drag fly-out header in Flow Editor, then pan/zoom canvas; confirm interactions remain responsive. | 2026-02-16 |
| Canvas / 2D switching / Flow commit churn | Reduce rebinds during renderer switching and interaction | Make Flow commit callback read schema and graph via refs (not captured objects) so the callback identity stays stable; this prevents interaction bindings from re-installing due to memo churn when graph/layout objects refresh during renderer switching. | `canvas/src/components/FlowCanvas/{FlowCanvas.tsx,useFlowRequestCommit.ts}`, `canvas/src/__tests__/flowRequestCommitUsesRefsRegression.test.ts`, `canvas/src/tests/run.ts`, `docs/documents/knowgrph-flow-editor-pan-zoom-overlay-failsafe-document.md` | FlowCanvas commit path | `useFlowRequestCommit` | Renderer switch + interaction frames | Fewer rebinds; stable pan/zoom/drag | Store schema/graph in refs; commit reads from refs inside RAF; keep dependency list free of per-render graph object identities. | Switch D3↔Flow↔Flow Editor repeatedly, interact each time, and confirm no post-switch input freeze or jitter. | 2026-02-16 |
| Canvas / Flow overlay pan proxy / Stuck pointer id | Prevent stuck overlay proxy state from blocking interactions | Clear overlay pan proxy pointer state on `pointerup` and `lostpointercapture`, and clear both `pendingProxyPan` and `proxyPanPointerId` on unmount; this prevents proxy state from persisting and breaking subsequent pan/zoom/drag. | `canvas/src/components/FlowCanvas/bindNativeInteractions.ts`, `canvas/src/__tests__/flowCanvasOverlayPanProxyClearsPointerIdRegression.test.ts`, `canvas/src/tests/run.ts`, `docs/documents/knowgrph-flow-editor-pan-zoom-overlay-failsafe-document.md` | FlowCanvas overlay pan proxy | `onWindowPointerUpCapture`, `onLostPointerCapture` | Overlay pan proxy lifecycle | No post-init freeze | Never return early before clearing `proxyPanPointerId`; treat lostpointercapture as cancellation; always clear proxy state on cleanup. | Drag an overlay that triggers proxy pan and release; confirm the next pan/zoom/drag works. | 2026-02-16 |
| Canvas / 2D switching / Pointer event routing | Prevent drag state from becoming stuck during renderer switches | Add window-capture fallback routing that can end active drags even if canvas listeners are briefly unmounted; dedupe pointer move/up processing by a stable per-event key `(type,clientX,clientY,buttons,button)` to avoid double-processing when both canvas and window handlers receive events. | `canvas/src/components/FlowCanvas/bindNativeInteractions.ts`, `canvas/src/__tests__/flowCanvasWindowPointerUpClearsDragRegression.test.ts`, `canvas/src/tests/run.ts`, `docs/documents/knowgrph-flow-editor-pan-zoom-overlay-failsafe-document.md` | FlowCanvas native interactions | `onWindowPointerMoveCapture`, `onWindowPointerUpCapture` | Switch renderers mid-drag or after overlay interactions | No “move a bit then freeze” | Always allow window handler to call the same drag end path; ensure idempotence via dedupe; clear proxy state on unmount. | Switch D3↔Flow↔Flow Editor while dragging/panning and confirm interactions keep working. | 2026-02-16 |
| Shared DOM / pointerDrag / Lost pointer capture | Prevent stuck locks when pointer capture is lost | Ensure `startPointerDrag` calls `onCancel` when `lostpointercapture` fires, so clients release locks and persist state consistently. | `grph-shared/src/dom/pointerDrag.ts`, `canvas/src/__tests__/pointerDragLostCaptureCallsCancelRegression.test.ts`, `canvas/src/tests/run.ts` | Shared pointer drag helper | `startPointerDrag` | lostpointercapture during drag | No stuck drag side-effects | On lost capture, invoke `onCancel` then cleanup; keep cleanup idempotent via active flag. | Drag-resize panels or node quick editor header, then cause capture loss (switch renderer) and confirm controls still work. | 2026-02-16 |
| Flow Editor / Overlay collision / Live tick loop | Prevent overlay collision resolution from thrashing and causing haywire | Schedule overlay collision resolution via RAF and a stable key (overlay ids + quantized zoom + viewport); remove dependency on per-frame live interaction ticks while still allowing convergence after applying offsets/positions. | `canvas/src/components/FlowEditorCanvas.tsx`, `canvas/src/__tests__/flowEditorOverlayCollisionResolverNotTiedToLiveTickRegression.test.ts`, `canvas/src/tests/run.ts`, `docs/documents/knowgrph-flow-editor-pan-zoom-overlay-failsafe-document.md` | FlowEditor overlay manager | `scheduleOverlayCollisionResolve` | Pan/zoom or open overlays | No post-init freeze/haywire | Never recompute collision per-frame; only recompute on structural changes or zoom changes; allow one follow-up pass after applying. | Open multiple quick editors, pan/zoom, and confirm no stutter, no drift, and interactions remain responsive. | 2026-02-16 |
| FlowCanvas / Stale drag recovery | Prevent wheel zoom/pan from being blocked by stale drag state | If `dragRef` is non-pan but the canvas no longer has pointer capture for that drag, cancel the drag, release locks, and retry wheel handling so controls remain responsive after capture-loss edge cases. | `canvas/src/components/FlowCanvas/bindNativeInteractions.ts`, `canvas/src/__tests__/flowCanvasWheelCancelsStaleDragRegression.test.ts`, `canvas/src/tests/run.ts`, `docs/documents/knowgrph-flow-editor-pan-zoom-overlay-failsafe-document.md` | FlowCanvas native interactions | `handleWheel` | Capture-loss after small movement | No “move a bit then freeze” | Detect stale drags via `hasPointerCapture`; clear drag + selection box + locks; immediately re-run wheel handler. | Drag a node/overlay, trigger capture loss (switch renderer), then wheel zoom and confirm it works. | 2026-02-16 |
| Canvas UI / Floating Panel / View Embedding | Standardize Floating Panel view containers using Props Panel pattern | Reuse Props Panel lightweight embedding: single scroll owner (shell by default); forbid nested `h-full`/`overflow-*` scrollers and redundant panel bg/borders/shadows; if a view needs internal scroll (chat), switch shell body to `overflow-hidden`; Inspector must support a parent-scroll embed mode to remain visible across 2D renderers (D3/Flow/Design) without layout conflicts. | `canvas/src/features/toolbar/ToolbarToolMenu.tsx`, `canvas/src/features/toolbar/FloatingPropsPanel.tsx`, `canvas/src/components/ui/FloatingPanel.tsx`, `canvas/src/features/graph-table/ui/{GraphTableInspector.tsx,GraphTableSelectionInspector.tsx}`, `canvas/src/features/chat/SidePanelChatSections.tsx`, `docs/documents/knowgrph-frontend-document.md` | Floating Panel shell + embedded Inspector mode + tokenized buttons | `FLOATING_PANEL_SCROLL_CLASSNAME`, `floatingPanelBodyClassName`, `GraphTableInspector(scrollMode)`, `InspectorView` | floatingPanelView + embedded view content | Consistent panel layout + no double-scroll + theme-consistent UI | Default to shell-owned scroll; allow view-owned scroll only when shell disables its own scroll; use theme tokens and minimal div wrappers for layout-only containers | Add a new floating panel view and confirm: no nested scroll, no hardcoded palette, and layout matches Props Panel without extra borders/width constraints | 2026-02-16 |
| Graph Data Table / Fast Grid / Scroll stability | Eliminate flicker, lag, and bleed-through while preserving selection + inspector | Implement host Graph Data Table as a canvas fast-grid with stable scroll extents and clipped drawing; forbid ResizeObserver→state loops, canvas resize jitter, scroll-jump effects, and any path where scrolled content can draw under pinned header/columns. Cache theme metrics outside the scroll loop. | `canvas/src/features/graph-table/ui/GraphTableFastGrid.tsx`, `canvas/src/features/graph-table/ui/fast-grid/canvasGridRender.ts` | GraphTableFastGrid + canvas renderer | `GraphTableFastGrid`, `drawGrid`, `readGridTheme` | Scroll/resize/theme change + selection | Smooth scrolling + correct vertical scroll + opaque pinned areas | Use an explicit spacer element sized to layout total width/height to drive native scroll; round scroll px; clip header band and pinned column band; only recompute theme on resize/theme mutation | Stress-scroll vertically/horizontally and confirm: (1) scroll works, (2) no shimmer/flicker, (3) no text under header/pinned columns, (4) no ResizeObserver loop warnings | 2026-02-16 |
| Canvas / 2D view keys / Schema layout fingerprint | Prevent keyed zoom misses across 2D renderers | Use one schema-layout engine JSON builder (include `schema.layout.flow`) for both zoom view keys and layout view keys across D3/Flow/Design/Flow Editor; this avoids missing keyed zoom state and layout cache drift when switching renderers. | `canvas/src/lib/canvas/{schema-layout-engine-json.ts,active-2d-zoom-view-key.ts}`, `canvas/src/components/{GraphCanvas.tsx,FlowCanvas.tsx,DesignCanvas.tsx}` | 2D view key SSOT | `buildSchemaLayoutEngineJson2d`, `buildActive2dZoomViewKey` | Schema + graph meta + toggles | Stable keyed zoom across variants | If schema fingerprint differs, keyed zoom lookups can miss; always derive from the shared helper and include Flow knobs. | Switch D3↔Flow↔Design↔Flow Editor and confirm zoom restores without jumps. | 2026-02-16 |
| Canvas / Flow Editor / Camera init key | Avoid cross-file camera init collisions | When `datasetKey` collapses to `rev:*`, compute a per-graph hashed Flow Editor init key and re-init if the current transform does not show the graph; this prevents “stuck offscreen” views across different files after restart. | `canvas/src/lib/canvas/flow-editor-init-key.ts`, `canvas/src/components/FlowCanvas.tsx`, `canvas/src/__tests__/flowEditorCameraInitKey.test.ts` | Flow Editor viewport init | `buildFlowEditorCameraInitKey`, `pickInitialZoomTransform` | datasetKey + GraphData | Correct initial fit per file | If dataset key is unstable, hash graph signature; if bounds guard fails, re-fit even when init key matches. | Restart dev, open multiple docs, and confirm pan/zoom always recovers to visible graph. | 2026-02-16 |
| Canvas / Flow Editor / Node Quick Editor positioning | Prevent corner-stuck overlays after restart | Default detached placement and all clamping must use live `viewportW/viewportH` (not stale refs) and prefer live runtime transforms when available; this prevents overlays clamping to `{8,8}` and keeps pan/zoom recovery consistent across files. | `canvas/src/components/FlowEditor/{NodeOverlayEditor.tsx,nodeQuickEditorLayout.ts}` | NodeOverlayEditor overlay | `computeDefaultNodeQuickEditorFloatingPos`, `applyOverlayPosition` | viewport + node id + zoom | Stable overlay placement | If viewport dims are 0×0 when memoized/clamped, overlays stick to a corner; always use live viewport props and re-run on resize. | Restart dev, open multiple quick editors, and confirm none clamp to extreme corners. | 2026-02-16 |

## 2026-02-14

| Context | Intent | Directive | Module | Class/Object | Function/Method | Input | Output | Decision Logic | Next Step Recommendation | Updated Date |
|--------|--------|-----------|--------|-----------------|-------|--------|----------------|--------------------------|--------------------------|--------------|
| Import→Render / Webpage Markdown Artifact / Mode Contract | Align artifact doc structure while preserving view-switch invariants | Generate a fixture-like, site-agnostic Webpage Markdown Artifact (TOC + Page Structure Overview + Document Structure + Asset Catalog) with bounded fidelity via `webpageArtifactFidelityMaxLevel`, and ensure `kgWebpageView` switches preserve existing frontmatter keys (incl. `kgWebsiteImportId/kgWebsiteNodeId/kgWebsiteOutputDirRel`) so artifact-backed HTML/JSON rendering remains stable; forbid any graph/layout/zoom mutations on view switches. | `canvas/src/lib/websites/{webpageMarkdownArtifactGenerator.ts,webpageMarkdownArtifact.ts}`, `canvas/src/lib/markdown/frontmatter.ts`, `canvas/src/features/settings/registry-ui.import-webpage.ts`, `canvas/src/components/BottomPanel/markdownWorkspace/{MarkdownWorkspace.tsx,workspaceImport.ts}` | Webpage artifact generator + frontmatter upsert + settings registry | `buildWebpageMarkdownArtifactFromMarkdown`, `buildWebpageMarkdownArtifactDoc`, `upsertWebpageFrontmatterMeta` | Converted webpage Markdown + URL + store settings | Deterministic artifact Markdown + stable view switching | Infer fidelity from extracted signals/structure then clamp to `webpageArtifactFidelityMaxLevel`; update only `kgWebpageUrl/kgWebpageView` lines in frontmatter and preserve the rest; render HTML/JSON as sandboxed iframe overlays using proxy-src (highest fidelity) or sanitized srcdoc snapshot when available. | Import a JS-heavy page and a static page, toggle `Markdown/HTML/JSON`, and confirm (1) website-import ids remain intact, (2) HTML works in proxy-src and srcdoc snapshot paths, and (3) switching views does not change graph/layout/zoom/table selection. | 2026-02-14 |
| Website Import / Status Progress / Workspace Write Perf | Prevent freezes and redundant re-computation during website imports | Expose lightweight `progress` snapshot in `/__website_import/status` and avoid per-page manifest `nodes/errors` array cloning; use bounded backoff polling and concurrency-limited workspace writes with yielding; keep iframe rendering sandboxed and view-only; forbid domain branching, infinite loops, re-index churn, or view-switch graph/layout/zoom mutations. | `canvas/src/lib/websites/{server/websiteImportServer.ts,webpageIframeSrcdoc.ts}`, `canvas/src/components/BottomPanel/markdownWorkspace/useWorkspaceFileActions.ts`, `docs/documents/knowgrph-webpage-proxy-and-srcdoc-iframe-document.md`, `huijoohwee.github.io/schema/AgenticRAG/pipeline.jsonld` | WebsiteImportServer + Workspace file actions + iframe proxy/srcdoc contract | `createWebsiteImportHandler`, `crawlInternalUrls`, `handleImportWebsite`, `buildWebpageHtmlSrcdoc` | Website root URL + import options + workspace FS + frontmatter | Progress UI + website tree + stable sandboxed iframe render | During run: update manifest `progress` only; after done: fetch manifest nodes once; polling backs off when progress stalls; writing uses concurrency limit and yields to avoid blocking; HTML view uses proxy-src by default and may use sanitized srcdoc when a safe snapshot is available. | Manual smoke import a small site (≤10 pages) and confirm progress updates, no UI freeze, and HTML view works for both stored-artifact srcdoc and proxy-src paths without view-switch side effects. | 2026-02-14 |
| Editor Workspace / Monaco / Webpage view switching | Prevent empty editor during async hydration and mode toggles | Ensure Monaco initializes with the latest text value (not a stale first-render snapshot) and ensure `kgWebpageView` switches hydrate from disk before writing; forbid any path where switching `Markdown/HTML/JSON` can result in an empty editor buffer when the on-disk file is non-empty. | `canvas/src/features/monaco/MonacoTextEditor.tsx`, `canvas/src/components/BottomPanel/markdownWorkspace/MarkdownWorkspace.tsx`, `docs/documents/knowgrph-webpage-proxy-and-srcdoc-iframe-document.md` | MonacoTextEditor + MarkdownWorkspace | `MonacoTextEditor`, `switchActiveWebpageWorkspaceView` | Workspace file path + view mode + hydrated file text | Non-empty editor content when file content exists | Monaco must seed model value from a `latestValue` ref at create-time; view switching must read from FS/lastLoaded when editor is not dirty; markdown switches must clear HTML/JSON overrides before async conversions complete. | Import a large webpage (≥50k chars), immediately toggle `kgWebpageView: html → markdown`, and confirm the editor shows content without requiring a reload or extra toggle. | 2026-02-14 |

## 2026-02-13
