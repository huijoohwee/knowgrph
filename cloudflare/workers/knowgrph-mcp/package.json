{
  "name": "@knowgrph/mcp-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker hosting the Agents SDK McpAgent for the knowgrph control plane (airvio.co/knowgrph/mcp). Migrated from the deployed HTTP MCP at cloudflare/pages/knowgrph-agent-ready.mjs per ADR-7.",
  "license": "MIT",
  "main": "index.ts",
  "scripts": {
    "dev": "wrangler dev --config wrangler.toml",
    "deploy": "wrangler deploy --config wrangler.toml",
    "test": "node --test __tests__/*.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.1.0",
    "agents": "^0.15.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250529.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.95.0"
  },
  "engines": {
    "node": ">=18"
  }
}
